---
phase: 180-gate-contract-preservation
plan: 01
type: execute
wave: 1
depends_on:
  - 179-v1-20-closeout-evidence-reproducibility-repair
files_modified:
  - scripts/quality_gates.sh
  - tools/bench/quality_gates_tests.cpp
autonomous: true
requirements:
  - GATE-01
  - GATE-03
rule_constraints:
  - Preserve `scripts/quality_gates.sh` as the mandatory top-level gate.
  - Do not weaken coverage, lint snapshot, docs, fuzz, parity, or benchmark enforcement.
---

# Phase 180 Plan 01

<rule_constraints>
- Keep the top-level gate mandatory and fail-visible.
- Do not skip required lanes merely because the gate script changed.
- Keep changed-file scoping conservative where lane relevance is uncertain.
</rule_constraints>

## Objective

Preserve the quality-gate contract while preparing the script for manifest-backed selective runner
execution.

## Tasks

1. Keep `scripts/quality_gates.sh` as the maintained entrypoint for implementation validation.
2. Ensure gate-script changes select conservative full coverage, parity, benchmark, fuzz, docs, and
   lint behavior.
3. Preserve explicit benchmark failure handling through `bench_status`.
4. Add source-backed static coverage in `tools/bench/quality_gates_tests.cpp` for mandatory lane
   preservation.

## Verification

- `bash -n scripts/quality_gates.sh`
- `build/bench_tools_ninja/quality_gates_tests`
- `EMEL_QUALITY_GATES_CHANGED_FILES="scripts/quality_gates.sh:scripts/paritychecker.sh:tools/bench/quality_gates_tests.cpp" scripts/quality_gates.sh`

## Done Condition

`scripts/quality_gates.sh` remains the mandatory validation contract, gate-script changes are
conservative, and focused tests cover the contract.
