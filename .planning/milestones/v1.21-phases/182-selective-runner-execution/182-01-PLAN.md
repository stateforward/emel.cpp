---
phase: 182-selective-runner-execution
plan: 01
type: execute
wave: 1
depends_on:
  - 181-manifest-impact-resolution
files_modified:
  - scripts/paritychecker.sh
  - scripts/quality_gates.sh
  - tools/bench/quality_gates_tests.cpp
autonomous: true
requirements:
  - RUNNER-01
  - RUNNER-02
  - RUNNER-03
rule_constraints:
  - Execute selected runners only through maintained tool entrypoints.
  - Keep full fallback execution available through the same entrypoints.
---

# Phase 182 Plan 01

<rule_constraints>
- Do not bypass `scripts/paritychecker.sh` for parity validation.
- Do not bypass `scripts/bench.sh` for benchmark validation.
- Do not rely on hidden state to reproduce runner selection.
</rule_constraints>

## Objective

Add maintained selective parity execution and wire quality-gate runner choices into maintained
parity and benchmark entrypoints.

## Tasks

1. Add selected-runner argument parsing to `scripts/paritychecker.sh`.
2. Map maintained runner names to their doctest case filters.
3. Keep the existing no-filter full parity behavior unchanged.
4. Have `scripts/quality_gates.sh` pass selected parity runners as `--runner=<name>` arguments.
5. Preserve existing benchmark suite execution through `scripts/bench.sh --suite=<runner>`.
6. Add tests for selected runner support.

## Verification

- `bash -n scripts/paritychecker.sh`
- `scripts/paritychecker.sh --help`
- `scripts/paritychecker.sh --runner=kernel`
- `build/bench_tools_ninja/quality_gates_tests`

## Done Condition

Trustworthy impact selection can execute selected parity and benchmark runners through maintained
entrypoints, while full fallback still uses those same entrypoints.
