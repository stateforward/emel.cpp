---
phase: 181-manifest-impact-resolution
plan: 01
type: execute
wave: 1
depends_on:
  - 180-gate-contract-preservation
files_modified:
  - scripts/quality_gates.sh
  - tools/bench/quality_gates_tests.cpp
autonomous: true
requirements:
  - IMPACT-01
  - IMPACT-02
  - IMPACT-03
rule_constraints:
  - Use checked-in dependency manifests as the reproducible source for impact selection.
  - Full fallback is required whenever manifest trust is uncertain.
---

# Phase 181 Plan 01

<rule_constraints>
- Do not skip parity or benchmark work on unmatched relevant files.
- Do not treat stale or missing manifest data as selective-runner proof.
- Keep selection reasons visible in quality-gate output.
</rule_constraints>

## Objective

Add parity manifest impact resolution and preserve benchmark manifest impact resolution with clear
fallback reasons.

## Tasks

1. Add parity runner selection state and helper functions to `scripts/quality_gates.sh`.
2. Parse `tools/paritychecker/dependency_manifest.txt` records for changed-file impact.
3. Reuse the maintained paritychecker binary to emit and check dependency manifest freshness.
4. Keep benchmark manifest selection and full fallback behavior source-backed.
5. Add static tests covering parity manifest consumption and conservative fallback.

## Verification

- `bash -n scripts/quality_gates.sh`
- `build/bench_tools_ninja/quality_gates_tests`
- scoped quality-gate output includes `quality_gates: select parity runner=... reason=...`
- scoped quality-gate output includes `quality_gates: select benchmark runner=... reason=...`

## Done Condition

Changed-file impact can select parity and benchmark runners from manifests, and every uncertain
manifest state falls back to the full relevant gate.
