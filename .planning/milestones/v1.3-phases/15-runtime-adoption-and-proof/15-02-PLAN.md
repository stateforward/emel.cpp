---
phase: 15-runtime-adoption-and-proof
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - tests/generator/lifecycle_tests.cpp
  - src/emel/generator/sm.hpp
autonomous: true
requirements:
  - PORT-03
  - VER-02
user_setup: []
must_haves:
  truths:
    - unsupported or out-of-contract generation requests do not silently claim optimized AArch64 flash execution.
    - canonical and negative runtime cases stay explicit on the shipped generator seam.
  artifacts:
    - `tests/generator/lifecycle_tests.cpp` proves both positive and negative runtime outcomes with the new optimized/shared counters.
    - `src/emel/generator/sm.hpp` remains the single runtime proof seam consumed by tests and tools.
  key_links:
    - the existing `generator_generate_rejects_noncanonical_flash_request_without_claiming_flash` case should extend to optimized/shared counters instead of introducing a parallel negative harness.
---

<objective>
Preserve deterministic unsupported or out-of-contract runtime behavior without false optimized-path
claims.

Purpose: satisfy `PORT-03` and the negative-behavior half of `VER-02` by extending shipped runtime
proof, not by changing routing. Output: generator lifecycle tests distinguish canonical optimized
execution from negative cases that must leave optimized/shared counters at zero.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extend negative runtime proof over optimized/shared counter claims</name>
  <files>tests/generator/lifecycle_tests.cpp</files>
  <read_first>tests/generator/lifecycle_tests.cpp, src/emel/generator/sm.hpp</read_first>
  <action>Strengthen the existing generator negative flash test so it proves unsupported or
  non-canonical runtime requests do not increment either optimized or shared backend counters.
  Keep the same failure mode and callback expectations; only tighten the proof surface to cover the
  new backend attribution claims.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks</automated>
  <acceptance_criteria>
    - the negative test proves zero optimized/shared counts
    - no new negative-only tool harness is introduced
  </acceptance_criteria>
  <done>Unsupported runtime behavior is explicit and cannot be misread as optimized-path execution.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks`
- [ ] negative runtime proof shows zero optimized/shared counts
</verification>

<output>
After completion, create `.planning/phases/15-runtime-adoption-and-proof/15-02-SUMMARY.md`
</output>
