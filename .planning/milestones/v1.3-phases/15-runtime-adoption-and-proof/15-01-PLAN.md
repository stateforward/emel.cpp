---
phase: 15-runtime-adoption-and-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emel/kernel/aarch64/sm.hpp
  - src/emel/kernel/any.hpp
  - src/emel/generator/sm.hpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - ARCH-01
  - VER-02
user_setup: []
must_haves:
  truths:
    - the shipped generator runtime can report whether canonical flash dispatch used the optimized AArch64 backend path or a shared fallback.
    - the new proof stays inside existing wrappers and does not modify transition tables or runtime event contracts.
  artifacts:
    - `src/emel/kernel/aarch64/sm.hpp` exposes backend-local optimized/shared flash counts.
    - `src/emel/kernel/any.hpp` forwards those counts from the active backend without requiring new events.
    - `src/emel/generator/sm.hpp` surfaces runtime-level optimized/shared flash counters for existing proof consumers.
    - `tests/generator/lifecycle_tests.cpp` fails first until the new runtime observability exists.
  key_links:
    - `src/emel/generator/detail.hpp` remains the canonical generator -> kernel flash dispatch seam.
    - `src/emel/kernel/aarch64/context.hpp` remains the backend source of truth for optimized/shared counts.
---

<objective>
Expose backend-specific ARM flash path-selection evidence through the shipped runtime seam.

Purpose: satisfy the runtime-observability half of `ARCH-01` and `VER-02` without changing SML
structure. Output: the active kernel wrapper reports optimized/shared AArch64 flash dispatch counts
through `src/emel/generator/sm.hpp`, and generator lifecycle proof can fail-first on missing
support.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add failing-first runtime proof for missing optimized/shared backend attribution</name>
  <files>tests/generator/lifecycle_tests.cpp</files>
  <read_first>tests/generator/lifecycle_tests.cpp, src/emel/generator/sm.hpp, src/emel/kernel/any.hpp, src/emel/kernel/aarch64/sm.hpp</read_first>
  <action>Add focused generator runtime doctest assertions for canonical flash generation that
  expect explicit optimized/shared backend counters to be available through the shipped generator
  seam. Keep the test portable by checking ARM-specific truth only when the active kernel kind is
  AArch64. The first version must fail or compile-fail before the wrapper methods exist.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks</automated>
  <acceptance_criteria>
    - the new test reaches the shipped generator runtime instead of calling backend actions directly
    - compile or runtime failure occurs before wrapper observability is implemented
    - no parity or benchmark files are modified in this task
  </acceptance_criteria>
  <done>A failing-first runtime proof exists for missing optimized/shared backend attribution.</done>
</task>

<task type="auto">
  <name>Task 2: Surface optimized/shared backend counts through existing kernel and generator wrappers</name>
  <files>src/emel/kernel/aarch64/sm.hpp, src/emel/kernel/any.hpp, src/emel/generator/sm.hpp, tests/generator/lifecycle_tests.cpp</files>
  <read_first>src/emel/kernel/aarch64/sm.hpp, src/emel/kernel/any.hpp, src/emel/generator/sm.hpp, tests/generator/lifecycle_tests.cpp, AGENTS.md</read_first>
  <action>Add wrapper-level observability for optimized and shared flash dispatch counts without
  changing transition tables, events, or runtime control flow. `src/emel/kernel/aarch64/sm.hpp`
  should expose the backend-local counters already owned by context,
  `src/emel/kernel/any.hpp` should forward them from the active machine, and
  `src/emel/generator/sm.hpp` should expose generator-facing accessors used by existing proof
  surfaces. Make the runtime doctest pass and keep non-AArch64 backends explicit and benign.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks</automated>
  <acceptance_criteria>
    - no `sm.hpp` transition rows change
    - the shipped generator seam exposes optimized/shared flash counters
    - canonical ARM runtime proof can assert optimized > 0 and shared == 0
  </acceptance_criteria>
  <done>The runtime seam reports real backend path-selection counts without changing orchestration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks`
- [ ] `rg 'optimized_flash|shared_flash' src/emel/kernel/aarch64/sm.hpp src/emel/kernel/any.hpp src/emel/generator/sm.hpp tests/generator/lifecycle_tests.cpp`
- [ ] no transition table rows or public API signatures changed
</verification>

<output>
After completion, create `.planning/phases/15-runtime-adoption-and-proof/15-01-SUMMARY.md`
</output>
