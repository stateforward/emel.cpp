---
phase: 15-runtime-adoption-and-proof
plan: 03
type: execute
wave: 2
depends_on: [15-01, 15-02]
files_modified:
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/paritychecker_tests.cpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - PAR-03
  - VER-02
  - ARCH-01
user_setup: []
must_haves:
  truths:
    - `tools/paritychecker --generation` publishes explicit optimized/shared flash attribution for the canonical runtime surface.
    - ARM-specific optimized-path proof is enforced only when the active kernel is AArch64.
    - regression coverage preserves the existing generator -> graph -> processor -> kernel seam.
  artifacts:
    - `tools/paritychecker/parity_runner.cpp` emits optimized/shared flash metrics alongside existing generation proof output.
    - `tools/paritychecker/paritychecker_tests.cpp` verifies the published proof surface.
    - `tests/generator/lifecycle_tests.cpp` remains aligned with the shipped runtime seam used by paritychecker.
  key_links:
    - parity output should extend the existing `flash_dispatch:` line instead of inventing a second proof channel.
    - the canonical generation fixture remains `tests/models/Llama-68M-Chat-v1-Q2_K.gguf`.
---

<objective>
Publish optimized-path execution proof through parity and regression coverage on the maintained
canonical ARM workload.

Purpose: satisfy `PAR-03`, close `VER-02`, and confirm `ARCH-01` on the maintained tool surface.
Output: paritychecker generation output reports optimized/shared flash attribution and tests prove
the canonical ARM workload exercised the optimized backend path when running on AArch64.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extend paritychecker generation evidence with optimized/shared flash attribution</name>
  <files>tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp</files>
  <read_first>tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp, src/emel/generator/sm.hpp</read_first>
  <action>Extend the maintained generation proof output so paritychecker publishes optimized/shared
  flash counts together with the existing kernel/flash dispatch lines. Preserve the current CLI
  contract and row-free output shape; only add attribution fields to the proof comments. Update the
  paritychecker doctests to assert the new fields exist and to enforce optimized > 0 and shared ==
  0 when the active runtime kernel kind is AArch64.</action>
  <verify>ctest --test-dir build/zig --output-on-failure -R paritychecker_tests</verify>
  <automated>ctest --test-dir build/zig --output-on-failure -R paritychecker_tests</automated>
  <acceptance_criteria>
    - paritychecker output contains explicit optimized/shared flash attribution
    - ARM-specific proof fails if the canonical runtime falls back to shared flash
    - non-ARM builds remain deterministic and do not make false ARM claims
  </acceptance_criteria>
  <done>Paritychecker publishes truthful optimized/shared flash attribution for the canonical runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Run regression closure on the maintained runtime and parity surfaces</name>
  <files>tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp, tests/generator/lifecycle_tests.cpp</files>
  <read_first>tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp, tests/generator/lifecycle_tests.cpp</read_first>
  <action>Run the focused generator and parity tests, then the repo's standard quality gates. Keep
  any warning-only benchmark drift documented but untouched. Do not update benchmark snapshots in
  this phase.</action>
  <verify>scripts/quality_gates.sh</verify>
  <automated>scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - focused generator and parity tests pass
    - `scripts/quality_gates.sh` passes under current repo policy
    - no snapshot-bearing benchmark artifact is modified in Phase 15
  </acceptance_criteria>
  <done>Phase 15 closes with runtime, parity, and regression proof on maintained surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --test-dir build/zig --output-on-failure -R paritychecker_tests`
- [ ] `./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks`
- [ ] `scripts/quality_gates.sh`
- [ ] no benchmark snapshot artifacts changed
</verification>

<output>
After completion, create `.planning/phases/15-runtime-adoption-and-proof/15-03-SUMMARY.md`
</output>
