---
phase: 16-benchmark-attribution-and-evidence
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - snapshots/bench/generation_pre_arm_flash_optimized_baseline.txt
  - snapshots/bench/benchmarks_compare.txt
  - tools/docsgen/docsgen.cpp
  - docs/templates/benchmarks.md.j2
  - docs/benchmarks.md
autonomous: false
requirements:
  - BENCH-04
  - BENCH-05
  - BENCH-06
user_setup: []
must_haves:
  truths:
    - no checked-in benchmark snapshot or generated docs artifact changes until the user explicitly approves them.
    - maintained artifacts preserve the pre-optimization ARM flash baseline separately from the refreshed optimized compare surface.
    - generated benchmark docs publish the optimized/shared attribution and the improvement over the preserved shared-scalar ARM baseline.
  artifacts:
    - `snapshots/bench/generation_pre_arm_flash_optimized_baseline.txt` preserves the current canonical short compare row before the optimized publication refresh.
    - `snapshots/bench/benchmarks_compare.txt` carries the refreshed optimized/shared flash evidence metadata.
    - `docs/benchmarks.md` is regenerated through docsgen after approval.
  key_links:
    - `AGENTS.md` blocks `scripts/bench.sh --compare-update` and checked-in snapshot writes until approval is explicit.
    - `tools/bench/compare_flash_baseline.py` remains the improvement calculator against the preserved ARM baseline artifact.
---

<objective>
Honor the required approval gate, then publish maintained benchmark artifacts for the optimized ARM
flash path.

Purpose: satisfy `BENCH-04`, `BENCH-05`, and `BENCH-06` honestly through the maintained snapshot
and docs workflow. Output: an explicit approval decision, a preserved shared-scalar ARM baseline
artifact, refreshed compare snapshot metadata, and regenerated benchmark docs.
</objective>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Secure explicit approval before writing checked-in benchmark artifacts</name>
  <files>none - checkpoint only; do not modify repo files in this task</files>
  <read_first>AGENTS.md, .planning/phases/16-benchmark-attribution-and-evidence/16-RESEARCH.md, .planning/phases/16-benchmark-attribution-and-evidence/16-VALIDATION.md</read_first>
  <action>Present the exact Phase 16 publication changes before resuming execution. The follow-on
  work will write `snapshots/bench/generation_pre_arm_flash_optimized_baseline.txt`, refresh
  `snapshots/bench/benchmarks_compare.txt`, update `tools/docsgen/docsgen.cpp` plus
  `docs/templates/benchmarks.md.j2`, and regenerate `docs/benchmarks.md` through
  `scripts/generate_docs.sh`. Do not run `scripts/bench.sh --compare-update` and do not modify any
  checked-in benchmark artifact unless the user explicitly approves.</action>
  <decision>Approve Phase 16 checked-in benchmark publication updates?</decision>
  <context>`AGENTS.md` says `NEVER update snapshots without explicit user consent`. Phase 16 cannot
  publish truthful maintained benchmark evidence without refreshing the compare snapshot and docs
  surfaces after preserving the current shared-scalar ARM baseline.</context>
  <options>
    <option id="approve">
      <name>Approve</name>
      <pros>Phase 16 can finish end-to-end with refreshed maintained benchmark artifacts.</pros>
      <cons>Checked-in benchmark snapshot and docs files will change in this branch.</cons>
    </option>
    <option id="decline">
      <name>Decline</name>
      <pros>Leaves all checked-in benchmark artifacts unchanged.</pros>
      <cons>Phase 16 will stop short of published maintained benchmark evidence.</cons>
    </option>
  </options>
  <resume-signal>Reply `approve` to continue Phase 16 artifact publication, or `decline` to stop before any snapshot or docs change.</resume-signal>
  <verify>Explicit user response recorded as `approve` or `decline`.</verify>
  <automated>rg 'NEVER update snapshots without explicit user consent' AGENTS.md</automated>
  <acceptance_criteria>
    - the user is shown the exact files `snapshots/bench/generation_pre_arm_flash_optimized_baseline.txt`, `snapshots/bench/benchmarks_compare.txt`, and `docs/benchmarks.md`
    - the user is told that `scripts/bench.sh --compare-update` will not run without approval
    - execution does not proceed past this checkpoint unless the response is exactly `approve`
  </acceptance_criteria>
  <done>The approval decision is explicit and no checked-in benchmark artifact changed before approval.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg 'NEVER update snapshots without explicit user consent' AGENTS.md`
- [ ] user response recorded as `approve` or `decline`
- [ ] no checked-in benchmark snapshot or docs artifact was modified before approval
</verification>

<output>
After completion, create `.planning/phases/16-benchmark-attribution-and-evidence/16-02-SUMMARY.md`
</output>
