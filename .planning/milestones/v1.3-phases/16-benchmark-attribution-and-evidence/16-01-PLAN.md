---
phase: 16-benchmark-attribution-and-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bench/generation_bench.cpp
  - tools/bench/bench_main.cpp
autonomous: true
requirements:
  - BENCH-04
  - BENCH-05
user_setup: []
must_haves:
  truths:
    - the live canonical compare run publishes optimized/shared AArch64 flash attribution instead of aggregate flash counts alone.
    - the live compare surface fails ARM proof if the canonical run falls back to shared flash.
  artifacts:
    - `tools/bench/generation_bench.cpp` captures optimized/shared flash counts from the shipped generator seam.
    - `tools/bench/bench_main.cpp` validates and prints those counts on the compare surface.
  key_links:
    - the compare metadata should extend the existing `# generation_flash_evidence:` line rather than inventing a second benchmark proof channel.
---

<objective>
Extend the live benchmark compare surface with optimized/shared ARM flash attribution.

Purpose: satisfy the code-and-live-proof half of `BENCH-04` and `BENCH-05` before any snapshot
publication work. Output: `scripts/bench.sh --compare` reports optimized/shared flash evidence for
the canonical generation case and enforces optimized-path truth on ARM.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Capture and publish optimized/shared flash attribution on the compare surface</name>
  <files>tools/bench/generation_bench.cpp, tools/bench/bench_main.cpp</files>
  <read_first>tools/bench/generation_bench.cpp, tools/bench/bench_main.cpp, src/emel/generator/sm.hpp</read_first>
  <action>Extend the canonical generation flash evidence in `tools/bench` so the compare output
  records optimized and shared flash dispatch counts alongside the existing aggregate flash proof.
  When the active runtime kernel kind is AArch64, the compare surface must fail if the canonical
  run does not stay on the optimized path. Keep the existing compare row format unchanged; only
  extend the metadata comments.</action>
  <verify>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare | rg '^# generation_flash_evidence: case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 .*optimized_flash_dispatch_calls=[1-9][0-9]* .*shared_flash_dispatch_calls=0$'</verify>
  <automated>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare | rg '^# generation_flash_evidence: case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 .*optimized_flash_dispatch_calls=[1-9][0-9]* .*shared_flash_dispatch_calls=0$'</automated>
  <acceptance_criteria>
    - compare output contains optimized/shared flash attribution
    - the canonical ARM compare run hard-fails if shared flash is used
    - no checked-in snapshot or docs artifact changes in this plan
  </acceptance_criteria>
  <done>The live compare surface proves optimized ARM flash execution explicitly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare`
- [ ] `scripts/quality_gates.sh`
- [ ] no checked-in benchmark snapshot or docs artifact changed
</verification>

<output>
After completion, create `.planning/phases/16-benchmark-attribution-and-evidence/16-01-SUMMARY.md`
</output>
