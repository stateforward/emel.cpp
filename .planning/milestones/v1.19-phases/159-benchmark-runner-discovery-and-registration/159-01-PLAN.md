---
phase: 159
plan: 01
title: "Benchmark runner discovery and registration"
status: complete
requirements:
  - DISC-01
rule_constraints:
  - "Registration must be deterministic and fail closed for unknown suites."
  - "Do not change maintained benchmark behavior or output schemas."
  - "Keep build-target separation and manifests out of this phase."
---

# Plan 159-01: Benchmark Runner Discovery And Registration

<rule_constraints>
- Move suite registration to a localized registry surface consumed by the orchestrator.
- Keep all existing suite names, append functions, tokenizer behavior, and host filtering intact.
- Add focused source checks proving broad static registration no longer lives in `bench_runner.cpp`.
</rule_constraints>

## Goal

Replace broad static benchmark case wiring in the orchestrator with a localized runner registry
that supports deterministic lookup and future runner additions.

## Tasks

1. Add `bench_runner_registry.hpp` / `.cpp` for registered suite metadata.
2. Move default and kernel suite lists into the registry.
3. Update `bench_runner.cpp` to consume `default_runner_cases()` and `kernel_runner_cases()`.
4. Register tokenizer as an ordinary suite entry while preserving `include_tokenizer` filtering.
5. Add tests for registration lookup and source ownership.

## Verification

- `git diff --check`
- `cmake -S tools/bench -B build/bench_tools_ninja -G Ninja -DCMAKE_BUILD_TYPE=Release -DEMEL_BENCH_SUITE_FILTER=`
- `cmake --build build/bench_tools_ninja --target bench_runner_tests -j2`
- `ctest --test-dir build/bench_tools_ninja --output-on-failure -R bench_runner_tests`
- Changed-file scoped `scripts/quality_gates.sh`
