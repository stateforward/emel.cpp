---
phase: 163
plan: 01
title: "Benchmark behavior and lane-isolation closure"
status: complete
requirements:
  - ORCH-02
  - LANE-02
rule_constraints:
  - "Do not change maintained benchmark behavior or output schemas."
  - "Do not reach into actor actions, guards, or detail helpers from benchmark shared code."
  - "Keep EMEL/reference lane-owned runtime objects separate."
---

# Plan 163-01: Benchmark Behavior And Lane-Isolation Closure

<rule_constraints>
- Add proof tests only; do not alter benchmark runtime behavior.
- Keep the shared benchmark orchestration lane-neutral.
- Use full-suite focused tests to prove maintained generation and diarization behavior still pass.
</rule_constraints>

## Goal

Close the benchmark runner refactor by proving maintained benchmark behavior remains stable and
shared orchestration enforces lane-isolation boundaries.

## Tasks

1. Add source checks for shared benchmark orchestration actor-boundary cleanliness.
2. Add checks that broad generation/diarization append wiring does not drift back into
   `bench_runner.cpp`.
3. Add meta-coverage checks that maintained behavior tests remain present.
4. Validate full-suite `bench_runner_tests`.
5. Run the changed-file scoped quality gate.

## Verification

- `git diff --check`
- `cmake -S tools/bench -B build/bench_tools_ninja -G Ninja -DCMAKE_BUILD_TYPE=Release -DEMEL_BENCH_SUITE_FILTER=`
- `cmake --build build/bench_tools_ninja --target bench_runner_tests -j2`
- `ctest --test-dir build/bench_tools_ninja --output-on-failure -R bench_runner_tests`
- Changed-file scoped `scripts/quality_gates.sh`
