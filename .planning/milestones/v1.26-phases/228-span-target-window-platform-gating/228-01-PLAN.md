---
phase: 228-span-target-window-platform-gating
plan: "01"
type: execute
wave: 1
depends_on:
  - 227-staged-read-strategy-component-boundary
files_modified:
  - src/emel/io/staged_read/actions.hpp
  - src/emel/io/staged_read/detail.hpp
  - src/emel/io/staged_read/errors.hpp
  - src/emel/io/staged_read/events.hpp
  - src/emel/io/staged_read/guards.hpp
  - src/emel/io/staged_read/sm.hpp
  - tests/io/staged_read/lifecycle_tests.cpp
autonomous: true
requirements:
  - STG-02
  - STG-03
  - PLAT-02
rule_constraints:
  - RC-SML-RTC-NO-QUEUE
  - RC-SML-GUARDS-OWN-RUNTIME-CHOICE
  - RC-SML-ACTIONS-BOUNDED-NONBLOCKING
  - RC-SML-NO-DISPATCH-LOCAL-CONTEXT
  - RC-SML-UNEXPECTED-HANDLING
must_haves:
  truths:
    - "Invalid staged source span/chunk contracts are rejected only via guarded transitions before any staged I/O readiness state."
    - "Invalid target-window/layout preconditions are rejected only via guarded transitions before staged I/O readiness."
    - "Unsupported staged platform/host enumerations fail closed via explicit guarded error terminal transitions."
    - "No mmap/device syscall/async introduced; actions allocate nothing during dispatch."
  artifacts:
    - path: src/emel/io/staged_read/guards.hpp
      provides: "Complementary guard predicates for span, staging contract, target window, platform (STG-02/03, PLAT-02)."
    - path: src/emel/io/staged_read/sm.hpp
      provides: "Destination-first transitions wiring validation-only decision chain + error terminals."
    - path: tests/io/staged_read/lifecycle_tests.cpp
      provides: "Dispatch-level doctests proving guard-modelled rejections + at least one fully validated acceptance preamble."
---

<objective>

Satisfy **ROADMAP Phase 228** and **STG-02**, **STG-03**, **PLAT-02** by extending `io/staged_read` with a **read-parity
validation skeleton**: public intent event → INTERNAL completion carrier → sequential guard-checked decision states ending
either in **`state_staged_pre_ready`** (structural acceptance placeholder ahead of Phase 229 copy semantics) or explicit
terminal error callbacks/states analogous to **`io/read`**.

Purpose: Preconditions are entirely **explicit in `sm.hpp` + `guards.hpp`** prior to staged byte copy work.

Output: updated staged_read headers, extended errors/events/detail, tests exercising **dispatch-only** evidence.

</objective>

<execution_context>

@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md

</execution_context>

<context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/228-span-target-window-platform-gating/228-CONTEXT.md
@.planning/phases/227-staged-read-strategy-component-boundary/227-01-PLAN.md
@AGENTS.md
@docs/rules/sml.rules.md
@docs/rules/cpp.rules.md
@src/emel/io/read/sm.hpp
@src/emel/io/read/guards.hpp
@src/emel/io/read/actions.hpp
@src/emel/io/read/detail.hpp
@src/emel/io/read/events.hpp
@src/emel/io/staged_read/sm.hpp
@src/emel/io/staged_read/actions.hpp
@tests/io/staged_read/lifecycle_tests.cpp

</context>

<rule_constraints>

| ID | Directive | Enforced By |
|----|-----------|-------------|
| RC-SML-RTC-NO-QUEUE | No defer/mailbox/actor self-recursion during validation chain. | All tasks |
| RC-SML-GUARDS-OWN-RUNTIME-CHOICE | Every branch between next state vs terminal error pairs uses guards only (`guards.hpp` + `sm.hpp`). | Tasks 2–4 |
| RC-SML-ACTIONS-BOUNDED-NONBLOCKING | Actions: mark bookkeeping, invoke synchronous callbacks once, no branching on runtime policy. | Tasks 2–4 |
| RC-SML-NO-DISPATCH-LOCAL-CONTEXT | `action::context` stays empty—no mirrored request/target pointers/phase indices. | Tasks 2–3 |
| RC-SML-UNEXPECTED-HANDLING | Idle `unexpected_event` rows remain anchored on `state_ready` (+ egress if read pattern requires). | Task 4 |

</rule_constraints>

<source_audit>

| Source Item | Status | Plan Coverage |
|-------------|--------|----------------|
| ROADMAP Phase 228 (3 bullets) | COVERED | Tasks 3–5 |
| STG-02 / STG-03 / PLAT-02 text | COVERED | Tasks 2–5 |
| AGENTS staged-read forbids | COVERED | Tasks 3–4 (no syscalls/async) |

</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Staged precondition event + INTERNAL runtime carrier skeleton</name>
  <files>src/emel/io/staged_read/events.hpp, src/emel/io/staged_read/detail.hpp</files>
  <read_first>`io/read/events.hpp`; `io/read/detail.hpp` (read_tensor_runtime pattern); `io/events.hpp`</read_first>
  <behavior>
    - Add **public** intent event in `emel::io::staged_read::event` (noun-like name, e.g. `staged_load_window`) carrying:
      **source** logical span (`file_offset`, `logical_byte_length`), **staging contract** (`stage_chunk_bytes`), **target**
      window (`target_buffer`, `target_window_bytes`), optional **layout** scalars if required for STG-03 (default contiguous
      byte slab if omitted), and **callbacks** `on_valid` / `on_error` mirroring read event callback style.
    - Define `events::*_done` / `events::*_error` outcome types with explicit suffixes; avoid `cmd_*` names.
    - Add INTERNAL `detail::staged_load_window_runtime` (name adjustable) bridging public event → phased
      `completion<detail::staged_load_window_runtime>` chain analogous to **`read_tensor_runtime`** (mutable references
      allowed for INTERNAL-only synchronous handoff per rules).
    - **No allocations** constructing carriers beyond existing stack/tuple patterns used in read.
  </behavior>
  <verify>Compile `emel_tests_io` incremental build after edits.</verify>
</task>

<task type="auto">
  <name>Task 2: Error taxonomy extensions for precondition failures</name>
  <files>src/emel/io/staged_read/errors.hpp</files>
  <read_first>`io/read/errors.hpp` enumeration style</read_first>
  <behavior>Add distinct **`error` enum entries** consumed by STG-02/03/PLAT-02 terminal paths (invalid span contract, invalid
    target window/layout, unsupported platform). Map to `emel::error::type` via existing `error::cast` pattern used in sibling
    strategies.</behavior>
  <verify>Compile-only + grep ensures new enumerators referenced from guards/actions only through typed surfaces.</verify>
</task>

<task type="auto">
  <name>Task 3: Pure guards for STG-02, STG-03, PLAT-02</name>
  <files>src/emel/io/staged_read/guards.hpp</files>
  <read_first>`io/read/guards.hpp` (span arithmetic, layout, platform macros)</read_first>
  <behavior>
    - Implement **complementary guard structs** (supported vs invalid mirror pairs) evaluating only `(event, context)` with
      **no side effects**.
    - **STG-02:** validate positive lengths, **uint64 overflow** rules on `file_offset + logical_byte_length`, chunk sizing
      (`stage_chunk_bytes &gt; 0`, upper bounds consistent with logical span and allowed remainder policy—encode explicitly in
      comments adjacent to predicates).
    - **STG-03:** validate non-null target, `target_window_bytes` covers **minimum staging slab** required for the accepted
      contract (document equality: e.g. `target_window_bytes &gt;= stage_chunk_bytes` while Phase 229 may tighten per partial
      final chunk).
    - **PLAT-02:** `EMEL_IO_STAGED_READ_PLATFORM_SUPPORTED` compile-time gate + guard pair mirroring **`platform_read_*`** semantics.
  </behavior>
  <verify>Header-only unit compilation via dependent translation units; no runtime until Task 5 tests.</verify>
</task>

<task type="auto">
  <name>Task 4: Actions for bookkeeping + synchronous callbacks (no policy branching)</name>
  <files>src/emel/io/staged_read/actions.hpp</files>
  <read_first>`io/read/actions.hpp` near `effect_mark_*` and callback fan-in</read_first>
  <behavior>
    - Provide `effect_begin_staged_window`, `effect_mark_invalid_span`, `effect_mark_invalid_target`, `effect_mark_unsupported_platform`,
      `effect_emit_pre_ready` (or equivalent) as **linear** handlers without `if/switch` selecting behavior—each action bound from
      **one** transition row only; use `if constexpr` only if compiling platform variants without runtime branch.
    - Maintain **`effect_on_unexpected`** parity with Phase 227 table expectations.
    - Callbacks invoked **once** synchronously inside the RTC chain terminating validation (success stub or error structs).
  </behavior>
</task>

<task type="auto">
  <name>Task 5: Transition table — validation-only spine + terminals</name>
  <files>src/emel/io/staged_read/sm.hpp</files>
  <read_first>`io/read/sm.hpp` first ~150 lines + unexpected egress rows</read_first>
  <behavior>
    - Introduce explicit decision states (**names prefixed per AGENTS**, e.g. `state_guard_source_span_contract_decision`).
    - **Destination-first rows** exclusively; partitioned with divider comments matching `gbnf`/read style.
    - Flow mirrors read: **`state_ready` + public intent → INTERNAL runtime + `effect_begin_*` → completion chain** gated by Task 3
      guards until **`state_staged_pre_ready`** (placeholder) or error decision states that chain to callback actions.
    - Terminal success path for Phase 228 only proves **preconditions accepted** (callback with a done event carrying echo
      references—**no** byte copy counts yet; bytes may remain zero until Phase 229).
    - Expand **unexpected_event** handling if new states require egress back to `state_ready` (mirror read file).
  </behavior>
  <verify>`ninja -C build emel_tests_bin` + targeted `ctest -R emel_tests_io`.</verify>
</task>

<task type="auto">
  <name>Task 6: Doctest regression matrix (dispatch-through-machine only)</name>
  <files>tests/io/staged_read/lifecycle_tests.cpp</files>
  <behavior>
    - Retain Phase 227 default construction smoke unchanged.
    - Add cases dispatching **`process_event`** with **invalid span/chunk combos** ⇒ expect error callback fired / machine visiting
      expected terminal/error sink (inspect via observable callback flags or introspection helpers used elsewhere—**no reach into
      `actions.hpp` internals** directly).
    - Add **invalid target window** permutation.
    - Add **unsupported platform** path when macro forces false (conditionally compiled case or guarded build flag consistent with repo test patterns—**document** chosen approach in test comments if macro prevents runtime flip).
    - Add **golden happy-path**: preconditions satisfy all guards ⇒ success callback observes **`state_staged_pre_ready`** or equivalent
      introspection bit before returning to idle (match machine pattern from read tests if available).
  </behavior>
  <verify>
    <automated>`ctest --test-dir build --output-on-failure -R emel_tests_io`</automated>
  </verify>
</task>

</tasks>

<verification>

- `ninja -C build emel_tests_bin`
- `ctest --test-dir build --output-on-failure -R emel_tests_io`
- After implementation batch: `EMEL_QUALITY_GATES_CHANGED_FILES` listing only touched paths + `scripts/quality_gates.sh` per manager policy.

</verification>
