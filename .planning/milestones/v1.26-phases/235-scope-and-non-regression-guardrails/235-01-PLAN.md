# Phase 235 Plan 01

## Goal

Close GRD-01..GRD-05 with milestone-native guardrail tests and focused validation only.

## Files Updated

- `tests/model/loader/lifecycle_tests.cpp`
- `tests/model/tensor/lifecycle_tests.cpp`

## Planned Changes

1. **GRD-01**: broaden model/io boundary ownership scan coverage in loader tests to include `model/loader`, `io/loader`, `io/read`, `io/mmap`, and `model/tensor` relevant headers for forbidden syscall/file-loop tokens.
2. **GRD-02**: broaden staged-read residency ownership guardrail so `lifecycle::resident` remains tensor-owned and absent from `model/loader` and `io/loader` surfaces (plus staged-read surfaces).
3. **GRD-03**: add broad coroutine-token denial guardrail over staged scheduling component surfaces.
4. **GRD-04**: preserve shipped `mmap` semantics using `tests/io/mmap/lifecycle_tests.cpp` regression cases.
5. **GRD-05**: preserve shipped bulk `io/read` semantics using `tests/io/loader/lifecycle_tests.cpp` bulk read-copy routing cases.
6. Run required milestone-only validation commands and record exact outcomes.

## Constraints

- Milestone worktree only.
- No root-worktree edits.
- No runtime source changes for this phase.
- No ledger updates before validation and artifact completion.
