---
phase: 233-public-loader-and-maintained-entrypoints
plan: "01"
type: plan
wave: 1
depends_on:
  - 232-tensor-owned-integration-graph
files_modified:
  - .planning/phases/233-public-loader-and-maintained-entrypoints/233-CONTEXT.md
  - .planning/phases/233-public-loader-and-maintained-entrypoints/233-01-PLAN.md
  - .planning/phases/233-public-loader-and-maintained-entrypoints/233-01-SUMMARY.md
  - .planning/phases/233-public-loader-and-maintained-entrypoints/233-VERIFICATION.md
autonomous: false
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
  - PUB-05
---

<objective>

Close Phase 233 by proving each **`PUB-*`** requirement with **source-backed** and **test- or
tool-backed** evidence: public contracts only on loader, benchmarks, parity, embedded-size probes,
and no duplicate unconstrained staged-read shim in tools.

</objective>

<tasks>

<task type="auto">
  <name>PUB-01: model/loader vs staged_read surface</name>
  <behavior>
    Confirm `model/loader` wires `io::loader` public events only (no `staged_read` component
    headers). Scan `src/emel/model/loader/**` includes and tensor↔loader dispatch paths.
  </behavior>
</task>

<task type="auto">
  <name>PUB-02: benchmark entrypoints</name>
  <behavior>
    Confirm maintained bench strategy selection documents `staged_read` via public
    `emel::io::loader::event::strategy_kind` (`tools/bench/model_load_strategy.hpp` and consumers).
  </behavior>
</task>

<task type="auto">
  <name>PUB-03 / PUB-04: parity and probe lanes</name>
  <behavior>
    Map paritychecker / embedded-size entrypoints to public loader or tensor contracts without
    `staged_read` detail reach-through; record gaps as open work.
  </behavior>
</task>

<task type="auto">
  <name>PUB-05: no duplicate POSIX staged shim</name>
  <behavior>
    Script or doctest enforcement per ROADMAP success criterion; document residual search commands.
  </behavior>
</task>

<task type="auto">
  <name>Regression commands</name>
  <behavior>
    `ninja -C build emel_tests_bin`; `ctest -R 'emel_tests_(io|model)'`; optional scoped
    `scripts/quality_gates.sh` when Phase 233 changed-file corpus is defined — record exit codes in
    **`233-VERIFICATION.md`**.
  </behavior>
</task>

</tasks>
