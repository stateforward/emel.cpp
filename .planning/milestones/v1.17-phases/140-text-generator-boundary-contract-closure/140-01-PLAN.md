---
phase: 140
plan: 01
title: Text Generator Boundary Contract Closure
status: ready
requirements:
  - TEXTGEN-01
  - TEXTGEN-06
rule_constraints:
  - docs/rules/sml.rules.md
  - docs/rules/cpp.rules.md
  - AGENTS.md
---

# Plan 140-01: Text Generator Boundary Contract Closure

<rule_constraints>
- Preserve Stateforward.SML actor boundaries; do not add actor-internal benchmark or parity bypasses.
- Keep generation runtime behavior unchanged.
- Keep domain-boundary checks deterministic, bounded, and portable across Unix-based OSes.
- Do not introduce duplicate implementation ownership under `include/`.
</rule_constraints>

## Objective

Close `TEXTGEN-01` and `TEXTGEN-06` from `.planning/v1.17-MILESTONE-AUDIT.md`.

## Tasks

1. Document the source-backed include-root decision in phase evidence:
   `emel/text/generator/**` is the logical include root served from `src` by `emel_core`.
2. Extend `scripts/check_domain_boundaries.sh` so maintained generation parity/benchmark lanes
   fail on text-generator actor-internal includes, namespaces, or deleted bridge headers.
3. Run the focused boundary script and direct source scan.
4. Update ROADMAP/REQUIREMENTS/STATE after verification passes.

## Verification

- `scripts/check_domain_boundaries.sh`
- `rg -n 'emel/text/generator/(detail|actions|guards)\.hpp|emel::text::generator::(detail|action|guard)::|emel::text::generator::prefill::guard::|generation_internal_diagnostics' tools/bench/generation_bench.cpp tools/paritychecker/parity_runner.cpp tools/paritychecker/parity_runner.hpp`
