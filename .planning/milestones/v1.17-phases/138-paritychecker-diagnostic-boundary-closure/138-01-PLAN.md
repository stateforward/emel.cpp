---
phase: 138
plan: 1
title: Paritychecker Diagnostic Boundary Closure
status: complete
requirements:
  - TEXTGEN-07
rule_constraints:
  - "Do not let paritychecker or benchmark maintained proof reach into actor actions/detail/guard internals."
  - "Keep maintained generation behavior actor-driven through public events and process_event(...)."
  - "Do not introduce new model family, fixture, sampling, or performance claims."
---

# Plan

<rule_constraints>
- Paritychecker maintained generation proof must not include, re-export, or call
  `emel/text/generator/detail.hpp`, `emel::text::generator::detail`, generator `action`, or
  generator `guard` internals.
- Optional diagnostic behavior that cannot satisfy the boundary must be removed or rejected rather
  than hidden behind another bridge.
- Verification must include source scans that catch bridge headers as well as the runner body.
</rule_constraints>

## Goal

Close `TEXTGEN-07` by removing the remaining paritychecker diagnostic bridge into text-generator
actor internals while preserving the maintained actor-driven generation parity result path.

## Tasks

1. Reproduce the blocker with a source scan over paritychecker runner and bridge headers.
2. Remove `tools/paritychecker/generation_internal_diagnostics.hpp` from the maintained build path.
3. Remove or reject optional generator attribution/debug paths that depend on generator actor
   internals, while keeping public generation initialize/generate parity intact.
4. Strengthen paritychecker source regression tests so hidden bridge headers cannot satisfy the
   check.
5. Run focused paritychecker tests, generation benchmark gate or scoped quality gate, domain
   boundary checks, and update Phase 138 summary/verification/validation artifacts.

## Verification

- `rg -n 'generation_internal_diagnostics|emel/text/generator/detail|emel::text::generator::detail::|emel::text::generator::action::|emel::text::generator::guard::' tools/paritychecker`
- `cmake --build build/paritychecker_zig --target paritychecker_tests -j2 && ctest --test-dir build/paritychecker_zig --output-on-failure`
- `EMEL_QUALITY_GATES_CHANGED_FILES="tools/paritychecker/parity_runner.cpp,tools/paritychecker/paritychecker_tests.cpp,tools/paritychecker/generation_internal_diagnostics.hpp" EMEL_QUALITY_GATES_BENCH_SUITE=generation scripts/quality_gates.sh`
- `scripts/check_domain_boundaries.sh`

## Notes

If the deleted bridge file is no longer present, omit it from the changed-file quality gate input.

## Result

Completed on 2026-04-29. The paritychecker no longer has a live diagnostic bridge header into
`emel/text/generator/detail.hpp`; optional generation attribution is rejected until a public
diagnostics contract exists; and the strengthened paritychecker test scans runner and bridge
sources for hidden actor-internal access.
