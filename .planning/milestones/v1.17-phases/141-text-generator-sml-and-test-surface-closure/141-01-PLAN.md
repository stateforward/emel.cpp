---
phase: 141
plan: 01
status: executed_with_gaps
requirements:
  - TEXTGEN-04
  - TEXTGEN-05
---

# Phase 141 Plan

## Goal

Close the remaining text generator SML/detail/test-surface audit gaps while preserving maintained
generation behavior.

## Tasks

1. Replace public `sm` context-reading inspection wrappers with a synchronous public diagnostic
   event.
2. Move row-storage dtype dispatch used by generator tensor binding into kernel-owned helpers.
3. Update lifecycle tests to use the new event-driven graph lifecycle snapshot.
4. Run focused generator/runtime tests, domain-boundary checks, changed-file quality gates, and
   source scans for residual actor-internal coupling.

## Verification

- `emel_tests_generator_and_runtime` must pass.
- `scripts/check_domain_boundaries.sh` must pass.
- Scoped `scripts/quality_gates.sh` must pass with the changed files list.
- Source scans must prove whether actor-internal test coupling and generator detail routing remain.
