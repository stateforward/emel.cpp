---
phase: 143
plan: 01
status: complete
requirements:
  - TEXTGEN-04
rule_constraints:
  - "Runtime behavior selection belongs in guards and SML transitions, not detail helper outputs."
  - "Add a failing source-backed regression before fixing the reported route-ownership bug."
  - "Preserve generation request/output behavior and maintained parity/benchmark lane isolation."
---

# Plan 143-01: Text Generator Flash Route Closure

<goal>
Close the remaining `TEXTGEN-04` audit blockers by moving flash/nonflash route-support ownership
out of `detail.hpp`, broadening the route-regression scan, and repairing closeout evidence.
</goal>

<tasks>

## 1. Reproduce The Audit Blocker

Files:
- `tests/text/generator/lifecycle_tests.cpp`

Actions:
- Extend the source-backed route regression so parent and prefill guards fail if they call
  `emel::text::generator::detail::flash_attention_supported(`.
- Run the focused generator/runtime shard and confirm the new regression fails before production
  changes.

## 2. Move Flash Route Support To Guards

Files:
- `src/emel/text/generator/guards.hpp`
- `src/emel/text/generator/prefill/guards.hpp`
- `src/emel/text/generator/detail.hpp`

Actions:
- Add a guard-owned pure predicate for flash attention route support.
- Update `decode_flash_runtime_supported` and prefill `flash_runtime_supported` to call the
  guard-owned predicate.
- Remove the behavior-selecting `detail.hpp` support predicate while keeping already-selected flash
  execution helpers in `detail.hpp`.

## 3. Repair Evidence Artifacts

Files:
- `.planning/phases/142-text-generator-detail-routing-closure/142-01-SUMMARY.md`
- `.planning/milestones/v1.17-phases/142-text-generator-detail-routing-closure/142-01-SUMMARY.md`
- `.planning/phases/143-text-generator-flash-route-closure/143-01-SUMMARY.md`
- `.planning/phases/143-text-generator-flash-route-closure/143-VERIFICATION.md`
- `.planning/phases/143-text-generator-flash-route-closure/143-VALIDATION.md`
- `.planning/ROADMAP.md`
- `.planning/REQUIREMENTS.md`
- `.planning/STATE.md`
- `.planning/MILESTONES.md`

Actions:
- Use `requirements-completed:` frontmatter where required.
- Record source-backed evidence and align active planning files after execution.

## 4. Verify

Commands:
- `git diff --check -- <phase changed files>`
- `cmake --build build/zig --target emel_tests_bin -j2`
- `ctest --test-dir build/debug -R emel_tests_generator_and_runtime --output-on-failure`
- `scripts/check_domain_boundaries.sh`
- focused source scans for guard-to-detail route predicates and maintained parity/benchmark
  actor-internal bridges
- `EMEL_QUALITY_GATES_CHANGED_FILES=<phase changed files> EMEL_QUALITY_GATES_BENCH_SUITE=generation scripts/quality_gates.sh`

</tasks>

<verification>
The phase is complete when live source shows no parent or prefill guard dependency on
behavior-selecting `detail.hpp` support predicates for flash/nonflash route selection, focused and
scoped gates pass, and `TEXTGEN-04` evidence is consistent across active planning files.
</verification>

<rule_constraints>
- Guards may contain pure runtime predicates and compile-time conditionals.
- Actions and detail helpers may execute an already-selected path but must not choose which runtime
  route runs next.
- Maintained proof must continue through public generator lifecycle, paritychecker, benchmark, and
  domain-boundary entrypoints.
</rule_constraints>
