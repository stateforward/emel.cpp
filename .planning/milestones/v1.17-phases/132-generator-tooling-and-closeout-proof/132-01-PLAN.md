# Phase 132 Plan: Generator Tooling And Closeout Proof

## Tasks

1. Cut CMake test paths and shard filters over to `tests/text/generator`.
2. Cut coverage and quality gate inference over to `src/emel/text/generator`.
3. Cut paritychecker, generation benchmark, and embedded-size probe includes over to
   `emel/text/generator`.
4. Add a stale-domain boundary check for old generator roots.
5. Run source search, domain-boundary checks, targeted generator tests, and quality gates.

## Verification

- `scripts/check_domain_boundaries.sh`
- `git diff --check`
- Targeted zig generator/runtime build and test
- Changed-file `scripts/quality_gates.sh`
