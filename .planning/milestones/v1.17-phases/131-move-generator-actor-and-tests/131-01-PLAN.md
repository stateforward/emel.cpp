# Phase 131 Plan: Move Generator Actor And Tests

## Tasks

1. Move generator source files into `src/emel/text/generator/**`.
2. Move generator tests into `tests/text/generator/**`.
3. Rewrite includes and namespaces to the canonical text-domain names.
4. Update top-level `emel::Generator` alias to point at `emel::text::generator::sm`.
5. Fix relative test-helper includes caused by the deeper test path.
6. Build and run the generator/runtime shard.

## Verification

- Configure a scoped zig build for `generator_and_runtime`.
- Build `emel_tests_bin`.
- Run `ctest -R emel_tests_generator_and_runtime`.
