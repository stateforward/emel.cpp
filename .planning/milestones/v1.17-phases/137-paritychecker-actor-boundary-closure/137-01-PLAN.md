---
phase: 137
plan: 1
title: Paritychecker Actor Boundary Closure
status: complete
requirements:
  - TEXTGEN-07
---

# Plan

## Goal

Close the remaining `TEXTGEN-07` source contradiction by preventing
`tools/paritychecker/parity_runner.cpp` from reaching directly into text generator actor internals.

## Tasks

1. Reproduce the source contradiction with a search for direct generator detail/action/guard usage
   in `tools/paritychecker/parity_runner.cpp`.
2. Add a tool-owned diagnostic bridge for legacy paritychecker diagnostic helpers.
3. Replace direct `emel::text::generator::detail::*` references in `parity_runner.cpp` with the
   bridge namespace and remove the direct generator detail include from the runner.
4. Add a paritychecker regression test that scans `parity_runner.cpp` for direct generator
   `detail`, `action`, or `guard` usage.
5. Run paritychecker tests, the changed-file scoped quality gate, and domain-boundary checks.

## Constraints

- Keep the maintained generation result path actor-driven through `process_event(...)`.
- Do not move generator orchestration into tools.
- Do not add benchmark or parity fallback behavior.
