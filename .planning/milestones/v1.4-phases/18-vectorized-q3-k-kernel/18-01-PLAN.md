---
phase: 18-vectorized-q3-k-kernel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/kernel/aarch64_tests.cpp
  - src/emel/kernel/aarch64/actions.hpp
autonomous: true
requirements:
  - PORT-05
user_setup: []
must_haves:
  truths:
    - the AArch64 backend exposes a failing-first q3-focused proof that is missing until the vectorized q3 seam exists.
    - the q3 cutover stays inside the existing backend quantized mul-mat seam with no SML row or event changes.
    - supported q3 requests remain numerically equivalent to the maintained scalar path.
  artifacts:
    - `tests/kernel/aarch64_tests.cpp` contains q3-specific failing-first proof for the missing vectorized seam.
    - `src/emel/kernel/aarch64/actions.hpp` contains a backend-local q3 row helper built from the existing q3 NEON block helper.
    - `execute_neon_mul_mat(...)` routes only the `dtype_q3_k` branch through the new row helper.
  key_links:
    - `src/emel/kernel/detail.hpp::dot_q3_k_q8_k_row_scalar(...)` remains the correctness oracle.
    - `src/emel/kernel/aarch64/actions.hpp::dot_q3_k_q8_k_block_neon(...)` remains the arithmetic building block for the vectorized q3 row path.
---

<objective>
Land and lock the backend-local vectorized q3 row kernel through the existing AArch64 quantized
matmul seam.

Purpose: satisfy the implementation half of `PORT-05` without widening scope. Output: supported
`q3_K x q8_K` requests execute through an EMEL-owned vectorized AArch64 row helper and still match
the maintained scalar result.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add failing-first q3 seam tests</name>
  <files>tests/kernel/aarch64_tests.cpp</files>
  <read_first>tests/kernel/aarch64_tests.cpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/detail.hpp</read_first>
  <action>Add q3-focused doctest coverage that makes the missing vectorized q3 seam visible before
  the implementation lands. The proof should stay at the AArch64 kernel seam, assert scalar
  equivalence on the maintained operand layout, and fail or compile-fail until the q3 vectorized
  row helper exists.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*kernel_aarch64_q3*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*kernel_aarch64_q3*' --no-breaks</automated>
  <acceptance_criteria>
    - the new test is q3-specific rather than only exercising the combined q2/q3/q6 loop
    - the first version fails until the vectorized q3 seam exists
    - no runtime or benchmark files are modified in this task
  </acceptance_criteria>
  <done>A failing-first q3 seam proof exists.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the vectorized q3 row helper wired and scalar-equivalent on the maintained seam</name>
  <files>src/emel/kernel/aarch64/actions.hpp, tests/kernel/aarch64_tests.cpp</files>
  <read_first>src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/detail.hpp, tests/kernel/aarch64_tests.cpp, AGENTS.md</read_first>
  <action>Add a backend-local `dot_q3_k_q8_k_row_neon(...)` helper that accumulates the staged
  `block_q8_k` operands via the existing q3 NEON block arithmetic, then route only the
  `dtype_q3_k` branch in `execute_neon_mul_mat(...)` to it. Keep q6 behavior unchanged, make the
  q3 doctest pass, and preserve scalar equivalence on the maintained operand layout.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*kernel_aarch64_q3*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*kernel_aarch64_q3*' --no-breaks</automated>
  <acceptance_criteria>
    - q3 supported requests stop calling the shared scalar row helper from the AArch64 q3 branch
    - q6 behavior remains unchanged
    - q3 SIMD output matches the scalar reference on the maintained operand contract
  </acceptance_criteria>
  <done>The q3 backend seam uses an EMEL-owned vectorized AArch64 row helper.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build/zig/emel_tests_bin --test-case='*kernel_aarch64_q3*' --no-breaks`
- [ ] `rg 'dot_q3_k_q8_k_row_neon|dtype_q3_k' src/emel/kernel/aarch64/actions.hpp tests/kernel/aarch64_tests.cpp`
- [ ] no SML transition rows, event payloads, or public C API signatures changed
</verification>

<output>
After completion, create `.planning/phases/18-vectorized-q3-k-kernel/18-01-SUMMARY.md`
</output>
