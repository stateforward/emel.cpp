---
phase: 18-vectorized-q3-k-kernel
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/emel/kernel/aarch64/context.hpp
  - src/emel/kernel/aarch64/actions.hpp
  - src/emel/kernel/aarch64/sm.hpp
  - tests/kernel/aarch64_tests.cpp
  - tests/kernel/lifecycle_tests.cpp
autonomous: true
requirements:
  - PORT-05
user_setup: []
must_haves:
  truths:
    - the maintained kernel seam can distinguish supported vectorized q3 execution from the prior scalar helper.
    - q3 path attribution stays backend-local in Phase 18 and does not widen runtime, parity, or benchmark surfaces.
    - focused kernel regression remains green and repo-wide longer-decode parity debt stays explicitly deferred rather than hidden.
  artifacts:
    - `src/emel/kernel/aarch64/context.hpp` holds backend-local q3 path attribution counters.
    - `src/emel/kernel/aarch64/sm.hpp` exposes backend-local q3 attribution only for kernel-seam proof.
    - `tests/kernel/aarch64_tests.cpp` and `tests/kernel/lifecycle_tests.cpp` assert supported q3 requests exercise the vectorized path and preserve canonical behavior.
  key_links:
    - `src/emel/kernel/aarch64/actions.hpp` increments q3 path attribution at the backend seam.
    - `src/emel/kernel/aarch64/sm.hpp` exposes backend-local q3 attribution to maintained kernel tests.
    - `tests/kernel/aarch64_tests.cpp` and `tests/kernel/lifecycle_tests.cpp` assert supported q3 path selection without widening runtime wrappers.
    - `src/emel/kernel/any.hpp` and `src/emel/generator/sm.hpp` remain unchanged in this phase.
    - `scripts/quality_gates.sh` remains the required repo-wide verification gate.
---

<objective>
Add truthful kernel-seam proof for supported q3 vectorized execution and close focused regression.

Purpose: satisfy the proof half of `PORT-05` for Phase 18 while staying inside backend-local
surfaces. Output: the AArch64 kernel seam can report supported q3 vectorized dispatch and the
focused tests pass, while any repo-wide longer-decode parity failure remains explicitly recorded as
deferred debt.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add backend-local q3 path attribution and seam proof</name>
  <files>src/emel/kernel/aarch64/context.hpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/aarch64/sm.hpp, tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp</files>
  <read_first>src/emel/kernel/aarch64/context.hpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/aarch64/sm.hpp, tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp</read_first>
  <action>Add backend-local counters or equivalent test-visible attribution for supported q3
  vectorized execution only. Expose that attribution through `aarch64::sm` if needed for kernel
  tests, keep it out of broader runtime wrappers, and extend kernel/lifecycle doctests to assert
  that supported q3 requests exercise the vectorized path while unsupported or non-q3 requests do
  not make false claims.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*kernel_aarch64*q3*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*kernel_aarch64*q3*' --no-breaks</automated>
  <acceptance_criteria>
    - supported q3 requests are distinguishable from the old scalar helper at the maintained kernel seam
    - attribution stays local to the AArch64 backend seam
    - q6 still does not make vectorized claims in Phase 18
  </acceptance_criteria>
  <done>The kernel seam publishes truthful q3 vectorized-path attribution.</done>
</task>

<task type="auto">
  <name>Task 2: Run focused regression and record deferred repo-wide parity debt</name>
  <files>src/emel/kernel/aarch64/context.hpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/aarch64/sm.hpp, tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp</files>
  <read_first>tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp, scripts/quality_gates.sh</read_first>
  <action>Run focused kernel tests for q3 path correctness and attribution, then run the required
  repo quality gates. If the existing widened longer-decode parity surface still fails, record it
  as deferred debt instead of stretching Phase 18 into parity repair. Do not update parity,
  benchmark, or snapshot artifacts in this phase.</action>
  <verify>scripts/quality_gates.sh</verify>
  <automated>scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - focused kernel and lifecycle tests pass
    - any repo-wide parity failure is documented accurately rather than misattributed to the q3 seam
    - no benchmark or snapshot files are modified in Phase 18
  </acceptance_criteria>
  <done>Phase 18 closes with maintained kernel-seam proof and explicit deferred verification debt if the wider parity gate still fails.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build/zig/emel_tests_bin --test-case='*kernel_aarch64*q3*' --no-breaks`
- [ ] `rg 'q3.*dispatch|vectorized.*q3|optimized_q3' src/emel/kernel/aarch64/context.hpp src/emel/kernel/aarch64/actions.hpp src/emel/kernel/aarch64/sm.hpp tests/kernel/aarch64_tests.cpp tests/kernel/lifecycle_tests.cpp`
- [ ] any repo-wide parity debt is recorded explicitly if `scripts/quality_gates.sh` still fails outside the q3 seam
- [ ] no benchmark or snapshot artifacts changed
</verification>

<output>
After completion, create `.planning/phases/18-vectorized-q3-k-kernel/18-02-SUMMARY.md`
</output>
