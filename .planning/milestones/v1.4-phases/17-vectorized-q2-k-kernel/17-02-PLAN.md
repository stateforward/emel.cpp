---
phase: 17-vectorized-q2-k-kernel
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/emel/kernel/aarch64/context.hpp
  - src/emel/kernel/aarch64/actions.hpp
  - src/emel/kernel/aarch64/sm.hpp
  - tests/kernel/aarch64_tests.cpp
  - tests/kernel/lifecycle_tests.cpp
autonomous: true
requirements:
  - PORT-04
user_setup: []
must_haves:
  truths:
    - the maintained kernel seam can distinguish supported vectorized q2 execution from the prior scalar helper.
    - q2 path attribution stays backend-local in Phase 17 and does not widen runtime, parity, or benchmark surfaces.
    - focused kernel regression and repo quality gates pass after the q2 cutover.
  artifacts:
    - `src/emel/kernel/aarch64/context.hpp` holds backend-local q2 path attribution counters.
    - `src/emel/kernel/aarch64/sm.hpp` exposes backend-local q2 attribution only for kernel-seam proof.
    - `tests/kernel/aarch64_tests.cpp` and `tests/kernel/lifecycle_tests.cpp` assert supported q2 requests exercise the vectorized path and preserve canonical behavior.
  key_links:
    - `src/emel/kernel/aarch64/actions.hpp` increments q2 path attribution at the backend seam.
    - `src/emel/kernel/aarch64/sm.hpp` exposes backend-local q2 attribution to maintained kernel tests.
    - `tests/kernel/aarch64_tests.cpp` and `tests/kernel/lifecycle_tests.cpp` assert supported q2 path selection without widening runtime wrappers.
    - `src/emel/kernel/any.hpp` and `src/emel/generator/sm.hpp` remain unchanged in this phase.
    - `scripts/quality_gates.sh` remains the required repo-wide verification gate.
---

<objective>
Add truthful kernel-seam proof for supported q2 vectorized execution and close focused regression.

Purpose: satisfy the proof half of `PORT-04` for Phase 17 while staying inside backend-local
surfaces. Output: the AArch64 kernel seam can report supported q2 vectorized dispatch and the
focused tests plus required quality gates pass.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add backend-local q2 path attribution and seam proof</name>
  <files>src/emel/kernel/aarch64/context.hpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/aarch64/sm.hpp, tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp</files>
  <read_first>src/emel/kernel/aarch64/context.hpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/aarch64/sm.hpp, tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp</read_first>
  <action>Add backend-local counters or equivalent test-visible attribution for supported q2
  vectorized execution only. Expose that attribution through `aarch64::sm` if needed for kernel
  tests, keep it out of broader runtime wrappers, and extend kernel/lifecycle doctests to assert
  that supported q2 requests exercise the vectorized path while unsupported or non-q2 requests do
  not make false claims.</action>
  <verify>ctest --test-dir build/zig --output-on-failure -R '(aarch64_tests|lifecycle_tests)'</verify>
  <automated>ctest --test-dir build/zig --output-on-failure -R '(aarch64_tests|lifecycle_tests)'</automated>
  <acceptance_criteria>
    - supported q2 requests are distinguishable from the old scalar helper at the maintained kernel seam
    - attribution stays local to the AArch64 backend seam
    - q3 and q6 still do not make vectorized claims in Phase 17
  </acceptance_criteria>
  <done>The kernel seam publishes truthful q2 vectorized-path attribution.</done>
</task>

<task type="auto">
  <name>Task 2: Run focused regression and required repo gates</name>
  <files>src/emel/kernel/aarch64/context.hpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/aarch64/sm.hpp, tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp</files>
  <read_first>tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp, scripts/quality_gates.sh</read_first>
  <action>Run focused kernel tests for q2 path correctness and attribution, then run the required
  repo quality gates. Keep any non-blocking warnings documented but unchanged. Do not update
  parity, benchmark, or snapshot artifacts in this phase.</action>
  <verify>scripts/quality_gates.sh</verify>
  <automated>scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - focused kernel and lifecycle tests pass
    - `scripts/quality_gates.sh` passes under current repo policy
    - no paritychecker, benchmark, or snapshot files are modified in Phase 17
  </acceptance_criteria>
  <done>Phase 17 closes with maintained kernel-seam proof and required repo verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --test-dir build/zig --output-on-failure -R '(aarch64_tests|lifecycle_tests)'`
- [ ] `scripts/quality_gates.sh`
- [ ] `rg 'q2.*dispatch|vectorized.*q2|optimized_q2' src/emel/kernel/aarch64/context.hpp src/emel/kernel/aarch64/actions.hpp src/emel/kernel/aarch64/sm.hpp tests/kernel/aarch64_tests.cpp tests/kernel/lifecycle_tests.cpp`
- [ ] no paritychecker, benchmark, or snapshot artifacts changed
</verification>

<output>
After completion, create `.planning/phases/17-vectorized-q2-k-kernel/17-02-SUMMARY.md`
</output>
