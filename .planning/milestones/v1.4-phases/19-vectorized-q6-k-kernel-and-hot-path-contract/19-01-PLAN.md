---
phase: 19-vectorized-q6-k-kernel-and-hot-path-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/kernel/aarch64_tests.cpp
  - src/emel/kernel/aarch64/actions.hpp
autonomous: true
requirements:
  - PORT-06
must_haves:
  truths:
    - the AArch64 backend exposes a failing-first q6-focused proof that is missing until the vectorized q6 seam exists.
    - the q6 cutover stays inside the existing backend quantized mul-mat seam with no SML row or event changes.
    - supported q6 requests remain numerically equivalent to the maintained scalar path.
  artifacts:
    - `tests/kernel/aarch64_tests.cpp` contains q6-specific failing-first proof for the missing vectorized seam.
    - `src/emel/kernel/aarch64/actions.hpp` contains a backend-local q6 row helper built from the existing q6 NEON block helper.
    - `execute_neon_mul_mat(...)` routes only the `dtype_q6_k` branch through the new row helper.
  key_links:
    - `src/emel/kernel/detail.hpp::dot_q6_k_q8_k_row_scalar(...)` remains the correctness oracle.
    - `src/emel/kernel/aarch64/actions.hpp::dot_q6_k_q8_k_block_neon(...)` remains the arithmetic building block for the vectorized q6 row path.
---

<objective>
Land and lock the backend-local vectorized q6 row kernel through the existing AArch64 quantized
matmul seam.

Purpose: satisfy the implementation half of `PORT-06` without widening scope. Output: supported
`q6_K x q8_K` requests execute through an EMEL-owned vectorized AArch64 row helper and still match
the maintained scalar result.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add failing-first q6 seam tests</name>
  <files>tests/kernel/aarch64_tests.cpp</files>
  <read_first>tests/kernel/aarch64_tests.cpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/detail.hpp</read_first>
  <action>Add q6-focused doctest coverage that makes the missing vectorized q6 seam visible before
  the implementation lands. The proof should stay at the AArch64 kernel seam, assert scalar
  equivalence on the maintained operand layout, and fail or compile-fail until the q6 vectorized
  row helper exists.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*kernel_aarch64_q6*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*kernel_aarch64_q6*' --no-breaks</automated>
  <acceptance_criteria>
    - the new test is q6-specific rather than only exercising the combined q2/q3/q6 loop
    - the first version fails until the vectorized q6 seam exists
    - no runtime or benchmark files are modified in this task
  </acceptance_criteria>
  <done>A failing-first q6 seam proof exists.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the vectorized q6 row helper wired and scalar-equivalent on the maintained seam</name>
  <files>src/emel/kernel/aarch64/actions.hpp, tests/kernel/aarch64_tests.cpp</files>
  <read_first>src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/detail.hpp, tests/kernel/aarch64_tests.cpp, AGENTS.md</read_first>
  <action>Add a backend-local `dot_q6_k_q8_k_row_neon(...)` helper that accumulates the staged
  `block_q8_k` operands via the existing q6 NEON block arithmetic, then route only the
  `dtype_q6_k` branch in `execute_neon_mul_mat(...)` to it. Make the q6 doctest pass and preserve
  scalar equivalence on the maintained operand layout.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*kernel_aarch64_q6*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*kernel_aarch64_q6*' --no-breaks</automated>
  <acceptance_criteria>
    - q6 supported requests stop calling the shared scalar row helper from the AArch64 q6 branch
    - q2/q3 behavior remains unchanged
    - q6 SIMD output matches the scalar reference on the maintained operand contract
  </acceptance_criteria>
  <done>The q6 backend seam uses an EMEL-owned vectorized AArch64 row helper.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build/zig/emel_tests_bin --test-case='*kernel_aarch64_q6*' --no-breaks`
- [ ] `rg 'dot_q6_k_q8_k_row_neon|dtype_q6_k' src/emel/kernel/aarch64/actions.hpp tests/kernel/aarch64_tests.cpp`
- [ ] no SML transition rows, event payloads, or public C API signatures changed
</verification>

<output>
After completion, create `.planning/phases/19-vectorized-q6-k-kernel-and-hot-path-contract/19-01-SUMMARY.md`
</output>
