---
phase: 19-vectorized-q6-k-kernel-and-hot-path-contract
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/emel/kernel/aarch64/context.hpp
  - src/emel/kernel/aarch64/actions.hpp
  - src/emel/kernel/aarch64/sm.hpp
  - tests/allocation_tracker.hpp
  - tests/graph/graph_tests.cpp
  - tests/kernel/aarch64_tests.cpp
  - tests/kernel/lifecycle_tests.cpp
autonomous: true
requirements:
  - PORT-06
  - PORT-07
must_haves:
  truths:
    - the maintained kernel seam can distinguish supported vectorized q6 execution from the prior scalar helper.
    - supported maintained q2/q3/q6 optimized requests prove zero-allocation dispatch through the existing quantized operand path.
    - the supported optimized quantized path no longer depends on shared scalar row helpers for q2/q3/q6 on canonical AArch64 execution.
  artifacts:
    - `src/emel/kernel/aarch64/context.hpp` holds backend-local q6 path attribution counters.
    - `src/emel/kernel/aarch64/sm.hpp` exposes backend-local q6 attribution only for kernel-seam proof.
    - `tests/allocation_tracker.hpp` exposes the existing test-binary allocation tracker for kernel tests.
    - `tests/kernel/aarch64_tests.cpp` and `tests/kernel/lifecycle_tests.cpp` assert q6 optimized-path proof plus alloc-free cross-dtype dispatch.
  key_links:
    - `src/emel/kernel/aarch64/actions.hpp` increments q6 path attribution at the backend seam.
    - `tests/graph/graph_tests.cpp` remains the single home of the global allocation hook definitions.
    - `src/emel/kernel/any.hpp` and `src/emel/generator/sm.hpp` remain unchanged in this phase.
    - `scripts/quality_gates.sh` remains the required repo-wide verification gate.
---

<objective>
Add truthful q6 kernel-seam proof and lock the maintained optimized quantized hot path to
alloc-free quantized execution for q2/q3/q6.

Purpose: satisfy the proof half of `PORT-06` plus `PORT-07` while staying inside backend-local and
test-only surfaces. Output: the AArch64 kernel seam can report supported q6 vectorized dispatch,
supported q2/q3/q6 dispatch proves zero allocations, and any repo-wide longer-decode parity
failure remains explicitly recorded as deferred debt.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add backend-local q6 path attribution and alloc-free cross-dtype proof</name>
  <files>src/emel/kernel/aarch64/context.hpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/aarch64/sm.hpp, tests/allocation_tracker.hpp, tests/graph/graph_tests.cpp, tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp</files>
  <read_first>src/emel/kernel/aarch64/context.hpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/aarch64/sm.hpp, tests/graph/graph_tests.cpp, tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp</read_first>
  <action>Add backend-local q6 counters, expose them through `aarch64::sm`, surface the existing
  test allocation tracker through a shared test helper, and extend kernel/lifecycle doctests to
  assert that supported q6 requests exercise the vectorized path while supported q2/q3/q6
  quantized dispatch remains alloc-free and stays out of shared fallback on canonical AArch64
  execution.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*kernel_aarch64*q6*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*kernel_aarch64*q6*' --no-breaks</automated>
  <acceptance_criteria>
    - supported q6 requests are distinguishable from the old scalar helper at the maintained kernel seam
    - supported q2/q3/q6 requests prove zero allocations during dispatch under the shared test tracker
    - q2/q3/q6 supported AArch64 execution does not make shared scalar fallback claims
  </acceptance_criteria>
  <done>The kernel seam publishes truthful q6 vectorized-path attribution and alloc-free cross-dtype proof.</done>
</task>

<task type="auto">
  <name>Task 2: Run focused regression and record deferred repo-wide parity debt</name>
  <files>src/emel/kernel/aarch64/context.hpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/aarch64/sm.hpp, tests/allocation_tracker.hpp, tests/graph/graph_tests.cpp, tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp</files>
  <read_first>tests/kernel/aarch64_tests.cpp, tests/kernel/lifecycle_tests.cpp, scripts/quality_gates.sh</read_first>
  <action>Run focused kernel tests for q6 correctness, attribution, and alloc-free proof, then run
  the required repo quality gates. If the existing widened longer-decode parity surface still
  fails, record it as deferred debt instead of stretching Phase 19 into parity repair. Do not
  update parity, benchmark, or snapshot artifacts in this phase.</action>
  <verify>scripts/quality_gates.sh</verify>
  <automated>scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - focused kernel and lifecycle tests pass
    - any repo-wide parity failure is documented accurately rather than misattributed to the q6 seam or alloc-free proof
    - no benchmark or snapshot artifacts are intentionally updated in Phase 19
  </acceptance_criteria>
  <done>Phase 19 closes with maintained q6 seam proof, alloc-free hot-path proof, and explicit deferred verification debt if the wider parity gate still fails.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build/zig/emel_tests_bin --test-case='*kernel_aarch64*q6*' --no-breaks`
- [ ] `rg 'q6.*dispatch|optimized_q6|shared_q6|allocation_scope' src/emel/kernel/aarch64/context.hpp src/emel/kernel/aarch64/actions.hpp src/emel/kernel/aarch64/sm.hpp tests/allocation_tracker.hpp tests/kernel/aarch64_tests.cpp tests/kernel/lifecycle_tests.cpp tests/graph/graph_tests.cpp`
- [ ] any repo-wide parity debt is recorded explicitly if `scripts/quality_gates.sh` still fails outside the q6 seam
- [ ] no benchmark or snapshot artifacts changed by Phase 19 implementation
</verification>

<output>
After completion, create `.planning/phases/19-vectorized-q6-k-kernel-and-hot-path-contract/19-02-SUMMARY.md`
</output>
