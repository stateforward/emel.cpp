---
phase: 20-runtime-integration-and-proof
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/paritychecker_tests.cpp
autonomous: true
requirements:
  - PAR-04
must_haves:
  truths:
    - canonical quantized generation proof publishes q2/q3/q6 runtime attribution on the shipped path.
    - the active parity gate matches the user-approved `1/10` scope while keeping longer lengths explicitly deferred.
  artifacts:
    - `tools/paritychecker/parity_runner.cpp` prints q2/q3/q6 runtime attribution and validates canonical AArch64 optimized-path claims.
    - `tools/paritychecker/paritychecker_tests.cpp` checks q2/q3/q6 runtime attribution on the canonical fixture and enforces active `1/10` parity lengths.
---

<objective>
Publish maintained canonical runtime proof for q2/q3/q6 vectorized execution on the shipped
generation path.

Purpose: satisfy the active parity/publication half of Phase 20 at the user-approved scope.
Output: `paritychecker --generation` reports q2/q3/q6 runtime attribution, and the maintained
blocking generation parity test passes for `1` and `10` token lengths.
</objective>

<output>
After completion, create `.planning/phases/20-runtime-integration-and-proof/20-02-SUMMARY.md`
</output>
