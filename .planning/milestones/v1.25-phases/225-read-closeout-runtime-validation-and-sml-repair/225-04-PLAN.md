---
phase: 225-read-closeout-runtime-validation-and-sml-repair
plan: 04
type: execute
wave: 4
depends_on:
  - 225-03
files_modified:
  - tools/bench/generation_bench.cpp
  - tools/bench/diarization/sortformer_fixture.hpp
  - tools/embedded_size/emel_probe/main.cpp
  - tools/paritychecker/parity_engines.cpp
  - tests/model/loader/lifecycle_tests.cpp
autonomous: true
requirements:
  - TIO-03
  - VAL-04
  - VAL-01
rule_constraints:
  - RC-VALIDATE-CURRENT-SOURCE
  - RC-NO-TOOL-ONLY-READ-SCAFFOLD
  - RC-NO-SNAPSHOT-UPDATE-WITHOUT-CONSENT
  - RC-DOMAIN-BOUNDARY-CHECKS
must_haves:
  truths:
    - "Maintained generation, Sortformer, embedded probe, and paritychecker read_copy lanes provide request-owned model-loader io_load_spans."
    - "Maintained lanes still use public emel::io::source::load_file_bytes and never include io/read detail or direct read events."
    - "Maintained read_copy evidence still reports used_io_strategy from public model-loader completion data."
  artifacts:
    - path: "tools/bench/generation_bench.cpp"
      provides: "generation benchmark io_load_spans caller wiring"
    - path: "tools/bench/diarization/sortformer_fixture.hpp"
      provides: "Sortformer fixture io_load_spans caller wiring"
    - path: "tools/embedded_size/emel_probe/main.cpp"
      provides: "embedded probe io_load_spans caller wiring"
    - path: "tools/paritychecker/parity_engines.cpp"
      provides: "paritychecker EMEL lane io_load_spans caller wiring"
    - path: "tests/model/loader/lifecycle_tests.cpp"
      provides: "maintained caller guardrails"
  key_links:
    - from: "tools/*"
      to: "src/emel/model/loader/events.hpp"
      via: "request.io_load_spans"
    - from: "tests/model/loader/lifecycle_tests.cpp"
      to: "tools/*"
      via: "source guardrail requiring io_load_spans and public source API"
---

<objective>
Wire maintained callers to the new model-loader batch scratch surface and keep read/copy
evidence on public runtime paths.

Purpose: Plans 01-03 add the repaired batch route; maintained tools must provide the
request-owned scratch span required by that route without reintroducing tool-local read
scaffolds or actor-internal reach-through.
Output: maintained caller updates and guardrails proving public source/runtime usage.
</objective>

<execution_context>
@/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.25-MILESTONE-AUDIT.md
@.planning/phases/225-read-closeout-runtime-validation-and-sml-repair/225-CONTEXT.md
@.planning/phases/225-read-closeout-runtime-validation-and-sml-repair/225-RESEARCH.md
@.planning/phases/225-read-closeout-runtime-validation-and-sml-repair/225-PATTERNS.md
@AGENTS.md
@docs/rules/sml.rules.md
@docs/rules/cpp.rules.md
@src/emel/model/loader/events.hpp
@tools/bench/generation_bench.cpp
@tools/bench/diarization/sortformer_fixture.hpp
@tools/embedded_size/emel_probe/main.cpp
@tools/paritychecker/parity_engines.cpp
@tests/model/loader/lifecycle_tests.cpp
</context>

<rule_constraints>
| ID | Directive | Enforced By |
|----|-----------|-------------|
| RC-VALIDATE-CURRENT-SOURCE | Runtime evidence must be source-backed through maintained caller code, not planning artifacts alone. | Tasks 1, 2 |
| RC-NO-TOOL-ONLY-READ-SCAFFOLD | Maintained lanes must use public `emel::io::source::load_file_bytes` and model-loader strategy surfaces, not direct `io/read` detail/events. | Tasks 1, 2 |
| RC-NO-SNAPSHOT-UPDATE-WITHOUT-CONSENT | Do not update snapshots or benchmark baselines in this phase; no explicit Phase 225 consent exists. | Tasks 1, 2 |
| RC-DOMAIN-BOUNDARY-CHECKS | Because model/I/O boundaries and maintained lanes are touched, run `scripts/check_domain_boundaries.sh`. | Task 2 |
</rule_constraints>

<source_audit>
| Source Item | Status | Plan Coverage |
|-------------|--------|---------------|
| GOAL: preserve public maintained read/copy evidence | COVERED | Maintained lanes provide `io_load_spans` to model-loader batch route. |
| REQ TIO-03 | COVERED | Public runtime surfaces only; no actor-internal read reach-through. |
| REQ VAL-04 | COVERED | Maintained output remains tied to `ev.used_io_strategy`. |
| REQ VAL-01 | COVERED | Focused model/batch doctest command validates caller guardrails. |
| REQ VAL-03 | COVERED_BY_OTHER_PLAN | Plans 05 and 06 reconcile docs and publish validation. |
| CONTEXT deferred scope | EXCLUDED | No mmap, staged/chunked, async, device, model-family, snapshot, or benchmark-regression override work. |
</source_audit>

<tasks>
<task type="auto">
  <name>Task 1: Wire maintained callers to request-owned io_load_spans</name>
  <files>tools/bench/generation_bench.cpp, tools/bench/diarization/sortformer_fixture.hpp, tools/embedded_size/emel_probe/main.cpp, tools/paritychecker/parity_engines.cpp</files>
  <read_first>
    AGENTS.md; docs/rules/sml.rules.md; docs/rules/cpp.rules.md;
    src/emel/model/loader/events.hpp; tools/bench/generation_bench.cpp;
    tools/bench/diarization/sortformer_fixture.hpp; tools/embedded_size/emel_probe/main.cpp;
    tools/paritychecker/parity_engines.cpp
  </read_first>
  <action>
    In each maintained EMEL lane that creates `emel::model::loader::event::load` with
    `EMEL_MODEL_LOAD_IO_STRATEGY` or `bind_model_load_io_strategy`, allocate or reuse
    request-lifetime fixed-capacity storage named `io_load_spans` with element type
    `emel::io::event::tensor_load_span` and capacity at least
    `emel::model::data::k_max_tensors`. Set
    `request.io_load_spans = std::span<emel::io::event::tensor_load_span>{io_load_spans.data(), io_load_spans.size()}`.
    Keep existing `emel::io::source::load_file_bytes` setup-time source loading and
    existing `.used_io_strategy = ev.used_io_strategy` output propagation. Do not add
    `emel/io/read/events.hpp`, `emel/io/read/detail.hpp`, direct `read_tensor_request`
    construction, snapshot updates, or benchmark-regression overrides.
  </action>
  <verify>
    <automated>rg -n "io_load_spans|emel::io::source::load_file_bytes|\\.used_io_strategy = ev.used_io_strategy" tools/bench/generation_bench.cpp tools/bench/diarization/sortformer_fixture.hpp tools/embedded_size/emel_probe/main.cpp tools/paritychecker/parity_engines.cpp</automated>
    <automated>test "$(rg -n "emel/io/read/detail.hpp|emel/io/read/events.hpp|read_tensor_request|EMEL_QUALITY_GATES_ALLOW_BENCH_REGRESSION|--update" tools/bench/generation_bench.cpp tools/bench/diarization/sortformer_fixture.hpp tools/embedded_size/emel_probe/main.cpp tools/paritychecker/parity_engines.cpp | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    Every maintained tool file contains `io_load_spans`, still contains
    `emel::io::source::load_file_bytes`, still propagates `.used_io_strategy =
    ev.used_io_strategy`, and contains no `emel/io/read/detail.hpp`,
    `emel/io/read/events.hpp`, `read_tensor_request`, snapshot update command, or
    benchmark-regression override.
  </acceptance_criteria>
  <done>Maintained lanes supply model-loader batch scratch through public request-owned surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Extend maintained caller guardrails</name>
  <files>tests/model/loader/lifecycle_tests.cpp</files>
  <read_first>
    AGENTS.md; docs/rules/sml.rules.md; docs/rules/cpp.rules.md;
    tests/model/loader/lifecycle_tests.cpp; tools/bench/generation_bench.cpp;
    tools/bench/diarization/sortformer_fixture.hpp; tools/embedded_size/emel_probe/main.cpp;
    tools/paritychecker/parity_engines.cpp
  </read_first>
  <action>
    Extend the maintained-tool source guardrail in `tests/model/loader/lifecycle_tests.cpp`
    so it requires the literal `io_load_spans` in all four maintained caller files and keeps
    existing checks for `emel::io::source::load_file_bytes`, absence of
    `emel/io/read/detail.hpp`, absence of `emel/io/read/events.hpp`, absence of direct
    `read_tensor_request`, and preservation of `.used_io_strategy = ev.used_io_strategy`.
    Keep the guardrail scoped to maintained EMEL lanes and do not weaken existing checks.
  </action>
  <verify>
    <automated>ctest --test-dir build/zig --output-on-failure -R emel_tests_model_and_batch</automated>
    <automated>scripts/check_domain_boundaries.sh</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "io_load_spans|emel::io::source::load_file_bytes|used_io_strategy" tests/model/loader/lifecycle_tests.cpp` finds the new/retained guardrail checks; focused model/batch CTest passes or records a dyld launch blocker for Plan 06 validation; domain-boundary checks pass.
  </acceptance_criteria>
  <done>Guardrails prove maintained caller wiring remains public and source-backed.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintained tools -> model/loader | Tool lanes supply source bytes, strategy selection, and batch scratch to public model-loader events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-225-10 | R | `used_io_strategy` maintained tool output | high | mitigate | Guardrails require `.used_io_strategy = ev.used_io_strategy` and no direct read-event construction in tools. |
| T-225-11 | T/I | maintained caller source loading | high | mitigate | Guardrails require public `emel::io::source::load_file_bytes` and reject `io/read` detail/event reach-through. |
| T-225-12 | D | caller scratch storage | medium | mitigate | Use request-lifetime fixed-capacity `io_load_spans`; no dispatch-time allocation or retained callbacks. |
</threat_model>

<verification>
Run focused caller validation:
`ctest --test-dir build/zig --output-on-failure -R emel_tests_model_and_batch`
`scripts/check_domain_boundaries.sh`
</verification>

<success_criteria>
Maintained lanes supply request-owned `io_load_spans`, still prove read/copy through public
runtime surfaces, and contain no tool-only read scaffolds, snapshot updates, or benchmark
override instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/225-read-closeout-runtime-validation-and-sml-repair/225-04-SUMMARY.md`.
</output>
