---
phase: 225-read-closeout-runtime-validation-and-sml-repair
plan: 03
type: execute
wave: 3
depends_on:
  - 225-02
files_modified:
  - src/emel/model/loader/events.hpp
  - src/emel/model/loader/guards.hpp
  - src/emel/model/loader/actions.hpp
  - src/emel/model/loader/sm.hpp
  - tests/model/loader/lifecycle_tests.cpp
autonomous: true
requirements:
  - TIO-03
  - VAL-04
  - VAL-01
rule_constraints:
  - RC-SML-RTC-NO-QUEUE
  - RC-SML-NO-PER-TENSOR-COMPLETION
  - RC-SML-GUARDS-OWN-RUNTIME-CHOICE
  - RC-MODEL-LOADER-ORCHESTRATION-ONLY
  - RC-USED-STRATEGY-AFTER-PUBLIC-RUNTIME
must_haves:
  truths:
    - "model/loader no longer loops over tensor effects and calls io_loader->process_event from an action."
    - "model/loader dispatches one public io/loader load_tensor_batch request for read_copy tensor effects."
    - "used_io_strategy becomes read_copy only after the public batch route succeeds."
  artifacts:
    - path: "src/emel/model/loader/actions.hpp"
      provides: "effect_bind_io_load_batch and effect_dispatch_io_load_batch"
    - path: "src/emel/model/loader/sm.hpp"
      provides: "explicit batch I/O decision states and guards"
    - path: "tests/model/loader/lifecycle_tests.cpp"
      provides: "model-loader public-dispatch read_copy batch evidence"
  key_links:
    - from: "src/emel/model/loader/actions.hpp"
      to: "src/emel/io/loader/sm.hpp"
      via: "one ev.request.io_loader->process_event(load_tensor_batch)"
    - from: "src/emel/model/loader/sm.hpp"
      to: "src/emel/model/loader/guards.hpp"
      via: "io_load_done_all and io_load_error_raised decide publication"
---

<objective>
Replace the audited model-loader per-tensor child-dispatch action loop with one public batch
dispatch through `io/loader`.

Purpose: The maintained read/copy path must remain public and truthful while satisfying the SML
rule that per-item orchestration does not live in action loops or unbounded completion chains.
Output: request-owned batch scratch, explicit model-loader guards/transitions, one batch dispatch
action, and model-loader doctests/guardrails.
</objective>

<execution_context>
@/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.25-MILESTONE-AUDIT.md
@.planning/phases/225-read-closeout-runtime-validation-and-sml-repair/225-RESEARCH.md
@.planning/phases/225-read-closeout-runtime-validation-and-sml-repair/225-PATTERNS.md
@AGENTS.md
@docs/rules/sml.rules.md
@docs/rules/cpp.rules.md
@src/emel/io/events.hpp
@src/emel/io/loader/events.hpp
@src/emel/model/loader/events.hpp
@src/emel/model/loader/guards.hpp
@src/emel/model/loader/actions.hpp
@src/emel/model/loader/sm.hpp
@tests/model/loader/lifecycle_tests.cpp

<interfaces>
Audited legacy risk in `src/emel/model/loader/actions.hpp`:
```cpp
struct effect_dispatch_io_loads {
  void operator()(const event::load_runtime &ev, context &) const noexcept {
    for (uint32_t index = 0u; index < effect_count; ++index) {
      emel::io::loader::event::load_tensor load{tensor, policy};
      static_cast<void>(ev.request.io_loader->process_event(load));
    }
  }
};
```

Plan 02 provides:
```cpp
namespace emel::io::loader::event {
struct load_tensor_batch {
  std::span<const tensor_load_span> tensors;
  const strategy_policy &policy;
};
}
```
</interfaces>
</context>

<rule_constraints>
| ID | Directive | Enforced By |
|----|-----------|-------------|
| RC-SML-RTC-NO-QUEUE | Keep the actor chain synchronous RTC with no queue/defer/mailbox mechanism. | Tasks 1, 2 |
| RC-SML-NO-PER-TENSOR-COMPLETION | Do not replace the action loop with a 65,536-step completion/anonymous loop. | Task 2 |
| RC-SML-GUARDS-OWN-RUNTIME-CHOICE | Batch readiness, unavailable strategy, success, and failure choices live in `guards.hpp` and `sm.hpp`. | Task 2 |
| RC-MODEL-LOADER-ORCHESTRATION-ONLY | `model/loader` may prepare metadata spans and dispatch to `io/loader`; it must not copy source bytes or include `io/read` events/detail. | Tasks 1, 2 |
| RC-USED-STRATEGY-AFTER-PUBLIC-RUNTIME | `used_io_strategy` must be set to `read_copy` only after public `io/loader -> io/read` success. | Task 2 |
</rule_constraints>

<source_audit>
| Source Item | Status | Plan Coverage |
|-------------|--------|---------------|
| GOAL gap 2: model/loader action loop dispatching per tensor | COVERED | Removes `effect_dispatch_io_loads` and adds one batch dispatch. |
| GOAL gap 4: preserve public read/copy evidence | COVERED | Keeps used strategy marking after successful public batch completion. |
| REQ TIO-03 | COVERED | Public runtime surfaces only; no low-level read logic in model-loader. |
| REQ VAL-04 | COVERED | `read_copy` evidence gated by batch done. |
| REQ VAL-01 | COVERED | model-loader doctests and state/guardrail checks. |
| REQ VAL-03 | COVERED_BY_OTHER_PLAN | Plan 04 updates docs and closeout artifacts. |
| CONTEXT deferred scope | EXCLUDED | No mmap, staged/chunked, async, device, or model-family changes. |
</source_audit>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add model-loader batch orchestration regression tests</name>
  <files>tests/model/loader/lifecycle_tests.cpp</files>
  <read_first>
    AGENTS.md; docs/rules/sml.rules.md; docs/rules/cpp.rules.md;
    .planning/v1.25-MILESTONE-AUDIT.md;
    src/emel/model/loader/actions.hpp; src/emel/model/loader/sm.hpp;
    tests/model/loader/lifecycle_tests.cpp
  </read_first>
  <behavior>
    - Public model-loader read_copy load succeeds with two planned tensor effects, one `io/loader` batch dispatch, and `owner.used_io_strategy == strategy_kind::read_copy`.
    - Public model-loader read_copy load with missing batch scratch fails with `io_strategy_unavailable` and `owner.used_io_strategy == strategy_kind::none`.
    - Source guardrail rejects `effect_dispatch_io_loads`, `for (` plus `io_loader->process_event`, and direct `emel/io/read/events.hpp` includes in model-loader actions.
  </behavior>
  <action>
    Add failing doctests named exactly `model loader read copy uses one io loader batch dispatch`
    and `model loader read copy requires request owned io batch span`. Extend the existing
    `model loader io boundary uses actor events without helper exposure` guardrail so it checks:
    `actions_source.find("effect_dispatch_io_loads") == std::string::npos`,
    `actions_source.find("effect_dispatch_io_load_batch") != std::string::npos`, and
    the source slice for `effect_dispatch_io_load_batch` contains one `io_loader->process_event(load)`
    and no `for (` loop.
  </action>
  <verify>
    <automated>ctest --test-dir build/zig --output-on-failure -R emel_tests_model_and_batch</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "model loader read copy uses one io loader batch dispatch|model loader read copy requires request owned io batch span|effect_dispatch_io_loads" tests/model/loader/lifecycle_tests.cpp` finds the new tests/guardrail, and the tests fail before Task 2 removes the audited action loop.
  </acceptance_criteria>
  <done>Regression coverage exists for the audited SML readiness gap and public evidence behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Replace model-loader per-tensor I/O loop with one batch dispatch</name>
  <files>src/emel/model/loader/events.hpp, src/emel/model/loader/guards.hpp, src/emel/model/loader/actions.hpp, src/emel/model/loader/sm.hpp, tests/model/loader/lifecycle_tests.cpp</files>
  <read_first>
    AGENTS.md; docs/rules/sml.rules.md; docs/rules/cpp.rules.md;
    src/emel/io/events.hpp; src/emel/io/loader/events.hpp;
    src/emel/model/loader/events.hpp; src/emel/model/loader/guards.hpp;
    src/emel/model/loader/actions.hpp; src/emel/model/loader/sm.hpp;
    tests/model/loader/lifecycle_tests.cpp
  </read_first>
  <action>
    In `events.hpp`, include `emel/io/events.hpp` and add request-owned
    `std::span<emel::io::event::tensor_load_span> io_load_spans = {};` to
    `event::load`. Do not include `emel/io/read/events.hpp` in model-loader files.

    In `events.hpp`, extend `events::io_load_done` with `uint64_t bytes_done` and extend
    `events::io_load_error` with `emel::error::type strategy_err` and
    `uint32_t failed_index`. These fields are the batch done/error carrier inside
    `event::io_phase_events`; do not add them to persistent `action::context`.

    In `guards.hpp`, add `io_load_batch_span_ready` and `io_load_batch_span_missing`
    predicates that compare `ev.request.io_load_spans.size()` against
    `ev.tensor_events.plan_done.effect_count`. In `actions.hpp`, delete
    `effect_dispatch_io_loads` and its inline constexpr. Update
    `effect_reset_io_load_events` so it resets `load_done.raised`,
    `load_done.expected_count`, `load_done.done_count`, `load_done.bytes_done`,
    `load_error.raised`, `load_error.err`, `load_error.strategy_err`, and
    `load_error.failed_index`.

    Add concrete batch callbacks named
    `effect_record_io_load_batch_done_event(void *, const emel::io::loader::events::load_tensor_batch_done &) noexcept`
    and
    `effect_record_io_load_batch_error_event(void *, const emel::io::loader::events::load_tensor_batch_error &) noexcept`.
    The done callback must cast the object to `event::io_phase_events *`, set
    `load_done.raised = true`, `load_error.raised = false`,
    `load_done.done_count = ev.done_count`, and `load_done.bytes_done = ev.bytes_done`.
    The error callback must set `load_done.raised = false`, `load_error.raised = true`,
    `load_error.err = ev.err`, `load_error.strategy_err = ev.strategy_err`, and
    `load_error.failed_index = ev.failed_index`.

    Add `effect_dispatch_io_load_batch` that resets I/O phase events for `effect_count`,
    fills `ev.request.io_load_spans[index]` from each `effect_request` plus `model_path`,
    `file_image`, and `file_size`, then constructs one
    `emel::io::loader::event::load_tensor_batch load{span, policy}`. Attach
    `load.on_done = {ev.io_events, effect_record_io_load_batch_done_event}` and
    `load.on_error = {ev.io_events, effect_record_io_load_batch_error_event}` before calling
    `ev.request.io_loader->process_event(load)` once. The span-fill loop is allowed only as
    metadata binding for the already selected batch phase; it must not call `process_event`,
    choose strategy, choose outcome, allocate, or include `emel/io/read/events.hpp`.

    In `sm.hpp`, route `state_tensor_plan_decision` with
    `tensor_plan_done_with_io_strategy_with_loader && io_load_batch_span_ready` to a batch
    dispatch state using `action::effect_dispatch_io_load_batch`; route
    `io_load_batch_span_missing` to `effect_mark_io_strategy_unavailable`. Keep
    `action::effect_mark_io_strategy_used` after `guard::io_load_done_all`.
  </action>
  <verify>
    <automated>ctest --test-dir build/zig --output-on-failure -R emel_tests_model_and_batch</automated>
    <automated>rg -n "io_load_spans|io_load_batch_span_ready|effect_dispatch_io_load_batch|effect_record_io_load_batch_done_event|effect_record_io_load_batch_error_event" src/emel/model/loader/events.hpp src/emel/model/loader/guards.hpp src/emel/model/loader/actions.hpp src/emel/model/loader/sm.hpp</automated>
    <automated>test "$(rg -n "effect_dispatch_io_loads|for \\(.*io_loader->process_event|emel/io/read/events.hpp" src/emel/model/loader/actions.hpp | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    `src/emel/model/loader/actions.hpp` no longer contains `effect_dispatch_io_loads`; the same file contains `effect_dispatch_io_load_batch`; `rg -n "for \\(.*io_loader->process_event|emel/io/read/events.hpp|read_tensor_request" src/emel/model/loader` returns no matches; model-loader focused tests pass or Plan 04 records the dyld launch blocker with source-backed substitute evidence.
  </acceptance_criteria>
  <done>`model/loader` prepares one request-owned batch span, dispatches one public `io/loader` batch event, and marks `used_io_strategy` only after batch success.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model/loader -> io/loader | Model tensor effect metadata is converted to public I/O tensor spans. |
| model/loader -> public evidence callbacks | `load_done`/`load_error` expose requested and used strategy evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-225-07 | R | `effect_mark_io_strategy_used` | high | mitigate | Keep mark action after `io_load_done_all`; missing span and batch error paths must leave `used_io_strategy == none`. |
| T-225-08 | D | `effect_dispatch_io_load_batch` | high | mitigate | One child actor dispatch per model-load I/O phase; no per-tensor `process_event` loop and no completion-chain iteration. |
| T-225-09 | T | `io_load_spans` request scratch | medium | mitigate | Guard `io_load_batch_span_ready` must prove scratch capacity before the action writes spans. |
</threat_model>

<verification>
Run:
`ctest --test-dir build/zig --output-on-failure -R emel_tests_model_and_batch`
`rg -n "effect_dispatch_io_loads|for \\(.*io_loader->process_event|emel/io/read/events.hpp|read_tensor_request" src/emel/model/loader`
</verification>

<success_criteria>
The audited model-loader action loop is gone, the maintained read/copy path uses
`model/loader -> io/loader -> io/read`, and public `used_io_strategy` reports `read_copy` only
after successful public runtime execution.
</success_criteria>

<output>
After completion, create `.planning/phases/225-read-closeout-runtime-validation-and-sml-repair/225-03-SUMMARY.md`.
</output>
