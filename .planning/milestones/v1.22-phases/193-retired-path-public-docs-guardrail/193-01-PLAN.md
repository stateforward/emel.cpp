---
phase: 193-retired-path-public-docs-guardrail
plan: 01
type: execute
wave: 1
requirements:
  - CUTOVER-03
  - CUTOVER-04
  - IO-02
rule_constraints:
  - Public docs must not describe the retired weight-loader callback path as maintained truth.
  - Guardrails must catch semantic stale retired-owner prose.
  - The future `emel/io` strategy seam remains deferred.
---

# Phase 193 Plan 01

## Objective

Remove stale public retired-owner wording and extend the domain-boundary guardrail so equivalent
stale prose fails before milestone closeout.

## Tasks

1. Add a semantic public-doc prose check to `scripts/check_domain_boundaries.sh`.
2. Reproduce the audit gap by running the guardrail against the still-stale `docs/roadmap.md`.
3. Update `docs/roadmap.md` to describe tensor-owned model loading and deferred concrete I/O
   strategy work.
4. Rerun boundary, lint, stale-prose search, and scoped quality-gate checks.

<rule_constraints>
- Do not weaken the existing exact retired-owner path and identifier checks.
- Do not add public claims for asynchronous loading or backend-specific I/O implementation in this
  milestone.
</rule_constraints>

## Verification

- `scripts/check_domain_boundaries.sh`
- `rg -n 'weight loader|weight-loader|weight-loading|loader callback parity|async[[:space:]]+upload|loader/parser/weight loader|loader/parser/weight-loader' docs/roadmap.md`
- `scripts/lint_snapshot.sh`
- `EMEL_QUALITY_GATES_CHANGED_FILES='docs/roadmap.md:scripts/check_domain_boundaries.sh' scripts/quality_gates.sh`
