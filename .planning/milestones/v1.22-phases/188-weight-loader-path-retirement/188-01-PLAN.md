---
phase: 188-weight-loader-path-retirement
plan: 01
type: execute
wave: 1
requirements:
  - CUTOVER-01
  - CUTOVER-02
  - CUTOVER-03
---

# Phase 188 Plan 01

## Objective

Remove the old weight-loader owner surface and avoid introducing a replacement shadow owner.

## Tasks

1. Delete `src/emel/model/weight_loader/**`.
2. Delete the old weight-loader lifecycle test target and CMake entry.
3. Remove top-level `WeightLoader` alias exposure.
4. Verify no maintained source, tool, test, or doc path still references `weight_loader` or
   `load_weights`.

## Verification

- `rg -n "weight_loader|load_weights|model/weight_loader|WeightLoader" src tools tests docs CMakeLists.txt -g '!tmp/**'`
- Scoped quality-gate build, benchmark, coverage, and parity lanes.
