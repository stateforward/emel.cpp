---
phase: 191-ownership-guardrail-closeout
plan: 01
type: execute
wave: 1
requirements:
  - CUTOVER-03
  - CUTOVER-04
rule_constraints:
  - Guardrails must be source-backed, not artifact-only claims.
  - Retired `model/weight_loader` paths must not remain as parallel ownership docs or aliases.
  - Phase 191 must not introduce the deferred future `emel/io` strategy implementation.
---

# Phase 191 Plan 01

## Objective

Close the ownership guardrail and stale-artifact audit gaps so the milestone can reject a
reintroduced model weight-loader owner before closeout.

## Tasks

1. Extend the existing domain-boundary check to fail on retired weight-loader owner paths and stale
   retired owner references.
2. Remove or refresh stale generated planning/codebase artifacts that still describe
   `model/weight_loader` as a live owner.
3. Refresh `tools/mock_main.cpp` to the current loader/tensor public event shape or bound it as a
   manual probe.
4. Verify lint snapshots, boundary checks, the refreshed mock harness, and the scoped quality gate.
5. Update requirements, roadmap, and state to show CUTOVER-03/CUTOVER-04 closure.

## Verification

- `scripts/check_domain_boundaries.sh`
- `scripts/lint_snapshot.sh`
- Zig compile check for `tools/mock_main.cpp`
- Scoped `scripts/quality_gates.sh` with generation and Sortformer benchmark suites
