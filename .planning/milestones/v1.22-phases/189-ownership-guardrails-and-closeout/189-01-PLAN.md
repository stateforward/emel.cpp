---
phase: 189-ownership-guardrails-and-closeout
plan: 01
type: execute
wave: 1
requirements:
  - CUTOVER-04
---

# Phase 189 Plan 01

## Objective

Validate the ownership cutover and close the milestone without weakening the quality gate.

## Tasks

1. Add focused tensor and loader guard/action coverage for new ownership paths.
2. Search maintained source, tools, tests, docs, and CMake for retired weight-loader references.
3. Run the default changed-file scoped quality gate with generation and Sortformer benchmark suites.
4. Update the lint snapshot only if explicitly approved, because the retired file list changes the
   snapshot baseline.

## Verification

- `cmake --build build/zig --target emel_tests_bin --parallel`
- `ctest --test-dir build/zig --output-on-failure -R 'emel_tests_(model_and_batch|diarization|kernel_and_graph)'`
- `cmake --build build/paritychecker_zig --target paritychecker_tests --parallel`
- `ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests`
- `EMEL_QUALITY_GATES_CHANGED_FILES=... EMEL_QUALITY_GATES_BENCH_SUITE="generation,diarization_sortformer" scripts/quality_gates.sh`
