---
phase: 153
plan: 01
title: "Parity runner config ownership closure"
status: pending
requirements:
  - PARITY-01
rule_constraints:
  - "AGENTS.md parity harnesses must preserve EMEL/reference lane isolation."
  - "AGENTS.md requires explicit maintained-path proof instead of artifact-only closeout."
  - "docs/rules/sml.rules.md remains authoritative for actor-boundary constraints touched by tests."
---

# Plan 153-01: Parity Runner Config Ownership Closure

<rule_constraints>
- Keep the change scoped to `tools/paritychecker` and planning artifacts.
- Do not change parity semantics, maintained fixtures, output schemas, or reference-lane ownership.
- Preserve existing CLI exit codes: usage/validation errors return `2`, parity failures return the
  engine result.
- Keep source checks deterministic and allocation concerns irrelevant to non-hot-path CLI parsing.
</rule_constraints>

## Goal

Make the shared parity runner own CLI/config parsing and request normalization so `PARITY-01` is
source-backed by live code.

## Tasks

1. Move CLI parsing, usage text, text-file loading, and CLI validation from `parity_main.cpp` into
   the runner boundary.
2. Expose a runner-owned CLI entrypoint from `parity_runner.hpp` and keep `parity_main.cpp` as a
   minimal process shim.
3. Add tests for runner-owned CLI behavior and source ownership.
4. Run focused paritychecker tests and changed-file scoped quality gates.

## Verification

- `git diff --check`
- `cmake --build build/paritychecker_zig --target paritychecker_tests -j2`
- `ctest --test-dir build/paritychecker_zig --output-on-failure`
- `cmake --build build/paritychecker_zig --target paritychecker -j2`
- `EMEL_QUALITY_GATES_CHANGED_FILES="tools/paritychecker/parity_runner.hpp tools/paritychecker/parity_runner.cpp tools/paritychecker/parity_main.cpp tools/paritychecker/paritychecker_tests.cpp .planning/REQUIREMENTS.md .planning/ROADMAP.md" scripts/quality_gates.sh`
