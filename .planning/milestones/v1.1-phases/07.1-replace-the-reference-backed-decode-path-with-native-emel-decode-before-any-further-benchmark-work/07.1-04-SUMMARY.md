---
phase: 07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work
plan: 04
subsystem: generation-benchmark-truth
tags: [generation, benchmark, native-backend, compare, truthfulness]
requires:
  - phase: 07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work
    provides: paritychecker-native backend proof from 07.1-03
provides:
  - truthful native-backend reuse in `tools/bench`
  - seam-audited benchmark proof for the canonical EMEL generation case
  - a clean handoff into Phase 8 compare-output work
affects: [08-generation-compare-output, 09-benchmark-integration-hardening]
tech-stack:
  added: []
  patterns: [shared backend reuse, env-gated seam audit, case-scoped compare verification]
key-files:
  created: []
  modified: [tools/bench/CMakeLists.txt, tools/bench/generation_bench.cpp]
key-decisions:
  - "Reuse the paritychecker-proven native backend directly inside `tools/bench` instead of keeping a benchmark-only decode shim."
  - "Gate the seam audit behind an env var so verification can prove the boundary without perturbing normal benchmark snapshot output."
  - "Keep compare output formatting unchanged in this phase and reserve presentation hardening for Phase 8."
patterns-established:
  - "Benchmark truth checks can run through the normal `scripts/bench.sh` surface when extra audit output is emitted on stderr only."
  - "The canonical generation benchmark now measures the same native EMEL decode path already proven in paritychecker."
requirements-completed: [TRUTH-03]
duration: 35min
completed: 2026-03-10
---

# Phase 07.1 Plan 4 Summary

**`tools/bench` now measures the same native EMEL generation path as paritychecker**

## Accomplishments

- Updated [generation_bench.cpp](/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/tools/bench/generation_bench.cpp) so the EMEL benchmark session prepares and uses the shared native backend instead of its old reference-backed decode context.
- Added the shared backend source to [CMakeLists.txt](/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/tools/bench/CMakeLists.txt) so `bench_runner` builds the same native path paritychecker now uses.
- Added an env-gated seam audit in [generation_bench.cpp](/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/tools/bench/generation_bench.cpp) that proves the EMEL benchmark case records zero reference-wrapper hits while the explicit reference benchmark case still records positive hits.

## Task Commits

None. Execution stayed local with `commit_docs` disabled.

## Verification

- `cmake --build build/bench_tools_ninja --parallel --target bench_runner`
- `EMEL_BENCH_CASE_INDEX=7 EMEL_BENCH_AUDIT_GENERATION_SEAMS=1 EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 BENCH_BUILD_DIR=build/bench_tools_phase071_emel scripts/bench.sh --emel-only`
- `EMEL_BENCH_CASE_INDEX=7 EMEL_BENCH_AUDIT_GENERATION_SEAMS=1 EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 BENCH_COMPARE_BUILD_DIR=build/bench_tools_phase071_compare scripts/bench.sh --compare`
- `rg -n "llama_decode|llama_get_logits_ith" tools/generation_backend.cpp tools/bench/generation_bench.cpp tools/paritychecker/parity_runner.cpp`

## Deviations from Plan

- The shared native backend itself did not need additional edits in this wave; the remaining work was bench-side reuse and seam auditing only.
- I used isolated bench build directories for the audit commands because the default shared bench build tree was already in use by a concurrent repo-wide gate run.

## Next Readiness

- Phase 07.1 is ready to close.
- Phase 8 can now focus on compare-output hardening instead of debating whether the benchmark path is still circular.

---
*Phase: 07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work*
*Completed: 2026-03-10*
