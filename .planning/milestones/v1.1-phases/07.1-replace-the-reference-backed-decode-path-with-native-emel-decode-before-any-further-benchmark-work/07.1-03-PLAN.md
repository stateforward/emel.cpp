---
phase: 07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work
plan: 03
type: execute
wave: 3
depends_on:
  - "07.1-02"
files_modified:
  - tools/generation_backend.hpp
  - tools/generation_backend.cpp
  - tools/paritychecker/CMakeLists.txt
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/paritychecker_tests.cpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - TRUTH-01
  - TRUTH-02
user_setup: []
must_haves:
  truths:
    - paritychecker's EMEL generation path performs native decode/logits extraction without
      touching the reference decode wrappers.
    - the explicit parity/reference path still uses the reference wrappers, preserving an honest
      external comparison boundary.
  artifacts:
    - one shared native generation backend exists under `tools/` and paritychecker uses it for the
      EMEL path instead of a `llama_context_ptr`-centered backend.
    - paritychecker truthfulness tests prove zero reference-wrapper hits on the EMEL path and
      positive hits on the explicit reference path.
  key_links:
    - the canonical paritychecker slice becomes the truthful source of evidence that later benchmark
      work must reuse.
---

<objective>
Replace paritychecker's EMEL-side reference decode backend with a native EMEL decode backend built
on the new substrate.

Purpose: Make the existing parity truth surface honest before benchmark comparison resumes.
Output: Paritychecker generation still passes on the canonical fixture, but the EMEL path now owns
its decode/logits work natively while the explicit reference path remains separate.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-RESEARCH.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-VALIDATION.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-02-PLAN.md
@tools/generation_backend.hpp
@tools/generation_backend.cpp
@tools/paritychecker/CMakeLists.txt
@tools/paritychecker/parity_runner.cpp
@tools/paritychecker/paritychecker_tests.cpp
@tests/generator/lifecycle_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce one shared native generation backend for the canonical parity slice</name>
  <files>tools/generation_backend.hpp, tools/generation_backend.cpp, tools/paritychecker/CMakeLists.txt, tools/paritychecker/parity_runner.cpp</files>
  <action>Implement a shared tool-level native backend that consumes the canonical typed substrate
  from `src/` and provides real validate/prepare/alloc/bind/run/extract hooks for the EMEL path.
  Wire paritychecker's EMEL generation path to this backend, keep the existing generator/graph
  actor structure intact, and leave the explicit reference generation helper as the only place that
  still performs direct reference decode.</action>
  <verify>build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 1 --dump</verify>
  <done>The canonical paritychecker EMEL path runs through a native backend instead of the current
  reference-backed decode seam.</done>
</task>

<task type="auto">
  <name>Task 2: Turn the seam instrumentation into a passing truthfulness regression</name>
  <files>tools/paritychecker/paritychecker_tests.cpp, tests/generator/lifecycle_tests.cpp, tools/paritychecker/parity_runner.cpp</files>
  <action>Make the seam assertions pass against the new native backend. The passing state must
  prove the EMEL path does not touch the reference wrappers while the explicit reference path still
  does. Keep lifecycle coverage honest about what is orchestration-only versus native decode proof.</action>
  <verify>ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests</verify>
  <done>Automated tests prove the paritychecker EMEL path is no longer circular for decode/logits
  extraction, while the direct reference path remains available for comparison.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/paritychecker_zig --parallel --target paritychecker paritychecker_tests`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 1 --dump`
- [ ] `ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests`
- [ ] `build/zig/emel_tests_bin --dt-test-case="*generator*"`
- [ ] `rg -n "llama_decode|llama_get_logits_ith" tools/generation_backend.cpp tools/paritychecker/parity_runner.cpp`
- [ ] The EMEL paritychecker path shows zero reference-wrapper hits while the explicit reference path still shows positive hits
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Paritychecker becomes a truthful native-EMEL generation surface for the canonical Llama-68M slice
  </success_criteria>

<output>
After completion, create `.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-03-SUMMARY.md`
</output>
