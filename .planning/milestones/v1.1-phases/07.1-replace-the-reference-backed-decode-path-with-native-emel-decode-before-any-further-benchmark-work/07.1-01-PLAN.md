---
phase: 07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/paritychecker/parity_runner.cpp
  - tools/bench/generation_bench.cpp
  - tools/paritychecker/paritychecker_tests.cpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - TRUTH-01
  - TRUTH-02
user_setup: []
must_haves:
  truths:
    - the repo has explicit reference-only decode seams that can distinguish legitimate reference
      decode from the EMEL path's own decode work.
    - generator and parity tests stop overstating the current reference-backed backend as native
      EMEL inference.
  artifacts:
    - `tools/paritychecker/parity_runner.cpp` and `tools/bench/generation_bench.cpp` route
      reference decode/logits access through explicit wrappers or counters instead of anonymous
      inline use in the EMEL backend path.
    - paritychecker and generator lifecycle coverage can observe whether the EMEL path touches the
      reference seam.
  key_links:
    - the same seam instrumentation is reusable by both paritychecker and bench, so later native
      backend work proves one truth contract instead of two diverging ones.
---

<objective>
Isolate the reference decode seam and make the corrective truth contract testable.

Purpose: Prepare the repo for a real failing-first native-backend fix by making every legitimate
reference decode call explicit, observable, and separate from the EMEL path.
Output: Explicit reference-only wrappers/counters plus tightened lifecycle/parity assertions that
stop calling the current reference-backed EMEL path "native inference".
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-RESEARCH.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-VALIDATION.md
@tools/paritychecker/parity_runner.cpp
@tools/paritychecker/paritychecker_tests.cpp
@tools/bench/generation_bench.cpp
@tests/generator/lifecycle_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make every legitimate reference decode/logits call explicit and observable</name>
  <files>tools/paritychecker/parity_runner.cpp, tools/bench/generation_bench.cpp</files>
  <action>Refactor the current inline `llama_decode` / `llama_get_logits_ith` usage into explicit
  reference-only wrappers or counters that can distinguish EMEL-path decode from the direct
  reference path. Preserve current behavior for now, but remove anonymous direct reference calls
  from the EMEL backend helpers so the next plan can prove zero reference usage on the EMEL path
  with automated checks.</action>
  <verify>cmake --build build/paritychecker_zig --parallel --target paritychecker paritychecker_tests && cmake --build build/bench_tools_ninja --parallel --target bench_runner</verify>
  <done>The reference seam is explicit and tool-local, and later tests can count or forbid EMEL-path
  hits deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten lifecycle and parity wording so fake-backend success is no longer treated as native inference</name>
  <files>tools/paritychecker/paritychecker_tests.cpp, tests/generator/lifecycle_tests.cpp</files>
  <action>Update the current tests and wording so they distinguish orchestration success from native
  decode proof. Add seam-aware assertions or scaffolding that make the current defect observable,
  but keep this plan green by reserving the actual native-no-reference expectation for the next
  plan's failing-first implementation step.</action>
  <verify>build/zig/emel_tests_bin --dt-test-case="*generator*"</verify>
  <done>The repo no longer overclaims the current backend, and the seam is ready for a truthful
  native-backend replacement in paritychecker.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/paritychecker_zig --parallel --target paritychecker paritychecker_tests`
- [ ] `cmake --build build/bench_tools_ninja --parallel --target bench_runner`
- [ ] `ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests`
- [ ] `build/zig/emel_tests_bin --dt-test-case="*generator*"`
- [ ] The EMEL-path reference seam is explicit and observable instead of anonymous inline calls
- [ ] No test or user-facing wording still calls the current reference-backed backend "native EMEL decode"
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo can now distinguish legitimate reference-only decode from the EMEL path's own decode seam
  </success_criteria>

<output>
After completion, create `.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-01-SUMMARY.md`
</output>
