---
phase: 07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work
plan: 02
type: execute
wave: 2
depends_on:
  - "07.1-01"
files_modified:
  - src/emel/model/data.hpp
  - src/emel/model/data.cpp
  - src/emel/graph/assembler/actions.hpp
  - src/emel/graph/assembler/reserve_build_pass/actions.hpp
  - src/emel/graph/assembler/reuse_decision_pass/actions.hpp
  - tests/model/loader/lifecycle_tests.cpp
  - tests/graph/assembler/assembler_tests.cpp
  - tests/graph/graph_tests.cpp
autonomous: true
requirements:
  - TRUTH-01
  - TRUTH-02
user_setup: []
must_haves:
  truths:
    - `src/` exposes one canonical Llama typed execution substrate instead of leaving the EMEL
      path dependent on dummy integers and flat tensor scans inside tools.
    - the canonical substrate is sufficient for paritychecker to stop using the reference decode
      seam in the next plan without changing SML machine structure.
  artifacts:
    - `src/` contains stable typed views for the canonical Llama-68M tensor set plus deterministic
      validation for missing or malformed required tensors.
    - real canonical `model_topology`, `prefill_plan`, and `decode_plan` objects exist for the
      bounded prefill/decode slice, replacing the current placeholder handles.
  key_links:
    - paritychecker and bench will both consume one shared native backend built on this substrate,
      so the next plans do not invent separate model bindings.
---

<objective>
Add the missing canonical Llama typed substrate in `src/` before touching paritychecker wiring
again.

Purpose: Turn the discovered blocker into explicit execution work so the native paritychecker
backend has real typed data and real plan/topology objects to target.
Output: The repo has typed canonical Llama tensor views plus concrete `model_topology`,
`prefill_plan`, and `decode_plan` objects for the bounded generation slice, all without changing
the existing generator/graph SML structure.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-RESEARCH.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-VALIDATION.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-01-PLAN.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/deferred-items.md
@src/emel/model/data.hpp
@src/emel/model/loader/events.hpp
@src/emel/graph/events.hpp
@src/emel/graph/actions.hpp
@src/emel/graph/assembler/actions.hpp
@src/emel/graph/assembler/reserve_build_pass/actions.hpp
@src/emel/graph/assembler/reuse_decision_pass/actions.hpp
@src/emel/generator/events.hpp
@src/emel/generator/actions.hpp
@tests/model/loader/lifecycle_tests.cpp
@tests/graph/assembler/assembler_tests.cpp
@tests/graph/graph_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize canonical typed Llama execution views from loaded model data</name>
  <files>src/emel/model/data.hpp, src/emel/model/data.cpp, tests/model/loader/lifecycle_tests.cpp</files>
  <action>Introduce deterministic typed accessors or `detail.hpp` helpers that map the loaded flat
  tensor table in `emel::model::data` into stable named views for the canonical Llama-68M slice.
  The mapping must validate required tensors, shapes, and canonical metadata instead of relying on
  tool-local flat-name scans. Add failing-first unit coverage that proves missing required tensors
  or malformed canonical metadata are rejected clearly.</action>
  <verify>build/zig/emel_tests_bin --dt-test-case="*model*"</verify>
  <done>`src/` can expose and validate the canonical Llama execution data needed by native decode
  without depending on tool-local naming hacks.</done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder topology and step-plan handles with real canonical descriptors</name>
  <files>src/emel/graph/assembler/actions.hpp, src/emel/graph/assembler/reserve_build_pass/actions.hpp, src/emel/graph/assembler/reuse_decision_pass/actions.hpp, tests/graph/assembler/assembler_tests.cpp, tests/graph/graph_tests.cpp</files>
  <action>Build one real canonical `model_topology`, `prefill_plan`, and `decode_plan` for the
  bounded Llama-68M generation slice. The assembler must derive node counts, tensor counts, and
  reuse decisions from those descriptors rather than from dummy tool-local integers. Keep the
  existing graph/generator state-machine structure intact; if execution proves that impossible, stop
  and request user approval before changing machine structure.</action>
  <verify>build/zig/emel_tests_bin --dt-test-case="*graph*"</verify>
  <done>The next plan can wire paritychecker to real canonical descriptors in `src/` instead of
  placeholder handles.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/zig --parallel --target emel_tests`
- [ ] `build/zig/emel_tests_bin --dt-test-case="*model*"`
- [ ] `build/zig/emel_tests_bin --dt-test-case="*graph*"`
- [ ] The canonical Llama fixture has deterministic typed execution views in `src/`
- [ ] The canonical Llama reserve/compute descriptors no longer depend on placeholder integers from tools
- [ ] No state-machine structure changes were made without user approval
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The missing typed substrate is now in `src/`, so native paritychecker wiring can proceed honestly
  </success_criteria>

<output>
After completion, create `.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-02-SUMMARY.md`
</output>
