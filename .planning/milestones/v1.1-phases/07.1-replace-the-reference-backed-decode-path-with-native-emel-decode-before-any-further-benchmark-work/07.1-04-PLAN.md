---
phase: 07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work
plan: 04
type: execute
wave: 4
depends_on:
  - "07.1-03"
files_modified:
  - tools/generation_backend.hpp
  - tools/generation_backend.cpp
  - tools/bench/CMakeLists.txt
  - tools/bench/generation_bench.cpp
autonomous: true
requirements:
  - TRUTH-01
  - TRUTH-03
user_setup: []
must_haves:
  truths:
    - the EMEL benchmark case uses the same native backend already proven in paritychecker.
    - benchmark compare mode still retains a direct reference path separate from the EMEL backend.
  artifacts:
    - `tools/bench/generation_bench.cpp` no longer prepares an EMEL session around a reference
      decode context.
    - shared native backend code under `tools/` is reused by both paritychecker and bench instead
      of being duplicated.
  key_links:
    - Phase 8 will measure the same native EMEL path already proven in paritychecker, so benchmark
      ratios become meaningful again.
---

<objective>
Make the benchmark harness reuse the same native EMEL decode backend already proven in
paritychecker.

Purpose: Remove the benchmark-side circularity so Phase 8 can compare native EMEL against the
explicit reference path honestly.
Output: The canonical bench EMEL generation case runs through the shared native backend, while the
reference compare case remains intact.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-RESEARCH.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-VALIDATION.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-03-PLAN.md
@tools/generation_backend.hpp
@tools/generation_backend.cpp
@tools/bench/CMakeLists.txt
@tools/bench/generation_bench.cpp
@tools/paritychecker/parity_runner.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reuse the shared native backend in `tools/bench` for the EMEL generation case</name>
  <files>tools/generation_backend.hpp, tools/generation_backend.cpp, tools/bench/CMakeLists.txt, tools/bench/generation_bench.cpp</files>
  <action>Remove the EMEL benchmark session's dependency on a reference decode context and point it
  at the shared native backend already proven in paritychecker. Keep the direct reference benchmark
  case intact for compare mode and do not widen scope into benchmark presentation or snapshot-policy
  work reserved for later phases.</action>
  <verify>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --emel-only</verify>
  <done>The canonical EMEL benchmark case uses the same native decode backend as paritychecker
  instead of a reference-backed decode context.</done>
</task>

<task type="auto">
  <name>Task 2: Add benchmark truthfulness verification and seam audit</name>
  <files>tools/bench/generation_bench.cpp, tools/generation_backend.cpp</files>
  <action>Add focused automated checks that prove the EMEL benchmark path does not touch the
  reference decode wrappers, while the explicit benchmark reference path still does. Keep the seam
  audit simple and reproducible so Phase 8 can build compare output on top of a truthful backend
  rather than re-litigating the correction.</action>
  <verify>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare</verify>
  <done>Bench automation proves the EMEL case is native and the direct reference case is still the
  only path that uses reference decode.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/bench_tools_ninja --parallel --target bench_runner`
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --emel-only`
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare`
- [ ] `rg -n "llama_decode|llama_get_logits_ith" tools/generation_backend.cpp tools/bench/generation_bench.cpp tools/paritychecker/parity_runner.cpp`
- [ ] The EMEL benchmark case shows zero reference-wrapper hits while the explicit reference benchmark case still shows positive hits
- [ ] Phase 8 compare output can now measure a truthful native EMEL path instead of a circular reference-backed backend
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `tools/bench` now measures the same native EMEL decode path already proven in paritychecker
  </success_criteria>

<output>
After completion, create `.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-04-SUMMARY.md`
</output>
