---
phase: 09-benchmark-integration-hardening
plan: 01
type: execute
wave: 1
depends_on:
  - 08-02
files_modified:
  - docs/benchmarking.md
  - README.md
autonomous: true
requirements:
  - VBEN-01
user_setup: []
must_haves:
  truths:
    - the canonical generation benchmark continues to run through the existing `scripts/bench.sh --compare` flow.
    - Phase 9 does not reopen Phase 8 compare-surface or runtime pairing work.
  artifacts:
    - user-facing docs show the normal generation compare command, the stable case name, and the interpretation of the printed row.
    - user-facing docs describe generation-specific benchmark env overrides without inventing a second benchmark workflow.
  key_links:
    - Plan 09-02 can refresh snapshots through the existing tooling without guesswork because Plan 09-01 publishes the stable operator runbook first.
---

<objective>
Document the existing generation compare flow as the normal benchmark entrypoint.

Purpose: Satisfy `VBEN-01` by making the already-working canonical generation compare surface
discoverable, repeatable, and interpretable through normal project docs.
Output: Users can run `scripts/bench.sh --compare`, isolate the canonical generation row by stable
case name, and understand the supported generation-specific env overrides without needing a second
benchmark workflow.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-benchmark-integration-hardening/09-RESEARCH.md
@.planning/phases/09-benchmark-integration-hardening/09-VALIDATION.md
@docs/benchmarking.md
@README.md
@scripts/bench.sh
@tools/bench/bench_cases.hpp
@tools/bench/generation_bench.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the canonical generation compare runbook in the existing benchmark docs</name>
  <files>docs/benchmarking.md</files>
  <action>Update the benchmark workflow documentation so it explicitly tells users how to run the
  canonical generation benchmark through `scripts/bench.sh --compare` and how to isolate the
  stable case name `generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1` from the
  normal compare output. Keep the instructions anchored to the existing compare workflow instead of
  inventing a generation-only command.</action>
  <verify>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare | rg '^generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 .* ratio='</verify>
  <done>The benchmark docs publish the canonical generation compare flow through the existing
  script surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add interpretation guidance and supported generation override documentation</name>
  <files>docs/benchmarking.md, README.md</files>
  <action>Document how to read the canonical generation compare row, what the `emel.cpp`,
  `llama.cpp`, and `ratio` fields mean, and which generation-specific env overrides are supported
  for bounded local validation. If the workflow is too hidden from the main project surface, add
  the smallest honest pointer from `README.md` to the benchmark docs without broadening scope into
  new tooling.</action>
  <verify>rg 'generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1|scripts/bench.sh --compare|EMEL_BENCH_GENERATION_' docs/benchmarking.md</verify>
  <done>Users can run the existing compare flow and understand the published generation row from
  the documented workflow alone.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg 'scripts/bench.sh --compare|generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1|EMEL_BENCH_GENERATION_' docs/benchmarking.md`
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare | rg '^generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 .* ratio='`
- [ ] The published docs describe the normal compare command, stable case-name filtering, and row interpretation
- [ ] Any README change is only a discovery pointer to the existing benchmark docs, not a second workflow
- [ ] No Phase 8 compare-surface or runtime behavior is reopened
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `VBEN-01` is satisfied through documentation of the existing compare flow
- The canonical generation benchmark remains documented as one existing script workflow, not a parallel benchmark path
  </success_criteria>

<output>
After completion, create `.planning/phases/09-benchmark-integration-hardening/09-01-SUMMARY.md`
</output>
