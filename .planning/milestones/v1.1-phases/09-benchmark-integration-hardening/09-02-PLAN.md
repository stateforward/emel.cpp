---
phase: 09-benchmark-integration-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - snapshots/bench/benchmarks.txt
  - snapshots/bench/benchmarks_compare.txt
  - docs/benchmarks.md
  - docs/benchmarking.md
autonomous: false
requirements:
  - VBEN-02
user_setup:
  - explicit user approval to refresh benchmark baselines before running `scripts/bench.sh --snapshot --update` or `scripts/bench.sh --compare-update`
must_haves:
  truths:
    - the new generation benchmark is maintained through the existing snapshot/update tooling with no parallel baseline workflow.
    - snapshot churn remains explicit and user-approved per `AGENTS.md`.
  artifacts:
    - `snapshots/bench/benchmarks.txt` contains the canonical generation benchmark entry through the existing snapshot path.
    - `snapshots/bench/benchmarks_compare.txt` and generated `docs/benchmarks.md` contain the canonical generation compare row.
  key_links:
    - Phase 9 closes the milestone by aligning durable baselines and generated docs with the runtime compare surface already published in Phase 8.
---

<objective>
Close snapshot and generated-doc integration for the canonical generation benchmark.

Purpose: Satisfy `VBEN-02` by refreshing the existing benchmark baselines, through explicit user
approval, so the generation case is recognized by the normal snapshot/update tooling and the
generated benchmark docs match the live compare surface.
Output: The canonical generation benchmark appears in both benchmark snapshots and in generated
`docs/benchmarks.md`, with no separate generation-only baseline mechanism.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-benchmark-integration-hardening/09-RESEARCH.md
@.planning/phases/09-benchmark-integration-hardening/09-VALIDATION.md
@docs/benchmarking.md
@docs/benchmarks.md
@scripts/bench.sh
@scripts/generate_docs.sh
@scripts/quality_gates.sh
@tools/docsgen/docsgen.cpp
@snapshots/bench/benchmarks.txt
@snapshots/bench/benchmarks_compare.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the EMEL snapshot baseline through the existing approved snapshot path</name>
  <files>snapshots/bench/benchmarks.txt, docs/benchmarking.md</files>
  <action>Before making any snapshot baseline change, stop and obtain explicit user approval for
  snapshot refresh. Once approved, use the existing `scripts/bench.sh --snapshot --update` path to
  refresh `snapshots/bench/benchmarks.txt` so the canonical generation benchmark is maintained by
  the normal EMEL snapshot tooling. Document the approval-gated update step in the benchmark docs
  if that intent is not already explicit.</action>
  <verify>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --snapshot --compare</verify>
  <done>The normal snapshot gate recognizes the canonical generation benchmark entry without a
  parallel workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the compare snapshot and regenerate benchmark docs through the existing toolchain</name>
  <files>snapshots/bench/benchmarks_compare.txt, docs/benchmarks.md, docs/benchmarking.md</files>
  <action>After the same explicit user approval, run the existing `scripts/bench.sh --compare-update`
  path and regenerate `docs/benchmarks.md` through `scripts/generate_docs.sh` so the canonical
  generation compare row appears in both the compare snapshot and generated docs. Keep the flow
  bound to the existing compare snapshot plus docsgen pipeline and do not hand-edit generated
  benchmark tables.</action>
  <verify>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare-update && scripts/generate_docs.sh && rg 'generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1' snapshots/bench/benchmarks_compare.txt docs/benchmarks.md</verify>
  <done>The canonical generation compare row is durably present in the compare snapshot and in the
  generated benchmark docs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Explicit user approval was obtained before any snapshot baseline modification
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --snapshot --compare`
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare-update`
- [ ] `scripts/generate_docs.sh`
- [ ] `rg 'generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1' snapshots/bench/benchmarks.txt snapshots/bench/benchmarks_compare.txt docs/benchmarks.md`
- [ ] The generation benchmark is maintained through the existing snapshot and docsgen surfaces only
- [ ] No Phase 8 compare-runtime or row-shape work is reopened
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `VBEN-02` is satisfied through the existing snapshot/update workflow with explicit approval
- The durable benchmark baselines and generated docs now match the already-published runtime compare surface
  </success_criteria>

<output>
After completion, create `.planning/phases/09-benchmark-integration-hardening/09-02-SUMMARY.md`
</output>
