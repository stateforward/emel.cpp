---
phase: 08-generation-compare-output
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - tools/bench/bench_main.cpp
  - tools/bench/generation_bench.cpp
  - scripts/bench.sh
autonomous: true
requirements:
  - COMP-01
  - COMP-02
user_setup: []
must_haves:
  truths:
    - the published compare row shows EMEL timing, `llama.cpp` timing, and ratio for the canonical generation workload.
    - truth-support diagnostics remain opt-in and do not pollute normal compare stdout.
  artifacts:
    - the normal `scripts/bench.sh --compare` surface exposes the canonical generation compare row in the existing bench style.
    - any required compare-surface alignment stays minimal and does not turn into broader Phase 9 snapshot-policy work.
  key_links:
    - Phase 9 can focus on snapshot and tooling hardening because Phase 8 already publishes one stable compare row through the normal script surface.
---

<objective>
Publish stable compare evidence for the canonical generation benchmark through the normal bench
surface.

Purpose: Finish `COMP-02` and close Phase 8 by proving that users can inspect one truthful compare
row with EMEL timing, `llama.cpp` timing, and ratio through the existing script-driven workflow.
Output: The standard compare flow exposes one stable generation compare row while seam-audit proof
remains optional and compatible with that output.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-generation-compare-output/08-RESEARCH.md
@.planning/phases/08-generation-compare-output/08-VALIDATION.md
@.planning/phases/07.1-replace-the-reference-backed-decode-path-with-native-emel-decode-before-any-further-benchmark-work/07.1-VERIFICATION.md
@tools/bench/bench_main.cpp
@tools/bench/generation_bench.cpp
@scripts/bench.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the published compare row shape for the canonical generation case</name>
  <files>tools/bench/bench_main.cpp, tools/bench/generation_bench.cpp</files>
  <action>Make the canonical generation compare row explicit and stable in the existing bench
  style so users can inspect EMEL timing, `llama.cpp` timing, and ratio directly from normal
  compare output. Preserve current style compatibility instead of inventing a second reporting
  format.</action>
  <verify>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare</verify>
  <done>The canonical generation compare row has a stable published shape in normal compare
  output.</done>
</task>

<task type="auto">
  <name>Task 2: Prove published compare evidence through the standard script flow</name>
  <files>scripts/bench.sh, tools/bench/bench_main.cpp</files>
  <action>Align the normal `scripts/bench.sh --compare` path only as much as needed so the
  canonical generation compare row is visible and truthful without requiring audit-only flags.
  Keep seam-audit output opt-in and stderr-only so compare snapshots remain compatible with the
  existing tooling surface.</action>
  <verify>EMEL_BENCH_CASE_INDEX=7 EMEL_BENCH_AUDIT_GENERATION_SEAMS=1 EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare</verify>
  <done>The normal compare script surface publishes the canonical generation compare row while
  audit diagnostics remain optional support evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/bench_tools_ninja --parallel --target bench_runner`
- [ ] `EMEL_BENCH_CASE_INDEX=7 build/bench_tools_ninja/bench_runner --mode=compare | rg "emel\\.cpp .* llama\\.cpp .* ratio="`
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare`
- [ ] `EMEL_BENCH_CASE_INDEX=7 EMEL_BENCH_AUDIT_GENERATION_SEAMS=1 EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare`
- [ ] Normal compare stdout includes EMEL timing, `llama.cpp` timing, and ratio for the canonical generation row
- [ ] Seam-audit diagnostics stay opt-in and do not pollute the standard compare row output
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The canonical generation compare row is published through the normal script surface
- Phase 8 finishes without reopening native backend or broader snapshot-policy work
  </success_criteria>

<output>
After completion, create `.planning/phases/08-generation-compare-output/08-02-SUMMARY.md`
</output>
