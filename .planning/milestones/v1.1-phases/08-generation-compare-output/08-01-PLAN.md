---
phase: 08-generation-compare-output
plan: 01
type: execute
wave: 1
depends_on:
  - 07.1-04
files_modified:
  - tools/bench/bench_main.cpp
  - tools/bench/generation_bench.cpp
autonomous: true
requirements:
  - COMP-01
user_setup: []
must_haves:
  truths:
    - compare mode continues to run paired EMEL and explicit reference generation cases for the same canonical Llama-68M workload.
    - Phase 8 does not reopen native backend work already completed in Phase 07.1.
  artifacts:
    - the canonical generation compare row is identifiable and stable in the normal `bench_runner --mode=compare` surface.
    - compare-mode pairing fails clearly if the generation case identity drifts or disappears on either side.
  key_links:
    - Plan 08-02 can publish compare evidence through the existing script flow because Plan 08-01 locks the canonical row contract first.
---

<objective>
Harden the canonical generation compare contract in the existing bench compare flow.

Purpose: Make `COMP-01` truthful at the user-facing compare boundary by ensuring the canonical
generation case remains visibly paired between EMEL and `llama.cpp` in normal compare mode.
Output: The compare path preserves one stable canonical generation row and fails clearly on case
drift instead of silently relying on incidental ordering.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-generation-compare-output/08-RESEARCH.md
@.planning/phases/08-generation-compare-output/08-VALIDATION.md
@tools/bench/bench_main.cpp
@tools/bench/generation_bench.cpp
@tools/bench/bench_cases.hpp
@scripts/bench.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock compare-mode pairing around the canonical generation case</name>
  <files>tools/bench/bench_main.cpp, tools/bench/generation_bench.cpp</files>
  <action>Tighten compare-mode behavior so the canonical generation row is matched by stable case
  identity rather than incidental list position. Keep the normal compare flow and existing bench
  style intact, but fail explicitly if the EMEL or reference generation case disappears, drifts in
  name, or becomes ambiguous.</action>
  <verify>cmake --build build/bench_tools_ninja --parallel --target bench_runner</verify>
  <done>The compare runner enforces a stable canonical generation pairing for the existing
  EMEL-vs-reference workload.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused compare-surface coverage for the generation row</name>
  <files>tools/bench/bench_main.cpp, tools/bench/generation_bench.cpp</files>
  <action>Add the smallest honest generation-row hardening needed so users can observe the
  canonical compare case through `bench_runner --mode=compare` without reopening Phase 07.1
  backend scope. Keep seam auditing opt-in and avoid creating a second compare workflow.</action>
  <verify>EMEL_BENCH_CASE_INDEX=7 build/bench_tools_ninja/bench_runner --mode=compare | rg "^generation/"</verify>
  <done>The canonical generation compare row is stably visible in normal compare output and remains
  grounded in the existing native EMEL path plus explicit reference path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/bench_tools_ninja --parallel --target bench_runner`
- [ ] `EMEL_BENCH_CASE_INDEX=7 build/bench_tools_ninja/bench_runner --mode=compare`
- [ ] `EMEL_BENCH_CASE_INDEX=7 build/bench_tools_ninja/bench_runner --mode=compare | rg "^generation/"`
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare`
- [ ] The canonical generation compare row is produced from the existing native EMEL case and explicit reference case, not a benchmark-only shortcut
- [ ] Case drift or mismatch causes explicit compare failure rather than silent pairing
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No native backend work from Phase 07.1 is reopened
- The canonical generation workload is truthfully present in the normal compare flow
  </success_criteria>

<output>
After completion, create `.planning/phases/08-generation-compare-output/08-01-SUMMARY.md`
</output>
