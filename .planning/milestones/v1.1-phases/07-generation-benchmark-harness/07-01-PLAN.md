---
phase: 07-generation-benchmark-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bench/CMakeLists.txt
  - tools/bench/bench_cases.hpp
  - tools/bench/bench_main.cpp
  - tools/bench/generation_bench.cpp
autonomous: true
requirements:
  - BENCH-01
  - BENCH-02
user_setup: []
must_haves:
  truths:
    - `bench_runner` gains one canonical generation benchmark case for the pinned Llama-68M fixture.
    - the benchmark path reuses the proven generation slice instead of inventing a benchmark-only runtime.
  artifacts:
    - `tools/bench/generation_bench.cpp` contains an EMEL generation benchmark harness for the canonical fixture.
    - `tools/bench/bench_cases.hpp`, `bench_main.cpp`, and `CMakeLists.txt` register and build the new benchmark case.
  key_links:
    - the emel-only bench flow can execute the new generation case with a deterministic bounded workload.
---

<objective>
Add the canonical generation benchmark harness to `tools/bench`.

Purpose: Establish one EMEL benchmark case for the already-proven Llama-68M generation slice so
later compare-mode work has a truthful harness to build on.
Output: `bench_runner --mode=emel` includes one canonical generation benchmark case for
`tests/models/Llama-68M-Chat-v1-Q2_K.gguf`.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/07-generation-benchmark-harness/07-RESEARCH.md
@.planning/phases/07-generation-benchmark-harness/07-VALIDATION.md
@tools/bench/CMakeLists.txt
@tools/bench/bench_cases.hpp
@tools/bench/bench_main.cpp
@tools/paritychecker/parity_runner.cpp
@tests/models/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generation benchmark source wiring and case registration</name>
  <files>tools/bench/CMakeLists.txt, tools/bench/bench_cases.hpp, tools/bench/bench_main.cpp</files>
  <action>Add one new generation benchmark source file to `bench_runner`, declare paired EMEL and
  reference case appenders in `bench_cases.hpp`, and register the case in `bench_main.cpp` so it
  participates in the existing case list. Keep naming stable and scoped to one canonical
  generation workload rather than a matrix.</action>
  <verify>cmake --build build/bench_tools_ninja --parallel --target bench_runner</verify>
  <done>`bench_runner` builds with the new generation benchmark entrypoint wired into the standard
  bench case registry.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the EMEL generation benchmark harness for the canonical Llama-68M workload</name>
  <files>tools/bench/generation_bench.cpp</files>
  <action>Create the new benchmark source and implement one EMEL-only benchmark case for the
  canonical fixture. Reuse the proven generation slice behavior, pin a deterministic prompt and
  token budget, keep model loading and one-time fixture setup outside the timed loop, and make the
  benchmark case bounded enough for repeated iterations with the existing bench config controls.</action>
  <verify>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --emel-only</verify>
  <done>The emel-only bench flow runs one canonical generation case for
  `tests/models/Llama-68M-Chat-v1-Q2_K.gguf` and satisfies the harness portion of BENCH-01 and
  BENCH-02.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/bench_tools_ninja --parallel --target bench_runner`
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --emel-only`
- [ ] `bench_runner --mode=emel` includes one generation benchmark case for the canonical Llama-68M fixture
- [ ] The new benchmark path stays inside `tools/bench/`
- [ ] The workload remains deterministic and bounded
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- One canonical EMEL generation benchmark case exists for the pinned Llama-68M fixture
  </success_criteria>

<output>
After completion, create `.planning/phases/07-generation-benchmark-harness/07-01-SUMMARY.md`
</output>
