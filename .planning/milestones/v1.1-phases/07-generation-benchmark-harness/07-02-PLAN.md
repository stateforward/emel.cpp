---
phase: 07-generation-benchmark-harness
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - tools/bench/generation_bench.cpp
  - tools/bench/bench_main.cpp
autonomous: true
requirements:
  - BENCH-02
  - BENCH-03
user_setup: []
must_haves:
  truths:
    - the benchmark surface explicitly states what portion of generation is being measured.
    - the canonical workload contract is deterministic enough for repeatable bench output.
  artifacts:
    - the generation benchmark case name or output clearly communicates the measured segment.
    - the benchmark harness locks one canonical prompt/token-budget contract rather than drifting into a matrix.
  key_links:
    - the new generation case can be observed through the existing emel-only bench surface before compare-mode hardening begins.
---

<objective>
Harden the generation benchmark contract and make the measured segment explicit.

Purpose: Finish the Phase 7 harness by locking the canonical workload contract and making the
benchmark surface itself tell the user what portion of generation is measured.
Output: The generation benchmark case has stable deterministic defaults and a user-visible
measurement contract.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/07-generation-benchmark-harness/07-RESEARCH.md
@.planning/phases/07-generation-benchmark-harness/07-VALIDATION.md
@tools/bench/bench_main.cpp
@tools/bench/generation_bench.cpp
@scripts/bench.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the bounded workload contract for the canonical generation benchmark</name>
  <files>tools/bench/generation_bench.cpp</files>
  <action>Make the benchmark defaults explicit and stable: pin one canonical prompt, one token
  budget, and one preloaded-request measurement contract. If optional env overrides are needed for
  local experimentation, keep them narrow and ensure the default benchmark name still reflects the
  canonical workload rather than a broad parameter matrix.</action>
  <verify>build/bench_tools_ninja/bench_runner --mode=emel | rg "generation/"</verify>
  <done>The generation benchmark defaults are deterministic and clearly tied to the canonical
  Llama-68M workload.</done>
</task>

<task type="auto">
  <name>Task 2: Make the benchmark surface tell the user what is being measured</name>
  <files>tools/bench/generation_bench.cpp, tools/bench/bench_main.cpp</files>
  <action>Ensure the benchmark case naming and emitted output make the measurement contract obvious,
  such as whether the case measures a preloaded full request path versus some narrower decode-only
  segment. Keep the output compatible with the existing bench snapshot/compare parser and defer full
  EMEL-vs-reference compare presentation to Phase 8.</action>
  <verify>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare</verify>
  <done>The harness satisfies BENCH-03 and is ready for Phase 8 to add truthful compare-mode
  timings.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `build/bench_tools_ninja/bench_runner --mode=emel | rg "generation/"`
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare`
- [ ] The benchmark name or output explicitly communicates the measured generation segment
- [ ] The default workload remains one canonical Llama-68M prompt/token-budget contract
- [ ] No benchmark-only runtime path is introduced outside `tools/bench/`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Phase 7 harness explicitly communicates and enforces the canonical generation benchmark contract
  </success_criteria>

<output>
After completion, create `.planning/phases/07-generation-benchmark-harness/07-02-SUMMARY.md`
</output>
