---
phase: 01-generation-harness-contract
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tools/paritychecker/parity_runner.hpp
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/parity_main.cpp
autonomous: true
requirements:
  - HARN-01
  - HARN-02
user_setup: []
must_haves:
  truths:
    - the generation harness has one explicit first-slice contract for
      `tests/models/Llama-68M-Chat-v1-Q2_K.gguf`.
    - paritychecker can invoke generation mode through `run_parity(...)` with a deterministic
      request envelope and fixture validation at the tool boundary.
  artifacts:
    - parity_runner.cpp contains deterministic fixture-selection or request-normalization helpers
      for the first slice.
    - parity_runner.cpp contains a generation-mode dispatch branch that uses the normalized request
      envelope without routing directly into reference-side generation.
  key_links:
    - the generation request envelope in parity_runner.cpp matches the CLI contract added in
      01-01 and names the pinned Llama-68M fixture unambiguously.
---

<objective>
Pin the first-slice Llama-68M fixture and define the generation harness envelope.

Purpose: Ensure later phases inherit one deterministic target model and one stable request shape
instead of reverse-engineering them during loader and generator integration.
Output: Fixture-resolution logic, request-normalization helpers, and explicit Phase 1 generation
mode behavior that prepares for later phases without faking parity.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-generation-harness-contract/01-RESEARCH.md
@tests/models/README.md
@tools/paritychecker/parity_runner.hpp
@tools/paritychecker/parity_runner.cpp
@tools/paritychecker/parity_main.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode the first-slice fixture contract</name>
  <files>tools/paritychecker/parity_runner.cpp, tools/paritychecker/parity_runner.hpp</files>
  <action>Add deterministic helper logic that resolves or validates the Phase 1 target fixture for
  `tests/models/Llama-68M-Chat-v1-Q2_K.gguf`. Keep the contract explicit so the harness cannot
  silently drift to another `.gguf` file. Reuse existing paritychecker patterns for deterministic
  error output rather than inventing new logging surfaces.</action>
  <verify>rg -n "Llama-68M-Chat-v1-Q2_K.gguf|generation" tools/paritychecker/parity_runner.*</verify>
  <done>The tool has one unambiguous first-slice model contract and deterministic failure behavior
  when the fixture does not match or does not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add a bounded Phase 1 generation harness outcome</name>
  <files>tools/paritychecker/parity_runner.cpp, tools/paritychecker/parity_main.cpp</files>
  <action>Implement the generation-mode dispatch branch far enough to normalize inputs and return a
  deliberate Phase 1 harness result for later phases to build on. Make that result explicit and
  deterministic: the mode should be invokable at the tool boundary, validate the pinned
  Llama-68M contract, and stop before any fake reference-side or EMEL-side generation proof is
  claimed. Keep the boundary honest: Phase 1 sets the harness, Phase 2 begins real EMEL loading.</action>
  <verify>scripts/paritychecker.sh</verify>
  <done>Generation mode has deterministic Phase 1 behavior, the pinned Llama-68M contract is
  enforced, and existing paritychecker verification still passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/paritychecker.sh`
- [ ] `! build/paritychecker_zig/paritychecker --generation --model tests/models/distilgpt2.Q2_K.gguf --text "hello"`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello"`
- [ ] `scripts/test_with_coverage.sh`
- [ ] `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R lint_snapshot`
- [ ] `scripts/quality_gates.sh`
- [ ] The generation mode does not bypass directly into `llama.cpp` generation in Phase 1
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The first-slice fixture and request envelope are explicit and stable for Phase 2
  </success_criteria>

<output>
After completion, create `.planning/phases/01-generation-harness-contract/01-02-SUMMARY.md`
</output>
