---
phase: 01-generation-harness-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/paritychecker/parity_runner.hpp
  - tools/paritychecker/parity_main.cpp
autonomous: true
requirements:
  - HARN-01
user_setup: []
must_haves:
  truths:
    - paritychecker exposes a `generation` mode without regressing the existing tokenizer, GBNF,
      kernel, and Jinja modes.
    - generation mode argument parsing rejects incomplete invocations deterministically.
  artifacts:
    - tools/paritychecker/parity_runner.hpp defines the generation mode and required option fields.
    - tools/paritychecker/parity_main.cpp documents and parses the generation-mode CLI contract.
  key_links:
    - parity_main.cpp parsing and usage text align with the parity_mode enum and parity_options
      fields in parity_runner.hpp.
---

<objective>
Establish the paritychecker generation CLI contract.

Purpose: Create a stable entry surface for the first generation slice before any EMEL loader or
generator wiring begins.
Output: Updated parity mode enum, option schema, usage text, and deterministic argument validation
for generation mode.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-generation-harness-contract/01-RESEARCH.md
@tools/paritychecker/parity_runner.hpp
@tools/paritychecker/parity_main.cpp
@tools/paritychecker/parity_runner.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend paritychecker mode and options schema</name>
  <files>tools/paritychecker/parity_runner.hpp</files>
  <action>Add a `generation` entry to `parity_mode` and extend `parity_options` with the minimum
  fields Phase 1 needs for a stable generation contract, such as generation-mode prompt and any
  bounded decode settings that later phases will consume. Keep the schema narrow and avoid adding
  fields that imply Phase 2-4 behavior prematurely.</action>
  <verify>rg -n "generation" tools/paritychecker/parity_runner.hpp</verify>
  <done>`parity_runner.hpp` exposes a coherent generation-mode contract without disturbing the
  existing mode defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI parsing and usage text for generation mode</name>
  <files>tools/paritychecker/parity_main.cpp</files>
  <action>Extend `parse_args(...)` and `print_usage(...)` to recognize the new generation mode and
  validate its required inputs deterministically. Preserve current behavior for tokenizer, GBNF,
  kernel, and Jinja modes. Do not wire real generation logic here; this plan is only responsible
  for the harness contract.</action>
  <verify>scripts/paritychecker.sh</verify>
  <done>The CLI advertises generation mode, rejects malformed generation invocations, and leaves
  existing paritychecker tests green.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/paritychecker.sh`
- [ ] `build/paritychecker_zig/paritychecker --help`
- [ ] `! build/paritychecker_zig/paritychecker --generation --text "hello"`
- [ ] `scripts/test_with_coverage.sh`
- [ ] `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R lint_snapshot`
- [ ] `scripts/quality_gates.sh`
- [ ] Existing paritychecker modes still parse and run through the standard test target
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 now has a stable generation-mode CLI surface for later wiring
  </success_criteria>

<output>
After completion, create `.planning/phases/01-generation-harness-contract/01-01-SUMMARY.md`
</output>
