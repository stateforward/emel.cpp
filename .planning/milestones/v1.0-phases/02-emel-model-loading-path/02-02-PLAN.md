---
phase: 02-emel-model-loading-path
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - tools/paritychecker/parity_runner.cpp
  - tests/model/loader/lifecycle_tests.cpp
autonomous: true
requirements:
  - LOAD-01
  - LOAD-02
user_setup: []
must_haves:
  truths:
    - paritychecker generation mode publishes deterministic success evidence when EMEL model
      loading completes for the pinned fixture.
    - invalid model paths or loader rejections publish deterministic failure output and non-zero
      exit status at the paritychecker boundary.
  artifacts:
    - tools/paritychecker/parity_runner.cpp captures `events::load_done` and `events::load_error`
      into stable generation-harness output.
    - focused loader-path tests cover the load callback outcome mapping needed by the paritychecker
      harness.
  key_links:
    - `model::loader::events::load_done` and `load_error` outcomes map directly to paritychecker
      stdout/stderr and exit behavior for generation mode.
---

<objective>
Publish deterministic load success and failure signals for the harness.

Purpose: Turn the new EMEL load path into an auditable tool contract so later phases can trust the
loaded model handoff and diagnose failures without guessing where the load failed.
Output: Stable paritychecker load success output, deterministic error mapping, and focused loader
verification for the generation slice.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-emel-model-loading-path/02-RESEARCH.md
@.planning/phases/02-emel-model-loading-path/02-01-PLAN.md
@src/emel/model/loader/events.hpp
@src/emel/model/loader/errors.hpp
@tools/paritychecker/parity_runner.cpp
@tests/model/loader/lifecycle_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture and publish deterministic load success evidence</name>
  <files>tools/paritychecker/parity_runner.cpp</files>
  <action>Add bounded capture structs and callback handlers for `events::load_done` so the
  generation success path reports stable load evidence such as bytes processed, mmap usage, tensor
  count, and layer count. Keep the output Phase 2 scoped: prove the model is loaded and available,
  but do not start generator initialization.</action>
  <verify>build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello"</verify>
  <done>The pinned-fixture success path prints deterministic EMEL load evidence and returns zero
  only after `load_done` has been observed.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize failure-path reporting for missing file and loader rejection</name>
  <files>tools/paritychecker/parity_runner.cpp, tests/model/loader/lifecycle_tests.cpp</files>
  <action>Map missing-file failures and `events::load_error` outcomes into deterministic stderr and
  non-zero exit status at the paritychecker boundary. If helper extraction is needed to keep the
  mapping bounded and testable, add focused loader lifecycle coverage rather than broad subprocess
  CLI coverage, which belongs to Phase 5.</action>
  <verify>! build/paritychecker_zig/paritychecker --generation --model tests/models/does-not-exist.gguf --text "hello"</verify>
  <done>Invalid path and loader-path rejection failures surface explicit, repeatable reasons at the
  tool boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/paritychecker.sh`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello"`
- [ ] `! build/paritychecker_zig/paritychecker --generation --model tests/models/does-not-exist.gguf --text "hello"`
- [ ] `scripts/test_with_coverage.sh`
- [ ] `scripts/quality_gates.sh`
- [ ] Success and failure output both identify the EMEL load path rather than the Phase 1 harness stub
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Generation mode now emits deterministic load success and failure signals for the EMEL load path
  </success_criteria>

<output>
After completion, create `.planning/phases/02-emel-model-loading-path/02-02-SUMMARY.md`
</output>
