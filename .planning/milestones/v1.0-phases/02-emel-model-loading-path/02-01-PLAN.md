---
phase: 02-emel-model-loading-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emel/gguf/loader/detail.hpp
  - src/emel/gguf/loader/actions.hpp
  - tools/paritychecker/parity_runner.cpp
  - tests/gguf/loader/lifecycle_tests.cpp
autonomous: true
requirements:
  - LOAD-01
user_setup: []
must_haves:
  truths:
    - generation mode reaches `emel::model::loader::sm` through real EMEL callback wiring instead
      of stopping at the Phase 1 harness stub or loading only through llama.cpp.
    - the pinned Llama-68M fixture can populate caller-owned `emel::model::data` through the EMEL
      GGUF/model-loading path.
  artifacts:
    - src/emel/gguf/loader/detail.hpp performs real first-slice probe and parse work instead of
      returning stub success.
    - tools/paritychecker/parity_runner.cpp contains a paritychecker-local adapter that drives the
      GGUF and model loader actors for generation mode.
  key_links:
    - parity_runner.cpp generation dispatch constructs `model::loader::event::load` with concrete
      parse/load/map/validate callbacks that use the EMEL loader actors.
---

<objective>
Connect paritychecker generation mode to the EMEL GGUF and model loader events.

Purpose: Replace the Phase 1 placeholder with a real EMEL load entry path so the generation slice
owns a loaded `emel::model::data` before generator initialization work begins.
Output: Concrete GGUF backend support for the pinned slice plus a paritychecker-local adapter that
routes generation mode into `model::loader::sm`.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-emel-model-loading-path/02-RESEARCH.md
@src/emel/gguf/loader/detail.hpp
@src/emel/gguf/loader/sm.hpp
@src/emel/model/loader/events.hpp
@src/emel/model/loader/sm.hpp
@src/emel/model/weight_loader/events.hpp
@tools/paritychecker/parity_runner.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the GGUF first-slice stubs with real probe/parse behavior</name>
  <files>src/emel/gguf/loader/detail.hpp, src/emel/gguf/loader/actions.hpp, tests/gguf/loader/lifecycle_tests.cpp</files>
  <action>Implement the minimum real GGUF backend required for `tests/models/Llama-68M-Chat-v1-Q2_K.gguf`
  behind the existing `gguf::loader` actor. The actor boundary must stay intact: probe computes
  real requirements, bind uses caller-provided storage, and parse populates the bound storage
  deterministically. Keep work allocation-free during dispatch by sizing scratch storage before
  actor calls and confining bulk parsing to detail helpers.</action>
  <verify>scripts/paritychecker.sh</verify>
  <done>The GGUF loader no longer returns placeholder success for the Phase 2 slice and can drive
  real storage requirements plus parse results for the pinned fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Route generation mode into `model::loader::sm` with a paritychecker adapter</name>
  <files>tools/paritychecker/parity_runner.cpp</files>
  <action>Add a paritychecker-local adapter that owns file bytes, scratch storage, callback
  captures, and caller-owned `emel::model::data`, then constructs `model::loader::event::load`
  with concrete `parse_model`, `load_weights`, `map_layers`, and validation callbacks. The
  generation branch must stop after a verified EMEL load; do not initialize the generator actor or
  invoke reference-side generation in this plan.</action>
  <verify>build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello"</verify>
  <done>Generation mode now executes a real EMEL model-load path and leaves a loaded
  `emel::model::data` available for later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/paritychecker.sh`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello"`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello" | rg "load"`
- [ ] `scripts/test_with_coverage.sh`
- [ ] `scripts/quality_gates.sh`
- [ ] The Phase 1 reserved harness message is gone from the success path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Paritychecker generation mode now reaches the EMEL GGUF/model loader path and owns a loaded
  `emel::model::data`
  </success_criteria>

<output>
After completion, create `.planning/phases/02-emel-model-loading-path/02-01-SUMMARY.md`
</output>
