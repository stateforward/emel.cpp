---
phase: 06-fixture-contract-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/paritychecker/paritychecker_tests.cpp
  - tools/paritychecker/parity_runner.cpp
autonomous: true
requirements:
  - HARN-02
user_setup: []
must_haves:
  truths:
    - generation mode no longer accepts arbitrary files that only share the pinned basename.
    - the fixture hardening stays paritychecker-local and does not require `src/` machine changes.
  artifacts:
    - `tools/paritychecker/paritychecker_tests.cpp` contains a subprocess regression that proves a
      same-basename non-canonical fixture is rejected.
    - `tools/paritychecker/parity_runner.cpp` compares against one canonical first-slice fixture
      contract instead of basename-only matching.
  key_links:
    - the existing canonical success path remains green while the new wrong-location same-basename
      regression closes the audit gap in `HARN-02`.
---

<objective>
Reproduce the basename-impostor gap and harden the runtime fixture identity check.

Purpose: Close the audit finding at the narrowest possible boundary by replacing basename-only
acceptance with one canonical fixture contract for generation mode.
Output: Paritychecker rejects a same-basename file outside `tests/models/` and still accepts the
canonical Llama-68M fixture.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-fixture-contract-hardening/06-RESEARCH.md
@.planning/phases/06-fixture-contract-hardening/06-VALIDATION.md
@tools/paritychecker/parity_runner.cpp
@tools/paritychecker/paritychecker_tests.cpp
@tests/models/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a failing subprocess regression for the basename-only impostor case first</name>
  <files>tools/paritychecker/paritychecker_tests.cpp</files>
  <action>Add a generation subprocess doctest that creates or copies a file named
  `Llama-68M-Chat-v1-Q2_K.gguf` outside the canonical `tests/models/` location and proves the
  current basename-only acceptance is too weak. Reuse the existing subprocess capture helpers,
  prefer a copied temporary file over symlink-only behavior, and assert non-zero exit plus a
  reason-specific fixture-contract stderr substring rather than full-path equality.</action>
  <verify>ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests</verify>
  <done>The test suite contains a same-basename wrong-location regression that fails before the
  runtime fixture guard is tightened.</done>
</task>

<task type="auto">
  <name>Task 2: Replace basename-only matching with a canonical fixture-path contract</name>
  <files>tools/paritychecker/parity_runner.cpp</files>
  <action>Introduce a paritychecker-local helper that resolves the one accepted generation fixture
  path and compares the provided `--model` path against that canonical contract using deterministic
  normalization. Keep failure output explicit, preserve the existing missing-model rejection path,
  and do not broaden the check into model-content hashing or new public API surface.</action>
  <verify>build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 1</verify>
  <done>The canonical fixture succeeds, the same-basename impostor fails, and `HARN-02` is closed
  at the runtime boundary without touching `src/` machines.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 1`
- [ ] A same-basename non-canonical fixture path now fails deterministically at the subprocess boundary
- [ ] The existing missing-model failure path still reports `generation load failed: missing model file`
- [ ] No `src/` files are required to close the gap
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Generation mode accepts only the canonical first-slice fixture and rejects same-basename
  impostors outside `tests/models/`
  </success_criteria>

<output>
After completion, create `.planning/phases/06-fixture-contract-hardening/06-01-SUMMARY.md`
</output>
