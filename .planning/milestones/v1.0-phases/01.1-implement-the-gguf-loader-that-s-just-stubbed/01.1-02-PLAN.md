---
phase: 01.1-implement-the-gguf-loader-that-s-just-stubbed
plan: 02
type: execute
wave: 2
depends_on:
  - "01.1-01"
files_modified:
  - src/emel/gguf/loader/detail.hpp
  - src/emel/gguf/loader/actions.hpp
  - tests/gguf/loader/lifecycle_tests.cpp
  - tests/fuzz/gguf_parser_fuzz.cpp
autonomous: true
requirements:
  - GGUF-01
  - GGUF-02
user_setup: []
must_haves:
  truths:
    - `bind_storage` plus `parse` populate caller-provided KV and tensor storage deterministically
      for a valid GGUF file image.
    - insufficient bound storage and parse-time format failures surface explicit `gguf::loader`
      errors instead of falling through to success.
  artifacts:
    - src/emel/gguf/loader/actions.hpp passes bound storage context into the real parse backend.
    - src/emel/gguf/loader/detail.hpp fills `kv_arena`, `kv_entries`, and
      `emel::model::data::tensor_record` for the actor's caller-owned buffers.
  key_links:
    - the `bind_storage` event establishes the spans later consumed by parse, and the parse action
      uses that exact bound storage instead of an out-of-band buffer.
---

<objective>
Populate bound storage through the real parse path and harden explicit parse-time errors.

Purpose: Finish the existing `gguf::loader` actor contract so `bind_storage` and `parse` do real
work against caller-owned buffers before downstream model-loading phases depend on it.
Output: Real parse-time storage population, parse-action wiring to bound context, and deterministic
capacity/error behavior for valid and invalid GGUF images.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-implement-the-gguf-loader-that-s-just-stubbed/01.1-RESEARCH.md
@.planning/phases/01.1-implement-the-gguf-loader-that-s-just-stubbed/01.1-01-PLAN.md
@src/emel/gguf/loader/actions.hpp
@src/emel/gguf/loader/context.hpp
@src/emel/gguf/loader/detail.hpp
@src/emel/gguf/loader/sm.hpp
@tests/gguf/loader/lifecycle_tests.cpp
@tests/fuzz/gguf_parser_fuzz.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire parse to the previously bound storage context</name>
  <files>src/emel/gguf/loader/actions.hpp, src/emel/gguf/loader/detail.hpp</files>
  <action>Update the parse action boundary so the real backend receives the bound KV arena,
  `kv_entry` span, tensor-record span, and previously probed requirements. Preserve the current
  actor structure and no-queue semantics; do not introduce a new state machine or external scratch
  ownership model.</action>
  <verify>scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests</verify>
  <done>The parse path consumes the exact storage established by `bind_storage` and fills it for a
  valid GGUF image.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic population and failure-path verification</name>
  <files>tests/gguf/loader/lifecycle_tests.cpp, tests/fuzz/gguf_parser_fuzz.cpp</files>
  <action>Extend lifecycle coverage so valid parse requests assert populated KV/tensor descriptors,
  and invalid capacity or malformed parse cases assert explicit error classification. Keep fuzzing
  aligned with the actor contract rather than broad whole-repo behavior.</action>
  <verify>scripts/test_with_coverage.sh</verify>
  <done>The full `probe -> bind_storage -> parse` lifecycle is covered for both success and failure
  paths, and the phase can prove `gguf::loader` is no longer stubbed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/build_with_zig.sh`
- [ ] `ctest --test-dir build/zig --output-on-failure -R emel_tests`
- [ ] `scripts/test_with_coverage.sh`
- [ ] `scripts/quality_gates.sh`
- [ ] Success-path tests assert populated bound storage rather than callback-only acceptance
- [ ] Failure-path tests assert explicit `gguf::loader::error` values
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `gguf::loader` now performs real probe, bind, and parse work with deterministic error handling
  </success_criteria>

<output>
After completion, create `.planning/phases/01.1-implement-the-gguf-loader-that-s-just-stubbed/01.1-02-SUMMARY.md`
</output>
