---
phase: 01.1-implement-the-gguf-loader-that-s-just-stubbed
plan: 02
subsystem: core
tags: [gguf, loader, parse, sml]
requires:
  - 01.1-01
provides:
  - Real parse-time bound storage population
  - Parse-action wiring from bound context into the GGUF backend
  - Explicit parse-time capacity and malformed-image failures
affects: [gguf-loader, emel-model-loading-path]
tech-stack:
  added: []
  patterns: [Use caller-owned spans established by bind_storage as the only parse destination]
key-files:
  created: []
  modified:
    - src/emel/gguf/loader/actions.hpp
    - src/emel/gguf/loader/detail.hpp
    - src/emel/gguf/loader/sm.hpp
    - tests/gguf/loader/lifecycle_tests.cpp
key-decisions:
  - "Used the previously bound spans as the sole parse destination and revalidated the parse image against the earlier probe contract instead of inventing scratch ownership."
  - "Changed parse-capacity dispatch to emit `capacity` rather than `invalid_request` so the dedicated parse error channel becomes reachable for real storage mismatches."
patterns-established:
  - "Pattern: `bind_storage` establishes caller-owned memory once, and parse reuses that exact bound context without copying request state into machine context."
  - "Pattern: tensor descriptors publish absolute file offsets plus in-memory data pointers derived from the aligned GGUF tensor blob."
requirements-completed: [GGUF-01, GGUF-02]
duration: 16min
completed: 2026-03-08
---

# Phase 1.1 Plan 02 Summary

**The loader now performs real `probe -> bind_storage -> parse` work through the existing actor contract**

## Accomplishments
- Wired [actions.hpp](/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/src/emel/gguf/loader/actions.hpp) so parse receives the exact bound KV arena, KV entry span, tensor-record span, and probed requirements from machine context.
- Extended [detail.hpp](/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/src/emel/gguf/loader/detail.hpp) to populate KV storage and tensor descriptors deterministically, including absolute file offsets and tensor data pointers into the GGUF image.
- Updated [sm.hpp](/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/src/emel/gguf/loader/sm.hpp) so parse-time storage mismatches now surface `gguf::loader::error::capacity` rather than collapsing into `invalid_request`.
- Expanded [lifecycle_tests.cpp](/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/tests/gguf/loader/lifecycle_tests.cpp) with success-path bound-storage assertions plus explicit parse-time capacity and malformed-image failures.

## Task Commits
- No commit created during this execution. The phase was completed locally on `next` while preserving unrelated workspace changes.

## Deviations from Plan
- The existing fuzz target already followed the actor lifecycle and remained compatible with the real parse path, so no fuzz-source changes were necessary to satisfy the plan.

## Verification
- `scripts/build_with_zig.sh`
- `ctest --test-dir build/zig --output-on-failure -R emel_tests`
- `scripts/test_with_coverage.sh`
- `scripts/quality_gates.sh`

## Next Readiness
- Phase 2 can now route paritychecker generation mode into a non-stub GGUF loader that exposes real requirements, populated KV metadata, and tensor descriptors for downstream model loading.
