---
phase: 01.1
slug: implement-the-gguf-loader-that-s-just-stubbed
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-08
---

# Phase 01.1 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | doctest + CTest + existing fuzz target |
| **Config file** | repo root `CMakeLists.txt` |
| **Quick run command** | `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests` |
| **Full suite command** | `scripts/quality_gates.sh` |
| **Estimated runtime** | ~900 seconds |

---

## Sampling Rate

- **After every task commit:** `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests`
- **After every plan wave:** `scripts/test_with_coverage.sh`
- **Before `$gsd-verify-work`:** `scripts/quality_gates.sh`
- **Max feedback latency:** 900 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01.1-01-01 | 01 | 1 | GGUF-01 | unit | `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests` | ✅ | ⬜ pending |
| 01.1-01-02 | 01 | 1 | GGUF-02 | negative | `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests` | ✅ | ⬜ pending |
| 01.1-02-01 | 02 | 2 | GGUF-01 | integration | `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests` | ✅ | ⬜ pending |
| 01.1-02-02 | 02 | 2 | GGUF-02 | negative/fuzz | `scripts/test_with_coverage.sh` | ✅ | ⬜ pending |
| 01.1-02-03 | 02 | 2 | GGUF-01, GGUF-02 | repo-gate | `scripts/quality_gates.sh` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers the phase. No external service setup or fixture download is required
as long as the checked-in `tests/models/` catalog is present locally.

---

## Manual-Only Verifications

None planned. The inserted phase should be verifiable from actor-level tests, fuzz coverage, and
standard repo gates.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 900s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
