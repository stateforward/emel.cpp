---
phase: 01.1-implement-the-gguf-loader-that-s-just-stubbed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emel/gguf/loader/detail.hpp
  - tests/gguf/loader/lifecycle_tests.cpp
  - tests/fuzz/gguf_parser_fuzz.cpp
autonomous: true
requirements:
  - GGUF-01
  - GGUF-02
user_setup: []
must_haves:
  truths:
    - `gguf::loader::event::probe` computes real requirements from a valid GGUF file image instead
      of returning placeholder zeros.
    - malformed file images and unsupported GGUF layout features surface explicit loader errors.
  artifacts:
    - src/emel/gguf/loader/detail.hpp contains a bounded GGUF reader and real probe logic.
    - tests/gguf/loader/lifecycle_tests.cpp covers valid and invalid probe behavior with
      deterministic expectations.
  key_links:
    - the same reader rules that size KV and tensor requirements are exercised by lifecycle and fuzz
      coverage, preventing probe/parse drift.
---

<objective>
Replace the GGUF probe stubs with a real bounded reader and explicit error classification.

Purpose: Make the `probe` half of `gguf::loader` truthful so later bind and parse work can rely on
real tensor/KV requirements instead of placeholder success paths.
Output: Allocation-free GGUF reader helpers, real requirement sizing, and explicit malformed-input
coverage for the existing actor boundary.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-implement-the-gguf-loader-that-s-just-stubbed/01.1-RESEARCH.md
@src/emel/gguf/loader/detail.hpp
@src/emel/gguf/loader/events.hpp
@src/emel/gguf/loader/guards.hpp
@tests/gguf/loader/lifecycle_tests.cpp
@tests/fuzz/gguf_parser_fuzz.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bounded GGUF header and inventory reading</name>
  <files>src/emel/gguf/loader/detail.hpp</files>
  <action>Add bounded reader helpers over `std::span<const uint8_t>` and implement real GGUF
  header, KV inventory, and tensor-catalog scanning for `probe_requirements(...)`. The helper must
  stay allocation-free during dispatch and classify malformed magic, version, truncation, and
  unsupported layout deterministically.</action>
  <verify>scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests</verify>
  <done>`probe_requirements(...)` returns real counts and maximum storage sizes for valid GGUF file
  images and explicit errors for malformed ones.</done>
</task>

<task type="auto">
  <name>Task 2: Harden probe-path lifecycle and fuzz coverage</name>
  <files>tests/gguf/loader/lifecycle_tests.cpp, tests/fuzz/gguf_parser_fuzz.cpp</files>
  <action>Replace placeholder probe assumptions with assertions against real requirement values and
  explicit error classes. Extend the existing fuzz target or seed expectations only as needed to
  keep malformed-input coverage meaningful for the new bounded reader.</action>
  <verify>scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests</verify>
  <done>Probe-path tests and fuzzing exercise the real backend behavior instead of the previous
  always-success stub.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/build_with_zig.sh`
- [ ] `ctest --test-dir build/zig --output-on-failure -R emel_tests`
- [ ] `scripts/test_with_coverage.sh`
- [ ] Probe on malformed input no longer reports placeholder success
- [ ] `scripts/quality_gates.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `gguf::loader::probe` now computes real requirements and explicit probe failures
  </success_criteria>

<output>
After completion, create `.planning/phases/01.1-implement-the-gguf-loader-that-s-just-stubbed/01.1-01-SUMMARY.md`
</output>
