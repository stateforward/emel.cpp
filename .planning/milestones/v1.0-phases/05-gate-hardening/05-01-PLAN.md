---
phase: 05-gate-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/paritychecker/paritychecker_tests.cpp
  - tools/paritychecker/parity_runner.cpp
autonomous: true
requirements:
  - VER-02
user_setup: []
must_haves:
  truths:
    - automated paritychecker subprocess coverage detects at least one expected generation failure
      path and fails for the right reason.
    - the new failure coverage stays tool-local and does not require `src/` machine changes.
  artifacts:
    - tools/paritychecker/paritychecker_tests.cpp contains a generation-specific negative subprocess
      doctest with exit-code and stderr assertions.
    - paritychecker generation diagnostics remain deterministic enough for substring-based failure
      assertions.
  key_links:
    - the Phase 4 success-path subprocess test stays green while the new failure-path test closes
      `VER-02`.
---

<objective>
Add one deterministic generation failure-path subprocess regression to `paritychecker_tests`.

Purpose: Close the only remaining Phase 5 product gap, `VER-02`, with the smallest possible change
surface.
Output: A tool-local negative generation doctest that proves the CLI subprocess path fails for an
expected reason and remains visible through the existing `paritychecker_tests` target.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-gate-hardening/05-RESEARCH.md
@.planning/phases/04-deterministic-generation-parity/04-VERIFICATION.md
@tools/paritychecker/paritychecker_tests.cpp
@tools/paritychecker/parity_runner.cpp
@scripts/paritychecker.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the missing failure-path gap with a subprocess doctest first</name>
  <files>tools/paritychecker/paritychecker_tests.cpp</files>
  <action>Add a failing doctest in the existing generation subprocess section before changing any
  production diagnostics. Keep it tool-local and generation-specific. Prefer the narrowest stable
  negative case already supported by the CLI harness, starting with a missing-model invocation or
  another already-documented generation rejection that does not require synthetic fault injection.
  Assert non-zero exit, absence of the success marker, and a stable stderr reason substring rather
  than full-path or full-message equality.</action>
  <verify>ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests</verify>
  <done>The test suite now contains a generation subprocess failure case that fails before the
  implementation is hardened.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize the generation subprocess helper and harden diagnostics only if needed</name>
  <files>tools/paritychecker/paritychecker_tests.cpp, tools/paritychecker/parity_runner.cpp</files>
  <action>Refactor the existing generation subprocess capture helper only as much as needed to
  express the new negative case cleanly while preserving the Phase 4 success-path test. If the new
  doctest exposes unstable or overly generic generation stderr text, make the smallest possible
  paritychecker-local diagnostic adjustment in `parity_runner.cpp`. Do not add synthetic
  fault-injection controls, do not widen the generation matrix, and do not change `src/` machine
  structure.</action>
  <verify>scripts/paritychecker.sh</verify>
  <done>`VER-02` is satisfied by a stable, automated negative subprocess regression and the
  existing success-path coverage still passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests`
- [ ] `scripts/paritychecker.sh`
- [ ] The existing success-path generation subprocess test still passes unchanged
- [ ] At least one generation-specific negative subprocess test now asserts both exit code and
      reason-specific stderr text
- [ ] No `src/` files are required to satisfy the phase unless a new failing test proves a tiny
      paritychecker diagnostic tweak is insufficient
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `VER-02` is closed by automated `paritychecker_tests` coverage for one expected generation
  failure path
  </success_criteria>

<output>
After completion, create `.planning/phases/05-gate-hardening/05-01-SUMMARY.md`
</output>
