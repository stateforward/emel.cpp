---
phase: 05-gate-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - tools/paritychecker/paritychecker_tests.cpp
  - scripts/paritychecker.sh
autonomous: true
requirements:
  - VER-02
user_setup: []
must_haves:
  truths:
    - Phase 4 already satisfied `VER-01`, so Phase 5 only needs failure-path hardening plus
      explicit gate/doc alignment.
    - the default parity gate already flows through `paritychecker_tests`; Phase 5 should verify
      that path rather than invent a new one.
  artifacts:
    - stale Phase 5 roadmap wording is corrected so it no longer claims success-path subprocess
      coverage is still missing.
    - the default parity gate evidence explicitly includes the new negative generation regression.
  key_links:
    - `scripts/paritychecker.sh` remains the standard parity gate entrypoint and runs
      `ctest -R paritychecker_tests`.
---

<objective>
Align the Phase 5 docs with the post-Phase-4 reality and verify the default parity gate path.

Purpose: Finish Gate Hardening cleanly by documenting the narrowed scope and proving the existing
gate path carries both the success and failure generation subprocess slices.
Output: Updated planning language and verified default-gate evidence without widening the repo
verification surface.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-gate-hardening/05-RESEARCH.md
@.planning/phases/04-deterministic-generation-parity/04-VERIFICATION.md
@.planning/phases/05-gate-hardening/05-01-PLAN.md
@tools/paritychecker/paritychecker_tests.cpp
@scripts/paritychecker.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stale Phase 5 planning language to match the completed success-path work</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>Revise Phase 5 wording so it no longer describes success-path subprocess coverage as
  future work. Keep `VER-01` complete, keep `VER-02` as the only remaining requirement this phase
  closes, and rephrase the plan descriptions around failure-path subprocess coverage plus gate
  alignment. Only touch planning docs that are now inaccurate because Phase 4 pulled work forward.</action>
  <verify>sh -c '! rg -n "Add subprocess generation parity coverage to `paritychecker_tests.cpp`|`paritychecker_tests` exercises the generation mode through the subprocess CLI path\\." .planning/ROADMAP.md && rg -n "VER-01 .* Complete|VER-02 .* Pending" .planning/REQUIREMENTS.md'</verify>
  <done>The planning docs accurately describe the remaining Phase 5 scope and no longer imply that
  success-path subprocess coverage is still missing.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the default parity gate carries the final Phase 5 regression surface</name>
  <files>tools/paritychecker/paritychecker_tests.cpp, scripts/paritychecker.sh</files>
  <action>Run the standard parity gate path and verify it already includes both the Phase 4
  success-path subprocess test and the new Phase 5 failure-path subprocess test. Only change
  `scripts/paritychecker.sh` if the new negative coverage reveals an actual gate visibility problem;
  otherwise leave the script unchanged and treat this task as verification evidence, not gate
  redesign.</action>
  <verify>scripts/quality_gates.sh</verify>
  <done>The existing gate chain proves both generation success and one expected generation failure
  without adding a new script, target, or manual-only check.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests`
- [ ] `scripts/paritychecker.sh`
- [ ] `scripts/quality_gates.sh`
- [ ] `.planning/ROADMAP.md` no longer contains the stale success-path subprocess wording that
      Phase 4 already completed
- [ ] The final gate evidence shows the generation slice is covered through the normal parity path,
      not a manual-only command
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The roadmap and requirement text now match the actual post-Phase-4 scope, and the default gate
  path is confirmed as the normal verification surface for the final generation regressions
  </success_criteria>

<output>
After completion, create `.planning/phases/05-gate-hardening/05-02-SUMMARY.md`
</output>
