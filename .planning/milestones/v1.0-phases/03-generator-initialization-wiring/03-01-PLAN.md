---
phase: 03-generator-initialization-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/paritychecker/parity_runner.cpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - INIT-01
user_setup: []
must_haves:
  truths:
    - paritychecker generation mode constructs and owns the existing EMEL actor graph required to
      initialize the generator after model load.
    - the loaded `emel::model::data` is handed directly into `emel::generator::sm` rather than
      being mirrored into a tool-local orchestration shortcut.
  artifacts:
    - tools/paritychecker/parity_runner.cpp contains a bounded initialization harness with
      tokenizer, conditioner, generator, sampler functions, opaque init handles, and callback
      capture.
    - tests/generator/lifecycle_tests.cpp covers the paritychecker-style initialize contract for
      the pinned slice.
  key_links:
    - paritychecker load success dispatches `emel::generator::event::initialize` instead of
      stopping after `model::loader::events::load_done`.
---

<objective>
Bridge paritychecker into the real generator initialize contract.

Purpose: Turn the Phase 2 loaded-model handoff into a real EMEL generator initialization path so
the tool can reach `generator::ready` for the pinned model without widening scope into decode.
Output: A paritychecker-owned initialization harness and a successful `event::initialize`
dispatch through the existing actor graph.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-generator-initialization-wiring/03-RESEARCH.md
@src/emel/generator/events.hpp
@src/emel/generator/sm.hpp
@tests/generator/lifecycle_tests.cpp
@tools/paritychecker/parity_runner.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paritychecker-style initialize coverage before wiring the bridge</name>
  <files>tests/generator/lifecycle_tests.cpp</files>
  <action>Extend generator lifecycle coverage with a fixture that mirrors the ownership shape the
  paritychecker bridge needs: loaded model reference, tokenizer actor, conditioner actor, generator
  actor, sampler span, opaque topology/plan handles, and deterministic backend callbacks. Add the
  failing expectation for the current gap before implementing the bridge, then keep that fixture as
  the reference for the tool wiring.</action>
  <verify>build/zig/emel_tests_bin --dt-test-case="*generator*"</verify>
  <done>The expected initialize contract for paritychecker is pinned in focused generator tests and
  can drive the tool work without guessing.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch `generator::event::initialize` after the load path succeeds</name>
  <files>tools/paritychecker/parity_runner.cpp</files>
  <action>Add a paritychecker-local harness state that owns `emel::text::tokenizer::sm`,
  `emel::text::conditioner::sm`, `emel::generator::sm`, sampler functions, backend placeholder
  state, and opaque init handles. After `load_done`, build a deterministic initialize request from
  the loaded model and dispatch it through the real generator actor graph. Do not call
  `event::generate` in this plan.</action>
  <verify>build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello"</verify>
  <done>Generation mode can initialize the EMEL generator and reach `ready` for the pinned fixture
  through the existing actor graph.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/paritychecker.sh`
- [ ] `build/zig/emel_tests_bin --dt-test-case="*generator*"`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello"`
- [ ] `scripts/test_with_coverage.sh`
- [ ] `scripts/quality_gates.sh`
- [ ] The Phase 2 "generator initialization reserved for later phases" success message is gone
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Paritychecker reaches `emel::generator::event::initialize` and the pinned model can enter
  `generator::ready`
  </success_criteria>

<output>
After completion, create `.planning/phases/03-generator-initialization-wiring/03-01-SUMMARY.md`
</output>
