---
phase: 03-generator-initialization-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - tools/paritychecker/parity_runner.cpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - INIT-01
  - INIT-02
user_setup: []
must_haves:
  truths:
    - paritychecker reports initialize success only after `emel::generator::events::initialize_done`
      and reports failure on rejected init or `initialize_error`.
    - the initialize path keeps explicit error channels and does not bypass the generator actor
      graph for recovery or status reporting.
  artifacts:
    - tools/paritychecker/parity_runner.cpp publishes stable initialize success and failure output
      for generation mode.
    - focused generator lifecycle coverage exercises the init error mapping that paritychecker
      depends on.
  key_links:
    - `emel::generator::events::initialize_done` and `initialize_error` map directly to
      paritychecker stdout, stderr, and exit status.
---

<objective>
Publish deterministic initialize outcomes and preserve SML invariants.

Purpose: Turn the new initialize bridge into a stable harness contract so later phases can trust
that the generator is ready before decode work starts and can diagnose init failures cleanly.
Output: Deterministic initialize success/failure publication plus focused error-path verification.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-generator-initialization-wiring/03-RESEARCH.md
@.planning/phases/03-generator-initialization-wiring/03-01-PLAN.md
@src/emel/generator/events.hpp
@src/emel/generator/errors.hpp
@tools/paritychecker/parity_runner.cpp
@tests/generator/lifecycle_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture and publish deterministic initialize success evidence</name>
  <files>tools/paritychecker/parity_runner.cpp</files>
  <action>Add bounded capture structs and callback handlers for `events::initialize_done` so the
  generation success path reports stable initialize evidence for the pinned fixture. Keep the
  message phase-scoped: prove that generator initialization completed and the actor graph is ready,
  but do not claim prompt execution or token parity yet.</action>
  <verify>build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello"</verify>
  <done>The paritychecker success path exits zero only after `initialize_done` and prints explicit
  initialize-ready evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize initialize rejection and error publication</name>
  <files>tools/paritychecker/parity_runner.cpp, tests/generator/lifecycle_tests.cpp</files>
  <action>Map rejected initialize requests and `events::initialize_error` outcomes into
  deterministic stderr and non-zero exit status at the paritychecker boundary. If helper extraction
  is needed to keep the logic bounded, add focused generator lifecycle coverage for invalid-request
  and backend-error surfaces rather than widening into new CLI knobs or subprocess matrices.</action>
  <verify>build/zig/emel_tests_bin --dt-test-case="*generator*"</verify>
  <done>Initialize failure modes surface explicit, repeatable reasons without violating the SML
  RTC actor contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/paritychecker.sh`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello"`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello" | rg "initialize"`
- [ ] `build/zig/emel_tests_bin --dt-test-case="*generator*"`
- [ ] `scripts/test_with_coverage.sh`
- [ ] `scripts/quality_gates.sh`
- [ ] Success and failure output both identify generator initialization rather than the Phase 2 load-only seam
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Generation mode now emits deterministic initialize success and failure signals for the EMEL
  generator path
  </success_criteria>

<output>
After completion, create `.planning/phases/03-generator-initialization-wiring/03-02-SUMMARY.md`
</output>
