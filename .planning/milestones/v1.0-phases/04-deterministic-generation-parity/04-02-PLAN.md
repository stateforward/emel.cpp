---
phase: 04-deterministic-generation-parity
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - tools/paritychecker/parity_runner.cpp
autonomous: true
requirements:
  - PARI-01
user_setup: []
must_haves:
  truths:
    - the same prompt and deterministic settings run through both the EMEL generator path and a
      direct reference `llama.cpp` path inside paritychecker.
    - parity success is based on comparison output, not just on EMEL generate completion.
  artifacts:
    - tools/paritychecker/parity_runner.cpp contains a tool-local reference generation path and a
      shared parity comparison flow.
  key_links:
    - generation mode returns success only when the EMEL and reference results match for the pinned
      deterministic slice.
---

<objective>
Add the reference-side generation invocation and parity comparison flow.

Purpose: Turn the new EMEL generation path into a real parity tool by running the same bounded
request against the reference implementation in the same binary.
Output: Dual-path deterministic generation plus a parity success/failure decision.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-deterministic-generation-parity/04-RESEARCH.md
@.planning/phases/04-deterministic-generation-parity/04-01-PLAN.md
@tools/bench/memory/bench_common.hpp
@tools/paritychecker/parity_runner.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the paritychecker fake decode scaffolding with a truthful tool-local bridge</name>
  <files>tools/paritychecker/parity_runner.cpp</files>
  <action>Promote the current initialize-only backend callbacks into a deterministic decode bridge
  that can supply real logits for the EMEL generator path using paritychecker-local reference
  facilities. Keep all `llama_*` and `ggml_*` identifiers inside `tools/paritychecker/`; do not
  leak reference concepts into `src/`.</action>
  <verify>build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello" --max-tokens 1</verify>
  <done>The EMEL generation path is backed by truthful model-dependent compute instead of the Phase 3
  placeholder callbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Run the direct reference generation path and compare it against EMEL output</name>
  <files>tools/paritychecker/parity_runner.cpp</files>
  <action>Add a direct `llama.cpp` generation invocation for the same prompt, max-token limit, and
  deterministic sampler configuration used by the EMEL path, then compare the two results inside
  generation mode. Keep the first slice bounded to the pinned Llama-68M fixture and one narrow
  request shape.</action>
  <verify>build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello" --max-tokens 1 | rg "parity"</verify>
  <done>Generation mode now emits parity success only when EMEL output matches the reference output
  for the bounded Phase 4 slice.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/paritychecker.sh`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello" --max-tokens 1`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello" --max-tokens 1 | rg "parity"`
- [ ] Success output is contingent on EMEL-vs-reference comparison, not only on EMEL generate completion
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The paritychecker generation mode runs both EMEL and direct reference generation for the same
  deterministic request
  </success_criteria>

<output>
After completion, create `.planning/phases/04-deterministic-generation-parity/04-02-SUMMARY.md`
</output>
