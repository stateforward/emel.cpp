---
phase: 04-deterministic-generation-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/paritychecker/parity_runner.cpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - GEN-01
  - GEN-02
user_setup: []
must_haves:
  truths:
    - paritychecker dispatches the real `emel::generator::event::generate` path after Phase 3
      initialization instead of stopping at initialize-ready output.
    - generation mode uses deterministic first-slice settings for the pinned fixture.
  artifacts:
    - tools/paritychecker/parity_runner.cpp owns a bounded generation request/callback capture and
      no longer prints the initialize-only reservation message on success.
    - focused generator coverage documents the deterministic request shape the CLI harness depends
      on.
  key_links:
    - `run_generation_harness_contract(...)` now reaches `generator::events::generation_done` or
      `generation_error` for the EMEL path.
---

<objective>
Execute one bounded EMEL prompt-to-output generation request.

Purpose: Turn the Phase 3 initialize seam into a truthful Phase 4 generate seam so paritychecker
can run one deterministic request through the real generator actor graph.
Output: A paritychecker-owned generation request path with deterministic bounds and explicit
generate success/failure handling.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-deterministic-generation-parity/04-RESEARCH.md
@src/emel/generator/events.hpp
@src/emel/generator/sm.hpp
@tests/generator/lifecycle_tests.cpp
@tools/paritychecker/parity_runner.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the deterministic Phase 4 generate contract in focused coverage</name>
  <files>tests/generator/lifecycle_tests.cpp</files>
  <action>Extend focused generator coverage so the paritychecker Phase 4 request shape is explicit:
  one bounded prompt, deterministic sampler choice, bounded output buffer, and stable
  `generation_done` / `generation_error` expectations. Keep the machine contract as the source of
  truth and add the failing expectation before wiring the CLI path.</action>
  <verify>build/zig/emel_tests_bin --dt-test-case="*generator*"</verify>
  <done>The deterministic generate request shape is pinned in tests before the paritychecker bridge
  is widened.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch `generator::event::generate` from paritychecker with bounded settings</name>
  <files>tools/paritychecker/parity_runner.cpp</files>
  <action>After Phase 3 initialization succeeds, construct one bounded
  `emel::generator::event::generate` request using the CLI prompt, `--max-tokens`, fixed output
  storage, and deterministic sampler configuration. Capture `generation_done` and `generation_error`
  explicitly and replace the initialize-only success message with generate-phase evidence. Do not
  add the reference comparison flow in this plan.</action>
  <verify>build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello" --max-tokens 1</verify>
  <done>Paritychecker can execute one bounded EMEL generation request and report generate success or
  failure deterministically.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/paritychecker.sh`
- [ ] `build/zig/emel_tests_bin --dt-test-case="*generator*"`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello" --max-tokens 1`
- [ ] The Phase 3 initialize-only reservation message is gone from generation success output
- [ ] Success output identifies generate completion rather than initialize readiness
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Paritychecker executes a bounded EMEL `event::generate` request for the pinned slice
  </success_criteria>

<output>
After completion, create `.planning/phases/04-deterministic-generation-parity/04-01-SUMMARY.md`
</output>
