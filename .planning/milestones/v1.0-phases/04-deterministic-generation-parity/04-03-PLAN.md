---
phase: 04-deterministic-generation-parity
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - tools/paritychecker/parity_runner.cpp
autonomous: true
requirements:
  - GEN-01
  - GEN-02
  - PARI-01
  - PARI-02
user_setup: []
must_haves:
  truths:
    - parity success and failure report structured deterministic evidence beyond final rendered
      text.
    - mismatch output is specific enough to localize whether drift is in generation length,
      rendered bytes, or other exposed deterministic signals.
  artifacts:
    - tools/paritychecker/parity_runner.cpp prints a compact structured parity record and mismatch
      diagnostics for the Phase 4 slice.
  key_links:
    - generation mode output includes multiple parity signals, not just one string comparison.
---

<objective>
Add structured mismatch evidence and stop-condition diagnostics.

Purpose: Make the Phase 4 slice debuggable and requirement-complete by publishing deterministic
evidence instead of a pass/fail string.
Output: Structured parity success output and actionable mismatch diagnostics.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-deterministic-generation-parity/04-RESEARCH.md
@.planning/phases/04-deterministic-generation-parity/04-02-PLAN.md
@tools/paritychecker/parity_runner.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish a structured parity record for the bounded slice</name>
  <files>tools/paritychecker/parity_runner.cpp</files>
  <action>Define a compact parity result record for the EMEL and reference runs that includes the
  final rendered text plus additional deterministic evidence already available or derivable from the
  current contract, such as `tokens_generated`, `output_length`, prompt bytes, and first mismatch
  position. Add stop classification only if it can be derived cleanly without forcing a generator
  topology rewrite.</action>
  <verify>build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello" --max-tokens 1 --dump</verify>
  <done>Success and mismatch output now carry more than final text and are stable enough for later
  regression locking.</done>
</task>

<task type="auto">
  <name>Task 2: Finish the phase with full repo verification</name>
  <files>tools/paritychecker/parity_runner.cpp</files>
  <action>Keep the final parity output compact on success and explicit on failure, then run the
  normal coverage and quality gates. Do not expand into full subprocess regression suites here;
  Phase 5 owns gate hardening.</action>
  <verify>scripts/quality_gates.sh</verify>
  <done>The Phase 4 slice is executable, diagnosable, and green under the standard repo gates.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/paritychecker.sh`
- [ ] `build/paritychecker_zig/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text "hello" --max-tokens 1 --dump`
- [ ] `scripts/test_with_coverage.sh`
- [ ] `scripts/quality_gates.sh`
- [ ] Mismatch output reports structured evidence beyond the final string comparison
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Generation parity output now includes structured deterministic evidence suitable for Phase 5
  regression locking
  </success_criteria>

<output>
After completion, create `.planning/phases/04-deterministic-generation-parity/04-03-SUMMARY.md`
</output>
