---
phase: 92.4
plan: 01
status: completed
requirements:
  - BEN-01
  - DOC-01
  - PRF-01
depends_on:
  - 92.1
  - 92.3
---

# Phase 92.4 Plan

## Goal

Refresh the maintained Sortformer benchmark publication after Phase `92.3` kernel-domain dense
dispatch landed, then rerun the milestone audit from source-backed evidence.

## Tasks

1. Rerun the deterministic EMEL benchmark snapshot using the maintained bench runner.
2. Rerun the deterministic compare snapshot using the same `bench_runner --mode=compare` reporting
   path used by generation benchmarks.
3. Regenerate `docs/benchmarks.md` from checked-in benchmark snapshots rather than hand-editing
   publication text.
4. Validate the maintained parity path on the real pinned GGUF model and AMI audio fixture.
5. Rerun the milestone audit against live source paths, snapshots, and validation outputs.

## Acceptance

- `snapshots/bench/benchmarks.txt` contains Sortformer rows for `output_dim=71` and
  `output_checksum=2496121829252312372`.
- `snapshots/bench/benchmarks_compare.txt` contains matching EMEL/reference Sortformer rows for
  the same maintained model, audio fixture, output dimension, and checksum.
- `docs/benchmarks.md` publishes the refreshed Sortformer baseline from the compare snapshot.
- The maintained parity test and `diarization_compare/v1` wrapper report exact checksum and
  output-dimension match.
- The milestone audit reports no remaining closeout blocker.
