---
phase: 92.4
status: completed
---

# Phase 92.4 Verification

## Result

Passed for post-kernel publication refresh and milestone audit readiness.

## Checks

| Check | Result | Evidence |
|-------|--------|----------|
| EMEL benchmark snapshot refreshed | Passed | `snapshots/bench/benchmarks.txt` contains `output_dim=71`, `output_checksum=2496121829252312372`. |
| Compare snapshot refreshed | Passed | `snapshots/bench/benchmarks_compare.txt` contains matching EMEL/reference Sortformer rows for `output_dim=71`, `output_checksum=2496121829252312372`. |
| Docs regenerated from snapshots | Passed | `docs/benchmarks.md` now publishes the `71`-segment Sortformer baseline from `snapshots/bench/benchmarks_compare.txt`. |
| Docs generation is stable | Passed | `scripts/generate_docs.sh --check` exited successfully. |
| Maintained parity test passes | Passed | `build/debug/emel_tests_bin --test-case='sortformer parity matches maintained real model and audio checksum baseline' --success` passed with `71 == 71` and checksum `2496121829252312372`. |
| Operator-facing compare wrapper passes | Passed | `python3 tools/bench/diarization_compare.py --output-dir build/diarization_compare_92_4 --emel-runner build/bench_tools_ninja/bench_runner` reported `exact_match` for the primary and profile Sortformer groups. |

## Commands

- `scripts/bench.sh --snapshot --update`
- `scripts/bench.sh --compare-update`
- `scripts/generate_docs.sh`
- `scripts/generate_docs.sh --check`
- `build/debug/emel_tests_bin --test-case='sortformer parity matches maintained real model and audio checksum baseline' --success`
- `python3 tools/bench/diarization_compare.py --output-dir build/diarization_compare_92_4 --emel-runner build/bench_tools_ninja/bench_runner`
- `git diff --check -- docs/benchmarks.md snapshots/bench/benchmarks.txt snapshots/bench/benchmarks_compare.txt .planning/STATE.md .planning/ROADMAP.md .planning/v1.15-MILESTONE-AUDIT.md`

## Notes

The earlier full `scripts/quality_gates.sh` run cleared unit, coverage, paritychecker, and fuzz
lanes before timing out in the long compare tail. Phase `92.4` reran that compare tail explicitly
through `scripts/bench.sh --compare-update` and completed successfully.
