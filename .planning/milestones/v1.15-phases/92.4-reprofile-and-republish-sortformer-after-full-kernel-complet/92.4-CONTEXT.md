---
phase: 92.4
status: completed
---

# Phase 92.4 Context

Phase `92.4` exists because the milestone could not close with stale benchmark publication after
Phase `92.3` moved maintained Sortformer dense dispatch into the kernel domain and repaired the
output aliasing bug.

The source-backed audit path for the final publication claim is:

1. `tools/bench/diarization/sortformer_fixture.hpp` loads the pinned GGUF model, prepares the real
   AMI audio fixture, and drives `pipeline::sm`.
2. `src/emel/diarization/sortformer/detail.cpp` dispatches dense work through
   `emel::kernel::event::op_mul_mat`.
3. `src/emel/diarization/sortformer/output/detail.cpp` computes the speaker head using a
   non-aliased frame-hidden buffer.
4. `tools/bench/diarization/sortformer_bench.cpp` emits maintained Sortformer benchmark metadata.
5. `scripts/bench.sh --snapshot --update` and `scripts/bench.sh --compare-update` refresh the same
   checked-in benchmark reporting surfaces used by generation.
6. `tools/docsgen/docsgen.cpp` regenerates `docs/benchmarks.md` from the refreshed snapshots.

The maintained output baseline is now `segment_count=71` and
`output_checksum=2496121829252312372`.
