# Phase 83.3 Verification

## Commands

```sh
cmake --build build/coverage --target emel_tests_bin -j 8
ctest --test-dir build/coverage --output-on-failure -R '^emel_tests_diarization$' -j 1
EMEL_QUALITY_GATES_TIMEOUT=3600s scripts/quality_gates.sh
```

## Results

- Build: passed.
- Focused diarization shard: passed.
- Full quality gate: passed with exit `0`.
- Changed-source coverage from the full gate:
  - lines: `96.6%`
  - branches: `68.1%`

## Notes

The full quality gate printed benchmark regression lines for existing benchmark snapshot timing,
then completed with:

```text
warning: benchmark snapshot regression ignored by quality gates
```

No benchmark snapshot was intentionally updated.
