---
phase: 90
plan: 01
status: ready
rule_constraints:
  - AGENTS.md
  - docs/rules/sml.rules.md
  - docs/rules/cpp.rules.md
requirements:
  - PRF-01
  - PRF-02
  - BEN-01
  - DOC-01
  - RUN-03
  - OUT-03
---

# Phase 90 Plan 1: Runtime Parity And Benchmark Truth Repair

## Goal

Replace fabricated Sortformer parity and benchmark proof with maintained EMEL runtime output from
the Phase 89 pipeline while keeping EMEL/reference lanes isolated.

<rule_constraints>

- Do not introduce tool-only compute fallbacks for the EMEL lane.
- Do not share model/runtime/cache objects between EMEL and reference lanes.
- Do not update benchmark snapshots without explicit user consent.
- Keep benchmark and parity harnesses out of actor internals; drive the EMEL lane through
  `process_event(...)`.
- Preserve truthful limitations in generated benchmark docs.

</rule_constraints>

## Tasks

1. Update `tests/diarization/sortformer/parity/lifecycle_tests.cpp` so the EMEL side calls
   `sortformer::pipeline::sm` and compares its runtime segment output to a fixed canonical
   reference baseline.
2. Update `tools/bench/diarization/sortformer_bench.cpp` so the canonical EMEL benchmark lane
   measures the Phase 89 pipeline instead of `fill_reference_probabilities` and
   `decode_segments`.
3. Keep the reference lane fixed and separate, with no shared EMEL runtime/cache/model state.
4. Update benchmark docs/template text to state the EMEL lane is pipeline-backed and keep
   limitations explicit.
5. Record Phase 90 summary and verification artifacts with exact `PRF-01`, `PRF-02`, `BEN-01`,
   `DOC-01`, `RUN-03`, and `OUT-03` evidence.

## Verification

- `git diff --check -- tests/diarization/sortformer/parity tools/bench/diarization docs/templates/benchmarks.md.j2 docs/benchmarks.md .planning/phases/90-runtime-parity-and-benchmark-truth-repair`
- `cmake --build build/coverage --target emel_tests_bin -j 6`
- `ctest --test-dir build/coverage --output-on-failure -R '^emel_tests_diarization$' -j 1`
- `cmake --build build/bench_tools_ninja --target bench_runner -j 6`
- `EMEL_BENCH_SUITE=diarization_sortformer EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 build/bench_tools_ninja/bench_runner --mode=compare`
- `scripts/generate_docs.sh`

## Completion Evidence

Create `90-01-SUMMARY.md` and `90-VERIFICATION.md` after implementation.
