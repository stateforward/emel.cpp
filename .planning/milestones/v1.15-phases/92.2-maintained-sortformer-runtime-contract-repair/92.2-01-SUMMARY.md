---
phase: 92.2
plan: 01
status: complete
completed: 2026-04-23
requirements-completed:
  - SORT-03
  - DIA-02
  - RUN-01
  - RUN-03
  - OUT-01
  - OUT-02
---

# Phase 92.2 Plan 1 Summary

## Outcome

Phase `92.2` repaired the maintained Sortformer `src/` runtime so the pinned GGUF artifact and
the pinned AMI real-audio fixture now run through the truthful EMEL-owned path:

- real `prep.feat.fb` / `prep.feat.win` frontend
- real maintained `enc.pre.*` `dw_striding` pre-encode stack
- maintained encoder/module/transformer tensor binding
- maintained speaker head structure

The maintained pipeline now produces non-zero diarization segments instead of stopping at the old
synthetic-feature scaffold.

## What Changed

- Added `src/emel/diarization/sortformer/encoder/feature_extractor/detail.cpp` and rewired
  `request/actions.hpp` / `request/guards.hpp` so request preparation consumes the maintained
  frontend tensors and emits truthful `1504 x 128` log-mel features.
- Replaced the synthetic pre-encode shortcut in
  `src/emel/diarization/sortformer/encoder/detail.cpp` with the real three-stage maintained
  `dw_striding` contraction from `1504 x 128` features to `188 x 512` encoder embeddings.
- Kept the maintained binder fixes from the earlier `92.2` work and updated the focused request
  / encoder tests to match the truthful maintained path.
- Recorded the first truthful maintained parity baseline at
  `tests/fixtures/diarization/ami_en2002b_mix_headset_137.00_152.04_16khz_mono.baseline.txt`:
  `segment_count=17`, `output_checksum=17895507502300857007`.

## Evidence

- `build/debug/emel_tests_bin --test-case='diarization request extracts deterministic Sortformer features' --success`
  passed with `frame_count=1504`.
- `build/debug/emel_tests_bin --test-case='sortformer encoder derives deterministic frames from acoustic features' --success`
  passed.
- `build/debug/emel_tests_bin --test-case='sortformer executor consumes native feature-to-encoder frames' --success`
  passed.
- `build/debug/emel_tests_bin --test-case='sortformer pipeline runs maintained pcm to probabilities and segments' --success`
  passed with `segment_count=17`.
- `scripts/quality_gates.sh` passed after the parity baseline was added.
  - Coverage gate passed at `94.4%` lines and `69.4%` branches on changed `src/` files.
  - `paritychecker` and `fuzz_smoke` passed.
  - `bench_snapshot` reported non-blocking regressions and new diarization entries without
    snapshot baselines; the script intentionally treated that as a warning, not a gate failure.

## Handoff To 92.1

Phase `92.1` now owns publication on top of the repaired runtime:

- benchmark snapshot/baseline publication for the new diarization entries
- benchmark/docs refresh in `docs/benchmarks.md`
- milestone audit evidence that cites the truthful maintained path instead of the old synthetic
  scaffold
