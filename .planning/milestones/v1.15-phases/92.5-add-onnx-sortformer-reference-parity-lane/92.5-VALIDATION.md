---
phase: 92.5
slug: add-onnx-sortformer-reference-parity-lane
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-24
---

# Phase 92.5 - Validation Strategy

## Completion Preconditions

- [x] `92.5-01-SUMMARY.md` exists with `requirements-completed` frontmatter.
- [x] `92.5-VERIFICATION.md` exists and records ONNX lane evidence.
- [x] Roadmap and state no longer list Phase `92.5` as pending.
- [x] The original timeout blocker is resolved by the final changed-aware quality gate.

## Rule Compliance Review

| Rule Input | Read | Result |
|------------|------|--------|
| `AGENTS.md` | yes | The ONNX lane is explicit reference tooling and never substitutes the recorded baseline. |
| `docs/rules/sml.rules.md` | yes | No runtime actor behavior was widened by the reference lane. |
| `docs/rules/cpp.rules.md` | yes | Verification includes focused tests, wrapper execution, provenance docs, and quality gates. |

No rule violations were found within validation scope.

## Test Infrastructure

| Property | Value |
|----------|-------|
| Framework | doctest, ONNX Runtime reference lane, quality gates |
| Quick run command | `ctest --test-dir build/bench_tools_ninja --output-on-failure -R '^diarization_compare_tests$' -j 1` |
| Full suite command | `scripts/quality_gates.sh` |
| ONNX command | `scripts/bench_diarization_compare.sh --skip-emel-build --onnx-reference-model build/onnx_ref/diar_streaming_sortformer_4spk-v2.1.onnx --output-dir build/diarization_compare_92_5_script` |

## Manual-Only Verifications

None. The ONNX bounded drift was recorded as an executable compare outcome and routed to Phase
`92.6`.

## Validation Sign-Off

- [x] Completion preconditions satisfied.
- [x] Rule-compliance review recorded.
- [x] Executable verification commands documented.
- [x] No manual-only blockers remain.
- [x] `nyquist_compliant: true` is supported by artifact evidence.

Approval: approved 2026-04-24.
