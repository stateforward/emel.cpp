---
phase: 92.5
plan: 1
status: complete
completed: 2026-04-24
---

# Phase 92.5 Plan

## Goal

Add a real ONNX Sortformer reference lane to the diarization compare workflow while preserving the
self-recorded maintained regression lane and keeping the ONNX result explicit rather than falling
back to the recorded checksum fixture.

## Tasks

1. Validate the existing compare-path implementation in `tools/bench/diarization_compare.py`,
   `tools/bench/diarization_sortformer_onnx_reference.py`, and
   `scripts/bench_diarization_compare.sh`.
2. Tighten the ONNX contract around source identity and checksum validation.
3. Record the ONNX model provenance in operator-facing docs.
4. Run focused compare validation, then run `scripts/quality_gates.sh`.

## Exit Conditions

- `diarization_compare_tests` passes.
- The real maintained compare workflow can emit both self-recorded regression and ONNX results.
- Missing-model / missing-runtime / checksum-mismatch paths remain explicit failures.
- `scripts/quality_gates.sh` passes, or an exact blocker is recorded.
