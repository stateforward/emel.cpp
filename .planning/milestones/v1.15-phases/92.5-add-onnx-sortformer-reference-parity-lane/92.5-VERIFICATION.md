# Phase 92.5 Verification

| Check | Status | Evidence |
| --- | --- | --- |
| Focused diarization compare tests | Passed | `ctest --test-dir build/bench_tools_ninja --output-on-failure -R '^diarization_compare_tests$' -j 1` passed. |
| Real compare driver with ONNX lane | Passed | `build/onnx_ref/venv/bin/python tools/bench/diarization_compare.py --emel-runner build/bench_tools_ninja/bench_runner --onnx-reference-model build/onnx_ref/diar_streaming_sortformer_4spk-v2.1.onnx --output-dir build/diarization_compare_92_5` exited `0` and reported `onnx.sortformer.v2_1` as `bounded_drift` while `recorded.diarization.baseline` stayed `exact_match`. |
| Shell wrapper with ONNX lane | Passed | `PATH="$PWD/build/onnx_ref/venv/bin:$PATH" scripts/bench_diarization_compare.sh --skip-emel-build --onnx-reference-model build/onnx_ref/diar_streaming_sortformer_4spk-v2.1.onnx --output-dir build/diarization_compare_92_5_script` exited `0` and produced the same split outcome. |
| ONNX model provenance documented | Passed | `docs/benchmarking.md` and `tests/models/README.md` now record the ONNX source repo, expected local path, SHA-256, and runtime expectation. |
| Repo quality gate | Passed | The final changed-aware quality gate completed with the Sortformer benchmark and docs generation included. `snapshots/quality_gates/timing.txt` records `build_with_zig 5`, `test_with_coverage 12`, `bench_snapshot_diarization_sortformer 18`, `generate_docs 211`, and `total 246`. |

## Notes

- Validation used a repo-local Python virtualenv at `build/onnx_ref/venv` because the host
  Homebrew Python is PEP 668 managed and rejected `pip install --user onnxruntime`.
- The ONNX lane uses the maintained exported `raw/onnx_features.f32` tensor and never substitutes
  the self-recorded regression snapshot for ONNX outcomes.
- The initial ONNX bounded drift was deliberately left visible and routed to Phase `92.6` instead
  of being hidden behind the self-recorded regression snapshot.
