---
phase: 92.3
status: completed
---

# Phase 92.3 Verification

## Result

Passed for implementation and maintained-runtime validation.

## Checks

| Check | Result | Evidence |
|-------|--------|----------|
| Kernel-domain dense dispatch landed on the maintained Sortformer path | Passed | `src/emel/diarization/sortformer/detail.cpp` now issues `emel::kernel::event::op_mul_mat` requests. |
| Output path no longer depends on aliased in-place dense behavior | Passed | `src/emel/diarization/sortformer/output/detail.cpp` now projects into a separate `frame_hidden` buffer. |
| Non-aliased output regression is covered | Passed | `build/debug/emel_tests_bin --test-case='sortformer output uses non-aliased frame-hidden projection' --success` |
| Maintained parity baseline matches repaired runtime | Passed | `build/debug/emel_tests_bin --test-case='sortformer parity matches maintained real model and audio checksum baseline' --success` |
| Rebuilt compare tooling sees the new maintained baseline | Passed | `/tmp/diarization_truth_probe_current/raw/emel.jsonl` and `compare_summary.json` report `output_dim=71`, `output_checksum=2496121829252312372` |
| Repo validation remained green on touched code | Passed | `scripts/quality_gates.sh` cleared unit, coverage, paritychecker, and fuzz lanes before the long compare tail; no failure surfaced on the touched Sortformer slice. |

## Commands

- `git diff --check -- src/emel/diarization/sortformer/output/detail.cpp tests/diarization/sortformer/output/lifecycle_tests.cpp tests/fixtures/diarization/ami_en2002b_mix_headset_137.00_152.04_16khz_mono.baseline.txt`
- `cmake --build build/debug --target emel_tests_bin -j 6`
- `build/debug/emel_tests_bin --test-case='sortformer output computes deterministic speaker probabilities' --success`
- `build/debug/emel_tests_bin --test-case='sortformer output uses non-aliased frame-hidden projection' --success`
- `build/debug/emel_tests_bin --test-case='sortformer pipeline runs maintained pcm to probabilities and segments' --success`
- `build/debug/emel_tests_bin --test-case='sortformer parity matches maintained real model and audio checksum baseline' --success`
- `cmake --build build/bench_tools_ninja --target bench_runner bench_runner_tests diarization_compare_tests -j 6`
- `build/bench_tools_ninja/bench_runner_tests --test-case='bench_runner diarization jsonl emits structured maintained parity metadata'`
- `build/bench_tools_ninja/diarization_compare_tests`
- `python3 tools/bench/diarization_compare.py --output-dir /tmp/diarization_truth_probe_current --emel-runner build/bench_tools_ninja/bench_runner`
- `scripts/quality_gates.sh`
