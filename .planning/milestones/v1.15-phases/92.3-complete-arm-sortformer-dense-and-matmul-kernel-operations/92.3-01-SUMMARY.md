---
phase: 92.3
plan: 01
status: completed
requirements-completed:
  - RUN-03
  - BEN-01
---

# Phase 92.3 Summary

## Outcome

Phase `92.3` is complete. The maintained Sortformer dense/matmul lane now dispatches through the
kernel domain, and the maintained output path no longer depends on the stage-local in-place dense
behavior that previously masked a kernel migration bug.

## What Changed

- Routed `src/emel/diarization/sortformer/detail.cpp` through
  `emel::kernel::event::op_mul_mat`, using the same kernel request contract already proven by the
  maintained generator and embeddings lanes.
- Kept Sortformer dense work in the kernel domain instead of preserving a separate maintained
  stage-local matmul implementation.
- Repaired `src/emel/diarization/sortformer/output/detail.cpp` so the frame-hidden projection uses
  a separate buffer instead of an aliased in-place dense call.
- Added a focused regression in
  `tests/diarization/sortformer/output/lifecycle_tests.cpp` that proves the output head uses the
  non-aliased projection semantics.
- Updated the maintained AMI baseline fixture to the repaired runtime output:
  - `segment_count=71`
  - `output_checksum=2496121829252312372`

## Maintained Runtime Facts

- Real model: `tests/models/diar_streaming_sortformer_4spk-v2.1.gguf`
- Real audio: `tests/fixtures/diarization/ami_en2002b_mix_headset_137.00_152.04_16khz_mono.wav`
- Maintained parity baseline:
  - `segment_count=71`
  - `output_checksum=2496121829252312372`
- Rebuilt diarization compare evidence in `/tmp/diarization_truth_probe_current` now reports:
  - `output_dim=71`
  - `output_checksum=2496121829252312372`

## Next Phase

Phase `92.4` now owns the post-kernel publication refresh:

- rerun and publish benchmark artifacts from the repaired maintained lane
- refresh benchmark/docs outputs
- rerun the milestone audit for final closeout

That remaining publication work is blocked on explicit user consent before updating
`snapshots/bench/*`.
