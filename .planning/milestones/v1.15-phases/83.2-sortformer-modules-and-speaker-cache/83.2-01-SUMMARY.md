---
requirements-completed: []
---

# Phase 83.2 Summary: Sortformer Modules And Speaker Cache

## Outcome

Phase 83.2 is complete. The maintained Sortformer module/head tensors and speaker cache now have
stage-owned native contract surfaces under `src/emel/diarization/sortformer/modules/` and
`src/emel/diarization/sortformer/cache/`.

## Completed

- Added shared Sortformer dense numeric helper to avoid duplicated dense loops.
- Added maintained `mods.ep.*`, `mods.fh2h.*`, `mods.h2s.*`, and `mods.sh2s.*` tensor binding.
- Added fixed-shape rejection for the maintained module tensors.
- Added deterministic encoder-projection and speaker-logit primitive kernels.
- Added fixed-profile preallocated speaker-cache state with reset/write/read helpers.
- Added focused tests for binding, missing tensor rejection, shape drift rejection, cache
  determinism, and module kernels.

## Verification

- `git diff --check`
- `cmake --build build/coverage --target emel_tests_bin -j 8`
- `ctest --test-dir build/coverage --output-on-failure -R '^emel_tests_diarization$' -j 1`

## Deferred

- Transformer execution remains Phase 83.3.
- End-to-end execution composition remains Phase 83.4.
- Speaker probabilities and segments remain Phase 84.
