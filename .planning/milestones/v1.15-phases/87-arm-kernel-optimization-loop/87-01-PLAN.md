# Phase 87 Plan: ARM Kernel Optimization Loop

## Goal

Iterate on measured ARM hotspots until additional optimizations are no longer material for the
maintained Sortformer profile.

## Tasks

1. Optimize the Sortformer-local dense f32 helper used by the measured transformer hotspot.
2. Run before/after suite-filtered benchmark evidence against the maintained
   `diarization_sortformer` workload.
3. Verify correctness through focused tests and full quality gates.
4. Record whether another material optimization candidate remains.

## Verification

- `git diff --check`.
- `ctest --test-dir build/coverage --output-on-failure -R '^emel_tests_diarization$' -j 1`.
- `EMEL_BENCH_SUITE=diarization_sortformer ... bench_runner --mode=compare`.
- `scripts/quality_gates.sh`.
