# Phase 85 Plan: Parity Proof And Initial ARM Benchmark

## Goal

Establish lane-isolated proof and an initial maintained ARM benchmark before optimization work
begins.

## Tasks

1. Add a deterministic repository test for a canonical Sortformer multi-speaker output fixture.
2. Add a `diarization_sortformer` benchmark suite with EMEL and reference append lanes.
3. Emit benchmark metadata for fixture identity, profile parameters, and proof status.
4. Update benchmark documentation with supported model/input/output contracts and limitations.
5. Run focused tests, benchmark-suite smoke validation, and `scripts/quality_gates.sh`.

## Verification

- `git diff --check` for touched files.
- Build `emel_tests_bin` and `bench_runner`.
- `ctest --test-dir build/coverage --output-on-failure -R '^emel_tests_diarization$' -j 1`.
- `EMEL_BENCH_SUITE=diarization_sortformer EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 build/bench_tools_ninja/bench_runner --mode=compare`.
- `scripts/quality_gates.sh`.
