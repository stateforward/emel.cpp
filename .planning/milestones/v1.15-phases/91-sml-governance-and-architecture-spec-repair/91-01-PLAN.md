---
phase: 91
plan: 01
status: complete
rule_constraints:
  - AGENTS.md
  - docs/rules/sml.rules.md
  - docs/rules/cpp.rules.md
requirements:
  - RUN-02
  - DIA-03
  - DOC-01
---

# Phase 91 Plan 1: SML Governance And Architecture Spec Repair

## Goal

Restore SML/governance compliance for the maintained diarization actors and generated machine docs
before milestone closeout.

## Tasks

1. Remove action-side `error_out` branching from `src/emel/diarization/request/` and
   `src/emel/diarization/sortformer/executor/` by expressing optional sink behavior through guards
   and explicit publication states.
2. Remove executor-side runtime hidden-buffer selection from the action path by switching to
   compile-time-selected transformer layer execution.
3. Move docs generator architecture output out of `docs/architecture/`, update generated README
   links, and remove stale generated files from `docs/architecture/`.
4. Validate with focused diarization tests, docs regeneration, and the full quality gate.

## Verification

- `git diff --check -- src/emel/diarization/request src/emel/diarization/sortformer/executor tools/docsgen/docsgen.cpp docs/templates/README.md.j2 README.md .planning/architecture`
- `cmake --build build/coverage --target emel_tests_bin -j 6`
- `ctest --test-dir build/coverage --output-on-failure -R '^emel_tests_diarization$' -j 1`
- `scripts/generate_docs.sh`
- `scripts/quality_gates.sh`
