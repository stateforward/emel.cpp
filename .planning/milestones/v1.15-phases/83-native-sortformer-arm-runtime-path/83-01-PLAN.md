---
phase: 83
plan: 01
title: Maintained Sortformer Contract Repair Before Native Execution
status: complete
rule_constraints:
  - Native execution claims must be backed by EMEL-owned `src/` execution, not reference/runtime
    fallbacks.
  - Execution behavior choice belongs in SML guards and transitions.
  - Do not substitute a synthetic or head-only path for the maintained encoder/cache/transformer
    execution.
---

# Phase 83 Plan: Maintained Sortformer Contract Repair Before Native Execution

## Goal

Unblock Phase 83 by realigning the already-landed Sortformer model and diarization request
contracts with the maintained GGUF artifact. Do not claim RUN-01 complete until the full native
Conformer plus Sortformer transformer/cache path exists.

<rule_constraints>

- Keep Sortformer-specific metadata isolated under `src/emel/model/sortformer/`.
- Keep diarization request constants under the diarization request component.
- Do not add generic-model fields for Sortformer execution state.
- Do not add a tool-only, Python, ONNX, llama.cpp, ggml, or synthetic compute fallback.
- Do not update snapshot baselines.

</rule_constraints>

## Tasks

1. Update `model::sortformer::detail::execution_contract` to use the real maintained tensor
   families: `prep.`, `enc.`, `mods.`, and `te.`.
2. Update maintained streaming constants to the GGUF metadata values:
   `chunk_len=188`, `chunk_right_context=1`, `fifo_len=0`,
   `spkcache_update_period=188`, `spkcache_len=188`.
3. Update the diarization request feature-extractor contract to 188 frames and 128 feature bins.
4. Update focused tests and fixture documentation so contract drift fails locally.
5. Run focused model-loader and diarization request tests.

## Verification

- `cmake --build build/coverage --target emel_tests_bin -j 8`
- `ctest --test-dir build/coverage --output-on-failure -R '^emel_tests_(diarization|model_and_batch)$' -j 2`

## Follow-Up Split

RUN-01 remains incomplete and is split into decimal follow-up phases. The real native execution
path must use this stage-owned layout:

- `src/emel/diarization/sortformer/encoder/feature_extractor/` for Sortformer feature extraction.
- `src/emel/diarization/sortformer/encoder/` for Conformer encoder layers over `enc.*`.
- `src/emel/diarization/sortformer/modules/` for projection, speaker cache, and heads under
  `mods.*`.
- `src/emel/diarization/sortformer/transformer/` for transformer layers over `te.*`.
- `src/emel/diarization/sortformer/output/` for `T x 4` probabilities and segments.
