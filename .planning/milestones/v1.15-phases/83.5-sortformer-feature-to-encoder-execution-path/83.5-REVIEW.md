---
phase: 83.5
status: clean
depth: standard
reviewed: 2026-04-23
---

# Phase 83.5 Code Review

## Result

Clean after fix.

## Scope

- `src/emel/diarization/sortformer/encoder/detail.hpp`
- `src/emel/diarization/sortformer/encoder/detail.cpp`
- `tests/diarization/sortformer/encoder/lifecycle_tests.cpp`
- `tests/diarization/sortformer/executor/lifecycle_tests.cpp`

## Findings

No open findings.

## Fixed During Review

| Severity | Finding | Fix |
|----------|---------|-----|
| warning | `compute_encoder_frames_from_features` trusted the caller to pass a fully bound encoder contract and would dereference null `enc.pre.*` tensor views for an unbound contract. | Added `pre_contract_bound` validation before tensor data binding and added `sortformer encoder rejects unbound feature-to-frame contract`. |

## Verification After Fix

- `cmake --build build/coverage --target emel_tests_bin -j 8`
- `ctest --test-dir build/coverage --output-on-failure -R '^emel_tests_diarization$' -j 1`
- `scripts/quality_gates.sh`
