# Phase 92.1 Blocker: Resolved

## Status

Resolved on `2026-04-23`. The publication closeout work is complete.

## What Was Proven

- Parity and benchmark now run the real maintained GGUF + real AMI audio fixture through the
  maintained EMEL-owned pipeline path.
- The diarization bench no longer has to rely on suite-specific human text as its only publication
  surface.
- A deterministic compare workflow now exists:
  - `tools/bench/diarization_compare_contract.hpp`
  - `tools/bench/diarization_compare.py`
  - `scripts/bench_diarization_compare.sh`
- The maintained compare smoke produced `diarization_compare_summary/v1` with exact-match verdicts
  for:
  - `diarization/sortformer/ami_en2002b_mix_headset_137.00_152.04_16khz_mono`
  - `diarization/sortformer/profile_ami_en2002b_mix_headset_137.00_152.04_16khz_mono`

## Live Evidence

- `build/bench_tools_ninja/bench_runner_tests --test-case='bench_runner diarization jsonl emits structured maintained parity metadata'`
  passed.
- `build/bench_tools_ninja/diarization_compare_tests` passed.
- `python3 tools/bench/diarization_compare.py --output-dir build/diarization_compare_smoke --emel-runner build/bench_tools_ninja/bench_runner`
  exited `0` and wrote:
  - `build/diarization_compare_smoke/raw/emel.jsonl`
  - `build/diarization_compare_smoke/raw/reference.jsonl`
  - `build/diarization_compare_smoke/compare_summary.json`

## Resolution

1. Checked-in benchmark snapshots were refreshed:
   - `snapshots/bench/benchmarks.txt`
   - `snapshots/bench/benchmarks_compare.txt`
2. `docs/benchmarks.md` was regenerated from the snapshot-driven docsgen path.
3. The milestone audit was rerun against the new maintained deterministic compare workflow.

No blocker remains for `92.1`.
