---
phase: 92.1
status: passed
verified: 2026-04-23
requirements:
  - SORT-01
  - SORT-02
  - PRF-01
  - PRF-02
  - BEN-01
  - DOC-01
---

# Phase 92.1 Verification

## Requirement Evidence

| Requirement | Evidence | Status |
|-------------|----------|--------|
| SORT-01 | `tools/bench/diarization/sortformer_bench.cpp` publishes maintained metadata for `tests/models/diar_streaming_sortformer_4spk-v2.1.gguf` and the AMI proof fixture instead of the old synthetic bench-only contract story. | Passed |
| SORT-02 | `snapshots/bench/benchmarks_compare.txt` and `build/diarization_compare_final/compare_summary.json` now record the maintained loader/fixture provenance and truthful checksum-backed compare facts for the pinned artifact. | Passed |
| PRF-01 | `build/diarization_compare_final/compare_summary.json` reports `exact_match` for the maintained primary parity case with `output_dim=17` and `output_checksum=17895507502300857007`. | Passed |
| PRF-02 | `tools/bench/diarization_compare.py` and `tools/bench/bench_main.cpp` keep EMEL and reference lanes split into separate raw JSONL outputs and compare them only at the summary layer. | Passed |
| BEN-01 | `snapshots/bench/benchmarks.txt`, `snapshots/bench/benchmarks_compare.txt`, and `docs/benchmarks.md` now publish the maintained Sortformer steady-state baseline from the deterministic compare workflow rather than a suite-local prose shortcut. | Passed |
| DOC-01 | `docs/templates/benchmarks.md.j2`, `tools/docsgen/docsgen.cpp`, and regenerated `docs/benchmarks.md` now derive the Sortformer publication section from current snapshot metadata. | Passed |

## Commands

- `git diff --check -- tools/bench/CMakeLists.txt tools/bench/bench_main.cpp tools/bench/bench_runner_tests.cpp tools/bench/diarization/sortformer_bench.cpp tools/bench/diarization_compare_contract.hpp tools/bench/diarization_compare.py tools/bench/diarization_compare_tests.cpp docs/benchmarking.md docs/templates/benchmarks.md.j2 docs/benchmarks.md tools/docsgen/docsgen.cpp scripts/bench_diarization_compare.sh snapshots/bench/benchmarks.txt snapshots/bench/benchmarks_compare.txt .planning/phases/92.1-maintained-gguf-contract-parity-and-benchmark-truth-repair .planning/ROADMAP.md .planning/STATE.md .planning/v1.15-MILESTONE-AUDIT.md`
- `cmake --build build/bench_tools_ninja --target bench_runner bench_runner_tests diarization_compare_tests -j 6`
- `build/bench_tools_ninja/bench_runner_tests --test-case='bench_runner diarization jsonl emits structured maintained parity metadata'`
- `build/bench_tools_ninja/diarization_compare_tests`
- `python3 tools/bench/diarization_compare.py --output-dir build/diarization_compare_final --emel-runner build/bench_tools_ninja/bench_runner`
- `scripts/generate_docs.sh`
- `scripts/generate_docs.sh --check`
- `scripts/quality_gates.sh`

## Results

- `git diff --check` passed on the touched diarization benchmark/docs/planning files.
- Bench tooling build passed.
- Focused diarization bench-runner JSONL test passed.
- `diarization_compare_tests` passed.
- The operator-facing compare workflow passed and wrote:
  - `build/diarization_compare_final/raw/emel.jsonl`
  - `build/diarization_compare_final/raw/reference.jsonl`
  - `build/diarization_compare_final/compare_summary.json`
- `compare_summary.json` reported:
  - schema: `diarization_compare_summary/v1`
  - `failed: false`
  - both maintained compare groups: `exact_match`
  - exact checksum match: `true`
  - exact output dim match: `true`
- `scripts/generate_docs.sh` regenerated `docs/benchmarks.md`.
- `scripts/generate_docs.sh --check` passed against the regenerated docsgen binary.
- `scripts/quality_gates.sh` exited `0`.
  - Full test shards passed.
  - Coverage gate passed at `94.4%` lines and `69.4%` branches on changed `src/` files.
  - `paritychecker_tests` and `fuzz_smoke` passed.
  - The script reported a warning-only benchmark snapshot regression for
    `tokenizer/full_spm_short`; it did not fail the gate and is outside the Sortformer reopen
    scope.

## Notes

- The earlier full `bench_runner_tests` sweep was intentionally interrupted and is not used as
  completion evidence here.
- The maintained benchmark publication keeps prepare/setup timing visible, but the primary
  performance claim remains the steady-state runtime lane.
