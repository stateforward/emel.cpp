---
phase: 92.1
plan: 01
status: complete
completed: 2026-04-23
requirements-completed:
  - SORT-01
  - SORT-02
  - PRF-01
  - PRF-02
  - BEN-01
  - DOC-01
---

# Phase 92.1 Plan 1 Summary: Maintained GGUF Contract Parity And Benchmark Truth Repair

## Outcome

Phase `92.1` closed the last publication gap in `v1.15`. Sortformer parity and benchmark
publication now stand on the real maintained path:

- pinned maintained GGUF artifact
- pinned real AMI audio fixture
- maintained loader-built execution contract
- maintained `pipeline::sm` runtime lane
- deterministic raw-record-plus-summary benchmark reporting

## Completed Work

- Added a maintained diarization compare contract in
  `tools/bench/diarization_compare_contract.hpp` and a deterministic compare driver in
  `tools/bench/diarization_compare.py`.
- Added the operator-facing wrapper `scripts/bench_diarization_compare.sh` so diarization uses the
  same raw-record -> summary publication shape as generation.
- Updated `tools/bench/bench_main.cpp` and `tools/bench/diarization/sortformer_bench.cpp` to emit
  `diarization_compare/v1` JSONL records with maintained model, fixture, workload, checksum, and
  prepare/runtime metadata.
- Added focused validation in `tools/bench/bench_runner_tests.cpp` and
  `tools/bench/diarization_compare_tests.cpp`.
- Refreshed checked-in benchmark snapshots and regenerated `docs/benchmarks.md` from the snapshot-
  driven docsgen path so the published Sortformer section now cites the real maintained model,
  real AMI fixture, deterministic compare workflow, and the current truthful baseline.

## Evidence

- Maintained model: `tests/models/diar_streaming_sortformer_4spk-v2.1.gguf`
- Maintained proof fixture:
  `tests/fixtures/diarization/ami_en2002b_mix_headset_137.00_152.04_16khz_mono.wav`
- Maintained output baseline:
  - `output_dim=17`
  - `output_checksum=17895507502300857007`
- Deterministic compare verdicts:
  - `diarization/sortformer/ami_en2002b_mix_headset_137.00_152.04_16khz_mono`: `exact_match`
  - `diarization/sortformer/profile_ami_en2002b_mix_headset_137.00_152.04_16khz_mono`:
    `exact_match`
- Current checked-in steady-state benchmark rows:
  - primary: `2115609708.000 ns/op`
  - profile: `2278999458.000 ns/op`
- Current live compare smoke on the maintained lane:
  - primary runtime: `2100251125 ns/op`
  - primary prepare: `93082125 ns/op`
  - profile runtime: `2103181584 ns/op`
  - profile prepare: `93082125 ns/op`

## Notes

- The optimization claim stays focused on steady-state runtime. Prepare/setup timing is still
  recorded, but it remains secondary evidence.
- The reference lane stays isolated and non-authoritative; it exists only to compare deterministic
  maintained-output metadata.
