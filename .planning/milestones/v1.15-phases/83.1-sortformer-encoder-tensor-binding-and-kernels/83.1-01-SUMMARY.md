---
requirements-completed: []
---

# Phase 83.1 Summary: Sortformer Encoder Tensor Binding And Kernels

## Outcome

Phase 83.1 is complete. The maintained Sortformer encoder now has a stage-owned native contract
surface under `src/emel/diarization/sortformer/encoder/`.

## Completed

- Added fixed-profile binding for the maintained `enc.pre.*` tensors.
- Added fixed-profile binding for all `enc.l0.*` through `enc.l16.*` layer tensor contracts.
- Added shape validation for maintained f32 encoder dimensions.
- Added component-owned deterministic affine and dense kernel primitives.
- Added focused diarization encoder tests for valid binding, missing tensor rejection, shape drift
  rejection, deterministic kernel output, and invalid dense shape rejection.

## Verification

- `git diff --check`
- `cmake --build build/coverage --target emel_tests_bin -j 8`
- `ctest --test-dir build/coverage --output-on-failure -R '^emel_tests_diarization$' -j 1`

## Deferred

- Full module/cache execution remains Phase 83.2.
- Full transformer execution remains Phase 83.3.
- End-to-end execution composition remains Phase 83.4.
