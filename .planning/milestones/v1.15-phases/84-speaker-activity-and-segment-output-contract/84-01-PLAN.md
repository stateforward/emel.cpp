---
phase: 84
plan: 01
status: planned
wave: 1
autonomous: true
rule_constraints:
  - Keep output conversion under `src/emel/diarization/sortformer/output/`.
  - Keep output helpers allocation-free and caller-owned.
  - Preserve existing executor modules/cache/transformer behavior.
  - Do not add tool-only or benchmark-local output conversion.
---

# Plan 84-01: Deterministic Speaker Probability And Segment Output

<objective>
Add a native Sortformer output contract that converts maintained `188 x 192` hidden frames into
deterministic `188 x 4` probabilities and bounded snapshot-friendly segment records.
</objective>

<rule_constraints>
- Consume maintained `mods.sh2s.*` tensors from the existing modules contract.
- Decode speaker segments independently so overlap is explicit and allowed.
- Use caller-owned spans for probability and segment outputs.
- Add focused doctest coverage and keep CMake wiring explicit.
</rule_constraints>

<tasks>
1. Add `src/emel/diarization/sortformer/output/detail.hpp` and `.cpp` with fixed-profile output
   constants, `segment_record`, probability computation, and segment decoding.
2. Wire the output source into `CMakeLists.txt`.
3. Add `tests/diarization/sortformer/output/lifecycle_tests.cpp` and wire it into the diarization
   shard.
4. Verify focused tests and the full quality gate.
</tasks>

<verification>
- `cmake --build build/coverage --target emel_tests_bin -j 8`
- `ctest --test-dir build/coverage --output-on-failure -R '^emel_tests_diarization$' -j 1`
- `scripts/quality_gates.sh`
</verification>
