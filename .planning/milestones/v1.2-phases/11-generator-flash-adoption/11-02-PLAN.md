---
phase: 11-generator-flash-adoption
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/emel/generator/detail.hpp
  - tests/generator/lifecycle_tests.cpp
  - tests/generator/detail_tests.cpp
autonomous: true
requirements:
  - GEN-01
  - GEN-02
user_setup: []
must_haves:
  truths:
    - canonical generation completes through flash attention on supported requests.
    - unsupported or non-canonical generator flash formation fails deterministically and does not claim flash-path execution.
  artifacts:
    - lifecycle tests prove canonical flash success and explicit unsupported-request failure.
    - negative-path assertions verify the flash-specific counter does not falsely report flash execution.
  key_links:
    - Phase 12 parity proof depends on these tests to distinguish truthful flash execution from mere kernel activity.
---

<objective>
Prove canonical generator flash adoption and deterministic unsupported-request behavior.

Purpose: finish `GEN-01` and `GEN-02` by turning the new generator flash seam into explicit
automated behavior guarantees.
Output: generator tests prove the canonical request completes with flash selected, and deliberately
non-canonical runtime shapes fail clearly without incrementing flash-execution proof signals.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-generator-flash-adoption/11-CONTEXT.md
@.planning/phases/11-generator-flash-adoption/11-RESEARCH.md
@.planning/phases/11-generator-flash-adoption/11-VALIDATION.md
@.planning/phases/11-generator-flash-adoption/11-01-PLAN.md
@src/emel/generator/detail.hpp
@src/emel/generator/sm.hpp
@tests/generator/lifecycle_tests.cpp
@tests/generator/detail_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove canonical generation finishes with flash attention selected</name>
  <files>tests/generator/lifecycle_tests.cpp, tests/generator/detail_tests.cpp</files>
  <read_first>tests/generator/lifecycle_tests.cpp, tests/generator/detail_tests.cpp, src/emel/generator/detail.hpp, src/emel/generator/sm.hpp, .planning/phases/11-generator-flash-adoption/11-VALIDATION.md</read_first>
  <action>Extend `tests/generator/lifecycle_tests.cpp` so the canonical native generator fixture executes a one-token generation request and asserts all of the following: generation succeeds, the produced token/output remains the expected canonical result for the fixture, `generation_kernel_dispatch_calls()` remains greater than zero, and `generation_flash_attention_dispatch_calls()` is greater than zero. Add any focused helper assertions to `tests/generator/detail_tests.cpp` if the canonical flash request builder needs direct shape or scale verification.</action>
  <verify>ctest --output-on-failure -R generator</verify>
  <automated>ctest --output-on-failure -R generator</automated>
  <acceptance_criteria>
    - `tests/generator/lifecycle_tests.cpp` contains a canonical generation test that asserts `generation_flash_attention_dispatch_calls()` is greater than zero
    - the canonical generation test still asserts successful generation and expected output/token behavior
    - `ctest --output-on-failure -R generator` exits 0
  </acceptance_criteria>
  <done>The canonical generator request is proven to complete through the flash path, not merely through generic kernel work.</done>
</task>

<task type="auto">
  <name>Task 2: Prove unsupported flash formation fails explicitly without a false flash claim</name>
  <files>src/emel/generator/detail.hpp, tests/generator/lifecycle_tests.cpp, tests/generator/detail_tests.cpp</files>
  <read_first>src/emel/generator/detail.hpp, tests/generator/lifecycle_tests.cpp, tests/generator/detail_tests.cpp, .planning/phases/11-generator-flash-adoption/11-CONTEXT.md, .planning/phases/11-generator-flash-adoption/11-RESEARCH.md</read_first>
  <action>Create a deterministic negative test path by mutating the native generator fixture or request formation inputs so the generator cannot build a canonical flash request accepted by `src/emel/kernel/detail.hpp`. Propagate that rejection as an explicit generator failure rather than a silent materialized-attention fallback. Assert in `tests/generator/lifecycle_tests.cpp` that the generation call fails or reports error through the existing generator contract, and that `generation_flash_attention_dispatch_calls()` remains zero for that rejected case. Add any narrow helper-level negative coverage in `tests/generator/detail_tests.cpp` if the invalid shape detection is easier to prove there.</action>
  <verify>ctest --output-on-failure -R generator && scripts/quality_gates.sh</verify>
  <automated>ctest --output-on-failure -R generator && scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - `tests/generator/lifecycle_tests.cpp` contains an unsupported-request failure test for generator flash adoption
    - the unsupported-request test asserts `generation_flash_attention_dispatch_calls()` stays zero for the rejected case
    - the unsupported-request test does not assert or rely on a silent materialized-attention fallback
    - `scripts/quality_gates.sh` exits 0
  </acceptance_criteria>
  <done>Phase 11 has direct automated proof that unsupported generator flash requests fail truthfully and do not misreport flash execution.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --output-on-failure -R generator`
- [ ] `scripts/quality_gates.sh`
- [ ] `rg 'generation_flash_attention_dispatch_calls|op_flash_attn_ext' src/emel/generator tests/generator`
- [ ] canonical success and unsupported-failure tests both assert flash-specific observability correctly
- [ ] no test or code path silently relabels materialized attention as flash execution
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `GEN-01` is proven on the canonical generator request
- `GEN-02` is proven through explicit unsupported-request failure behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/11-generator-flash-adoption/11-02-SUMMARY.md`
</output>
