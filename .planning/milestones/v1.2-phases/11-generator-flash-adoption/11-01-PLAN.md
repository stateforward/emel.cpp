---
phase: 11-generator-flash-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emel/generator/detail.hpp
  - src/emel/generator/sm.hpp
  - src/emel/kernel/detail.hpp
  - tests/generator/detail_tests.cpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - GEN-01
user_setup: []
must_haves:
  truths:
    - the canonical generator runtime dispatches `op_flash_attn_ext` through the real kernel actor instead of using generator-local materialized attention.
    - generator-owned observability distinguishes flash-attention dispatch from generic kernel matmul traffic.
  artifacts:
    - `src/emel/generator/detail.hpp` builds a flash request over the existing Q/K/V and cache buffers and dispatches it from `run_layer(...)`.
    - `src/emel/kernel/detail.hpp` accepts the generator's position-major K/V cache layout as a truthful canonical flash input.
    - `src/emel/generator/sm.hpp` exposes flash-specific generator observability for tests and later parity work.
    - generator tests can assert that canonical generation used the flash path.
  key_links:
    - Phase 12 can only publish truthful flash-path proof if Phase 11 lands durable generator-local observability here.
---

<objective>
Replace the generator's materialized attention seam with real flash-kernel dispatch and add
flash-specific runtime observability.

Purpose: satisfy the routing half of `GEN-01` by making canonical generation use
`op_flash_attn_ext` through the existing generator -> graph -> processor -> kernel chain.
Output: generator detail code builds canonical flash requests from the current cache layout,
dispatches them through `backend.kernel`, and records flash-specific runtime signals that tests can
query.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-generator-flash-adoption/11-CONTEXT.md
@.planning/phases/11-generator-flash-adoption/11-RESEARCH.md
@.planning/phases/11-generator-flash-adoption/11-VALIDATION.md
@src/emel/generator/detail.hpp
@src/emel/generator/sm.hpp
@src/emel/kernel/detail.hpp
@src/emel/kernel/detail.hpp
@src/emel/kernel/events.hpp
@tests/generator/detail_tests.cpp
@tests/generator/lifecycle_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch canonical generator attention through `op_flash_attn_ext`</name>
  <files>src/emel/generator/detail.hpp, src/emel/kernel/detail.hpp, tests/generator/detail_tests.cpp</files>
  <read_first>src/emel/generator/detail.hpp, src/emel/kernel/detail.hpp, src/emel/kernel/events.hpp, .planning/phases/11-generator-flash-adoption/11-CONTEXT.md, .planning/phases/11-generator-flash-adoption/11-RESEARCH.md, tests/generator/detail_tests.cpp</read_first>
  <action>Add a generator-local helper in `src/emel/generator/detail.hpp` that constructs an `emel::kernel::event::op_flash_attn_ext` request over the existing `backend.q`, `backend.key_cache`, `backend.value_cache`, and `backend.attn_ctx` buffers using the current position-major cache layout and `1.0f / sqrt(head_dim)` scale in `op_params`. Update `src/emel/kernel/detail.hpp` so `can_run_flash_attn_ext(...)` accepts the generator's canonical position-major K/V cache strides as valid flash operands instead of requiring dense-contiguous K/V tensors, while preserving explicit rejection for non-canonical shapes. Update `run_layer(...)` so the canonical attention step dispatches that event through `backend.kernel.process_event(...)` instead of calling generator-local materialized attention from the active runtime path. Keep the current shared `compute_attention(...)` helper available only if other non-Phase-11 code still requires it, but the canonical generator runtime path itself must route through the real flash op. Extend `tests/generator/detail_tests.cpp` with focused coverage for the flash-request builder or request-shape helper if standalone validity logic was added.</action>
  <verify>ctest --output-on-failure -R generator</verify>
  <automated>ctest --output-on-failure -R generator</automated>
  <acceptance_criteria>
    - `src/emel/generator/detail.hpp` contains `emel::kernel::event::op_flash_attn_ext`
    - `src/emel/kernel/detail.hpp` no longer requires dense-contiguous `src1` and `src2` for the canonical flash-attention path
    - `src/emel/generator/detail.hpp` contains a generator-local flash dispatch helper or request-construction helper for `run_layer(...)`
    - the active `run_layer(...)` path in `src/emel/generator/detail.hpp` no longer invokes `compute_attention(...)` for the canonical runtime execution path
    - `tests/generator/detail_tests.cpp` contains flash-attention helper coverage if new helper logic was added
  </acceptance_criteria>
  <done>The canonical generator attention seam now dispatches the real flash kernel event through the existing kernel actor.</done>
</task>

<task type="auto">
  <name>Task 2: Add generator-owned flash observability for later proof surfaces</name>
  <files>src/emel/generator/detail.hpp, src/emel/generator/sm.hpp, tests/generator/lifecycle_tests.cpp</files>
  <read_first>src/emel/generator/detail.hpp, src/emel/generator/sm.hpp, tests/generator/lifecycle_tests.cpp, tools/paritychecker/paritychecker_tests.cpp, .planning/phases/11-generator-flash-adoption/11-CONTEXT.md</read_first>
  <action>Add generator-owned flash-specific observability in the native backend state under `src/emel/generator/detail.hpp`, such as `flash_attention_dispatch_calls` and any minimal supporting state needed to prove whether flash ran. Expose that signal from `src/emel/generator/sm.hpp` with a getter distinct from `generation_kernel_dispatch_calls()`. Extend `tests/generator/lifecycle_tests.cpp` so the canonical native generator fixture asserts the new flash-specific getter is greater than zero after successful generation, while keeping the existing generic kernel dispatch getter intact.</action>
  <verify>ctest --output-on-failure -R generator</verify>
  <automated>ctest --output-on-failure -R generator</automated>
  <acceptance_criteria>
    - `src/emel/generator/detail.hpp` contains `flash_attention_dispatch_calls`
    - `src/emel/generator/sm.hpp` contains `generation_flash_attention_dispatch_calls`
    - `tests/generator/lifecycle_tests.cpp` asserts the canonical generation path increments the flash-specific dispatch getter
    - `ctest --output-on-failure -R generator` exits 0
  </acceptance_criteria>
  <done>Phase 11 has a durable generator-local signal that proves flash dispatch separately from generic kernel traffic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --output-on-failure -R generator`
- [ ] `rg 'op_flash_attn_ext|flash_attention_dispatch_calls|generation_flash_attention_dispatch_calls' src/emel/generator tests/generator`
- [ ] canonical generator lifecycle coverage proves flash-specific dispatch count is non-zero
- [ ] no new public API or CLI knob is introduced for flash selection
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `GEN-01` routing is satisfied at the generator/runtime seam
- flash-path observability exists for later parity proof
  </success_criteria>

<output>
After completion, create `.planning/phases/11-generator-flash-adoption/11-01-SUMMARY.md`
</output>
