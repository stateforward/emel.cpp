---
phase: 10-flash-kernel-bring-up
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/emel/kernel/x86_64/actions.hpp
  - src/emel/kernel/x86_64/context.hpp
  - src/emel/kernel/x86_64/guards.hpp
  - src/emel/kernel/aarch64/actions.hpp
  - src/emel/kernel/aarch64/context.hpp
  - src/emel/kernel/aarch64/guards.hpp
  - tests/kernel/lifecycle_tests.cpp
  - tests/kernel/aarch64_tests.cpp
autonomous: true
requirements:
  - FLASH-01
  - FLASH-02
user_setup: []
must_haves:
  truths:
    - backend kernel machines execute canonical `op_flash_attn_ext` requests through the shared EMEL-owned implementation.
    - repeated canonical flash-kernel dispatches reuse persistent workspace/buffers without hot-path allocation churn.
  artifacts:
    - backend action and guard files explicitly accept canonical flash requests and reject unsupported ones.
    - tests prove repeated dispatch reuse and do not rely on generator, paritychecker, or benchmark surfaces.
  key_links:
    - Phase 11 can adopt flash attention in the generator path only after Phase 10 proves truthful backend dispatch and reusable workspace semantics at the kernel layer.
---

<objective>
Wire backend flash-attention dispatch to the shared implementation and prove workspace reuse.

Purpose: Finish `FLASH-01` and `FLASH-02` by making the shared canonical flash kernel reachable
through the real backend machines while keeping generator adoption deferred to Phase 11.
Output: Backend contexts/actions/guards execute or reject `op_flash_attn_ext` deterministically,
and kernel tests prove repeated canonical invocations reuse persistent workspace/buffers.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-flash-kernel-bring-up/10-CONTEXT.md
@.planning/phases/10-flash-kernel-bring-up/10-RESEARCH.md
@.planning/phases/10-flash-kernel-bring-up/10-VALIDATION.md
@.planning/phases/10-flash-kernel-bring-up/10-01-PLAN.md
@src/emel/kernel/detail.hpp
@src/emel/kernel/x86_64/actions.hpp
@src/emel/kernel/x86_64/context.hpp
@src/emel/kernel/x86_64/guards.hpp
@src/emel/kernel/aarch64/actions.hpp
@src/emel/kernel/aarch64/context.hpp
@src/emel/kernel/aarch64/guards.hpp
@tests/kernel/lifecycle_tests.cpp
@tests/kernel/aarch64_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire backend guards and actions to the shared canonical flash kernel</name>
  <files>src/emel/kernel/x86_64/actions.hpp, src/emel/kernel/x86_64/context.hpp, src/emel/kernel/x86_64/guards.hpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/aarch64/context.hpp, src/emel/kernel/aarch64/guards.hpp, tests/kernel/lifecycle_tests.cpp</files>
  <read_first>src/emel/kernel/detail.hpp, src/emel/kernel/x86_64/actions.hpp, src/emel/kernel/x86_64/context.hpp, src/emel/kernel/x86_64/guards.hpp, src/emel/kernel/aarch64/actions.hpp, src/emel/kernel/aarch64/context.hpp, src/emel/kernel/aarch64/guards.hpp, tests/kernel/lifecycle_tests.cpp</read_first>
  <action>Update the backend kernel layers so canonical `op_flash_attn_ext` requests are accepted
  by explicit guards and routed by explicit actions into the shared flash implementation from Plan
  10-01. Keep unsupported requests on the rejection path. Reuse backend-owned persistent context
  storage for flash workspace instead of introducing request-local mirrors. Extend lifecycle tests
  so the backend machines and the top-level kernel facade prove canonical success plus explicit
  unsupported-request rejection.</action>
  <verify>ctest --output-on-failure -R kernel && scripts/quality_gates.sh</verify>
  <automated>ctest --output-on-failure -R kernel && scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - `src/emel/kernel/x86_64/actions.hpp` contains `exec_op_flash_attn_ext`
    - `src/emel/kernel/aarch64/actions.hpp` contains `exec_op_flash_attn_ext`
    - `src/emel/kernel/x86_64/guards.hpp` contains flash-attention validity checks
    - `src/emel/kernel/aarch64/guards.hpp` contains flash-attention validity checks
    - `tests/kernel/lifecycle_tests.cpp` proves backend success for canonical flash requests and rejection for unsupported ones
  </acceptance_criteria>
  <done>Canonical flash requests traverse the actual backend machines and unsupported requests are
  explicitly rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Prove repeated flash dispatch reuses persistent workspace without allocation churn</name>
  <files>src/emel/kernel/x86_64/context.hpp, src/emel/kernel/aarch64/context.hpp, tests/kernel/lifecycle_tests.cpp, tests/kernel/aarch64_tests.cpp</files>
  <read_first>src/emel/kernel/x86_64/context.hpp, src/emel/kernel/aarch64/context.hpp, tests/kernel/lifecycle_tests.cpp, tests/kernel/aarch64_tests.cpp, .planning/phases/10-flash-kernel-bring-up/10-VALIDATION.md</read_first>
  <action>Add deterministic kernel-local tests that drive repeated canonical `op_flash_attn_ext`
  dispatches through the same backend machine/context and prove the persistent flash workspace or
  backing buffers are reused across calls. Use context-observable capacity/state checks or direct
  stable-storage checks that do not require benchmark or parity surfaces. Keep the proof local to
  the backend contexts and unit tests.</action>
  <verify>ctest --output-on-failure -R kernel && scripts/quality_gates.sh</verify>
  <automated>ctest --output-on-failure -R kernel && scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - `tests/kernel/lifecycle_tests.cpp` or `tests/kernel/aarch64_tests.cpp` contains a repeated flash-dispatch reuse test
    - the reuse test checks persistent workspace/buffer stability across multiple canonical calls
    - `ctest --output-on-failure -R kernel` exits 0
    - `scripts/quality_gates.sh` exits 0
  </acceptance_criteria>
  <done>Phase 10 has direct automated proof that repeated canonical flash dispatches reuse
  persistent backend workspace/buffers without hot-path allocation churn.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --output-on-failure -R kernel`
- [ ] `scripts/quality_gates.sh`
- [ ] `rg 'exec_op_flash_attn_ext|valid_op_flash_attn_ext' src/emel/kernel/x86_64 src/emel/kernel/aarch64`
- [ ] repeated-dispatch tests prove workspace reuse through backend context, not generator/tool-local state
- [ ] no generator, paritychecker, or benchmark file is modified to claim flash adoption in this phase
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `FLASH-01` is satisfied through backend-reachable shared flash execution
- `FLASH-02` is satisfied through automated repeated-dispatch reuse proof
  </success_criteria>

<output>
After completion, create `.planning/phases/10-flash-kernel-bring-up/10-02-SUMMARY.md`
</output>
