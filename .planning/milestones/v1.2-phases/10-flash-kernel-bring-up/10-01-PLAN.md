---
phase: 10-flash-kernel-bring-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emel/kernel/detail.hpp
  - src/emel/kernel/context.hpp
  - src/emel/kernel/events.hpp
  - tests/kernel/lifecycle_tests.cpp
  - tests/kernel/test_helpers.hpp
autonomous: true
requirements:
  - FLASH-01
  - FLASH-02
user_setup: []
must_haves:
  truths:
    - canonical Llama-68M flash-attention fixtures execute through a real EMEL-owned `op_flash_attn_ext` kernel path under `src/emel/kernel`.
    - unsupported flash-attention requests reject explicitly instead of silently falling back to the old materialized attention path.
  artifacts:
    - shared kernel code defines the canonical flash-attention request validation and execution surface.
    - kernel tests reproduce the missing flash path first, then prove canonical execution and explicit rejection.
  key_links:
    - backend action wiring in Plan 10-02 can delegate to a stable shared flash implementation because Plan 10-01 defines the canonical contract and reusable workspace surface first.
---

<objective>
Establish the shared canonical flash-attention kernel contract and implementation.

Purpose: Satisfy the core of `FLASH-01` and set up `FLASH-02` by defining one truthful
`op_flash_attn_ext` path in shared kernel code before any backend-specific wiring.
Output: Shared kernel code exposes canonical validation plus execution for `op_flash_attn_ext`,
and kernel tests reproduce the missing path, explicit rejection behavior, and the expected
canonical execution contract.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-flash-kernel-bring-up/10-CONTEXT.md
@.planning/phases/10-flash-kernel-bring-up/10-RESEARCH.md
@.planning/phases/10-flash-kernel-bring-up/10-VALIDATION.md
@AGENTS.md
@src/emel/kernel/detail.hpp
@src/emel/kernel/context.hpp
@src/emel/kernel/events.hpp
@src/emel/generator/detail.hpp
@tests/kernel/lifecycle_tests.cpp
@tests/kernel/test_helpers.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the missing canonical flash-kernel path with failing kernel tests</name>
  <files>tests/kernel/lifecycle_tests.cpp, tests/kernel/test_helpers.hpp</files>
  <read_first>tests/kernel/lifecycle_tests.cpp, tests/kernel/test_helpers.hpp, src/emel/kernel/events.hpp, src/emel/kernel/detail.hpp, src/emel/generator/detail.hpp</read_first>
  <action>Add focused doctest coverage that constructs the exact canonical Llama-68M-style
  `op_flash_attn_ext` request shape expected by this milestone, sends it through the kernel machine,
  and proves the current repo either rejects or lacks the required execution path. In the same test
  area, add an unsupported-shape request that Phase 10 must reject explicitly. Keep the fixtures
  kernel-local and bounded; do not route through the generator or tools.</action>
  <verify>ctest --output-on-failure -R kernel</verify>
  <automated>ctest --output-on-failure -R kernel</automated>
  <acceptance_criteria>
    - `tests/kernel/lifecycle_tests.cpp` contains `op_flash_attn_ext`
    - `tests/kernel/lifecycle_tests.cpp` contains a canonical-success expectation and an explicit unsupported-request rejection expectation
    - `ctest --output-on-failure -R kernel` fails before the implementation lands, proving the missing behavior was reproduced first
  </acceptance_criteria>
  <done>Kernel tests fail for the missing flash path before any shared-kernel implementation change
  is applied.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared canonical flash-attention validation, workspace, and execution</name>
  <files>src/emel/kernel/detail.hpp, src/emel/kernel/context.hpp, src/emel/kernel/events.hpp, tests/kernel/lifecycle_tests.cpp, tests/kernel/test_helpers.hpp</files>
  <read_first>src/emel/kernel/detail.hpp, src/emel/kernel/context.hpp, src/emel/kernel/events.hpp, src/emel/generator/detail.hpp, tests/kernel/lifecycle_tests.cpp, AGENTS.md</read_first>
  <action>Extend the shared kernel layer so `op_flash_attn_ext` has a real canonical execution
  surface under `src/emel/kernel`. Define the exact canonical validation helpers, reusable
  workspace/buffer structure, and scalar execution routine needed for the Llama-68M causal
  attention shape used in this milestone. Keep the contract narrow: unsupported requests must be
  detected by explicit validation helpers, and the implementation must not call or emulate the old
  generator-side `compute_attention(...)` path under a flash label. Update the tests from Task 1 so
  they now pass and prove both canonical execution and explicit rejection.</action>
  <verify>ctest --output-on-failure -R kernel && scripts/quality_gates.sh</verify>
  <automated>ctest --output-on-failure -R kernel && scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - `src/emel/kernel/detail.hpp` contains shared `op_flash_attn_ext` validation and execution code
    - `src/emel/kernel/context.hpp` contains reusable flash workspace state rather than per-dispatch temporary mirrors
    - `ctest --output-on-failure -R kernel` exits 0 after the implementation lands
    - no generator file is modified to adopt flash attention in this plan
  </acceptance_criteria>
  <done>The shared kernel layer owns a real canonical flash-attention implementation and the
  kernel tests pass for canonical execution plus explicit rejection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --output-on-failure -R kernel`
- [ ] `rg 'op_flash_attn_ext' src/emel/kernel/detail.hpp tests/kernel/lifecycle_tests.cpp`
- [ ] shared kernel code, not generator code, defines the canonical flash-attention path
- [ ] unsupported flash requests reject explicitly in tests
- [ ] the phase remains kernel-local and does not widen generator/parity/bench claims
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `FLASH-01` has a real EMEL-owned shared flash kernel surface
- `FLASH-02` has a defined reusable workspace contract ready for backend wiring
  </success_criteria>

<output>
After completion, create `.planning/phases/10-flash-kernel-bring-up/10-01-SUMMARY.md`
</output>
