---
phase: 12.1-enforce-sml-tensor-lifecycle-orchestration
plan: 02
type: execute
wave: 2
depends_on:
  - 12.1-01
files_modified:
  - src/emel/graph/processor/context.hpp
  - src/emel/graph/processor/events.hpp
  - src/emel/graph/processor/actions.hpp
  - src/emel/graph/processor/guards.hpp
  - src/emel/graph/processor/sm.hpp
  - src/emel/graph/processor/bind_step/actions.hpp
  - src/emel/graph/processor/kernel_step/actions.hpp
  - src/emel/graph/processor/extract_step/actions.hpp
  - tests/graph/processor/processor_action_branch_tests.cpp
  - tests/graph/graph_tests.cpp
  - tests/tensor/view/lifecycle_tests.cpp
autonomous: true
requirements:
  - CUTOVER-02
  - CUTOVER-03
user_setup: []
must_haves:
  truths:
    - the processor cannot dispatch kernel work until required tensors are verified as filled by tensor lifecycle truth.
    - produced tensors are formally published as filled after successful kernel execution.
    - tensor reuse is blocked until lifecycle release has been recorded through the tensor actor.
  artifacts:
    - processor orchestration contains explicit lifecycle gate/publish/release phases or equivalent explicit SML states
    - processor guards branch on typed lifecycle results rather than implicit callback success
    - tests cover both blocked-not-filled behavior and released-before-reuse behavior
  key_links:
    - this is the phase's mathematical guarantee layer; without it the hard cutover is only cosmetic plumbing
---

<objective>
Cut the graph processor over to lifecycle-gated execution.

Purpose: make kernel execution and tensor reuse depend on explicit tensor lifecycle truth from the
existing tensor actor.
Output: processor orchestration has explicit lifecycle gate, publish, and release behavior, and the
kernel path cannot run on unfilled inputs or reuse unreleased tensors.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-CONTEXT.md
@.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-RESEARCH.md
@.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-VALIDATION.md
@src/emel/tensor/events.hpp
@src/emel/tensor/sm.hpp
@src/emel/tensor/view/sm.hpp
@src/emel/graph/processor/context.hpp
@src/emel/graph/processor/events.hpp
@src/emel/graph/processor/actions.hpp
@src/emel/graph/processor/guards.hpp
@src/emel/graph/processor/sm.hpp
@src/emel/graph/processor/bind_step/actions.hpp
@src/emel/graph/processor/kernel_step/actions.hpp
@src/emel/graph/processor/extract_step/actions.hpp
@tests/graph/processor/processor_action_branch_tests.cpp
@tests/graph/graph_tests.cpp
@tests/tensor/view/lifecycle_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit filled-state gating ahead of kernel dispatch</name>
  <files>src/emel/graph/processor/events.hpp, src/emel/graph/processor/actions.hpp, src/emel/graph/processor/guards.hpp, src/emel/graph/processor/sm.hpp, src/emel/graph/processor/bind_step/actions.hpp, tests/graph/processor/processor_action_branch_tests.cpp, tests/graph/graph_tests.cpp</files>
  <read_first>src/emel/graph/processor/events.hpp, src/emel/graph/processor/guards.hpp, src/emel/graph/processor/sm.hpp, src/emel/tensor/view/sm.hpp, .planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-CONTEXT.md</read_first>
  <action>Add explicit processor phases or decision states that capture required tensor lifecycle state and branch on “inputs filled” before the kernel phase. The kernel transition must no longer depend only on generic bind success. Tests must prove the processor rejects or fails deterministically when an input tensor has not been published filled.</action>
  <verify>build/debug/emel_tests_bin --source-file=tests/graph/processor/processor_action_branch_tests.cpp,tests/graph/graph_tests.cpp</verify>
  <automated>build/debug/emel_tests_bin --source-file=tests/graph/processor/processor_action_branch_tests.cpp,tests/graph/graph_tests.cpp</automated>
  <acceptance_criteria>
    - processor orchestration contains an explicit lifecycle-readiness gate before kernel dispatch
    - kernel dispatch is impossible when required input tensors are not lifecycle-filled
    - tests prove blocked-not-filled behavior through the shipped graph/processor surface
  </acceptance_criteria>
  <done>The processor now enforces filled-before-kernel as an explicit SML orchestration rule.</done>
</task>

<task type="auto">
  <name>Task 2: Publish outputs and release expired tensors through lifecycle truth</name>
  <files>src/emel/graph/processor/events.hpp, src/emel/graph/processor/actions.hpp, src/emel/graph/processor/guards.hpp, src/emel/graph/processor/sm.hpp, src/emel/graph/processor/kernel_step/actions.hpp, src/emel/graph/processor/extract_step/actions.hpp, tests/graph/graph_tests.cpp, tests/tensor/view/lifecycle_tests.cpp</files>
  <read_first>src/emel/graph/processor/events.hpp, src/emel/graph/processor/actions.hpp, src/emel/tensor/sm.hpp, tests/tensor/view/lifecycle_tests.cpp, .planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-RESEARCH.md</read_first>
  <action>After successful kernel execution, formally publish produced tensors as filled and release tensors whose liveness has expired before reuse is considered valid. Keep the release contract explicit in SML orchestration, and prove by test that reuse does not occur until release has been recorded through the tensor actor.</action>
  <verify>build/debug/emel_tests_bin --source-file=tests/tensor/view/lifecycle_tests.cpp,tests/graph/graph_tests.cpp</verify>
  <automated>build/debug/emel_tests_bin --source-file=tests/tensor/view/lifecycle_tests.cpp,tests/graph/graph_tests.cpp</automated>
  <acceptance_criteria>
    - produced tensors are published filled through the tensor actor after successful kernel work
    - expired tensors are released through the tensor actor before reuse
    - tests prove released-before-reuse enforcement through the shipped graph runtime
  </acceptance_criteria>
  <done>The processor now enforces publish and release semantics through the tensor actor, removing the lifecycle bypass from execution math.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --test-dir build/debug --output-on-failure -R emel_tests`
- [ ] `build/debug/emel_tests_bin --source-file=tests/graph/processor/processor_action_branch_tests.cpp,tests/graph/graph_tests.cpp`
- [ ] `build/debug/emel_tests_bin --source-file=tests/tensor/view/lifecycle_tests.cpp,tests/graph/graph_tests.cpp`
- [ ] `scripts/quality_gates.sh`
- [ ] kernel dispatch is blocked until required tensors are lifecycle-filled
- [ ] tensor reuse is blocked until formal release is recorded
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- processor execution is lifecycle-gated and release-safe
- the former generic callback-only tensor readiness contract is gone

</success_criteria>

<output>
After completion, create `.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-02-SUMMARY.md`
</output>
