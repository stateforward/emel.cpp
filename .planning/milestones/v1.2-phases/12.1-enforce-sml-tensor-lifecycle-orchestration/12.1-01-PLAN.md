---
phase: 12.1-enforce-sml-tensor-lifecycle-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emel/graph/context.hpp
  - src/emel/graph/actions.hpp
  - src/emel/graph/sm.hpp
  - src/emel/graph/assembler/context.hpp
  - src/emel/graph/assembler/events.hpp
  - src/emel/graph/assembler/actions.hpp
  - src/emel/graph/processor/events.hpp
  - src/emel/generator/actions.hpp
  - src/emel/generator/detail.hpp
  - tests/graph/assembler/assembler_tests.cpp
  - tests/graph/graph_tests.cpp
  - tests/tensor/lifecycle_tests.cpp
autonomous: true
requirements:
  - CUTOVER-01
user_setup: []
must_haves:
  truths:
    - graph-owned runtime context carries the existing `emel::tensor::sm` as the only tensor lifecycle authority.
    - graph planning and execution no longer treat tensor lifecycle identity as implicit pointer math; lifecycle metadata becomes explicit internal runtime data.
    - reservation dispatch happens through the real tensor actor at the earliest truthful point where runtime buffers are known.
  artifacts:
    - graph and assembler contexts/events expose the lifecycle actor or lifecycle runtime plumbing needed for cutover.
    - reservation metadata is derived from graph planning/runtime data rather than left ad hoc in generator code.
    - tests prove reservation dispatch drives compute tensors to `empty` and leaf tensors to `leaf_filled`.
  key_links:
    - Phase 12.1 cannot enforce filled-before-kernel or released-before-reuse until graph owns tensor lifecycle truth.
---

<objective>
Create the graph-owned tensor lifecycle runtime needed for hard cutover.

Purpose: make `emel::tensor::sm` part of the core graph execution contract and eliminate the
assumption that tensor lifecycle is tracked implicitly outside the SML graph pipeline.
Output: graph/assembler/processor/generator runtime plumbing can identify active tensors and
dispatch truthful reservation events through the tensor actor.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-CONTEXT.md
@.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-RESEARCH.md
@.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-VALIDATION.md
@src/emel/tensor/context.hpp
@src/emel/tensor/actions.hpp
@src/emel/tensor/events.hpp
@src/emel/tensor/sm.hpp
@src/emel/graph/context.hpp
@src/emel/graph/actions.hpp
@src/emel/graph/sm.hpp
@src/emel/graph/assembler/context.hpp
@src/emel/graph/assembler/events.hpp
@src/emel/graph/assembler/actions.hpp
@src/emel/graph/processor/events.hpp
@src/emel/generator/actions.hpp
@src/emel/generator/detail.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject tensor lifecycle authority into graph-owned runtime</name>
  <files>src/emel/graph/context.hpp, src/emel/graph/actions.hpp, src/emel/graph/sm.hpp, src/emel/graph/assembler/context.hpp, src/emel/graph/assembler/events.hpp, src/emel/graph/processor/events.hpp</files>
  <read_first>src/emel/graph/context.hpp, src/emel/graph/actions.hpp, src/emel/graph/sm.hpp, src/emel/tensor/sm.hpp, .planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-CONTEXT.md</read_first>
  <action>Thread the existing `emel::tensor::sm` into graph-owned orchestration and add explicit internal lifecycle runtime payloads that can carry tensor identity, reserve intent, and release scheduling data across graph phases. Keep this runtime data dispatch-local and remove assumptions that lifecycle truth will be inferred from raw `tensor_view` or `tensor_record` math alone.</action>
  <verify>build/debug/emel_tests_bin --source-file=tests/graph/assembler/assembler_tests.cpp,tests/tensor/lifecycle_tests.cpp</verify>
  <automated>build/debug/emel_tests_bin --source-file=tests/graph/assembler/assembler_tests.cpp,tests/tensor/lifecycle_tests.cpp</automated>
  <acceptance_criteria>
    - `src/emel/graph/context.hpp` owns or injects the tensor lifecycle actor into the graph runtime path
    - graph/assembler/processor internal events include explicit lifecycle runtime data instead of relying on implicit caller-side state
    - no second tensor lifecycle tracker is introduced
  </acceptance_criteria>
  <done>Graph orchestration now has a real tensor lifecycle authority and a truthful internal runtime payload for cutover.</done>
</task>

<task type="auto">
  <name>Task 2: Hard-cutover reservation through the tensor actor</name>
  <files>src/emel/graph/assembler/actions.hpp, src/emel/graph/processor/events.hpp, src/emel/generator/actions.hpp, src/emel/generator/detail.hpp, tests/graph/assembler/assembler_tests.cpp, tests/graph/graph_tests.cpp, tests/tensor/lifecycle_tests.cpp</files>
  <read_first>src/emel/graph/assembler/actions.hpp, src/emel/graph/processor/events.hpp, src/emel/generator/detail.hpp, tests/graph/assembler/assembler_tests.cpp, .planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-RESEARCH.md</read_first>
  <action>Use the new graph/runtime lifecycle metadata to dispatch `reserve_tensor` for every active tensor at the earliest truthful point where runtime buffers are known, and remove the legacy assumption that tensor readiness can stay implicit. Tests should prove compute tensors land in `empty`, leaf tensors land in `leaf_filled`, and reservation is now part of the core graph path rather than an optional side behavior.</action>
  <verify>build/debug/emel_tests_bin --source-file=tests/graph/graph_tests.cpp</verify>
  <automated>build/debug/emel_tests_bin --source-file=tests/graph/graph_tests.cpp</automated>
  <acceptance_criteria>
    - active tensor reservation is driven through `emel::tensor::sm`
    - the new reservation path is part of the graph/generator runtime contract, not a tool-only helper
    - tests prove reserved lifecycle state matches tensor kind (`empty` for compute, `leaf_filled` for leaves)
  </acceptance_criteria>
  <done>The ad hoc reservation bypass is removed and active tensors are formally reserved through the tensor actor.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --test-dir build/debug --output-on-failure -R emel_tests`
- [ ] `build/debug/emel_tests_bin --source-file=tests/graph/assembler/assembler_tests.cpp,tests/tensor/lifecycle_tests.cpp`
- [ ] `build/debug/emel_tests_bin --source-file=tests/graph/graph_tests.cpp`
- [ ] `scripts/quality_gates.sh`
- [ ] graph-owned runtime now exposes tensor lifecycle authority with no parallel tracker
- [ ] reservation is dispatched through the tensor actor for active tensors in the core path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- graph owns tensor lifecycle truth for the hard-cutover path
- reservation no longer depends on the legacy ad hoc implementation

</success_criteria>

<output>
After completion, create `.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-01-SUMMARY.md`
</output>
