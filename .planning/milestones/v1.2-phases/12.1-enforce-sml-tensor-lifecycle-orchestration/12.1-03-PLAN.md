---
phase: 12.1-enforce-sml-tensor-lifecycle-orchestration
plan: 03
type: execute
wave: 3
depends_on:
  - 12.1-02
files_modified:
  - src/emel/generator/actions.hpp
  - src/emel/generator/detail.hpp
  - src/emel/generator/sm.hpp
  - tests/generator/lifecycle_tests.cpp
  - tests/graph/graph_tests.cpp
  - tests/graph/processor/processor_action_branch_tests.cpp
autonomous: true
requirements:
  - CUTOVER-04
  - CUTOVER-02
  - CUTOVER-03
  - CUTOVER-05
user_setup: []
must_haves:
  truths:
    - the generator/runtime path uses only the lifecycle-managed graph pipeline; the ad hoc tensor lifecycle bypass is removed or unreachable.
    - the shipped runtime proves filled-before-kernel and released-before-reuse through end-to-end tests.
    - repo regression gates remain green after the hard cutover.
  artifacts:
    - generator callback plumbing matches the new graph/tensor lifecycle contract
    - no legacy dual-path fallback remains in runtime code
    - end-to-end tests and full repo gates prove the hard cutover is shippable
  key_links:
    - this plan is the actual cutover completion point; without it the repo would still carry the old lifecycle path alongside the new one
---

<objective>
Finish the hard cutover by removing the runtime bypass and regressing the repo.

Purpose: ensure the graph/tensor lifecycle path is the only active shipped implementation and prove
it through end-to-end tests and full quality gates.
Output: generator/runtime code consumes only the lifecycle-managed graph contract, no dual-path
fallback remains, and repo gates pass.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-CONTEXT.md
@.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-RESEARCH.md
@.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-VALIDATION.md
@src/emel/generator/actions.hpp
@src/emel/generator/detail.hpp
@src/emel/generator/sm.hpp
@src/emel/graph/processor/events.hpp
@src/emel/graph/processor/sm.hpp
@tests/generator/lifecycle_tests.cpp
@tests/graph/graph_tests.cpp
@tests/graph/processor/processor_action_branch_tests.cpp
@scripts/quality_gates.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut generator/runtime callers over and remove the bypass</name>
  <files>src/emel/generator/actions.hpp, src/emel/generator/detail.hpp, src/emel/generator/sm.hpp, tests/generator/lifecycle_tests.cpp, tests/graph/graph_tests.cpp</files>
  <read_first>src/emel/generator/actions.hpp, src/emel/generator/detail.hpp, src/emel/graph/processor/events.hpp, tests/generator/lifecycle_tests.cpp, .planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-CONTEXT.md</read_first>
  <action>Update the generator/runtime callback layer to consume the lifecycle-managed graph contract and remove or render unreachable the legacy ad hoc tensor lifecycle path. Keep the cutover hard: once this lands, runtime behavior must depend on the graph/tensor SML path rather than preserving the old implementation as a fallback.</action>
  <verify>build/debug/emel_tests_bin --source-file=tests/generator/lifecycle_tests.cpp,tests/graph/graph_tests.cpp</verify>
  <automated>build/debug/emel_tests_bin --source-file=tests/generator/lifecycle_tests.cpp,tests/graph/graph_tests.cpp</automated>
  <acceptance_criteria>
    - generator/runtime code no longer relies on the legacy implicit tensor lifecycle path
    - the lifecycle-managed graph contract is the only active shipped runtime path
    - end-to-end tests cover the new runtime cutover
  </acceptance_criteria>
  <done>The runtime hard cutover is complete and the old lifecycle bypass is gone or unreachable.</done>
</task>

<task type="auto">
  <name>Task 2: Regress the repo after the hard cutover</name>
  <files>tests/generator/lifecycle_tests.cpp, tests/graph/graph_tests.cpp, tests/graph/processor/processor_action_branch_tests.cpp</files>
  <read_first>tests/generator/lifecycle_tests.cpp, tests/graph/graph_tests.cpp, scripts/quality_gates.sh</read_first>
  <action>Add an explicit no-allocation dispatch proof for the changed graph/processor/runtime path, then run the full regression loop after the hard cutover and document any remaining repo-level warnings that are outside this phase's architectural scope. Do not update snapshots in this phase unless the user explicitly approves that separately.</action>
  <verify>scripts/quality_gates.sh</verify>
  <automated>scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - targeted tests prove the changed graph/processor/runtime dispatch path does not allocate
    - `scripts/quality_gates.sh` exits 0
    - generator and graph tests prove the lifecycle-managed path is active end to end
    - any remaining non-blocking gate warnings are documented explicitly
  </acceptance_criteria>
  <done>The hard cutover is proven allocation-safe and the repo-level gate proves it is shippable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --test-dir build/debug --output-on-failure -R emel_tests`
- [ ] `build/debug/emel_tests_bin --source-file=tests/generator/lifecycle_tests.cpp,tests/graph/graph_tests.cpp`
- [ ] `build/debug/emel_tests_bin --source-file=tests/graph/graph_tests.cpp,tests/graph/processor/processor_action_branch_tests.cpp --test-case=*alloc*`
- [ ] `scripts/quality_gates.sh`
- [ ] no legacy ad hoc runtime lifecycle path remains reachable
- [ ] end-to-end tests prove lifecycle-managed execution is active
- [ ] the changed dispatch path is proven allocation-free
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- the hard cutover is complete with no dual-path fallback
- repo regression gates remain green

</success_criteria>

<output>
After completion, create `.planning/phases/12.1-enforce-sml-tensor-lifecycle-orchestration/12.1-03-SUMMARY.md`
</output>
