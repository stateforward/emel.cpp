---
phase: 12-parity-and-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/paritychecker/CMakeLists.txt
  - tools/paritychecker/parity_runner.cpp
autonomous: true
requirements:
  - PAR-01
  - PAR-02
user_setup: []
must_haves:
  truths:
    - paritychecker generation builds against the latest upstream `llama.cpp` through CMake instead of preferring repo-local `tmp/llama.cpp`.
    - the normal `paritychecker --generation` surface exposes flash proof and the reference identity directly.
  artifacts:
    - `tools/paritychecker/CMakeLists.txt` no longer prefers `tmp/llama.cpp`.
    - `tools/paritychecker/parity_runner.cpp` prints a normal proof block showing the fetched reference identity and flash-related seam counters.
    - post-initialize generation failures emit enough diagnostics without requiring `--dump`.
  key_links:
    - Phase 12 must tell the truth about which upstream reference it built against before benchmark evidence in Phase 13 can mean anything.
---

<objective>
Remove the repo-local tmp reference preference from paritychecker and publish truthful latest-upstream
proof metadata on the normal generation surface.

Purpose: satisfy the proof half of `PAR-01` and the reference-truth half of `PAR-02`.
Output: paritychecker fetches upstream `llama.cpp` through CMake, prints which fetched reference it
used, and shows flash proof on the normal success/failure surface.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-parity-and-verification-closure/12-CONTEXT.md
@.planning/phases/12-parity-and-verification-closure/12-RESEARCH.md
@.planning/phases/12-parity-and-verification-closure/12-VALIDATION.md
@tools/paritychecker/CMakeLists.txt
@tools/paritychecker/parity_runner.cpp
@tools/paritychecker/paritychecker_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove local tmp parity reference selection</name>
  <files>tools/paritychecker/CMakeLists.txt</files>
  <read_first>tools/paritychecker/CMakeLists.txt, .planning/phases/12-parity-and-verification-closure/12-CONTEXT.md</read_first>
  <action>Update `tools/paritychecker/CMakeLists.txt` so paritychecker no longer prefers a repo-local `tmp/llama.cpp` checkout. Make the reference implementation come from the upstream CMake fetch path, defaulting to the latest upstream branch that the user requested for parity validation, and capture the fetched reference identity for later publication.</action>
  <verify>ctest --test-dir build/paritychecker_zig_latest --output-on-failure -R paritychecker_tests</verify>
  <automated>ctest --test-dir build/paritychecker_zig_latest --output-on-failure -R paritychecker_tests</automated>
  <acceptance_criteria>
    - `tools/paritychecker/CMakeLists.txt` contains no logic that prefers `tmp/llama.cpp`
    - paritychecker can still configure and build successfully in a fresh build directory
    - the fetched reference identity is available to the runtime surface
  </acceptance_criteria>
  <done>Paritychecker truthfully builds against the fetched upstream reference instead of local tmp state.</done>
</task>

<task type="auto">
  <name>Task 2: Publish normal flash-proof and reference-proof output</name>
  <files>tools/paritychecker/parity_runner.cpp</files>
  <read_first>tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp, .planning/phases/12-parity-and-verification-closure/12-CONTEXT.md</read_first>
  <action>Promote the existing generation proof seam into the normal `--generation` surface. Normal success output should show the reference identity and flash-related counters directly, and post-initialize generation failures should auto-emit enough seam diagnostics to explain what broke even without `--dump`.</action>
  <verify>./build/paritychecker_zig_latest/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 1</verify>
  <automated>ctest --test-dir build/paritychecker_zig_latest --output-on-failure -R paritychecker_tests</automated>
  <acceptance_criteria>
    - normal generation output contains a proof block or equivalent direct proof surface
    - the proof surface includes fetched reference identity and flash-dispatch evidence
    - post-initialize failure paths emit proof diagnostics without requiring `--dump`
  </acceptance_criteria>
  <done>The normal paritychecker generation contract now tells the truth about both flash execution and the fetched reference source.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --test-dir build/paritychecker_zig_latest --output-on-failure -R paritychecker_tests`
- [ ] `./build/paritychecker_zig_latest/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 1`
- [ ] `rg 'tmp/llama.cpp|reference_impl: source=|flash_dispatch:' tools/paritychecker/CMakeLists.txt tools/paritychecker/parity_runner.cpp`
- [ ] normal generation output exposes fetched reference identity and flash proof directly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `PAR-01` is satisfied on the normal paritychecker surface
- paritychecker no longer depends on repo-local tmp state for reference selection
  </success_criteria>

<output>
After completion, create `.planning/phases/12-parity-and-verification-closure/12-01-SUMMARY.md`
</output>

