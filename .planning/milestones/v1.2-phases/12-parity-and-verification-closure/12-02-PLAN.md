---
phase: 12-parity-and-verification-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - tools/paritychecker/paritychecker_tests.cpp
autonomous: true
requirements:
  - PAR-02
  - VER-01
user_setup: []
must_haves:
  truths:
    - both the short canonical request and a bounded longer decode remain parity-stable against the fetched upstream reference.
    - paritychecker subprocess tests lock the normal proof surface and the bounded-long decode contract.
  artifacts:
    - `tools/paritychecker/paritychecker_tests.cpp` covers the longer bounded decode and the normal proof block.
    - fresh paritychecker builds pass against the fetched upstream reference.
    - full repo quality gates remain green after the paritychecker contract changes.
  key_links:
    - benchmark publication in Phase 13 depends on the parity surface already being stable on both bounded workloads.
---

<objective>
Extend the maintained paritychecker subprocess contract to cover the bounded longer decode and full
regression verification.

Purpose: satisfy the bounded-workload half of `PAR-02` and the paritychecker-facing part of
`VER-01`.
Output: paritychecker tests lock both short and bounded-long generation parity plus the new proof
surface, and full repo gates still pass.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-parity-and-verification-closure/12-CONTEXT.md
@.planning/phases/12-parity-and-verification-closure/12-RESEARCH.md
@.planning/phases/12-parity-and-verification-closure/12-VALIDATION.md
@tools/paritychecker/paritychecker_tests.cpp
@tools/paritychecker/parity_runner.cpp
@tests/kernel/lifecycle_tests.cpp
@tests/generator/lifecycle_tests.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the bounded longer generation parity contract</name>
  <files>tools/paritychecker/paritychecker_tests.cpp</files>
  <read_first>tools/paritychecker/paritychecker_tests.cpp, tools/paritychecker/parity_runner.cpp, .planning/phases/12-parity-and-verification-closure/12-CONTEXT.md</read_first>
  <action>Extend the subprocess-facing generation tests so they cover the bounded longer decode alongside the canonical one-token request. Keep the assertions focused on parity success, fetched-reference proof visibility, and flash-dispatch proof visibility on the normal output surface.</action>
  <verify>ctest --test-dir build/paritychecker_zig_latest --output-on-failure -R paritychecker_tests</verify>
  <automated>ctest --test-dir build/paritychecker_zig_latest --output-on-failure -R paritychecker_tests</automated>
  <acceptance_criteria>
    - `tools/paritychecker/paritychecker_tests.cpp` contains bounded-long generation coverage
    - normal success output assertions include the proof block or equivalent direct proof surface
    - the bounded-long test proves parity against the fetched upstream reference
  </acceptance_criteria>
  <done>Paritychecker subprocess coverage now locks both bounded workloads and the published proof surface.</done>
</task>

<task type="auto">
  <name>Task 2: Regress the repo after the parity contract change</name>
  <files>tools/paritychecker/paritychecker_tests.cpp</files>
  <read_first>tools/paritychecker/paritychecker_tests.cpp, scripts/quality_gates.sh</read_first>
  <action>Run the fresh paritychecker subprocess suite and then the normal full quality gates to confirm the latest-upstream parity contract change did not regress the repo. Record any remaining warning-only benchmark drift as non-blocking if the quality gates retain the existing policy.</action>
  <verify>ctest --test-dir build/paritychecker_zig_latest --output-on-failure -R paritychecker_tests && scripts/quality_gates.sh</verify>
  <automated>ctest --test-dir build/paritychecker_zig_latest --output-on-failure -R paritychecker_tests && scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - `ctest --test-dir build/paritychecker_zig_latest --output-on-failure -R paritychecker_tests` exits 0
    - `scripts/quality_gates.sh` exits 0
    - any remaining benchmark drift is warning-only and documented, not silently ignored
  </acceptance_criteria>
  <done>The repo-level regression gates confirm the new Phase 12 parity contract is shippable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ctest --test-dir build/paritychecker_zig_latest --output-on-failure -R paritychecker_tests`
- [ ] `./build/paritychecker_zig_latest/paritychecker --generation --model tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 8`
- [ ] `scripts/quality_gates.sh`
- [ ] existing Phase 10 and 11 kernel/generator tests remain the cited coverage for unsupported flash-selection behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `PAR-02` is satisfied for both bounded workloads
- `VER-01` is satisfied through combined prior-phase kernel/generator tests plus paritychecker subprocess coverage
  </success_criteria>

<output>
After completion, create `.planning/phases/12-parity-and-verification-closure/12-02-SUMMARY.md`
</output>
