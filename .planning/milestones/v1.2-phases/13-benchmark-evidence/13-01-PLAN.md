---
phase: 13-benchmark-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bench/CMakeLists.txt
  - tools/bench/bench_main.cpp
  - tools/bench/generation_bench.cpp
  - scripts/bench.sh
autonomous: true
requirements:
  - BENCH-01
user_setup: []
must_haves:
  truths:
    - "`scripts/bench.sh --compare` uses the configured fetched-or-pinned reference implementation instead of silently preferring `tmp/llama.cpp`."
    - "The normal compare surface publishes canonical short-case flash proof and reference identity without introducing a second benchmark command."
    - "The maintained short compare row remains `generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1`."
  artifacts:
    - "`tools/bench/CMakeLists.txt` encodes deterministic reference sourcing and resolved reference metadata for `bench_runner`."
    - "`tools/bench/bench_main.cpp` emits durable proof comments for `reference_impl` and `generation_flash_evidence` ahead of compare rows."
    - "`tools/bench/generation_bench.cpp` exposes canonical short-case flash dispatch and seam-proof data for compare-mode publication."
  key_links:
    - "`tools/bench/CMakeLists.txt` -> `bench_runner` compile definitions for `BENCH_REFERENCE_SOURCE` and `BENCH_REFERENCE_REF`."
    - "`tools/bench/generation_bench.cpp` -> `tools/bench/bench_main.cpp` proof metadata for `generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1`."
    - "`tools/bench/bench_main.cpp` -> `scripts/bench.sh --compare-update` so proof comments are preserved inside `snapshots/bench/benchmarks_compare.txt`."
---

<objective>
Make the existing benchmark compare workflow deterministic and self-proving for the canonical
flash-attention case.

Purpose: satisfy BENCH-01 without widening the runtime or adding a flash-only benchmark surface.
Output: `scripts/bench.sh --compare` publishes fetched reference identity plus canonical short-case
flash-proof comments on the same compare output that already carries the benchmark rows.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-benchmark-evidence/13-RESEARCH.md
@.planning/phases/13-benchmark-evidence/13-VALIDATION.md
@tools/bench/CMakeLists.txt
@tools/bench/bench_main.cpp
@tools/bench/generation_bench.cpp
@scripts/bench.sh
@tools/paritychecker/CMakeLists.txt
@tools/paritychecker/parity_runner.cpp
@src/emel/generator/sm.hpp

<interfaces>
From `src/emel/generator/sm.hpp`:
```cpp
uint64_t generation_kernel_dispatch_calls() const noexcept;
uint64_t generation_flash_attention_dispatch_calls() const noexcept;
```

From `tools/bench/generation_bench.cpp`:
```cpp
void print_generation_seam_audit(const char * label, const generation_seam_audit & seam);
void verify_emel_generation_seam(const generation_seam_audit & seam);
void verify_reference_generation_seam(const generation_seam_audit & seam);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make benchmark reference sourcing deterministic and visible</name>
  <files>tools/bench/CMakeLists.txt, scripts/bench.sh</files>
  <read_first>tools/bench/CMakeLists.txt, scripts/bench.sh, tools/paritychecker/CMakeLists.txt, .planning/phases/13-benchmark-evidence/13-RESEARCH.md, .planning/phases/13-benchmark-evidence/13-VALIDATION.md</read_first>
  <action>Update `tools/bench/CMakeLists.txt` to match the Phase 12 paritychecker policy instead of preferring `${EMEL_ROOT}/tmp/llama.cpp`: force the benchmark tool to fetch or pin `llama.cpp` through `REF_IMPL_REF`, resolve the fetched commit with `git -C "${reference_impl_SOURCE_DIR}" rev-parse HEAD`, and compile `bench_runner` with `BENCH_REFERENCE_SOURCE="cmake_fetch"` and `BENCH_REFERENCE_REF="<resolved_sha>"`. In `scripts/bench.sh`, keep the existing `--compare` and `--compare-update` workflow, but make both paths build against that deterministic reference policy and preserve the existing benchmark row format on stdout.</action>
  <verify>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare 2>&1 | rg '^# reference_impl: source=cmake_fetch ref='</verify>
  <automated>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare 2>&1 | rg '^# reference_impl: source=cmake_fetch ref='</automated>
  <acceptance_criteria>
    - `tools/bench/CMakeLists.txt` contains `set(BENCH_REFERENCE_SOURCE "cmake_fetch")`
    - `tools/bench/CMakeLists.txt` contains `execute_process(` and `rev-parse HEAD`
    - `tools/bench/CMakeLists.txt` does not contain `tmp/llama.cpp`
    - `scripts/bench.sh --compare` prints a comment line matching `^# reference_impl: source=cmake_fetch ref=[0-9a-f]+$`
  </acceptance_criteria>
  <done>The benchmark tool uses a deterministic fetched reference and publishes that identity on the normal compare surface.</done>
</task>

<task type="auto">
  <name>Task 2: Publish canonical short-case flash proof on the compare surface</name>
  <files>tools/bench/bench_main.cpp, tools/bench/generation_bench.cpp, scripts/bench.sh</files>
  <read_first>tools/bench/bench_main.cpp, tools/bench/generation_bench.cpp, scripts/bench.sh, tools/paritychecker/parity_runner.cpp, src/emel/generator/sm.hpp, .planning/phases/13-benchmark-evidence/13-RESEARCH.md</read_first>
  <action>Add always-on compare-mode proof metadata for the canonical short case `generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1`. Use the existing generator observability and seam-audit data in `tools/bench/generation_bench.cpp` to produce one durable comment line in `bench_runner --mode=compare` before the benchmark rows: `# generation_flash_evidence: case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 flash_dispatch_calls=<n> emel_decode_calls=0 emel_logits_calls=0 reference_decode_calls=0 reference_logits_calls=0`. Fail compare execution if `flash_dispatch_calls <= 0` for the short EMEL case or if any EMEL short-case reference seam counter is non-zero. Keep the existing row output unchanged so `scripts/bench.sh --compare` and `--compare-update` still feed the maintained compare snapshot workflow.</action>
  <verify>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare 2>&1 | rg '^# generation_flash_evidence: case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 flash_dispatch_calls=[1-9][0-9]* emel_decode_calls=0 emel_logits_calls=0 reference_decode_calls=0 reference_logits_calls=0$'</verify>
  <automated>EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare 2>&1 | rg '^# generation_flash_evidence: case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 flash_dispatch_calls=[1-9][0-9]* emel_decode_calls=0 emel_logits_calls=0 reference_decode_calls=0 reference_logits_calls=0$'</automated>
  <acceptance_criteria>
    - `tools/bench/bench_main.cpp` contains the literal prefix `# generation_flash_evidence: case=`
    - `tools/bench/bench_main.cpp` still prints compare rows matching `%s emel.cpp %.3f ns/op, llama.cpp %.3f ns/op, ratio=%.3fx`
    - `tools/bench/generation_bench.cpp` checks `generation_flash_attention_dispatch_calls()` for the short EMEL case
    - `scripts/bench.sh --compare` still prints the short compare row `generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 emel.cpp`
  </acceptance_criteria>
  <done>The benchmark compare path now proves the canonical flash-attention case actually executed and keeps that proof in the same maintained output stream as the benchmark rows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg 'tmp/llama.cpp|BENCH_REFERENCE_SOURCE|BENCH_REFERENCE_REF' tools/bench/CMakeLists.txt`
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare 2>&1 | rg '^# reference_impl: source=cmake_fetch ref=|^# generation_flash_evidence: case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 flash_dispatch_calls=[1-9][0-9]*'`
- [ ] `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare | rg '^generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 .* ratio='`
- [ ] `scripts/quality_gates.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `tools/bench` no longer depends on a machine-local `tmp/llama.cpp` checkout for Phase 13 evidence
- the normal compare surface proves the canonical short case executed flash attention

</success_criteria>

<output>
After completion, create `.planning/phases/13-benchmark-evidence/13-01-SUMMARY.md`
</output>
