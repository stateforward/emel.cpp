---
phase: 13-benchmark-evidence
plan: 04
type: execute
wave: 3
depends_on:
  - 13-03
files_modified:
  - snapshots/bench/generation_pre_flash_baseline.txt
  - snapshots/bench/benchmarks_compare.txt
  - tools/docsgen/docsgen.cpp
  - docs/templates/benchmarks.md.j2
  - docs/benchmarks.md
autonomous: true
requirements:
  - BENCH-02
  - BENCH-03
user_setup: []
must_haves:
  truths:
    - "Maintained repo artifacts preserve the canonical short pre-flash baseline separately from the current flash compare snapshot."
    - "Generated benchmark docs show the current flash evidence and the short-case improvement over the preserved non-flash baseline."
    - "Phase 13 publication keeps the existing `tools/bench` -> `scripts/bench.sh` -> `snapshots/bench` -> `tools/docsgen` -> `docs/benchmarks.md` workflow."
  artifacts:
    - "`snapshots/bench/generation_pre_flash_baseline.txt` stores the canonical short pre-flash baseline in the key-value format consumed by `tools/bench/compare_flash_baseline.py`."
    - "`tools/docsgen/docsgen.cpp` and `docs/templates/benchmarks.md.j2` render flash evidence plus baseline comparison."
    - "`docs/benchmarks.md` publishes the canonical short-case flash evidence and improvement summary."
  key_links:
    - "`git show 2acd4fe^:snapshots/bench/benchmarks_compare.txt` -> `snapshots/bench/generation_pre_flash_baseline.txt`."
    - "`snapshots/bench/benchmarks_compare.txt` proof comments -> `tools/docsgen/docsgen.cpp` -> `docs/benchmarks.md`."
    - "`tools/bench/compare_flash_baseline.py` -> final BENCH-03 verification on the short case."
---

<objective>
Publish the preserved non-flash baseline artifact and generated benchmark docs after approval.

Purpose: complete BENCH-02 and BENCH-03 through the maintained snapshot and docs workflow, with the
canonical short case as the benchmark-improvement gate.
Output: a checked-in short-case pre-flash baseline artifact plus generated benchmark docs that
explicitly compare flash evidence against that preserved baseline.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-benchmark-evidence/13-RESEARCH.md
@.planning/phases/13-benchmark-evidence/13-VALIDATION.md
@snapshots/bench/benchmarks_compare.txt
@tools/bench/compare_flash_baseline.py
@tools/docsgen/docsgen.cpp
@docs/templates/benchmarks.md.j2
@docs/benchmarks.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize the preserved short-case pre-flash baseline artifact</name>
  <files>snapshots/bench/generation_pre_flash_baseline.txt, snapshots/bench/benchmarks_compare.txt</files>
  <read_first>snapshots/bench/benchmarks_compare.txt, tools/bench/compare_flash_baseline.py, .planning/phases/13-benchmark-evidence/13-RESEARCH.md, .planning/phases/13-benchmark-evidence/13-VALIDATION.md</read_first>
  <action>Create `snapshots/bench/generation_pre_flash_baseline.txt` from the exact historical source `git show 2acd4fe^:snapshots/bench/benchmarks_compare.txt`, keeping only the canonical short-case record `generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1`. Write the artifact in the same key-value contract required by Plan 13-02: `source_commit=2acd4fe^`, `baseline_ref=ecbcb7ea9d3303097519723b264a8b5f1e977028`, `case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1`, `baseline_emel_ns=<historical_emel_ns>`, `baseline_reference_ns=<historical_reference_ns>`, and `baseline_ratio=<historical_ratio>x`. If `snapshots/bench/benchmarks_compare.txt` does not already contain the `# reference_impl:` and `# generation_flash_evidence:` comments added in Plan 13-01, rerun `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare-update` after approval so the maintained compare snapshot carries the proof metadata before docs generation.</action>
  <verify>python3 tools/bench/compare_flash_baseline.py --baseline snapshots/bench/generation_pre_flash_baseline.txt --current snapshots/bench/benchmarks_compare.txt --case generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 | rg '^case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 baseline_emel_ns=[0-9.]+ current_emel_ns=[0-9.]+ speedup=[0-9.]+x latency_drop_pct=[0-9.]+$'</verify>
  <automated>python3 tools/bench/compare_flash_baseline.py --baseline snapshots/bench/generation_pre_flash_baseline.txt --current snapshots/bench/benchmarks_compare.txt --case generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 | rg '^case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 baseline_emel_ns=[0-9.]+ current_emel_ns=[0-9.]+ speedup=[0-9.]+x latency_drop_pct=[0-9.]+$'</automated>
  <acceptance_criteria>
    - `snapshots/bench/generation_pre_flash_baseline.txt` contains `source_commit=2acd4fe^`
    - `snapshots/bench/generation_pre_flash_baseline.txt` contains `baseline_ref=ecbcb7ea9d3303097519723b264a8b5f1e977028`
    - `snapshots/bench/generation_pre_flash_baseline.txt` contains `case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1`
    - `snapshots/bench/generation_pre_flash_baseline.txt` contains `baseline_emel_ns=`
    - `snapshots/bench/generation_pre_flash_baseline.txt` contains `baseline_reference_ns=`
    - `snapshots/bench/generation_pre_flash_baseline.txt` contains `baseline_ratio=`
    - `snapshots/bench/benchmarks_compare.txt` contains `# generation_flash_evidence: case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1`
  </acceptance_criteria>
  <done>The repo now preserves the pre-flash canonical short-case evidence as a maintained artifact and the current compare snapshot carries the durable flash-proof metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Generate benchmark docs that distinguish flash evidence from the preserved baseline</name>
  <files>tools/docsgen/docsgen.cpp, docs/templates/benchmarks.md.j2, docs/benchmarks.md</files>
  <read_first>tools/docsgen/docsgen.cpp, docs/templates/benchmarks.md.j2, docs/benchmarks.md, snapshots/bench/generation_pre_flash_baseline.txt, snapshots/bench/benchmarks_compare.txt, .planning/phases/13-benchmark-evidence/13-RESEARCH.md</read_first>
  <action>Extend `tools/docsgen/docsgen.cpp` and `docs/templates/benchmarks.md.j2` so generated benchmark docs continue to render the full compare table from `snapshots/bench/benchmarks_compare.txt`, but also add a dedicated flash-publication section for the canonical short case. That section must read the key-value baseline artifact fields from `snapshots/bench/generation_pre_flash_baseline.txt`, show the current compare snapshot proof metadata from `# reference_impl:` and `# generation_flash_evidence:`, and compute the short-case improvement summary from the preserved baseline artifact plus the current compare row at docs-generation time. Regenerate `docs/benchmarks.md` through `scripts/generate_docs.sh` rather than hand-editing it.</action>
  <verify>scripts/generate_docs.sh --check && rg 'generation_pre_flash_baseline.txt|Current Flash Evidence|Pre-Flash Baseline Comparison|generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1|speedup=[0-9]+\\.[0-9]+x|latency_drop_pct=[0-9]+\\.[0-9]+' docs/benchmarks.md</verify>
  <automated>scripts/generate_docs.sh --check && rg 'generation_pre_flash_baseline.txt|Current Flash Evidence|Pre-Flash Baseline Comparison|generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1|speedup=[0-9]+\\.[0-9]+x|latency_drop_pct=[0-9]+\\.[0-9]+' docs/benchmarks.md</automated>
  <acceptance_criteria>
    - `tools/docsgen/docsgen.cpp` contains `generation_pre_flash_baseline.txt`
    - `docs/templates/benchmarks.md.j2` contains `Current Flash Evidence`
    - `docs/templates/benchmarks.md.j2` contains `Pre-Flash Baseline Comparison`
    - `docs/benchmarks.md` contains `speedup=`
    - `docs/benchmarks.md` contains `latency_drop_pct=`
  </acceptance_criteria>
  <done>The generated benchmark docs now publish flash-specific evidence and the preserved canonical short-case pre-flash baseline through the maintained docsgen workflow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 tools/bench/compare_flash_baseline.py --baseline snapshots/bench/generation_pre_flash_baseline.txt --current snapshots/bench/benchmarks_compare.txt --case generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1`
- [ ] `scripts/generate_docs.sh --check`
- [ ] `rg 'generation_pre_flash_baseline.txt|Current Flash Evidence|Pre-Flash Baseline Comparison|speedup=[0-9]+\\.[0-9]+x|latency_drop_pct=[0-9]+\\.[0-9]+' docs/benchmarks.md`
- [ ] `scripts/quality_gates.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- maintained repo artifacts preserve the canonical short pre-flash baseline separately from current flash evidence
- generated benchmark docs publish the short-case flash improvement without inventing a new benchmark workflow

</success_criteria>

<output>
After completion, create `.planning/phases/13-benchmark-evidence/13-04-SUMMARY.md`
</output>
