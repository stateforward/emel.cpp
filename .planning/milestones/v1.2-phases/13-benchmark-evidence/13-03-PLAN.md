---
phase: 13-benchmark-evidence
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified: []
autonomous: false
requirements:
  - BENCH-02
user_setup: []
must_haves:
  truths:
    - "No checked-in benchmark snapshot artifact is modified until the user explicitly approves Phase 13 publication."
    - "The user sees the exact files and commands that Plan 13-04 will run if approval is granted."
  artifacts:
    - "No repo files change in this plan; the execution summary records the approval or decline decision."
  key_links:
    - "Plan 13-03 blocks Plan 13-04 so snapshot and generated-doc publication cannot run implicitly."
---

<objective>
Pause execution for the repo-mandated snapshot publication approval gate.

Purpose: obey `AGENTS.md` before any Phase 13 plan writes or refreshes checked-in benchmark
artifacts.
Output: an explicit `approve` or `decline` decision that controls whether Plan 13-04 may run.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/benchmarking.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-benchmark-evidence/13-RESEARCH.md
@.planning/phases/13-benchmark-evidence/13-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Secure explicit approval before writing checked-in benchmark artifacts</name>
  <files>none - checkpoint only; do not modify repo files in this plan</files>
  <read_first>AGENTS.md, docs/benchmarking.md, .planning/phases/13-benchmark-evidence/13-RESEARCH.md, .planning/phases/13-benchmark-evidence/13-VALIDATION.md</read_first>
  <action>Present the exact Phase 13 publication changes before resuming execution. The follow-on plan will write `snapshots/bench/generation_pre_flash_baseline.txt`, may refresh `snapshots/bench/benchmarks_compare.txt` only if the new proof metadata is missing, and will regenerate `docs/benchmarks.md` through `tools/docsgen` / `scripts/generate_docs.sh`. Do not run `scripts/bench.sh --compare-update` and do not create any new checked-in snapshot artifact unless the user explicitly answers `approve`.</action>
  <decision>Approve Phase 13 checked-in benchmark artifact updates?</decision>
  <context>`AGENTS.md` says `NEVER update snapshots without explicit user consent`. Phase 13 cannot satisfy BENCH-02 publication honestly unless the preserved pre-flash baseline artifact and generated docs are written through the maintained snapshot/docs workflow.</context>
  <options>
    <option id="approve">
      <name>Approve</name>
      <pros>Plan 13-04 will materialize the preserved pre-flash baseline artifact and publish the generated benchmark docs.</pros>
      <cons>Checked-in benchmark artifacts will change in this branch.</cons>
    </option>
    <option id="decline">
      <name>Decline</name>
      <pros>Leaves all checked-in snapshot artifacts unchanged.</pros>
      <cons>Phase 13 will stop short of published BENCH-02 evidence.</cons>
    </option>
  </options>
  <resume-signal>Reply `approve` to continue with Plan 13-04, or `decline` to stop before any snapshot artifact change.</resume-signal>
  <verify>Explicit user response recorded as `approve` or `decline`.</verify>
  <automated>rg 'NEVER update snapshots without explicit user consent' AGENTS.md</automated>
  <acceptance_criteria>
    - the user is shown the exact files `snapshots/bench/generation_pre_flash_baseline.txt`, `snapshots/bench/benchmarks_compare.txt`, and `docs/benchmarks.md`
    - the user is told that `scripts/bench.sh --compare-update` will not run without approval
    - execution does not proceed to Plan 13-04 unless the response is exactly `approve`
  </acceptance_criteria>
  <done>The approval decision is explicit and no checked-in snapshot artifact has been modified without consent.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg 'NEVER update snapshots without explicit user consent' AGENTS.md`
- [ ] user response recorded as `approve` or `decline`
- [ ] no checked-in snapshot artifact was modified before approval
</verification>

<success_criteria>

- All tasks completed
- the approval gate was honored exactly
- execution either stops cleanly on `decline` or unlocks Plan 13-04 on `approve`

</success_criteria>

<output>
After completion, create `.planning/phases/13-benchmark-evidence/13-03-SUMMARY.md`
</output>
