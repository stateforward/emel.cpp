---
phase: 13-benchmark-evidence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bench/compare_flash_baseline.py
  - tools/bench/testdata/generation_pre_flash_baseline_pass.txt
  - tools/bench/testdata/generation_pre_flash_baseline_fail.txt
  - tools/bench/testdata/generation_compare_current.txt
  - docs/benchmarking.md
autonomous: true
requirements:
  - BENCH-02
  - BENCH-03
user_setup: []
must_haves:
  truths:
    - "Phase 13 verification can compute the canonical short-case flash improvement without manual `git show` arithmetic."
    - "The benchmark runbook explicitly treats checked-in snapshot updates as approval-gated work."
    - "BENCH-03 is anchored to `generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1` unless a trustworthy long-case baseline is later added."
  artifacts:
    - "`tools/bench/compare_flash_baseline.py` compares a key-value preserved baseline artifact against the current compare snapshot."
    - "`docs/benchmarking.md` names the approval-gated publication steps and the exact short-case evidence artifact paths."
  key_links:
    - "`tools/bench/compare_flash_baseline.py` -> `snapshots/bench/generation_pre_flash_baseline.txt` and `snapshots/bench/benchmarks_compare.txt`."
    - "`docs/benchmarking.md` -> the exact approval gate before `scripts/bench.sh --compare-update` or any new checked-in snapshot artifact."
---

<objective>
Prepare the short-case baseline comparison tooling and operator runbook that Phase 13 publication
will use.

Purpose: make BENCH-02 and BENCH-03 executable from repo-local tooling before any approval-gated
snapshot refresh.
Output: a canonical short-case comparator script and a benchmark runbook that names the exact
artifact paths, commands, and approval stop point.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-benchmark-evidence/13-RESEARCH.md
@.planning/phases/13-benchmark-evidence/13-VALIDATION.md
@docs/benchmarking.md
@snapshots/bench/benchmarks_compare.txt
@tools/bench/bench_cases.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a canonical short-case flash-vs-baseline comparator</name>
  <files>tools/bench/compare_flash_baseline.py, tools/bench/testdata/generation_pre_flash_baseline_pass.txt, tools/bench/testdata/generation_pre_flash_baseline_fail.txt, tools/bench/testdata/generation_compare_current.txt</files>
  <read_first>tools/bench/compare_flash_baseline.py, tools/bench/testdata/generation_pre_flash_baseline_pass.txt, tools/bench/testdata/generation_pre_flash_baseline_fail.txt, tools/bench/testdata/generation_compare_current.txt, snapshots/bench/benchmarks_compare.txt, tools/bench/bench_cases.hpp, .planning/phases/13-benchmark-evidence/13-RESEARCH.md, .planning/phases/13-benchmark-evidence/13-VALIDATION.md</read_first>
  <action>Create `tools/bench/compare_flash_baseline.py` as a repo-local CLI with the exact interface `--baseline <path> --current <path> --case <case-name>`. Make the `--baseline` input a key-value artifact compatible with the future preserved file `snapshots/bench/generation_pre_flash_baseline.txt`; require these exact fields: `source_commit=<commit>`, `baseline_ref=<ref>`, `case=<case-name>`, `baseline_emel_ns=<ns>`, `baseline_reference_ns=<ns>`, and `baseline_ratio=<ratio>x`. Keep `--current` as the current compare snapshot input and parse compare rows shaped like `generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 emel.cpp 6992875.000 ns/op, llama.cpp 4810834.000 ns/op, ratio=1.454x`. Print one deterministic summary line: `case=<case> baseline_emel_ns=<baseline> current_emel_ns=<current> speedup=<3-decimal>x latency_drop_pct=<1-decimal>`. Exit non-zero if either file is missing, a required baseline field is missing, the case does not match, the current compare row is missing, or `current_emel_ns >= baseline_emel_ns`. Add fixture files `tools/bench/testdata/generation_pre_flash_baseline_pass.txt`, `tools/bench/testdata/generation_pre_flash_baseline_fail.txt`, and `tools/bench/testdata/generation_compare_current.txt` so automated verification proves one passing comparison and one failing comparison.</action>
  <verify>sh -c 'python3 tools/bench/compare_flash_baseline.py --baseline tools/bench/testdata/generation_pre_flash_baseline_pass.txt --current tools/bench/testdata/generation_compare_current.txt --case generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 | rg \"^case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 baseline_emel_ns=[0-9.]+ current_emel_ns=[0-9.]+ speedup=[0-9.]+x latency_drop_pct=[0-9.]+$\" && ! python3 tools/bench/compare_flash_baseline.py --baseline tools/bench/testdata/generation_pre_flash_baseline_fail.txt --current tools/bench/testdata/generation_compare_current.txt --case generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 >/dev/null 2>&1'</verify>
  <automated>sh -c 'python3 tools/bench/compare_flash_baseline.py --baseline tools/bench/testdata/generation_pre_flash_baseline_pass.txt --current tools/bench/testdata/generation_compare_current.txt --case generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 | rg \"^case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 baseline_emel_ns=[0-9.]+ current_emel_ns=[0-9.]+ speedup=[0-9.]+x latency_drop_pct=[0-9.]+$\" && ! python3 tools/bench/compare_flash_baseline.py --baseline tools/bench/testdata/generation_pre_flash_baseline_fail.txt --current tools/bench/testdata/generation_compare_current.txt --case generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 >/dev/null 2>&1'</automated>
  <acceptance_criteria>
    - `tools/bench/compare_flash_baseline.py` contains `--baseline`
    - `tools/bench/compare_flash_baseline.py` contains `--current`
    - `tools/bench/compare_flash_baseline.py` contains `--case`
    - `tools/bench/compare_flash_baseline.py` reads `baseline_emel_ns=` from the baseline artifact instead of parsing a compare row from `--baseline`
    - `tools/bench/testdata/generation_pre_flash_baseline_pass.txt` and `tools/bench/testdata/generation_pre_flash_baseline_fail.txt` contain `source_commit=`, `baseline_ref=`, `case=`, `baseline_emel_ns=`, `baseline_reference_ns=`, and `baseline_ratio=`
    - the automated verify proves one success path and one failure path
  </acceptance_criteria>
  <done>Phase 13 has a deterministic repo-local comparator for the canonical short-case flash evidence versus a preserved pre-flash baseline artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Update the benchmark runbook for the approval-gated short-case publication flow</name>
  <files>docs/benchmarking.md</files>
  <read_first>docs/benchmarking.md, AGENTS.md, .planning/phases/13-benchmark-evidence/13-RESEARCH.md, .planning/phases/13-benchmark-evidence/13-VALIDATION.md</read_first>
  <action>Update `docs/benchmarking.md` so the Phase 13 runbook explicitly says all flash-evidence publication stays on `scripts/bench.sh --compare`, `scripts/bench.sh --compare-update`, `snapshots/bench`, `tools/docsgen`, and `docs/benchmarks.md`. Add a dedicated Phase 13 subsection that names `generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1` as the BENCH-03 gate, calls out `snapshots/bench/generation_pre_flash_baseline.txt` as the preserved non-flash artifact, documents the artifact fields `source_commit`, `baseline_ref`, `case`, `baseline_emel_ns`, `baseline_reference_ns`, and `baseline_ratio`, and tells the operator to stop and obtain explicit user approval before running `scripts/bench.sh --compare-update` or checking in any new snapshot artifact.</action>
  <verify>rg 'generation_pre_flash_baseline.txt|generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1|explicit user approval|scripts/bench.sh --compare-update' docs/benchmarking.md</verify>
  <automated>rg 'generation_pre_flash_baseline.txt|generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1|explicit user approval|scripts/bench.sh --compare-update' docs/benchmarking.md</automated>
  <acceptance_criteria>
    - `docs/benchmarking.md` contains `snapshots/bench/generation_pre_flash_baseline.txt`
    - `docs/benchmarking.md` contains `generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1`
    - `docs/benchmarking.md` contains the phrase `explicit user approval`
    - `docs/benchmarking.md` contains `scripts/bench.sh --compare-update`
  </acceptance_criteria>
  <done>The operator runbook now names the canonical short-case baseline workflow and the mandatory approval gate before any checked-in snapshot change.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `sh -c 'python3 tools/bench/compare_flash_baseline.py --baseline tools/bench/testdata/generation_pre_flash_baseline_pass.txt --current tools/bench/testdata/generation_compare_current.txt --case generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 | rg "^case=generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 baseline_emel_ns=[0-9.]+ current_emel_ns=[0-9.]+ speedup=[0-9.]+x latency_drop_pct=[0-9.]+$" && ! python3 tools/bench/compare_flash_baseline.py --baseline tools/bench/testdata/generation_pre_flash_baseline_fail.txt --current tools/bench/testdata/generation_compare_current.txt --case generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1 >/dev/null 2>&1'`
- [ ] `rg 'generation_pre_flash_baseline.txt|generation/preloaded_request/llama_68m_prompt_hello_max_tokens_1|explicit user approval|scripts/bench.sh --compare-update' docs/benchmarking.md`
- [ ] `scripts/quality_gates.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- the canonical short case is the documented BENCH-03 gate
- Phase 13 publication instructions now stop for approval before any checked-in snapshot artifact change

</success_criteria>

<output>
After completion, create `.planning/phases/13-benchmark-evidence/13-02-SUMMARY.md`
</output>
