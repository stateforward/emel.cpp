---
phase: 249
plan: 01
status: complete
autonomous: true
wave: 1
files_modified:
  - src/emel/io/async/events.hpp
  - src/emel/io/async/guards.hpp
  - tests/io/loader/lifecycle_tests.cpp
rule_constraints:
  - Scheduler/resource behavior choice must live in async guards and sm transitions.
  - Actions/detail helpers must not select runtime paths.
  - Tests must drive public dispatch and SML state inspection.
---

# Phase 249 Plan

<rule_constraints>
Scheduler/resource error selection belongs in `src/emel/io/async/guards.hpp` and
`src/emel/io/async/sm.hpp`. Actions may only publish the already-selected error. Do not add
test-only event fields or hidden helper routing.
</rule_constraints>

## Goal

Make async scheduler/resource terminal error semantics reachable through explicit guards and
transitions, closing `AIO-06` and `INT-02`.

## Tasks

1. Add a production scheduler/resource budget contract to the async load window event surface,
   defaulted so existing callers remain valid.
2. Replace the constant scheduler guard predicates in `src/emel/io/async/guards.hpp` with
   runtime predicates over the already-validated load window and next cooperative chunk.
3. Keep `src/emel/io/async/sm.hpp` as the routing source; only adjust transition comments if
   needed for clarity.
4. Add doctests proving scheduler/resource rejection, cancellation, and existing success paths
   through `emel::io::async::sm::process_event(...)` and ready-state inspection.
5. Run focused async/loader tests and changed-file scoped quality gates.

## Verification

- `./build/debug/emel_tests_bin --test-case="*io async*"`
- `EMEL_QUALITY_GATES_CHANGED_FILES="src/emel/io/async/events.hpp src/emel/io/async/guards.hpp tests/io/loader/lifecycle_tests.cpp" scripts/quality_gates.sh`
