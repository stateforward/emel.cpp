# Phase 241 Plan 01: Async I/O Strategy Component Boundary

**Status:** Ready
**Requirements:** AIO-01, AIO-02
**Date:** 2026-05-09

## Goal

Add the dedicated `io/async` component boundary and prove it fails closed without changing shipped
sync strategy semantics.

## Tasks

1. Add `src/emel/io/async/{context,errors,events,detail,guards,actions,sm}.hpp`.
2. Expose `emel::io::async::sm` and `emel::IoAsync`.
3. Add boundary tests in the existing I/O test shard.
4. Build and run focused tests plus changed-file scoped gates.

## Acceptance Checks

- `src/emel/io/async` has canonical component files and a public `sm`.
- The component uses `emel::co_sm` and starts in `state_ready`.
- Public `event::load_window` returns false and publishes `unsupported_strategy` error when called.
- No shipped mmap/read/staged-read runtime files are modified.
