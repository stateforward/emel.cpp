---
phase: 252
plan: 01
status: complete
autonomous: true
wave: 1
files_modified:
  - tools/bench/generation_bench.cpp
  - tools/bench/model_load_strategy.hpp
  - tools/bench/bench_runner.cpp
  - snapshots/quality_gates/timing.txt
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - README.md
rule_constraints:
  - Profiling must exercise the maintained public cooperative async path.
  - Do not report tool-only fallbacks or in-RAM shortcuts as async performance evidence.
  - Optimization must target the measured maintained path.
---

# Phase 252 Plan

<rule_constraints>
Large-model or constrained-RAM evidence must run through public model-loader, `io/loader`, and
tensor contracts. If a real larger-than-RAM model is unavailable, use maintained constrained-RAM
emulation and label it truthfully.
</rule_constraints>

## Goal

Produce source-backed constrained-RAM profiling and scoped optimization evidence for the maintained
cooperative async loading path, closing `PERF-02`.

## Tasks

1. Add or reuse a maintained profiling command/path that drives `cooperative_async` through the
   public model-loader/loader/tensor path.
2. Record model size or emulated model size, effective RAM constraint, chunk/window behavior,
   peak memory, throughput/latency, and whether the run is real large-model or constrained-RAM
   emulation.
3. Identify the highest-impact bottleneck from the same maintained path and apply one scoped
   optimization without widening async scope.
4. Rerun the same profiling command and document remaining bottlenecks honestly.
5. Update benchmark/reporting docs and milestone evidence to mark `PERF-02` satisfied only if the
   maintained path actually ran.
6. Run the relevant benchmark/profiling command and changed-file scoped quality gates.

## Verification

- `EMEL_MODEL_LOAD_IO_STRATEGY=cooperative_async scripts/bench.sh --snapshot --compare --suite=generation`
- `EMEL_QUALITY_GATES_BENCH_SUITE="generation" EMEL_QUALITY_GATES_CHANGED_FILES="tools/bench/generation_bench.cpp tools/bench/model_load_strategy.hpp tools/bench/bench_cases.hpp .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md snapshots/quality_gates/timing.txt" scripts/quality_gates.sh`
