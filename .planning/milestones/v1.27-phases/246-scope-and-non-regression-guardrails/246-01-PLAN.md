# Phase 246 Plan

## Goal

Add guardrails that keep cooperative async I/O scoped to its approved component and protect shipped
synchronous loading strategies.

## Tasks

1. Add source tests proving coroutine task/scheduler/awaitable types do not leak into public C ABI
   or generic model/generator contracts.
2. Add source tests proving maintained tools and model-loader code avoid async actor internals.
3. Add source tests proving async actions/detail do not choose runtime behavior that belongs in
   guards/transitions.
4. Add regression coverage checks for shipped mmap, read/copy, staged-read, loader, tensor, and
   model-loader public dispatch tests.
5. Run focused guardrail/regression tests and a changed-file scoped quality gate.
