# Phase 239 Plan 01: Coroutine Actor Contract

**Status:** Ready
**Requirement:** CO-01
**Date:** 2026-05-09

## Goal

Update the project actor-model rules so approved `co_sm` coroutine actors have a precise contract
that preserves EMEL's RTC/no-queue/allocation/lifetime invariants.

## Tasks

1. Update `docs/rules/sml.rules.md` with coroutine actor definitions and invariants.
2. Update `AGENTS.md` with the same operational guidance.
3. Update `CLAUDE.md` to stay aligned with `AGENTS.md`.
4. Record verification and summary artifacts for Phase 239.

## Acceptance Checks

- `rg 'co_sm|coroutine actor|process_event_async' docs/rules/sml.rules.md AGENTS.md CLAUDE.md`
  shows explicit coroutine actor guidance.
- The new rules keep default synchronous actor constraints intact.
- The new rules forbid hidden mailboxes, retained stack-backed suspension data, stored callbacks,
  public C ABI coroutine types, and hidden behavior selection in awaitables/actions/detail helpers.
- No runtime source code is changed in this phase.
