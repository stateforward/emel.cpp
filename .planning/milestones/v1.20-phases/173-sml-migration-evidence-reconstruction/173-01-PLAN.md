---
phase: 173-sml-migration-evidence-reconstruction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/173-sml-migration-evidence-reconstruction/173-01-SUMMARY.md
  - .planning/phases/173-sml-migration-evidence-reconstruction/173-VERIFICATION.md
  - .planning/phases/173-sml-migration-evidence-reconstruction/173-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
  - SRC-01
  - SRC-02
rule_constraints:
  - Source-backed claims must be traced to live repo files or git history.
  - Do not treat planning artifacts alone as completion evidence.
---

# Phase 173 Plan 01

## Objective

Reconstruct source-backed evidence for the SML dependency pin and source namespace migration
requirements reopened by the v1.20 milestone audit.

## Tasks

1. Trace the prior and current SML dependency pins from `cmake/sml_version.cmake` git history.
2. Verify active source/test/tool/doc paths use the `stateforward` include and namespace surface.
3. Verify active legacy `boost/sml`, `<sml.hpp>`, and `boost::sml` references are absent from the
   audited active paths.
4. Record SUMMARY, VERIFICATION, and VALIDATION artifacts and mark DEP-01, DEP-02, DEP-03, SRC-01,
   and SRC-02 complete.

## Verification

- `git log -p -n 2 -- cmake/sml_version.cmake`
- `rg -n '#\s*include\s*[<"](boost/sml|sml\.hpp|boost/sml\.hpp)' src include tests tools docs scripts .codex/get-shit-done cmake CMakeLists.txt || true`
- `rg -n '\bboost::sml\b|using\s+namespace\s+boost::sml|#\s*include\s*[<"]boost/sml' src include tests tools docs scripts .codex/get-shit-done cmake CMakeLists.txt || true`
- `rg -n '#\s*include\s*[<"]stateforward/sml|stateforward::sml' src include tests tools docs/rules tools/docsgen CMakeLists.txt cmake`

## Done Condition

The dependency/source migration requirements have live source-backed evidence and phase artifacts.

