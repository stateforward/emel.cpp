---
phase: 117
plan: 01
status: planned
requirements:
  - REOPEN-01
rule_constraints:
  - Follow `docs/rules/sml.rules.md`; no SML dispatch changes are planned.
  - Preserve Whisper domain boundaries from `AGENTS.md`.
  - Keep EMEL/reference lanes isolated.
---

# Plan 117.1: Whisper Compare Failure Contract Repair

<rule_constraints>
No actor action/guard/detail changes are planned. If scope expands into runtime code, runtime
behavior choices must remain in SML guards/transitions, and `scripts/check_domain_boundaries.sh`
must pass.
</rule_constraints>

## Goal

Make maintained Whisper transcript drift fail the compare and quality-gate path instead of
publishing a successful `bounded_drift` result.

## Tasks

1. Add a failing doctest case for compare transcript mismatch using fake EMEL/reference runners.
2. Update `tools/bench/whisper_compare.py` so only `exact_match` exits 0.
3. Verify exact-match compare still passes and mismatch compare fails with deterministic
   `bounded_drift` / `transcript_mismatch` evidence.
4. Run focused Whisper compare/benchmark tests and the maintained compare wrapper.

## Success Criteria

- `tools/bench/whisper_compare.py` returns nonzero for `bounded_drift`.
- Focused tests cover exact-match success and transcript-mismatch failure.
- `scripts/bench_whisper_compare.sh --skip-reference-build --skip-emel-build` still passes for
  current exact parity.
- `REOPEN-01` is source-backed by the maintained no-drift gate.
