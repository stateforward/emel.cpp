---
phase: 110
plan: 1
status: complete
created: 2026-04-27
rule_constraints:
  - docs/rules/sml.rules.md
  - AGENTS.md benchmark and reference policy
---

# Plan 110-01: Repair Maintained Whisper Benchmark Publication

<rule_constraints>
## Applicable Rules

- Keep EMEL and reference benchmark lanes isolated.
- Use EMEL-owned source conversion and runtime for the EMEL lane.
- Do not publish benchmark success when model or transcript truth contradicts the claim.
- Add focused doctest coverage for benchmark publication behavior.
</rule_constraints>

## Tasks

1. Change `scripts/bench_whisper_single_thread.sh` so the default EMEL model path is the same
   pinned Phase 99 source model used by the reference lane.
2. Update `tools/bench/whisper_benchmark.py` summary logic to include lane model SHA values and
   fail on model or transcript mismatch.
3. Align the benchmark reference CLI flags with the maintained compare lane's deterministic
   reference policy.
4. Add focused benchmark tests for model mismatch, transcript mismatch, and deterministic
   reference-policy flags.
5. Re-run focused tests and the repaired single-thread benchmark wrapper.

## Verification

- `build/whisper_compare_tools/whisper_benchmark_tests`
- `EMEL_WHISPER_BENCH_WARMUPS=0 EMEL_WHISPER_BENCH_ITERATIONS=1
  scripts/bench_whisper_single_thread.sh --skip-reference-build --skip-emel-build`
