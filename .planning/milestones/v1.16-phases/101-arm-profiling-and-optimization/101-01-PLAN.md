---
phase: 101-arm-profiling-and-optimization
plan: 01
goal: Profile the maintained EMEL runtime and optimize until EMEL beats the matched single-thread
  whisper.cpp lane.
status: complete
rule_constraints:
  - Optimizations must land in source-owned kernel/runtime code.
  - No tool-local shortcut may count as runtime optimization.
  - Quality gates must pass without weakening coverage thresholds.
---

# Phase 101 Plan 01: ARM Profiling And Optimization

<rule_constraints>
## Runtime Ownership

- Keep numeric optimization in `src/emel/kernel/whisper/detail.hpp`.
- Do not add `whisper.cpp` or ggml linkage to EMEL runtime.
- Preserve SML guard/transition routing and allocation-free dispatch.

## Evidence

- Use Phase 100 `whisper_benchmark/v1` records as the before/after benchmark surface.
- Keep the same pinned q8_0 model and Phase 99 audio fixture.
- Run focused Whisper tests and the scoped quality gate.
</rule_constraints>

## Tasks

1. Profile Phase 100 records.
   - Identify dominant EMEL stages from raw benchmark records.
   - Confirm matched reference timing on the same pinned fixture.

2. Optimize source-owned runtime.
   - Reduce repeated frontend trig work with caller-workspace tables.
   - Add AArch64 NEON q8_0 row dot acceleration for Whisper linear projections.

3. Add focused tests.
   - Cover q4 helper paths, shape/tensor lookup helpers, softmax, transcript writing, and q8 NEON
     row behavior.
   - Add missing `whisper` compile-time test shard support.

4. Verify.
   - Run focused Whisper tests.
   - Run benchmark wrapper.
   - Run scoped quality gate.

## Completion Evidence

All tasks are complete. The final scoped quality gate passed with
`src/emel/kernel/whisper/detail.hpp` line coverage `100.0%`, branch coverage `55.3%`, and
`whisper_single_thread` benchmark status `ok`.
