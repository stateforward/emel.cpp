---
phase: 63-python-reference-backend
plan: 01
type: execute
wave: 1
depends_on:
  - 62-reference-backend-contract
files_modified:
  - tools/bench/embedding_reference_python.py
  - tools/bench/reference_backends/te_python_goldens.json
  - tools/bench/reference_backends/te_python_live.json
  - tests/embeddings/fixtures/te75m/README.md
  - .planning/phases/63-python-reference-backend/63-CONTEXT.md
  - .planning/phases/63-python-reference-backend/63-01-PLAN.md
  - .planning/phases/63-python-reference-backend/63-01-SUMMARY.md
  - .planning/phases/63-python-reference-backend/63-VERIFICATION.md
autonomous: true
requirements:
  - PY-01
  - PY-02
---

# Phase 63 Plan 01

## Objective

Bring a maintained Python embedding backend onto the shared compare contract and make failure modes
explicit instead of leaking ambiguous partial output into the compare workflow.

## Tasks

1. Add a Python reference runner that emits the canonical compare contract and binary vectors.
2. Wire a maintained stored-golden backend for TE plus an optional live backend path.
3. Ensure Python-side failures show up as explicit compare records rather than silently breaking
   the compare workflow.

## Verification

- `python3 -m py_compile tools/bench/embedding_compare.py tools/bench/embedding_reference_python.py`
- `EMEL_EMBEDDING_BENCH_FORMAT=jsonl EMEL_EMBEDDING_RESULT_DIR=build/embedding_compare/python_goldens EMEL_BENCH_CASE_FILTER=text python3 tools/bench/embedding_reference_python.py --backend te75m_goldens`
- `ctest --test-dir build/bench_tools_ninja --output-on-failure -R '^embedding_compare_tests$'`

## Done Condition

The phase is complete when at least one Python backend emits canonical compare records and Python
backend failure modes are explicit at the compare surface.
