---
phase: 79
plan: 01
status: complete
rule_constraints: embedding benchmark tooling only; preserve TE runtime path
---

# Phase 79 Plan: Embedding Variant Discovery Cutover

<rule_constraints>
- Do not change `src/emel/embeddings/generator`.
- Keep existing TE payload behavior unchanged.
- Preserve Python-golden and Liquid C++ compare compatibility.
</rule_constraints>

## Tasks

- Drive EMEL embedding benchmark case metadata from variant manifests.
- Drive Python TE reference records from variant manifests.
- Add `--variant-id` selection to embedding compare and wrapper surfaces.
- Add focused tests for variant discovery, filtering, and Python-golden output.

## Verification

- Build `embedding_compare_tests`.
- Run `embedding_compare_tests`.
