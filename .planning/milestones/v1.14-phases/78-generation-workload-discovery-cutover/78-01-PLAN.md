---
phase: 78
plan: 01
status: complete
rule_constraints: generation benchmark tooling only; preserve compare record schema
---

# Phase 78 Plan: Generation Workload Discovery Cutover

<rule_constraints>
- Do not widen generation runtime support.
- Keep `generation_compare/v1` stable.
- Preserve comparable and single-lane metadata.
</rule_constraints>

## Tasks

- Add generation workload manifest directory loading.
- Replace the hard-coded generation workload path array with discovered manifests.
- Document the data-only generation workload add path.
- Add a focused discovery regression.

## Verification

- Build `bench_runner_tests`.
- Run focused generation workload discovery test.
- Run `generation_compare_tests`.
