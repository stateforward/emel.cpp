---
phase: 77
plan: 01
status: complete
rule_constraints: benchmark tooling only; preserve lane isolation; deterministic discovery
---

# Phase 77 Plan: Benchmark Variant Registry Contract

<rule_constraints>
- Keep changes under `tools/bench` and scripts.
- Do not modify `src/` runtime behavior.
- Preserve deterministic benchmark output and lane isolation.
</rule_constraints>

## Tasks

- Add shared manifest discovery and ID validation helpers.
- Add embedding variant manifest contract.
- Add checked-in embedding variant manifests for maintained text, image, and audio cases.
- Add focused duplicate-ID and deterministic-discovery tests.

## Verification

- Build affected bench test targets.
- Run `embedding_compare_tests`.
- Run focused generation discovery test.
