---
milestone: v1.18
audited: "2026-05-01T03:33:34Z"
status: gaps_found
scores:
  requirements: 6/12
  phases: 5/5
  integration: 3/5
  flows: 3/5
gaps:
  requirements:
    - id: "PARITY-01"
      status: "partial"
      phase: "148"
      claimed_by_plans:
        - ".planning/milestones/v1.18-phases/148-parity-runner-asset-boundary/148-01-PLAN.md"
      completed_by_plans:
        - ".planning/milestones/v1.18-phases/148-parity-runner-asset-boundary/148-01-SUMMARY.md"
      verification_status: "passed"
      evidence: "Requirement says the shared runner owns CLI/config parsing, but parse_args(...) and print_usage(...) remain in tools/paritychecker/parity_main.cpp while tools/paritychecker/parity_runner.cpp only resolves and invokes an engine adapter."
    - id: "PARITY-03"
      status: "partial"
      phase: "152"
      claimed_by_plans:
        - ".planning/milestones/v1.18-phases/152-parity-behavior-and-lane-isolation-closure/152-01-PLAN.md"
      completed_by_plans:
        - ".planning/milestones/v1.18-phases/152-parity-behavior-and-lane-isolation-closure/152-01-SUMMARY.md"
      verification_status: "passed"
      evidence: "Maintained generation success compares EMEL output against an append-only snapshot baseline instead of a live reference-runtime result; behavior smoke checks pass, but parity proof truth is not fully live-path backed."
    - id: "LANE-01"
      status: "partial"
      phase: "148"
      claimed_by_plans:
        - ".planning/milestones/v1.18-phases/148-parity-runner-asset-boundary/148-01-PLAN.md"
      completed_by_plans:
        - ".planning/milestones/v1.18-phases/148-parity-runner-asset-boundary/148-01-SUMMARY.md"
      verification_status: "passed"
      evidence: "Tokenizer and generation object ownership is separated, but the generation reference lane's live runtime/cache/output is not the normal success comparison source."
    - id: "LANE-02"
      status: "partial"
      phase: "152"
      claimed_by_plans:
        - ".planning/milestones/v1.18-phases/152-parity-behavior-and-lane-isolation-closure/152-01-PLAN.md"
      completed_by_plans:
        - ".planning/milestones/v1.18-phases/152-parity-behavior-and-lane-isolation-closure/152-01-SUMMARY.md"
      verification_status: "passed"
      evidence: "Source checks forbid selected generator/detokenizer/Jinja internals, but tools/paritychecker/parity_engines.cpp still includes or calls actor/detail/action internals such as emel/gguf/loader/detail.hpp, emel/kernel/aarch64/actions.hpp, emel/model/detail.hpp, and emel/model/llama/detail.hpp."
    - id: "MANIFEST-01"
      status: "partial"
      phase: "151"
      claimed_by_plans:
        - ".planning/milestones/v1.18-phases/151-parity-dependency-manifest-emission/151-01-PLAN.md"
      completed_by_plans:
        - ".planning/milestones/v1.18-phases/151-parity-dependency-manifest-emission/151-01-SUMMARY.md"
      verification_status: "passed"
      evidence: "Manifest records and render/write APIs exist and are tested, but no production runner, CLI, script, or gate path calls dependency_manifest::render() or dependency_manifest::write()."
    - id: "MANIFEST-02"
      status: "partial"
      phase: "151"
      claimed_by_plans:
        - ".planning/milestones/v1.18-phases/151-parity-dependency-manifest-emission/151-01-PLAN.md"
      completed_by_plans:
        - ".planning/milestones/v1.18-phases/151-parity-dependency-manifest-emission/151-01-SUMMARY.md"
      verification_status: "passed"
      evidence: "requires_full_gate(...) returns true for missing, stale, or uncertain manifest data, but that conservative freshness decision is not consumed by any production gate or runner path."
  integration:
    - id: "runner-boundary-cli-config"
      affects: ["PARITY-01"]
      status: "partial"
      evidence: "tools/paritychecker/parity_main.cpp owns parse_args(...) and print_usage(...); tools/paritychecker/CMakeLists.txt separates PARITYCHECKER_CLI_SOURCES from PARITYCHECKER_RUNNER_SOURCES."
    - id: "generation-reference-truth"
      affects: ["PARITY-03", "LANE-01"]
      status: "partial"
      evidence: "tools/paritychecker/parity_engines.cpp loads baseline_record and compares against baseline_record.result on the normal success path."
    - id: "actor-helper-boundary"
      affects: ["LANE-02"]
      status: "partial"
      evidence: "tools/paritychecker/parity_engines.cpp still reaches into detail/action internals outside the selected patterns guarded by paritychecker_tests.cpp."
    - id: "manifest-emission-consumer"
      affects: ["MANIFEST-01", "MANIFEST-02"]
      status: "partial"
      evidence: "dependency_manifest::render(), write(), and requires_full_gate(...) are used by tests/docs, not by the paritychecker CLI, runner, or quality gate."
  flows:
    - id: "generation-live-reference-flow"
      affects: ["PARITY-03", "LANE-01"]
      status: "partial"
      evidence: "The live reference backend is loaded and has run_reference_generate(...), but normal comparison uses a stored snapshot reference_result."
    - id: "manifest-to-gate-flow"
      affects: ["MANIFEST-01", "MANIFEST-02"]
      status: "partial"
      evidence: "Manifest production and full-gate freshness semantics stop at the library/test boundary."
  phase_artifacts: []
tech_debt:
  - phase: "150"
    items:
      - "Engine registration is localized, but the engine implementation remains a single large parity_engines.cpp bundle."
nyquist:
  compliant_phases:
    - 149
    - 150
  partial_phases:
    - 148
    - 151
    - 152
  invalid_phases: []
  missing_phases: []
  overall: partial
notes:
  - "Roadmap, STATE, REQUIREMENTS, SUMMARY, VERIFICATION, and VALIDATION artifacts consistently mark v1.18 complete; live source-backed integration checks override that artifact agreement."
  - "Focused paritychecker build/tests and CLI smoke flows passed during the integration check."
---

# v1.18 Milestone Audit

## Verdict

Gaps found. `v1.18 Parity Tool Boundary Refactor` has complete phase artifacts and passing focused
tests, but live source inspection contradicts several milestone-closeout claims. Artifact agreement
is not sufficient for this milestone because the open gaps are exactly in maintained path wiring,
lane isolation proof, and manifest consumption.

## Scope

In-scope phases:

| Phase | Name | Roadmap | Artifacts | Nyquist |
|-------|------|---------|-----------|---------|
| 148 | Parity Runner Asset Boundary | Complete | SUMMARY, VALIDATION, VERIFICATION present | Partial |
| 149 | Parity Engine Adapter Split | Complete | SUMMARY, VALIDATION, VERIFICATION present | Compliant |
| 150 | Parity Build Registration Boundary | Complete | SUMMARY, VALIDATION, VERIFICATION present | Compliant |
| 151 | Parity Dependency Manifest Emission | Complete | SUMMARY, VALIDATION, VERIFICATION present | Partial |
| 152 | Parity Behavior And Lane-Isolation Closure | Complete | SUMMARY, VALIDATION, VERIFICATION present | Partial |

`.planning/STATE.md` says no active milestone is planned and `v1.18` was shipped and archived.
This audit targets `v1.18` because no version argument was provided and the active branch is
`gsd/v1.18-parity-tool-boundary-refactor`.

## Requirements

| Requirement | Phase | REQUIREMENTS.md | SUMMARY frontmatter | VERIFICATION | Source-backed status |
|-------------|-------|-----------------|---------------------|--------------|----------------------|
| PARITY-01 | 148 | Complete | Listed | Complete | Partial |
| PARITY-02 | 149 | Complete | Listed | Complete | Satisfied |
| PARITY-03 | 152 | Complete | Listed | Passed | Partial |
| ENGINE-01 | 149 | Complete | Listed | Complete | Satisfied |
| ENGINE-02 | 150 | Complete | Listed | Complete | Satisfied |
| LANE-01 | 148 | Complete | Listed | Complete | Partial |
| LANE-02 | 152 | Complete | Listed | Passed | Partial |
| BUILD-01 | 150 | Complete | Listed | Complete | Satisfied |
| BUILD-02 | 150 | Complete | Listed | Complete | Satisfied |
| MANIFEST-01 | 151 | Complete | Listed | Passed | Partial |
| MANIFEST-02 | 151 | Complete | Listed | Passed | Partial |
| MANIFEST-03 | 151 | Complete | Listed | Passed | Satisfied |

No orphaned requirements were found. The final score is `6/12` satisfied after applying the
source-contradiction override.

## Source-Backed Checks

| Claim | Live Source Evidence | Result |
|-------|----------------------|--------|
| CLI reaches the runner and adapters | `parity_main.cpp` calls `run_parity(opts)`, `parity_runner.cpp` calls `find_engine(opts.mode)`, and `parity_engine.cpp` returns `nullptr` for unknown modes. | Passed |
| Shared runner owns CLI/config parsing | `parse_args(...)` and `print_usage(...)` remain in `parity_main.cpp`; CMake keeps CLI and runner sources separate. | Partial |
| Runner no longer owns bulk mode implementation | `parity_runner.cpp` only resolves an engine and invokes `engine->run(opts)`. | Passed |
| Explicit engine registration is fail-closed | `parity_engine.cpp` maps each registered `parity_mode` to one adapter and returns `nullptr` by default. | Passed |
| Build registration is modular | `tools/paritychecker/CMakeLists.txt` defines CLI, runner, manifest, engine-registration, engine-implementation, tokenizer-engine, reference-support, and common source groups. | Passed |
| Generation fixture identity is source-backed | `parity_assets.cpp` normalizes model paths against `tools/generation_fixture_registry.hpp`. | Passed |
| Generation parity proof is live-reference-backed | The normal success path loads a stored baseline with `load_generation_baseline_file(...)` and compares to `baseline_record.result`; `run_reference_generate(...)` is not used for the normal comparison. | Partial |
| Shared runner files stay free of lane runtime objects | Source scan found no llama/ggml/runtime objects in `parity_runner.cpp`, `parity_engine.cpp`, `parity_assets.cpp`, or `parity_dependency_manifest.cpp`. | Passed |
| Parity harnesses do not reach actor internals directly | Tests forbid selected text generator/detokenizer/Jinja patterns, but `parity_engines.cpp` still includes or calls several detail/action internals. | Partial |
| Dependency manifest is deterministic | `dependency_manifest::records()`, `records_for(...)`, `render()`, and `write()` exist and are covered by doctests. | Passed |
| Dependency manifest is emitted by a production path | `render()` and `write()` are only used by tests/docs; no CLI, runner, script, or gate path emits the manifest. | Partial |
| Missing/stale/uncertain manifest data triggers a production full gate | `requires_full_gate(...)` exists and is tested, but no quality gate or runner consumer uses it. | Partial |

## Integration

| Flow | Evidence | Result |
|------|----------|--------|
| CLI to runner to engine | `parity_main.cpp` -> `run_parity(...)` -> `find_engine(...)` -> per-mode adapter. | Passed |
| Runner asset boundary to generation fixtures | Generation uses `parity_assets::file_exists`, `read_file_bytes`, and `find_generation_fixture`. | Passed |
| Engine adapter split to build registration | `paritychecker` and `paritychecker_tests` both consume `${PARITYCHECKER_COMMON_SOURCES}`. | Passed |
| Generation parity to live reference truth | Live reference backend is loaded, but comparison truth comes from the stored baseline record. | Partial |
| Manifest to future gate input | Manifest data and docs exist, but there is no production emission or gate consumer. | Partial |

## Validation Evidence

The integration checker reran focused verification:

```sh
cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j2
ctest --test-dir build/paritychecker_zig --output-on-failure
```

Result: passed.

CLI smoke flows also passed:

| Flow | Result |
|------|--------|
| no args | usage exit `2` |
| `--kernel` | `kernel parity ok` |
| `--gbnf --text 'root ::= "a"'` | `parity ok` |
| missing generation model | deterministic missing-model failure |

Phase VALIDATION files also recorded `git diff --check`, focused paritychecker builds/tests, and
changed-file scoped quality gates. Those checks are useful evidence, but they do not cover the
source-backed gaps above.

## Deferred Items

No unrelated deferred items in `.planning/STATE.md` are counted against `v1.18`. The only v1.18
tech debt identified here is that engine registration is localized while mode implementation is
still concentrated in the large `parity_engines.cpp` bundle.
