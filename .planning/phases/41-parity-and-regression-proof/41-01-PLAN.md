---
phase: 41-parity-and-regression-proof
plan: 01
type: execute
wave: 1
depends_on:
  - 40-01
files_modified:
  - .planning/phases/41-parity-and-regression-proof/41-CONTEXT.md
  - .planning/phases/41-parity-and-regression-proof/41-01-PLAN.md
  - tools/generation_fixture_registry.hpp
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/paritychecker_tests.cpp
  - snapshots/parity/*
autonomous: true
requirements:
  - PAR-03
  - VER-03
---

# Phase 41 Plan 01

## Objective

Make the maintained Bonsai slice auditable on the parity surface by proving it against the pinned
Prism reference lane and widening maintained regression coverage only as far as that proof contract
can be stated honestly.

## Tasks

1. Resolve the proof contract explicitly:
   wire maintained generation proof to the per-model reference tuple
   `(fixture, engine, repository, commit?)`, compare Bonsai against the pinned Prism live result,
   and keep Qwen/Liquid on their existing append-only regression lane instead of forcing one
   global live-reference repo/ref pair.
2. Promote `Bonsai-1.7B.gguf` into the maintained supported generation fixture set only after the
   proof contract is explicit and truthful.
3. Add generation regression coverage that includes Bonsai and still protects the prior maintained
   Qwen and Liquid anchors, including fixture-path restrictions and formatter/reference identity
   publication.
4. If the phase uses maintained baseline files for Bonsai, generate the new append-only parity
   baselines only with explicit user approval before landing them.

## Verification

- `cmake --build build/paritychecker_zig --parallel --target paritychecker paritychecker_tests`
- `./build/paritychecker_zig/paritychecker_tests --test-case='*maintained generation*' --no-breaks`
- `./build/paritychecker_zig/paritychecker --generation --model tests/models/Bonsai-1.7B.gguf --text hello --max-tokens 1`
- `./build/paritychecker_zig/paritychecker --generation --model tests/models/Bonsai-1.7B.gguf --text hello --max-tokens 10`
- `./scripts/quality_gates.sh`

## Done Condition

The maintained Bonsai slice is part of the auditable maintained generation parity surface, the
published proof names the exact pinned Prism reference ref and formatter contract, and regression
coverage protects Bonsai plus the prior maintained anchors without any hidden snapshot or
reference-contract behavior.
