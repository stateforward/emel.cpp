---
phase: 54-omniembed-execution-contract-runtime-cutover
plan: 01
type: execute
wave: 1
depends_on:
  - 53-te-proof-and-regression
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/54-omniembed-execution-contract-runtime-cutover/54-CONTEXT.md
  - .planning/phases/54-omniembed-execution-contract-runtime-cutover/54-01-PLAN.md
  - .planning/phases/54-omniembed-execution-contract-runtime-cutover/54-01-SUMMARY.md
  - .planning/phases/54-omniembed-execution-contract-runtime-cutover/54-VERIFICATION.md
  - src/emel/embeddings/generator/context.hpp
  - src/emel/embeddings/generator/detail.hpp
  - src/emel/embeddings/generator/sm.hpp
  - tests/embeddings/shared_embedding_session_tests.cpp
autonomous: true
requirements:
  - MOD-02
  - EMB-01
---

# Phase 54 Plan 01

## Objective

Make the live TE embedding generator consume the explicit Phase 48 `omniembed` execution contract
as its runtime seam, and reject broken multimodal contract drift before initialization succeeds.

## Tasks

1. Add a failing regression test that proves the embedding generator currently accepts an
   `omniembed` fixture with a broken required modality contract.
2. Thread a validated `omniembed` execution contract into generator-owned runtime setup and use it
   during text/image/audio runtime binding instead of bypassing the Phase 48 seam entirely.
3. Re-run focused embedding tests plus full quality gates to confirm the maintained trimodal slice
   still works and the broken-contract case now fails before request execution.

## Verification

- `./build/zig/emel_tests_bin --no-breaks --source-file='*tests/embeddings/*'`
- `./build/zig/emel_tests_bin --no-breaks --source-file='*tests/model/*,*tests/embeddings/*'`
- `scripts/quality_gates.sh`

## Done Condition

The embedding generator refuses a broken `omniembed` multimodal contract during setup, the live
runtime binds through the validated Phase 48 contract seam, and the maintained text/image/audio TE
slice remains green under the normal repo gates.
