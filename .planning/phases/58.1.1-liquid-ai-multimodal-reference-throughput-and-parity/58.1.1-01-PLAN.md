---
phase: 58.1.1-liquid-ai-multimodal-reference-throughput-and-parity
plan: 01
type: execute
wave: 1
depends_on:
  - 58.1-arm-embedding-generator-throughput-optimization
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/58.1.1-liquid-ai-multimodal-reference-throughput-and-parity/58.1.1-CONTEXT.md
  - .planning/phases/58.1.1-liquid-ai-multimodal-reference-throughput-and-parity/58.1.1-01-PLAN.md
  - .planning/phases/58.1.1-liquid-ai-multimodal-reference-throughput-and-parity/58.1.1-01-SUMMARY.md
  - .planning/phases/58.1.1-liquid-ai-multimodal-reference-throughput-and-parity/58.1.1-VERIFICATION.md
  - scripts/bench_embedding_reference_liquid.sh
  - tools/bench/CMakeLists.txt
  - tools/bench/embedding_reference_bench.cpp
  - tools/bench/embedding_reference_bench_main.cpp
autonomous: true
requirements: []
must_haves:
  truths:
    - The Liquid reference lane is built and measured through `libmtmd` rather than a Python or
      CLI wrapper.
    - The reference runner reports deterministic output anchors alongside stage timings.
  artifacts:
    - scripts/bench_embedding_reference_liquid.sh
    - tools/bench/embedding_reference_bench.cpp
    - .planning/phases/58.1.1-liquid-ai-multimodal-reference-throughput-and-parity/58.1.1-VERIFICATION.md
  key_links:
    - The Liquid lane must remain separate from the EMEL embedding-generator lane.
    - The reference build must pin repository and commit in maintained repo state.
rule_constraints:
  - source: AGENTS.md
    requirement: "Benchmark and parity harnesses must keep EMEL and reference lanes clearly separated and must not let the EMEL side depend on llama.cpp objects"
    tasks: ["1", "2", "3"]
  - source: AGENTS.md
    requirement: "Reference work belongs in tools/bench or tools/paritychecker only; the EMEL runtime in src/ must remain independently owned"
    tasks: ["1", "2", "3"]
  - source: docs/rules/sml.rules.md
    requirement: "Do not add hidden runtime behavior choice to the embedding generator while adding reference infrastructure"
    tasks: ["2"]
---

<objective>
Add a maintained Liquid multimodal reference lane that can measure image/audio tower throughput and
publish deterministic parity anchors for comparison against EMEL's current ARM embedding stage data.

Purpose: Replace weak/non-multimodal reference guesses with a real C/C++ multimodal benchmark lane.
Output: New reference bench target, dedicated Liquid build workflow, and phase closeout evidence.
</objective>

<execution_context>
@/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/58.1.1-liquid-ai-multimodal-reference-throughput-and-parity/58.1.1-CONTEXT.md
@tools/bench/CMakeLists.txt
@tools/bench/embedding_generator_bench.cpp
@build/bench/_deps/reference_impl-src/tools/mtmd/mtmd.h
@build/bench/_deps/reference_impl-src/tools/mtmd/mtmd-helper.h
</context>

<rule_constraints>
- `AGENTS.md`: Keep the EMEL lane and the Liquid reference lane explicitly separated, enforced by
  Tasks 1 and 2.
- `AGENTS.md`: Keep reference implementation code confined to `tools/bench`, enforced by Tasks 1
  and 2.
- `docs/rules/sml.rules.md`: Do not touch generator control flow while adding reference
  infrastructure, enforced by Task 2.
</rule_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated Liquid multimodal reference runner</name>
  <files>tools/bench/CMakeLists.txt, tools/bench/embedding_reference_bench.cpp, tools/bench/embedding_reference_bench_main.cpp</files>
  <action>Add a standalone benchmark target that links the fetched reference repo's `mtmd` library
  and measures image/audio `prepare`, `encode`, and `publish` timing plus deterministic output
  metadata for a compatible multimodal model.</action>
  <verify>cmake --build build/bench_tools_liquid_ninja --target embedding_reference_bench_runner -j4</verify>
  <done>The new target builds cleanly and keeps the reference lane separate from the EMEL runner.</done>
</task>

<task type="auto">
  <name>Task 2: Capture a maintained Liquid build workflow</name>
  <files>scripts/bench_embedding_reference_liquid.sh</files>
  <action>Add a maintained Unix script that configures a dedicated benchmark build directory pinned
  to `Liquid4All/benchmarks-llama.cpp` and the chosen reference commit, then builds the new
  reference runner.</action>
  <verify>scripts/bench_embedding_reference_liquid.sh --build-only</verify>
  <done>The Liquid reference lane can be configured and rebuilt without ad hoc local CMake flags.</done>
</task>

<task type="auto">
  <name>Task 3: Verify skip-safe execution and reference metadata output</name>
  <files>tools/bench/embedding_reference_bench.cpp, tools/bench/embedding_reference_bench_main.cpp</files>
  <action>Run the new reference runner in the current environment, verify it prints the Liquid repo
  and commit metadata, and verify it skips gracefully when compatible multimodal reference assets
  are not configured locally.</action>
  <verify>./build/bench_tools_liquid_ninja/embedding_reference_bench_runner</verify>
  <done>The runner exits cleanly with explicit metadata and skip guidance instead of failing
  opaquely when model/mmproj assets are absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local bench workflow → fetched Liquid reference repo | External reference code enters the benchmark build here |
| reference runner → local model/mmproj paths | Environment-provided multimodal model assets cross into the reference lane here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.1.1-01 | T | reference build selection | mitigate | Pin the Liquid repo and commit in the maintained build script and print them in runner metadata |
| T-58.1.1-02 | D | reference runner asset loading | mitigate | Validate model/mmproj env vars up front and skip cleanly when assets are missing |
</threat_model>

<verification>
- `scripts/bench_embedding_reference_liquid.sh --build-only`
- `cmake --build build/bench_tools_liquid_ninja --target embedding_reference_bench_runner -j4`
- `./build/bench_tools_liquid_ninja/embedding_reference_bench_runner`
</verification>

<success_criteria>
- A maintained Liquid multimodal reference runner builds in its own configured benchmark lane.
- The runner exposes stage timings and deterministic output anchors for image/audio tower work.
- The repo contains a maintained workflow for rebuilding that Liquid lane.
</success_criteria>

<output>
After completion, create `.planning/phases/58.1.1-liquid-ai-multimodal-reference-throughput-and-parity/58.1.1-01-SUMMARY.md`
</output>
