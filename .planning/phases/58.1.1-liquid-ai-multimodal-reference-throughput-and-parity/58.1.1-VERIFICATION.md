---
phase: 58.1.1-liquid-ai-multimodal-reference-throughput-and-parity
status: passed_with_existing_quality_gate_failure
completed: 2026-04-15
---

# Phase 58.1.1 Verification

## Focused Verification

1. `scripts/bench_embedding_reference_liquid.sh --build-only`
   Result: passed. The maintained script configured and built the dedicated Liquid reference lane in
   `build/bench_tools_liquid_ninja`.
2. `cmake --build build/bench_tools_liquid_ninja --target embedding_reference_bench_runner -j4`
   Result: passed. The standalone reference runner target rebuilt cleanly after the initial Liquid
   API adjustments.
3. `./build/bench_tools_liquid_ninja/embedding_reference_bench_runner`
   Result: passed. The runner exited cleanly and printed the intended metadata plus skip guidance:
   - repository: `https://github.com/Liquid4All/benchmarks-llama.cpp.git`
   - ref: `0f345f3a1924ef274678a65bdec4b17f14e8fe9a`
   - source: `cmake_fetch_latest`
   - assets: `status=missing model_exists=false mmproj_exists=false`
   - guidance: set `EMEL_REFERENCE_MM_MODEL` and `EMEL_REFERENCE_MM_MMPROJ`

## Quality Gates

4. `scripts/quality_gates.sh`
   Result: failed in the existing embedding/runtime test suite, not in the new Liquid reference
   tooling.

   Failing area:
   - `emel_tests_generator_and_runtime`

   Reported failures:
   - `tests/embeddings/shared_embedding_session_tests.cpp:40`
     - truncation-surface checks produced `nan` norms instead of `~1.0`
   - `tests/embeddings/te_proof_and_regression_tests.cpp:176`
     - audio golden cosine similarity evaluated to `nan`
   - `tests/embeddings/te_proof_and_regression_tests.cpp:194`
     - cross-modal smoke relations involving audio evaluated to `nan`

## Evidence

- `tools/bench/CMakeLists.txt` now exposes `embedding_reference_bench_runner` behind the fetched
  reference `mtmd` target.
- `tools/bench/embedding_reference_bench.cpp` drives the Liquid multimodal lane directly through
  `mtmd_tokenize`, `mtmd_encode_chunk`, and `mtmd_get_output_embd`.
- `tools/bench/embedding_reference_bench_main.cpp` prints repository/ref metadata and output anchor
  fields in the maintained runner output.
- `scripts/bench_embedding_reference_liquid.sh` provides the maintained pinned build entrypoint for
  the new lane.

## Residual Blocker

| Area | Status | Details |
|------|--------|---------|
| Reference throughput measurement | OPEN | The maintained runner is ready, but the current environment still lacks a compatible Liquid multimodal model/projector pair, so no Liquid image/audio throughput numbers were produced yet. |
| Repo-wide quality gates | OPEN | `scripts/quality_gates.sh` currently fails in existing embedding/runtime tests due `nan` audio-related outputs unrelated to the new `tools/bench` reference lane. |
