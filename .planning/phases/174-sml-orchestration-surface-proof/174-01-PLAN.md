---
phase: 174-sml-orchestration-surface-proof
plan: 01
type: execute
wave: 1
depends_on:
  - 173-sml-migration-evidence-reconstruction
files_modified:
  - CMakeLists.txt
  - tests/sm/sm_policy_tests.cpp
  - .planning/phases/174-sml-orchestration-surface-proof/174-01-SUMMARY.md
  - .planning/phases/174-sml-orchestration-surface-proof/174-VERIFICATION.md
  - .planning/phases/174-sml-orchestration-surface-proof/174-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SRC-03
rule_constraints:
  - Use stateforward.SML for orchestration proof.
  - Keep new transition tables destination-first.
  - Use SML introspection for machine assertions.
---

# Phase 174 Plan 01

## Objective

Add active test coverage for the SML surfaces named by SRC-03: dispatch tables, logger wiring,
unexpected-event handling, and state inspection.

## Tasks

1. Add focused SML surface doctests to the existing `tests/sm/sm_policy_tests.cpp` shard file.
2. Cover `stateforward::sml::utility::make_dispatch_table` routing with runtime event IDs and state
   inspection.
3. Cover `stateforward::sml::logger<...>` callbacks during dispatch.
4. Add the new test file to `EMEL_TEST_SOURCES` and verify the `sm` shard.

## Verification

- `EMEL_ZIG_TEST_SHARDS=sm scripts/build_with_zig.sh`
- `ctest --test-dir build/zig -R '^emel_tests_sm$' --output-on-failure`

## Done Condition

SRC-03 has live code/test evidence instead of docs-only logger and dispatch-table proof.
