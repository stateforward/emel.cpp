---
phase: 47-te-truth-anchor
plan: 01
type: execute
wave: 1
depends_on:
  - 46.1-rename-planner-family-wrapper-names-to-remove-ambiguity-and-
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/47-te-truth-anchor/47-CONTEXT.md
  - .planning/phases/47-te-truth-anchor/47-01-PLAN.md
  - .planning/phases/47-te-truth-anchor/47-01-SUMMARY.md
  - .planning/phases/47-te-truth-anchor/47-VERIFICATION.md
  - CMakeLists.txt
  - tests/model/fixture_manifest_tests.cpp
  - tests/models/README.md
  - tests/models/TE-75M-q8_0.gguf
  - tests/embeddings/fixtures/te75m/README.md
  - tests/embeddings/fixtures/te75m/red-square.txt
  - tests/embeddings/fixtures/te75m/pure-tone-440hz.txt
autonomous: true
requirements:
  - FIX-01
  - FIX-02
---

# Phase 47 Plan 01

## Objective

Lock the maintained TE fixture and deterministic proof-input truth surface before `omniembed`
runtime work starts.

## Tasks

1. Record the maintained `TE-75M-q8_0.gguf` fixture under `tests/models/` with exact upstream
   provenance, local checksum, stable path, and an explicit non-maintained sibling note.
2. Define a tiny deterministic TE proof corpus as in-memory payload contracts with one text-image
   anchor and one text-audio anchor.
3. Add a small regression test that keeps the fixture metadata and proof corpus from drifting, then
   run focused model tests and the full quality gates before closing the phase.

## Verification

- `shasum -a 256 tests/models/TE-75M-q8_0.gguf`
- `cmake --build build/zig --target emel_tests_bin -j4`
- `./build/zig/emel_tests_bin --no-breaks --source-file='*tests/model/*'`
- `scripts/quality_gates.sh`

## Done Condition

The repo exposes one exact maintained TE fixture and one exact deterministic proof corpus with
regression coverage, while still making no runtime or family-wide support claim beyond the locked
Phase 47 truth anchor.
