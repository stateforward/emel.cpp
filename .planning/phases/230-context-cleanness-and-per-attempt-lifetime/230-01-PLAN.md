---
phase: 230-context-cleanness-and-per-attempt-lifetime
plan: "01"
type: execute
wave: 1
depends_on:
  - 229-staged-copy-progress-and-completion-semantics
files_modified:
  - tests/io/staged_read/lifecycle_tests.cpp
  - .planning/phases/230-context-cleanness-and-per-attempt-lifetime/230-CONTEXT.md
  - .planning/phases/230-context-cleanness-and-per-attempt-lifetime/230-01-PLAN.md
  - .planning/phases/230-context-cleanness-and-per-attempt-lifetime/230-01-SUMMARY.md
  - .planning/phases/230-context-cleanness-and-per-attempt-lifetime/230-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - STG-07
  - LIFE-02
  - SNR-01
rule_constraints:
  - RC-SML-RTC-NO-QUEUE
  - RC-SML-GUARDS-OWN-RUNTIME-CHOICE
  - RC-SML-ACTIONS-BOUNDED-NONBLOCKING
  - RC-SML-NO-DISPATCH-LOCAL-CONTEXT
must_haves:
  truths:
    - "staged_read::context remains empty and stores no request/event/status mirrors."
    - "Per-attempt staged payload and callback wiring is observable through process_event and same-RTC callback payload identity."
    - "staged done semantics remain caller-owned copy output only; no tensor residency ownership claims."
    - "Required validation commands and changed-file gate evidence are recorded."
---

<objective>
Close Phase 230 by proving context cleanness and per-attempt lifetime semantics with source-backed tests/scans and updated planning truth.
</objective>

<tasks>
1. Add public-surface doctest/static assertions for STG-07/LIFE-02/SNR-01 in `tests/io/staged_read/lifecycle_tests.cpp`.
2. Capture source scan evidence for empty context and stack-local per-attempt runtime carriers.
3. Run required validation commands: `ninja -C build emel_tests_bin`, `ctest --test-dir build --output-on-failure -R emel_tests_io`, and changed-file scoped `scripts/quality_gates.sh`.
4. Restore `snapshots/quality_gates/timing.txt` after quality-gate execution.
5. Publish Phase 230 summary/verification and update `ROADMAP`, `REQUIREMENTS`, and `STATE`.
</tasks>
