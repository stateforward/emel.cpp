---
phase: 31-generator-prefill-submachine-extraction
plan: 02
type: execute
wave: 1
depends_on:
  - 31-01
files_modified:
  - src/emel/generator/actions.hpp
  - src/emel/generator/guards.hpp
  - src/emel/generator/sm.hpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - PREF-01
  - PREF-02
---

# Phase 31 Plan 02

## Objective

Integrate the parent generator with the new child machine and remove the old inlined prefill
orchestration from the parent table.

## Tasks

1. Replace the parent prefill state family with one prefill dispatch boundary and shared result
   decision.
2. Route child-machine outcomes back into decode/error handling without behavior drift.
3. Remove parent-only prefill actions/guards that are now child-owned.
4. Refresh generator topology tests for the new parent/child boundary.

## Verification

- `./scripts/build_with_zig.sh`
- `build/zig/emel_tests_bin --test-case='*generator_sm_*'`
- `build/zig/emel_tests_bin --test-case='*generator_generate_runs_native_generator_contract*'`

## Done Condition

The parent generator delegates prefill through the child machine and no longer carries the old
prefill orchestration states directly.
