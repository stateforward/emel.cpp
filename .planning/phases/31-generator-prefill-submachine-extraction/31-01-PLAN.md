---
phase: 31-generator-prefill-submachine-extraction
plan: 01
type: execute
wave: 1
depends_on:
  - 30-generator-prefill-contract-collapse
files_modified:
  - src/emel/generator/context.hpp
  - src/emel/generator/prefill/context.hpp
  - src/emel/generator/prefill/detail.hpp
  - src/emel/generator/prefill/guards.hpp
  - src/emel/generator/prefill/actions.hpp
  - src/emel/generator/prefill/sm.hpp
  - tests/generator/prefill/lifecycle_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements:
  - PREF-01
  - PREF-02
---

# Phase 31 Plan 01

## Objective

Create the generator-owned prefill machine boundary, internal event, and child context seam.

## Tasks

1. Add the new `generator/prefill` component with its own explicit state table.
2. Define the internal prefill run event and child context wiring to the parent generator context.
3. Move prefill-specific request/guard logic into the child machine.
4. Add focused prefill-machine topology and flow tests.

## Verification

- `./scripts/build_with_zig.sh`
- `build/zig/emel_tests_bin --test-case='*generator_prefill*'`

## Done Condition

`src/emel/generator/prefill` exists as a real machine with typed internal event flow and focused
tests, without widening the public generator API.
