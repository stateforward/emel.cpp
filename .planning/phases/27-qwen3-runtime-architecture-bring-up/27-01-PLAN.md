---
phase: 27-qwen3-runtime-architecture-bring-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emel/model/llama/detail.hpp
  - src/emel/model/data.cpp
  - tests/model/loader/lifecycle_tests.cpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - RUN-02
must_haves:
  truths:
    - The maintained execution-view contract binds the canonical `qwen3` tensor topology explicitly instead of treating it as a renamed Llama slice.
    - Required canonical Qwen3 tensors include `blk.%d.attn_q_norm.weight` and `blk.%d.attn_k_norm.weight`, and missing either tensor fails explicitly with `model_invalid`.
    - Quantized-path auditing publishes the canonical Qwen3 Q/K norm stages explicitly without regressing the prior Llama slice.
  artifacts:
    - path: src/emel/model/llama/detail.hpp
      provides: explicit block-view contract for canonical Qwen3 Q/K norm tensors
      contains: attention_q_norm
    - path: src/emel/model/data.cpp
      provides: architecture-aware execution-view binding and audit accounting for `qwen3`
      contains: qwen3
    - path: tests/model/loader/lifecycle_tests.cpp
      provides: failing-test-first execution-view coverage for canonical Qwen3 topology
      contains: qwen3 execution view
    - path: tests/generator/lifecycle_tests.cpp
      provides: audit coverage for explicit Qwen3 norm-stage accounting
      contains: qwen3 quantized path audit
  key_links:
    - from: src/emel/model/data.cpp
      to: src/emel/model/llama/detail.hpp
      via: architecture-aware execution-view binding and block lookup
      pattern: attn_q_norm
    - from: tests/model/loader/lifecycle_tests.cpp
      to: src/emel/model/data.cpp
      via: explicit canonical-Qwen execution-view construction proof
      pattern: qwen3
---

<objective>
Extend model and execution-view support for the canonical Qwen3 slice.

Purpose: make the maintained execution-view and audit helpers explicitly understand the canonical
Qwen3 tensor topology before the generator runtime consumes it.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-qwen3-runtime-architecture-bring-up/27-CONTEXT.md
@.planning/phases/27-qwen3-runtime-architecture-bring-up/27-RESEARCH.md
@.planning/phases/27-qwen3-runtime-architecture-bring-up/27-VALIDATION.md
@src/emel/model/llama/detail.hpp
@src/emel/model/data.cpp
@tests/model/loader/lifecycle_tests.cpp
@tests/generator/lifecycle_tests.cpp

<interfaces>
Current Llama-only execution-view gate from `src/emel/model/data.cpp`:

```cpp
if (architecture_name_view(model_data) != k_llama_architecture ||
    model_data.n_tensors == 0u ||
    model_data.n_layers <= 0 ||
    model_data.params.n_embd <= 0 ||
    model_data.params.n_ctx <= 0 ||
    model_data.weights_data == nullptr ||
    model_data.weights_size == 0u) {
  return emel::error::cast(emel::model::loader::error::model_invalid);
}
```

Current canonical Qwen3 fixture facts from `27-RESEARCH.md`:

```text
general.architecture=qwen3
required block tensors:
  blk.%d.attn_q_norm.weight
  blk.%d.attn_k_norm.weight
required hparam keys:
  qwen3.block_count
  qwen3.context_length
  qwen3.embedding_length
  qwen3.feed_forward_length
  qwen3.attention.head_count
  qwen3.attention.head_count_kv
  qwen3.attention.key_length
  qwen3.attention.value_length
  qwen3.attention.layer_norm_rms_epsilon
  qwen3.rope.freq_base
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce missing Qwen3 execution-view support in tests</name>
  <files>tests/model/loader/lifecycle_tests.cpp, tests/generator/lifecycle_tests.cpp</files>
  <read_first>tests/model/loader/lifecycle_tests.cpp, tests/generator/lifecycle_tests.cpp, src/emel/model/data.cpp, src/emel/model/llama/detail.hpp, .planning/phases/27-qwen3-runtime-architecture-bring-up/27-RESEARCH.md</read_first>
  <action>Add failing prepared-model coverage that sets `architecture_name` to `qwen3`, adds
  `blk.0.attn_q_norm.weight` and `blk.0.attn_k_norm.weight`, and requires:
  1. canonical Qwen3 execution-view construction succeeds only when both norm tensors are present,
  2. omitting either norm tensor returns `model_invalid`, and
  3. the quantized-path audit publishes explicit `attention_q_norm` and `attention_k_norm` stage
  families as dense-vector-by-contract instead of hiding them under the old Llama inventory.
  Keep the existing Llama execution-view tests intact.</action>
  <verify>
    <automated>cmake --build build/zig --target emel_tests_bin -j4 &amp;&amp; ./build/zig/emel_tests_bin --test-case='*qwen3*execution*,*qwen3*quantized*' --no-breaks</automated>
  </verify>
  <acceptance_criteria>
    - `tests/model/loader/lifecycle_tests.cpp` contains `qwen3 execution view`
    - `tests/generator/lifecycle_tests.cpp` contains `qwen3 quantized path audit`
    - the new Qwen3 execution-view tests fail before model helper changes
    - existing Llama execution-view tests stay intact
  </acceptance_criteria>
  <done>Tests reproduce the missing explicit Qwen3 execution-view support.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit Qwen3 execution-view and audit support</name>
  <files>src/emel/model/llama/detail.hpp, src/emel/model/data.cpp</files>
  <read_first>src/emel/model/llama/detail.hpp, src/emel/model/data.cpp, tests/model/loader/lifecycle_tests.cpp, tests/generator/lifecycle_tests.cpp, .planning/phases/27-qwen3-runtime-architecture-bring-up/27-RESEARCH.md</read_first>
  <action>Extend the maintained execution-view contract for the canonical Qwen3 slice by adding
  explicit block-view fields for `attention_q_norm` and `attention_k_norm`, binding the canonical
  Qwen3 block tensor names `blk.%d.attn_q_norm.weight` and `blk.%d.attn_k_norm.weight`, and
  teaching the execution-view builder to accept `general.architecture=qwen3` only when the
  canonical Qwen3 tensor contract is present. Expand the quantized-path audit inventory to publish
  `attention_q_norm` and `attention_k_norm` as explicit stage families with truthful dense-vector
  contract classification. Preserve the prior Llama execution-view behavior and canonical
  `output.weight` binding.</action>
  <verify>
    <automated>cmake --build build/zig --target emel_tests_bin -j4 &amp;&amp; ./build/zig/emel_tests_bin --test-case='*qwen3*execution*,*qwen3*quantized*' --no-breaks &amp;&amp; rg -n "attention_q_norm|attention_k_norm|qwen3" src/emel/model/llama/detail.hpp src/emel/model/data.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `src/emel/model/llama/detail.hpp` contains `attention_q_norm`
    - `src/emel/model/llama/detail.hpp` contains `attention_k_norm`
    - `src/emel/model/data.cpp` contains `qwen3`
    - canonical `qwen3` execution-view creation succeeds only when the canonical tensor contract is present
    - Qwen3 contract inventory is explicit about additional dense-f32 vector stages
    - Llama execution-view behavior remains unchanged
  </acceptance_criteria>
  <done>The maintained execution-view helpers support the canonical Qwen3 slice explicitly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/zig --target emel_tests_bin -j4`
- [ ] `./build/zig/emel_tests_bin --test-case='*qwen3*execution*,*qwen3*quantized*' --no-breaks`
- [ ] `./build/zig/emel_tests_bin --test-case='*generator_quantized_path_audit*' --no-breaks`
- [ ] `sh -lc 'set +e; scripts/quality_gates.sh > /tmp/phase27_01_quality_gates.txt 2>&1; status=$?; cat /tmp/phase27_01_quality_gates.txt; test $status -ne 0; rg "paritychecker_tests\\.cpp|model_invalid" /tmp/phase27_01_quality_gates.txt'`
- [ ] `rg -n "attention_q_norm|attention_k_norm|qwen3" src/emel/model/llama/detail.hpp src/emel/model/data.cpp`
</verification>

<output>
After completion, create `.planning/phases/27-qwen3-runtime-architecture-bring-up/27-01-SUMMARY.md`
</output>
