---
phase: 27-qwen3-runtime-architecture-bring-up
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/emel/generator/detail.hpp
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/paritychecker_tests.cpp
  - tests/generator/detail_tests.cpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - RUN-01
  - RUN-02
must_haves:
  truths:
    - The shipped generator path can initialize and generate on the canonical Qwen3 fixture through the maintained EMEL runtime.
    - Qwen3 runtime handling explicitly reshapes Q/K by head, applies per-head Q/K RMS normalization, then applies RoPE instead of relying on an implicit Llama path.
    - The maintained paritychecker generation success assertions that currently block Phase 26 validation run green without reverting the Phase 26 formatter contract.
    - The runtime cutover stays narrow to the canonical dense Qwen3-0.6B slice and does not imply broader Qwen-family support.
  artifacts:
    - path: tests/generator/detail_tests.cpp
      provides: fail-first runtime coverage for canonical Qwen3 Q/K norm ordering
      contains: qwen3 generator
    - path: src/emel/generator/detail.hpp
      provides: shipped generator runtime support for canonical Qwen3 Q/K norm semantics
      contains: attention_q_norm
    - path: tools/paritychecker/parity_runner.cpp
      provides: maintained Qwen generation load path using canonical `qwen3.*` metadata
      contains: qwen3.context_length
    - path: tools/paritychecker/paritychecker_tests.cpp
      provides: maintained subprocess proof that canonical Qwen generation succeeds
      contains: generation parity ok
  key_links:
    - from: src/emel/generator/detail.hpp
      to: src/emel/model/data.cpp
      via: canonical Qwen execution-view consumption
      pattern: attention_q_norm
    - from: tools/paritychecker/parity_runner.cpp
      to: src/emel/generator/detail.hpp
      via: maintained generation load and runtime path
      pattern: run_emel_validate_architecture
---

<objective>
Bring the shipped generator path up on the canonical Qwen3 slice and close the maintained parity smoke gate.

Purpose: carry the explicit Qwen3 execution-view support through the maintained generator runtime
and maintained paritychecker generation path so EMEL can initialize and generate on the canonical
fixture without broad family claims and without leaving the current repo gate broken.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-qwen3-runtime-architecture-bring-up/27-CONTEXT.md
@.planning/phases/27-qwen3-runtime-architecture-bring-up/27-RESEARCH.md
@.planning/phases/27-qwen3-runtime-architecture-bring-up/27-VALIDATION.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-VALIDATION.md
@src/emel/generator/detail.hpp
@src/emel/model/data.cpp
@tools/paritychecker/parity_runner.cpp
@tools/paritychecker/paritychecker_tests.cpp
@tests/generator/detail_tests.cpp
@tests/generator/lifecycle_tests.cpp

<interfaces>
Current maintained tool-loader architecture gate from `tools/paritychecker/parity_runner.cpp`:

```cpp
return emel::model::architecture_name_view(req.model_data) == "llama"
           ? emel::error::cast(emel::model::loader::error::none)
           : emel::error::cast(emel::model::loader::error::model_invalid);
```

Current Qwen runtime blocker from `26-VALIDATION.md`:

```text
paritychecker_tests.cpp:609, 641, 667 already assert:
- exit_code == 0
- "generation parity ok"
- "reference_impl: source=maintained_generation_baseline"
- attribution bucket output
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce canonical Qwen3 runtime failure in generator and maintained parity tests</name>
  <files>tests/generator/detail_tests.cpp, tests/generator/lifecycle_tests.cpp, tools/paritychecker/paritychecker_tests.cpp</files>
  <read_first>tests/generator/detail_tests.cpp, tests/generator/lifecycle_tests.cpp, tools/paritychecker/paritychecker_tests.cpp, src/emel/generator/detail.hpp, tools/paritychecker/parity_runner.cpp, .planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-VALIDATION.md, .planning/phases/27-qwen3-runtime-architecture-bring-up/27-RESEARCH.md</read_first>
  <action>Add fail-first coverage that requires:
  1. prepared-model Qwen3 generator initialization and one-token generation success in doctest,
  2. explicit reshape-by-head plus per-head Q/K RMS normalization after Q/K projection and before RoPE on the maintained runtime path, and
  3. maintained paritychecker subprocess success on `--generation --model tests/models/Qwen3-0.6B-Q8_0.gguf --text hello --max-tokens 1`, `--dump`, and `--attribution`, expecting `exit_code == 0`, `generation parity ok`, `formatter_contract=source=tokenizer.chat_template`, `reference_impl: source=maintained_generation_baseline`, `reference_decode_calls=0`, and `generation_attribution.bucket: name=attention`.
  Keep the prior Llama generator fixture coverage intact.</action>
  <verify>
    <automated>cmake --build build/zig --target emel_tests_bin -j4 &amp;&amp; cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4 &amp;&amp; ./build/zig/emel_tests_bin --test-case='*qwen3*generator*' --no-breaks &amp;&amp; ./build/paritychecker_zig/paritychecker_tests --test-case='*qwen3*generation*' --no-breaks</automated>
  </verify>
  <acceptance_criteria>
    - `tests/generator/detail_tests.cpp` contains `qwen3 generator`
    - `tools/paritychecker/paritychecker_tests.cpp` contains `qwen3 generation`
    - the new Qwen3 generator/parity tests fail before runtime changes
    - the new tests assert reshape-by-head, per-head Q/K RMS normalization, then RoPE ordering explicitly
    - the existing Llama generator fixture still passes
  </acceptance_criteria>
  <done>Generator tests reproduce the missing canonical Qwen3 runtime behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Update the shipped runtime and maintained parity path for the canonical Qwen3 slice</name>
  <files>src/emel/generator/detail.hpp, tools/paritychecker/parity_runner.cpp</files>
  <read_first>src/emel/generator/detail.hpp, src/emel/model/data.cpp, tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp, tests/generator/detail_tests.cpp, tests/generator/lifecycle_tests.cpp, .planning/phases/27-qwen3-runtime-architecture-bring-up/27-RESEARCH.md</read_first>
  <action>Teach the maintained runtime and parity load path to recognize the canonical Qwen3 GGUF keys `qwen3.context_length`, `qwen3.embedding_length`, `qwen3.feed_forward_length`, `qwen3.attention.head_count`, `qwen3.attention.head_count_kv`, `qwen3.attention.key_length`, `qwen3.attention.value_length`, `qwen3.block_count`, `qwen3.attention.layer_norm_rms_epsilon`, and `qwen3.rope.freq_base`. Relax the maintained parity architecture validator from Llama-only to the explicitly supported canonical set. In the shipped generator runtime, after projecting Q/K/V, reshape them by head, apply per-head RMS normalization to Q and K using the explicit `block.attention_q_norm` and `block.attention_k_norm` tensors, then call `apply_rope` on the normalized Q/K views. Keep `output.weight` as the canonical output projection and allow a tied-embedding fallback only when the helper proves that case explicitly. Do not introduce broader Qwen-family behavior or benchmark publication changes in this phase.</action>
  <verify>
    <automated>cmake --build build/zig --target emel_tests_bin -j4 &amp;&amp; cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4 &amp;&amp; ./build/zig/emel_tests_bin --test-case='*qwen3*generator*' --no-breaks &amp;&amp; ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests &amp;&amp; scripts/quality_gates.sh</automated>
  </verify>
  <acceptance_criteria>
    - `src/emel/generator/detail.hpp` contains `attention_q_norm`
    - `tools/paritychecker/parity_runner.cpp` contains `qwen3.context_length`
    - canonical Qwen3 generator initialization succeeds through the shipped runtime path
    - Qwen3 attention uses explicit reshape-by-head, per-head Q/K RMS normalization, then RoPE ordering
    - `ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests` passes
    - `scripts/quality_gates.sh` exits `0`
    - the implementation stays narrow and does not claim broader Qwen-family coverage
  </acceptance_criteria>
  <done>The maintained EMEL generator path runs the canonical Qwen3 slice truthfully.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/zig --target emel_tests_bin -j4`
- [ ] `cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4`
- [ ] `./build/zig/emel_tests_bin --test-case='*qwen3*generator*' --no-breaks`
- [ ] `ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests`
- [ ] `sh -lc 'set +e; ./build/paritychecker_zig/paritychecker --generation --model tests/models/Qwen3-0.6B-Q8_0.gguf --text hello --max-tokens 1 > /tmp/phase27_qwen_generation.txt 2>&1; status=$?; cat /tmp/phase27_qwen_generation.txt; test $status -eq 0; rg "generation parity ok|formatter_contract=|reference_impl: source=maintained_generation_baseline|reference_decode_calls=0" /tmp/phase27_qwen_generation.txt'`
- [ ] `scripts/quality_gates.sh`
</verification>

<output>
After completion, create `.planning/phases/27-qwen3-runtime-architecture-bring-up/27-02-SUMMARY.md`
</output>
