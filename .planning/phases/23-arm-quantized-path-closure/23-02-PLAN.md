---
phase: 23-arm-quantized-path-closure
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - tests/generator/lifecycle_tests.cpp
  - src/emel/generator/sm.hpp
autonomous: true
requirements:
  - PATH-01
must_haves:
  truths:
    - supported canonical ARM requests prove zero remaining disallowed-fallback stages through the shipped runtime wrapper.
    - approved dense-f32-by-contract seams remain visible and are not mislabeled as disallowed fallback.
    - unsupported-stage no-claim behavior remains intact.
  artifacts:
    - focused generator/runtime tests assert the zero-disallowed-fallback contract on the canonical fixture.
    - focused generator/runtime tests preserve unsupported-stage explicit no-claim behavior.
    - phase verification can state whether Phase 23 found any remaining supported closure work or proved the gap was already zero.
---

<objective>
Prove the shipped runtime path preserves the approved Phase 22 contract after closure work.

Purpose: finish `PATH-01` honestly. Output: focused runtime proof shows the canonical supported path
has zero remaining disallowed fallback while approved dense-f32-by-contract seams and unsupported
no-claim behavior stay distinct.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add focused runtime proof for zero remaining disallowed fallback</name>
  <files>tests/generator/lifecycle_tests.cpp, src/emel/generator/sm.hpp</files>
  <read_first>tests/generator/lifecycle_tests.cpp, src/emel/generator/sm.hpp, .planning/phases/22-quantized-path-audit-and-contract/22-VERIFICATION.md</read_first>
  <action>Add focused generator/runtime assertions for the canonical supported fixture and the
  existing unsupported negative fixture so Phase 23 proves zero disallowed fallback without
  collapsing approved dense-f32-by-contract or no-claim cases together.</action>
  <verify>scripts/quality_gates.sh</verify>
  <automated>scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - supported canonical runtime proof reports zero disallowed-fallback stages
    - unsupported-stage proof still reports explicit no-claim rather than a misleading fallback label
    - repo gate remains green after the additive proof work
  </acceptance_criteria>
  <done>Phase 23 closes with runtime proof that the supported canonical path has no remaining disallowed fallback.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build/zig/emel_tests_bin --test-case='*generator*quantized*contract*' --no-breaks`
- [ ] `scripts/quality_gates.sh`
- [ ] no SML transition tables or public C API signatures changed
</verification>

<output>
After completion, create `.planning/phases/23-arm-quantized-path-closure/23-02-SUMMARY.md`
</output>
