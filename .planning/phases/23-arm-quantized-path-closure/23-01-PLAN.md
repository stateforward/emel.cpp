---
phase: 23-arm-quantized-path-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emel/generator/detail.hpp
  - src/emel/generator/sm.hpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - PATH-01
must_haves:
  truths:
    - the shipped generator runtime can report the audited quantized-path contract directly instead of relying on tool-local recomputation alone.
    - supported canonical ARM requests report zero disallowed-fallback audited stages at the runtime wrapper boundary.
    - the closure surface stays additive and does not change actor structure or public APIs.
  artifacts:
    - `src/emel/generator/detail.hpp` stores or derives the Phase 22 audit inside the shipped backend/runtime surface.
    - `src/emel/generator/sm.hpp` exposes additive audited-contract accessors.
    - `tests/generator/lifecycle_tests.cpp` proves the canonical supported runtime reports zero disallowed-fallback stages.
---

<objective>
Close the supported canonical runtime contract at the shipped generator boundary.

Purpose: satisfy the Phase 23 closure requirement without inventing a fake fallback bug. Output:
the shipped runtime wrapper can report that supported canonical ARM requests have zero
disallowed-fallback audited stages while preserving the approved dense-f32-by-contract seams.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Store the audited quantized-path contract on the shipped backend/runtime surface</name>
  <files>src/emel/generator/detail.hpp, src/emel/generator/sm.hpp</files>
  <read_first>src/emel/generator/detail.hpp, src/emel/generator/sm.hpp, src/emel/model/llama/detail.hpp, src/emel/model/data.cpp</read_first>
  <action>Thread the Phase 22 quantized-path audit into the initialized generator backend and add
  additive runtime accessors for the audited stage counts or equivalent zero-gap contract queries.
  Keep the work read-only with respect to model/runtime behavior.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*generator*quantized*contract*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*generator*quantized*contract*' --no-breaks</automated>
  <acceptance_criteria>
    - the shipped generator wrapper can report audited native/approved/disallowed/no-claim counts
    - supported canonical runtime reports zero disallowed-fallback stages
    - no SML transition table or public C API signature changes
  </acceptance_criteria>
  <done>The shipped runtime wrapper exposes the audited Phase 22 contract directly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build/zig/emel_tests_bin --test-case='*generator*quantized*contract*' --no-breaks`
- [ ] `rg 'quantized.*contract|disallowed_fallback|explicit_no_claim' src/emel/generator/detail.hpp src/emel/generator/sm.hpp tests/generator/lifecycle_tests.cpp`
- [ ] no SML transition tables or public C API signatures changed
</verification>

<output>
After completion, create `.planning/phases/23-arm-quantized-path-closure/23-01-SUMMARY.md`
</output>
