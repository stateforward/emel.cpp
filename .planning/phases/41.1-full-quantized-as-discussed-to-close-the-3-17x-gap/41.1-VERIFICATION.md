---
phase: 41.1-full-quantized-as-discussed-to-close-the-3-17x-gap
verified: 2026-04-03T04:24:56Z
status: passed
score: 3/3 phase truths verified
---

# Phase 41.1 Verification Report

**Phase Goal:** Close the remaining maintained Bonsai full-quantized runtime gap before benchmark
publication while keeping parity and the full quality gate green.
**Verified:** 2026-04-03T04:24:56Z
**Status:** passed

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The shipped `Q1_0_g128` hot path now uses a native EMEL-owned AArch64 optimized route instead of the previous scalar-only row dot product on supported hosts. | ✓ VERIFIED | [detail.hpp](/Users/gabrielwillen/.superset/worktrees/emel.cpp/feat/bonsai/src/emel/kernel/detail.hpp) now ports the Prism/ggml-style NEON/dotprod `Q1_0_g128 x q8_0` arithmetic into `dot_q1_0_g128_q8_0_row_scalar(...)` behind compile-time AArch64 guards. |
| 2 | Quantized token embedding is now treated as a native runtime stage on the maintained Bonsai slice instead of `approved_dense_f32_by_contract`. | ✓ VERIFIED | [data.cpp](/Users/gabrielwillen/.superset/worktrees/emel.cpp/feat/bonsai/src/emel/model/data.cpp) removes `token_embedding` from the vector-dequant fallback family, and the focused implementation parity probe reports `quantized_stage_audit: stage=token_embedding ... contract=native_quantized`. |
| 3 | The maintained Bonsai implementation bench improved materially while correctness stayed intact and the full gate remained green. | ✓ VERIFIED | `./scripts/bench.sh --implementation --model tests/models/Bonsai-1.7B.gguf --system` improved from the earlier `3.173x` ratio to `1.343x`, `./scripts/paritychecker.sh --implementation --model tests/models/Bonsai-1.7B.gguf --text hello --max-tokens 1` passed on the pinned Prism lane, and `./scripts/quality_gates.sh` exited `0`. |

**Score:** 3/3 phase truths verified

## Automated Checks

- `cmake --build build/zig --parallel --target emel_tests_bin`
- `cmake --build build/bench_tools_ninja --parallel --target bench_runner`
- `cmake --build build/paritychecker_system_implementation --parallel --target paritychecker`
- `./build/zig/emel_tests_bin --test-case='*q1_0_g128*' --no-breaks`
- `./build/zig/emel_tests_bin --test-case='*quantized_path_audit*' --no-breaks`
- `./scripts/paritychecker.sh --implementation --model tests/models/Bonsai-1.7B.gguf --text hello --max-tokens 1`
- `./scripts/bench.sh --implementation --model tests/models/Bonsai-1.7B.gguf --system`
- `./scripts/quality_gates.sh`

## Current Results

- Passed:
  - `./build/zig/emel_tests_bin --test-case='*q1_0_g128*' --no-breaks`
  - `./build/zig/emel_tests_bin --test-case='*quantized_path_audit*' --no-breaks`
  - `./scripts/paritychecker.sh --implementation --model tests/models/Bonsai-1.7B.gguf --text hello --max-tokens 1`
  - `./scripts/bench.sh --implementation --model tests/models/Bonsai-1.7B.gguf --system`
  - `./scripts/quality_gates.sh`

## Verification Notes

- The maintained Bonsai implementation bench now reports:
  - `emel.cpp 2107783084.000 ns/op`
  - `llama.cpp 1569242125.000 ns/op`
  - `ratio=1.343x`
- The focused parity probe stayed green on the pinned Prism lane and published the updated
  quantized runtime contract:
  - `native_quantized=9`
  - `approved_dense_f32_by_contract=5`
  - `disallowed_fallback=0`
- `./scripts/quality_gates.sh` remained green after the runtime change. The gate still prints the
  usual non-fatal toolchain and benchmark warnings, but it exited `0` with line coverage `90.0%`
  and branch coverage `54.6%`.
