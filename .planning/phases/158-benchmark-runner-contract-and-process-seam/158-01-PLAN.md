---
phase: 158
plan: 01
title: "Benchmark runner contract and process seam"
status: pending
requirements:
  - RUNNER-01
  - RUNNER-02
rule_constraints:
  - "The runner seam must not share EMEL/reference lane-owned runtime objects."
  - "The contract must preserve current benchmark behavior and output schemas."
  - "Process-seam payloads must fail closed when malformed."
---

# Plan 158-01: Benchmark Runner Contract And Process Seam

<rule_constraints>
- Keep the change scoped to runner contract types, serialization/parsing, runner request wiring,
  focused tests, and phase artifacts.
- Do not add process spawning, foreign-language runners, discovery metadata, CMake target splits,
  manifests, or quality-gate behavior in this phase.
- Preserve current mode semantics for `--mode=emel`, `--mode=reference`, `--mode=compare`, and
  kernel modes.
</rule_constraints>

## Goal

Define the narrow runner request/result contract and a serialized local process-seam payload while
continuing to execute the existing benchmark paths unchanged.

## Tasks

1. Add runner mode, request, result, serialization, and parsing helpers.
2. Wire `run_bench_cli(...)` to construct a `runner_request` from existing CLI/env inputs.
3. Keep all existing execution branches mapped to the normalized request.
4. Add focused tests for request/result serialization, round-trip parsing, and malformed payload
   rejection.
5. Run focused bench runner tests and changed-file scoped quality gates.

## Verification

- `git diff --check`
- `cmake --build build/bench_tools_ninja --target bench_runner_tests -j2`
- `ctest --test-dir build/bench_tools_ninja --output-on-failure -R bench_runner_tests`
- Changed-file scoped `scripts/quality_gates.sh`
