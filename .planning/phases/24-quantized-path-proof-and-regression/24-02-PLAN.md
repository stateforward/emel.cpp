---
phase: 24-quantized-path-proof-and-regression
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - tests/generator/lifecycle_tests.cpp
  - tools/paritychecker/paritychecker_tests.cpp
autonomous: true
requirements:
  - VER-04
  - PAR-05
must_haves:
  truths:
    - supported-path regression coverage keeps the canonical runtime locked to the approved `8/4/0/0` contract.
    - unsupported-stage proof remains explicit `no-claim` instead of collapsing into a fabricated supported fallback case.
    - the hardened proof surface stays additive and the repo gate remains green.
  artifacts:
    - `tests/generator/lifecycle_tests.cpp` adds or tightens runtime regression assertions around the supported contract.
    - `tools/paritychecker/paritychecker_tests.cpp` preserves supported versus unsupported proof semantics after the paritychecker hardening.
---

<objective>
Finish Phase 24 by adding the remaining regression coverage around the approved quantized-path
contract.

Purpose: satisfy `VER-04` and close the maintained proof loop around the canonical ARM workload.
Output: supported canonical regressions fail deterministically, unsupported no-claim proof remains
truthful, and the repo gate stays green.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add regression coverage that keeps supported-path and no-claim proof distinct</name>
  <files>tests/generator/lifecycle_tests.cpp, tools/paritychecker/paritychecker_tests.cpp</files>
  <read_first>tests/generator/lifecycle_tests.cpp, tools/paritychecker/paritychecker_tests.cpp, .planning/phases/22-quantized-path-audit-and-contract/22-VERIFICATION.md, .planning/phases/23-arm-quantized-path-closure/23-VERIFICATION.md</read_first>
  <action>Add the narrowest runtime and parity regression checks needed so the canonical supported
  path stays locked to `8/4/0/0`, while the existing unsupported `explicit_no_claim` proof remains
  the negative surface. Reuse existing negative fixtures instead of inventing a bogus supported
  fallback path.</action>
  <verify>scripts/quality_gates.sh</verify>
  <automated>scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - supported runtime/parity regressions fail if the canonical contract drifts away from `8/4/0/0`
    - unsupported-stage proof still reports explicit no-claim
    - repo gate remains green after the additive regression hardening
  </acceptance_criteria>
  <done>Phase 24 closes with maintained proof and regression surfaces that guard the approved runtime contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build/zig/emel_tests_bin --test-case='*generator*quantized*' --no-breaks`
- [ ] `./build/paritychecker_zig/paritychecker_tests`
- [ ] `scripts/quality_gates.sh`
- [ ] no SML transition tables or public C API signatures changed
</verification>

<output>
After completion, create `.planning/phases/24-quantized-path-proof-and-regression/24-02-SUMMARY.md`
</output>
