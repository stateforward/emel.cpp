---
phase: 24-quantized-path-proof-and-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/paritychecker_tests.cpp
autonomous: true
requirements:
  - ATTR-01
  - PAR-05
must_haves:
  truths:
    - maintained generation proof consumes the shipped runtime contract instead of relying only on model-local audit recomputation.
    - canonical `tools/paritychecker --generation` fails if the supported path regresses to `disallowed_fallback` or `explicit_no_claim`.
    - maintained `1/10/100/1000` parity tests assert the exact supported `8/4/0/0` contract.
  artifacts:
    - `tools/paritychecker/parity_runner.cpp` publishes runtime-contract attribution and regression failure semantics for generation mode.
    - `tools/paritychecker/paritychecker_tests.cpp` parses and asserts the exact supported contract across maintained decode lengths.
---

<objective>
Promote the Phase 23 shipped runtime contract into the maintained paritychecker proof surface.

Purpose: satisfy `ATTR-01` and the parity side of `PAR-05` honestly. Output: the canonical
generation proof surface publishes the runtime contract counts it actually ran with and fails
deterministically if that supported path regresses away from the approved contract.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Make paritychecker consume and enforce the shipped runtime contract</name>
  <files>tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp</files>
  <read_first>tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp, src/emel/generator/sm.hpp, .planning/phases/23-arm-quantized-path-closure/23-VERIFICATION.md</read_first>
  <action>Use the shipped generator stage-count accessors during generation proof, publish the
  runtime contract explicitly, and fail canonical generation mode if supported-path
  `disallowed_fallback` or `explicit_no_claim` becomes nonzero. Extend maintained paritychecker
  tests so the `1/10/100/1000` loop asserts the exact `8/4/0/0` contract and the regression
  failure behavior.</action>
  <verify>./build/paritychecker_zig/paritychecker_tests</verify>
  <automated>./build/paritychecker_zig/paritychecker_tests</automated>
  <acceptance_criteria>
    - paritychecker generation publishes runtime contract counts from the shipped wrapper
    - canonical supported generation fails if disallowed fallback or explicit no-claim appears
    - maintained decode-length tests assert exact `8/4/0/0` contract counts
  </acceptance_criteria>
  <done>The maintained generation proof surface now enforces the approved runtime contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4`
- [ ] `./build/paritychecker_zig/paritychecker_tests`
- [ ] `./build/paritychecker_zig/paritychecker --generation --model /Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 1`
- [ ] `./build/paritychecker_zig/paritychecker --generation --model /Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 1000`
- [ ] no SML transition tables or public C API signatures changed
</verification>

<output>
After completion, create `.planning/phases/24-quantized-path-proof-and-regression/24-01-SUMMARY.md`
</output>
