---
phase: 35-maintained-runtime-execution-on-arm
plan: 01
type: execute
wave: 1
depends_on:
  - 34-03
files_modified:
  - scripts/embedded_size.sh
  - tools/embedded_size/reference_probe/main.cpp
  - tools/embedded_size/emel_probe/main.cpp
autonomous: true
requirements:
  - REF-01
  - SMOKE-01
---

# Phase 35 Plan 01

## Objective

Prove the matched `llama.cpp` reference executable and the shared runtime-smoke boundary for the
maintained executable-size slice.

## Tasks

1. Verify the reference probe builds as a final executable for the same maintained fixture and
   request slice.
2. Verify the embedded-size flow smoke-runs both published rows on the same `hello` -> first-token
   path.
3. Keep the comparator set narrow to EMEL and one `llama.cpp` reference row.
4. Record the comparator and smoke evidence in phase artifacts.

## Verification

- `./scripts/embedded_size.sh --json`

## Done Condition

The executable-size flow proves both EMEL and the matched `llama.cpp` reference executable can run
the maintained workload and records that smoke proof for the published rows.
