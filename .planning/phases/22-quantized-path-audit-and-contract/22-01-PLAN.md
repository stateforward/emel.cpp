---
phase: 22-quantized-path-audit-and-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emel/model/llama/detail.hpp
  - src/emel/model/data.cpp
  - tests/generator/lifecycle_tests.cpp
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/paritychecker_tests.cpp
autonomous: true
requirements:
  - AUD-01
must_haves:
  truths:
    - the canonical ARM generation slice has one shared audit source that classifies maintained stage families without actor rewrites.
    - the audit distinguishes approved dense-f32-by-contract stages from disallowed fallback instead of only printing aggregate q2/q3/q6 dispatch counts.
    - maintained parity output publishes the stage audit alongside existing runtime dispatch attribution.
  artifacts:
    - `src/emel/model/llama/detail.hpp` and `src/emel/model/data.cpp` expose a reusable stage-audit helper for the canonical Llama execution view.
    - `tools/paritychecker/parity_runner.cpp` prints a maintained quantized-path audit surface grounded in the shipped runtime/model chain.
    - `tests/generator/lifecycle_tests.cpp` and `tools/paritychecker/paritychecker_tests.cpp` cover the new audit semantics and output.
  key_links:
    - `src/emel/generator/detail.hpp` remains the source for token-row dequantization and dense rhs matmul binding behavior being audited.
    - `src/emel/kernel/aarch64/actions.hpp` remains the source for the q*_K x q8_K dispatch contract that Phase 22 must classify honestly.
---

<objective>
Create and publish the maintained Phase 22 audit truth source for the canonical ARM quantized path.

Purpose: satisfy `AUD-01` by turning the current shipped model/runtime behavior into an explicit
stage-by-stage contract surface. Output: paritychecker can report which canonical stage families
are approved dense-f32-by-contract, which are disallowed fallback, and which existing q2/q3/q6
dispatch counters corroborate the shipped ARM path.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add shared quantized-path audit classification for canonical Llama stages</name>
  <files>src/emel/model/llama/detail.hpp, src/emel/model/data.cpp, tests/generator/lifecycle_tests.cpp</files>
  <read_first>src/emel/model/llama/detail.hpp, src/emel/model/data.cpp, src/emel/generator/detail.hpp, tests/generator/lifecycle_tests.cpp, AGENTS.md</read_first>
  <action>Add a reusable audit helper that inspects the canonical Llama execution view and
  classifies maintained stage families into explicit contract buckets. Cover token-row and norm
  dequant behavior versus quantized matmul behavior in generator-focused tests without changing any
  SML transition table or actor ownership.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*generator*quantized*audit*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*generator*quantized*audit*' --no-breaks</automated>
  <acceptance_criteria>
    - the audit helper is reusable and not embedded only in paritychecker
    - token embedding and norm-vector dequant stages can be distinguished from quantized matmul stages
    - no state-machine structure or public C API signatures change
  </acceptance_criteria>
  <done>The canonical Llama execution view exposes a shared quantized-path audit helper.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the audit in maintained paritychecker output and tests</name>
  <files>tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp, tests/generator/lifecycle_tests.cpp</files>
  <read_first>tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp, src/emel/model/llama/detail.hpp, src/emel/model/data.cpp</read_first>
  <action>Extend maintained paritychecker generation output with a stage-audit surface that reports
  the canonical quantized-path contract next to the existing q2/q3/q6 dispatch metrics. Add tests
  that assert the new audit surface exists and that the canonical ARM workload reports the expected
  stage classifications truthfully.</action>
  <verify>./build/zig/paritychecker_tests_bin --test-case='*generation*quantized*' --no-breaks</verify>
  <automated>./build/zig/paritychecker_tests_bin --test-case='*generation*quantized*' --no-breaks</automated>
  <acceptance_criteria>
    - paritychecker output includes explicit stage audit lines rather than only aggregate counters
    - canonical generation output still publishes existing q2/q3/q6 dispatch evidence
    - the new surface is parseable enough for later regression phases to consume
  </acceptance_criteria>
  <done>Paritychecker publishes a maintained stage-by-stage quantized-path audit surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build/zig/emel_tests_bin --test-case='*generator*quantized*audit*' --no-breaks`
- [ ] `./build/zig/paritychecker_tests_bin --test-case='*generation*quantized*' --no-breaks`
- [ ] `rg 'quantized.*audit|contract' src/emel/model/llama/detail.hpp src/emel/model/data.cpp tools/paritychecker/parity_runner.cpp tools/paritychecker/paritychecker_tests.cpp tests/generator/lifecycle_tests.cpp`
- [ ] no SML transition tables, event payloads, or public C API signatures changed
</verification>

<output>
After completion, create `.planning/phases/22-quantized-path-audit-and-contract/22-01-SUMMARY.md`
</output>
