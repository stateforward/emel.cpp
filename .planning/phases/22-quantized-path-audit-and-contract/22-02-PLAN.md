---
phase: 22-quantized-path-audit-and-contract
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/emel/model/llama/detail.hpp
  - src/emel/model/data.cpp
  - tests/generator/lifecycle_tests.cpp
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/paritychecker_tests.cpp
autonomous: true
requirements:
  - PATH-02
must_haves:
  truths:
    - unsupported or not-yet-ported quantized tensor types publish explicit no-claim behavior instead of being mistaken for approved dense-f32 or optimized quantized execution.
    - maintained parity output includes an operator inventory surface that makes unsupported quantized branches visible.
    - the no-claim behavior is additive proof/publication work, not an actor rewrite.
  artifacts:
    - the shared audit helper can mark unsupported quantized stage families as explicit no-claim.
    - paritychecker prints operator inventory / no-claim fields for unsupported quantized stage families.
    - generator/parity tests cover a negative unsupported-quantized case and prevent silent claim drift.
  key_links:
    - `src/emel/kernel/detail.hpp::quantized_row_storage_bytes(...)` remains the low-level support boundary for currently handled q2/q3/q6 row formats.
    - `src/emel/generator/detail.hpp::copy_tensor_row(...)` and `dequantize_tensor_vector(...)` remain the exact widening seams that must never be mislabeled as supported optimized quantized execution.
---

<objective>
Encode explicit no-claim behavior and operator inventory surfaces for unsupported quantized stages.

Purpose: satisfy `PATH-02` without changing the actor chain. Output: maintained parity output and
tests make unsupported or not-yet-ported quantized branches visible as no-claim instead of letting
them read like approved dense-f32 or optimized quantized coverage.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add unsupported-quantized no-claim audit coverage</name>
  <files>src/emel/model/llama/detail.hpp, src/emel/model/data.cpp, tests/generator/lifecycle_tests.cpp</files>
  <read_first>src/emel/model/llama/detail.hpp, src/emel/model/data.cpp, tests/generator/lifecycle_tests.cpp, src/emel/kernel/detail.hpp</read_first>
  <action>Extend the shared audit helper so unsupported quantized tensor dtypes are reported as
  explicit no-claim stage families. Add generator-focused tests that construct a narrow unsupported
  quantized fixture and assert the audit refuses to label it as approved or optimized coverage.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*generator*no*claim*' --no-breaks</verify>
  <automated>./build/zig/emel_tests_bin --test-case='*generator*no*claim*' --no-breaks</automated>
  <acceptance_criteria>
    - unsupported quantized tensor families do not collapse into approved dense-f32-by-contract labels
    - the audit surface stays additive and read-only
    - no benchmark or snapshot artifacts are modified in this plan
  </acceptance_criteria>
  <done>Unsupported quantized stage families are labeled explicitly as no-claim by the shared audit helper.</done>
</task>

<task type="auto">
  <name>Task 2: Publish operator inventory / no-claim parity output and run focused verification</name>
  <files>tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp, tests/generator/lifecycle_tests.cpp</files>
  <read_first>tools/paritychecker/parity_runner.cpp, tools/paritychecker/paritychecker_tests.cpp, tests/generator/lifecycle_tests.cpp, scripts/quality_gates.sh</read_first>
  <action>Emit a maintained operator-inventory or stage-inventory line in paritychecker output that
  includes explicit no-claim labels for unsupported quantized families, then add focused regression
  checks for both the canonical supported case and the unsupported negative case. Finish by running
  the required repo verification and recording any unrelated debt honestly.</action>
  <verify>scripts/quality_gates.sh</verify>
  <automated>scripts/quality_gates.sh</automated>
  <acceptance_criteria>
    - paritychecker output makes no-claim branches visible and parseable
    - supported canonical output still reports the expected audited contract without regressions
    - any unrelated repo-wide failures are recorded explicitly rather than hidden behind Phase 22
  </acceptance_criteria>
  <done>Phase 22 closes with maintained audit output, explicit no-claim publication, and focused regression proof.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./build/zig/emel_tests_bin --test-case='*generator*no*claim*' --no-breaks`
- [ ] `./build/zig/paritychecker_tests_bin --test-case='*generation*quantized*' --no-breaks`
- [ ] `scripts/quality_gates.sh`
- [ ] `rg 'no-claim|operator_inventory|stage_inventory|quantized.*audit' src/emel/model/llama/detail.hpp src/emel/model/data.cpp tools/paritychecker/parity_runner.cpp tools/paritychecker/paritychecker_tests.cpp tests/generator/lifecycle_tests.cpp`
- [ ] no benchmark snapshots or unrelated publication artifacts changed in Phase 22
</verification>

<output>
After completion, create `.planning/phases/22-quantized-path-audit-and-contract/22-02-SUMMARY.md`
</output>
