---
phase: 155
plan: 01
title: "Parity actor boundary enforcement closure"
status: pending
requirements:
  - LANE-02
rule_constraints:
  - "AGENTS.md forbids parity harnesses from reaching into actor actions, guards, or detail helpers directly."
  - "Kernel-owned arithmetic surfaces are allowed when they are explicitly documented and not actor orchestration paths."
---

# Plan 155-01: Parity Actor Boundary Enforcement Closure

<rule_constraints>
- Do not change parity semantics unless required to remove an internal dependency.
- Keep public wrapper surfaces thin and additive.
- Do not move runtime behavior selection into helper wrappers.
- Keep broad source checks deterministic and scoped to `tools/paritychecker`.
</rule_constraints>

## Goal

Make `tools/paritychecker` stop including or naming actor `actions.hpp`, `guards.hpp`, or
non-kernel `detail.hpp` internals while preserving generation/kernel parity behavior.

## Tasks

1. Add a failing broad source guard for paritychecker actor-helper includes and namespace reaches.
2. Add public wrapper surfaces for GGUF loader metadata, model metadata/vocab loading, and llama
   execution/audit inspection.
3. Switch `parity_engines.cpp` from direct detail/action includes and `detail::` names to those
   public wrappers.
4. Keep AArch64 flash debug comparison on a documented kernel-owned detail surface.
5. Run focused paritychecker tests and changed-file scoped quality gates.

## Verification

- `git diff --check`
- `cmake --build build/paritychecker_zig --target paritychecker_tests -j2`
- `ctest --test-dir build/paritychecker_zig --output-on-failure`
- `cmake --build build/paritychecker_zig --target paritychecker -j2`
- `EMEL_QUALITY_GATES_CHANGED_FILES="tools/paritychecker/parity_engines.cpp tools/paritychecker/paritychecker_tests.cpp src/emel/gguf/loader/any.hpp src/emel/model/any.hpp src/emel/model/llama/any.hpp .planning/REQUIREMENTS.md .planning/ROADMAP.md" scripts/quality_gates.sh`
