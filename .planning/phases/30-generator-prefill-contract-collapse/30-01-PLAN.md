---
phase: 30-generator-prefill-contract-collapse
plan: 01
type: execute
wave: 1
depends_on:
  - 29-qwen3-benchmark-publication
files_modified:
  - src/emel/generator/events.hpp
  - src/emel/generator/actions.hpp
  - src/emel/generator/guards.hpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
---

# Phase 30 Plan 01

## Objective

Define explicit request-scoped prefill contracts and the guard/action surface needed to route them
without copying orchestration state into generator context.

## Tasks

1. Add a closed prefill contract enum to the generator runtime-event surface.
2. Add compile-time contract-stamping actions that only write a constant contract value.
3. Add prefill-contract guards for materialized/preselected result routing.
4. Add generator topology tests that require the explicit prefill contract/result states.

## Verification

- `cmake --build build/zig --parallel --target emel_tests`
- `ctest --test-dir build/zig --output-on-failure -R generator_lifecycle_tests`

## Done Condition

The generator has an explicit request-scoped prefill contract type and test-covered contract/result
state topology, with no generator-context orchestration additions.
