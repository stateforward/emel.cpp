---
phase: 38-fixture-provenance-and-metadata-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/38-fixture-provenance-and-metadata-truth/38-CONTEXT.md
  - .planning/phases/38-fixture-provenance-and-metadata-truth/38-01-PLAN.md
  - tests/models/Bonsai-1.7B.gguf
  - tests/models/README.md
  - tools/generation_fixture_registry.hpp
  - tools/paritychecker/parity_main.cpp
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/paritychecker_tests.cpp
  - tools/bench/bench_runner_tests.cpp
  - tests/model/fixture_registry_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements:
  - FIX-03
  - META-02
  - META-03
---

# Phase 38 Plan 01

## Objective

Freeze the maintained Bonsai fixture identity and executable metadata truth while keeping parity
and benchmark tooling explicit that Bonsai is not generation-supported yet.

## Tasks

1. Add the verified `tests/models/Bonsai-1.7B.gguf` artifact and record its provenance in the test
   model ledger.
2. Extend the maintained fixture registry with Bonsai provenance, GGUF truth metadata, and a split
   between all maintained fixtures and generation-supported fixtures.
3. Update parity and bench subprocess tests to consume only the supported generation subset.
4. Add focused doctest coverage that pins Bonsai registry truth, file identity, and the
   filename-versus-format rule.

## Verification

- `cmake --build build/zig --target emel_tests_bin -j4`
- `./build/zig/emel_tests_bin --test-case='*fixture registry*' --no-breaks`
- `cmake --build build/paritychecker_zig --target paritychecker_tests -j4`
- `./build/paritychecker_zig/paritychecker_tests --test-case='*maintained generation fixture*' --no-breaks`
- `cmake --build build/bench_zig --target bench_runner_tests -j4`
- `./build/bench_zig/bench_runner_tests --test-case='*maintained Qwen and Liquid fixtures*' --no-breaks`
- `scripts/quality_gates.sh`

## Done Condition

The repo contains the real `tests/models/Bonsai-1.7B.gguf` file, repo-visible docs and registries
freeze its exact provenance and GGUF truth, and maintained generation tooling keeps Bonsai out of
the supported subset until later milestone phases bring the runtime path online.
