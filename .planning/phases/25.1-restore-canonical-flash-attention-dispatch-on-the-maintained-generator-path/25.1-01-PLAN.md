---
phase: 25.1-restore-canonical-flash-attention-dispatch-on-the-maintained-generator-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emel/generator/detail.hpp
  - tests/generator/lifecycle_tests.cpp
requirements:
  - TBD
must_haves:
  truths:
    - the supported canonical generator fixture dispatches flash attention again on the shipped runtime path.
    - the explicit `flash_kv_width_mismatch` fixture stays on the nonflash path without claiming flash dispatch.
    - no Boost.SML transition table or public C API signature changes are needed.
  artifacts:
    - failing-test-first coverage for the canonical flash regression
    - shipped generator runtime selection restored through existing detail helpers
---

<objective>
Restore canonical runtime flash selection inside the shipped generator path.

Purpose: move the supported canonical generator workload back onto the existing flash-attention
kernel seam while preserving the explicit nonflash mismatch path. Output: generator runtime and
unit tests prove supported flash dispatch and explicit unsupported nonflash behavior.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the regression with generator lifecycle coverage</name>
  <files>tests/generator/lifecycle_tests.cpp</files>
  <read_first>tests/generator/lifecycle_tests.cpp, .planning/milestones/v1.3-phases/15-runtime-adoption-and-proof/15-03-SUMMARY.md</read_first>
  <action>Replace the current zero-flash canonical expectations with failing assertions that require
  flash dispatch on the supported canonical fixture while keeping the mismatch fixture as the
  explicit zero-flash negative case.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks</verify>
  <acceptance_criteria>
    - canonical generator coverage fails before the runtime fix
    - mismatch fixture still encodes truthful nonflash behavior
  </acceptance_criteria>
  <done>Generator tests reproduce the flash-dispatch regression explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Restore supported flash dispatch in the shipped generator runtime</name>
  <files>src/emel/generator/detail.hpp</files>
  <read_first>src/emel/generator/detail.hpp, tests/generator/lifecycle_tests.cpp</read_first>
  <action>Route supported canonical attention execution back through the existing flash-attention
  request and dispatch helper, while keeping the manual generator-space attention helper for
  unsupported request shapes only.</action>
  <verify>./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks</verify>
  <acceptance_criteria>
    - supported canonical fixture reports flash dispatch on generate
    - mismatch fixture still reports zero flash dispatch
    - quantized runtime contract remains `8/4/0/0`
  </acceptance_criteria>
  <done>The shipped generator runtime again uses flash dispatch on the supported canonical path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/zig --target emel_tests_bin -j4`
- [ ] `./build/zig/emel_tests_bin --test-case='*generator*flash*' --no-breaks`
- [ ] `./build/zig/emel_tests_bin --test-case='*generator*quantized*' --no-breaks`
- [ ] no SML transition tables or public C API signatures changed
</verification>

<output>
After completion, create `.planning/phases/25.1-restore-canonical-flash-attention-dispatch-on-the-maintained-generator-path/25.1-01-SUMMARY.md`
</output>
