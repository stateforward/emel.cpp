# Phase 25.1 Verification

## Status

Implementation and live verification are complete. Stored benchmark snapshot and generated docs
refresh remain pending explicit approval.

## Verified Behavior

1. The shipped generator path again reports flash dispatch on the maintained canonical ARM route.
2. The explicit `flash_kv_width_mismatch` fixture still stays on the nonflash path.
3. Maintained parity stays green across `1/10/100/1000` while retaining nonzero flash evidence.
4. Live compare output now rejects stale zero-flash ARM attribution and still publishes the
   canonical `8/4/0/0` quantized runtime contract.

## Commands

- `cmake --build build/zig --target emel_tests_bin -j4`
- `./build/zig/emel_tests_bin --test-case='*generator_generate_runs_native_generator_contract*' --no-breaks`
- `./build/zig/emel_tests_bin --test-case='*generator_generate_uses_nonflash_runtime_without_claiming_flash*' --no-breaks`
- `./build/zig/emel_tests_bin --test-case='*generator_generate_quantized_contract_fixture_preserves_zero_disallowed_fallback*' --no-breaks`
- `cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4`
- `./build/paritychecker_zig/paritychecker_tests --test-case='*generation keeps parity across the maintained decode lengths*' --no-breaks`
- `./build/paritychecker_zig/paritychecker --generation --model ../../tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 1`
- `./build/paritychecker_zig/paritychecker --generation --model ../../tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 10`
- `./build/paritychecker_zig/paritychecker --generation --model ../../tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 100`
- `./build/paritychecker_zig/paritychecker --generation --model ../../tests/models/Llama-68M-Chat-v1-Q2_K.gguf --text hello --max-tokens 1000`
- `EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare`
- `scripts/quality_gates.sh`

## Evidence

- Direct maintained generation runs ended green at `1/10/100/1000`.
- Flash attribution from direct runs:
  - `1` token: `flash_dispatch_calls=2`, `optimized_flash_dispatch_calls=2`, `shared_flash_dispatch_calls=0`
  - `10` tokens: `flash_dispatch_calls=20`, `optimized_flash_dispatch_calls=20`, `shared_flash_dispatch_calls=0`
  - `100` tokens: `flash_dispatch_calls=200`, `optimized_flash_dispatch_calls=200`, `shared_flash_dispatch_calls=0`
  - `1000` tokens: `flash_dispatch_calls=512`, `optimized_flash_dispatch_calls=512`, `shared_flash_dispatch_calls=0`
- Every maintained direct run also preserved `quantized_runtime_contract: native_quantized=8 approved_dense_f32_by_contract=4 disallowed_fallback=0 explicit_no_claim=0`.
- `scripts/bench.sh --compare` printed:
  - `# generation_flash_evidence: ... flash_dispatch_calls=2 optimized_flash_dispatch_calls=2 shared_flash_dispatch_calls=0`
  - `# generation_runtime_contract: ... native_quantized=8 approved_dense_f32_by_contract=4 disallowed_fallback=0 explicit_no_claim=0`
- `scripts/quality_gates.sh` exited `0`. It emitted `warning: benchmark snapshot regression ignored by quality gates` after a non-blocking benchmark regression line for `kernel/aarch64/op_soft_max`.

## Approval Boundary

Per `AGENTS.md`, the following stored publication files were not refreshed without explicit user
approval:

- [snapshots/bench/benchmarks_compare.txt](/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/snapshots/bench/benchmarks_compare.txt)
- [docs/benchmarks.md](/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/docs/benchmarks.md)
