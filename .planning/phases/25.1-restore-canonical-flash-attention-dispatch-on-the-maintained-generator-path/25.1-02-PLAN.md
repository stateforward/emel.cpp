---
phase: 25.1-restore-canonical-flash-attention-dispatch-on-the-maintained-generator-path
plan: 02
type: execute
wave: 2
depends_on: [25.1-01]
files_modified:
  - tools/paritychecker/paritychecker_tests.cpp
  - tools/bench/bench_main.cpp
  - snapshots/bench/benchmarks_compare.txt
  - docs/benchmarks.md
requirements:
  - TBD
must_haves:
  truths:
    - maintained parity and benchmark proof reject zero-flash attribution on the supported canonical path.
    - stored publication artifacts stay truthful about the restored flash dispatch contract.
    - snapshot updates follow the existing approval rule if publication refresh is needed.
  artifacts:
    - updated paritychecker flash-attribution assertions
    - compare-mode validation aligned with restored flash dispatch
    - refreshed benchmark/docs artifacts if approved and needed
---

<objective>
Re-align maintained proof and publication with the restored canonical flash path.

Purpose: ensure paritychecker, compare-mode validation, and stored benchmark publication all tell
the truth again once the shipped runtime is back on the supported flash path.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Restore maintained flash proof expectations</name>
  <files>tools/paritychecker/paritychecker_tests.cpp, tools/bench/bench_main.cpp</files>
  <read_first>tools/paritychecker/paritychecker_tests.cpp, tools/bench/bench_main.cpp, .planning/milestones/v1.3-phases/15-runtime-adoption-and-proof/15-03-SUMMARY.md</read_first>
  <action>Update paritychecker and compare-mode proof so the supported canonical path requires
  flash dispatch again, with optimized-vs-shared host-aware assertions on AArch64.</action>
  <verify>./build/paritychecker_zig/paritychecker_tests</verify>
  <acceptance_criteria>
    - paritychecker tests reject zero flash dispatch on the supported canonical surface
    - compare-mode validation accepts only truthful restored flash attribution
  </acceptance_criteria>
  <done>Maintained proof surfaces align with the restored runtime flash path.</done>
</task>

<task type="approval">
  <name>Task 2: Refresh stored benchmark/docs artifacts if the restored flash path changes published evidence</name>
  <files>snapshots/bench/benchmarks_compare.txt, docs/benchmarks.md</files>
  <read_first>AGENTS.md, snapshots/bench/benchmarks_compare.txt, docs/benchmarks.md</read_first>
  <action>Use the maintained compare-update and docs generation workflow to refresh stored flash
  attribution only if the runtime fix changes the published evidence, and only after confirming the
  repo's snapshot-update approval rule is satisfied.</action>
  <verify>`scripts/bench.sh --compare-update`; `scripts/generate_docs.sh`</verify>
  <acceptance_criteria>
    - stored artifacts do not silently preserve stale zero-flash evidence
    - snapshot/docs refresh does not happen without approval
  </acceptance_criteria>
  <done>Stored benchmark/docs artifacts match the restored flash-attribution truth, or remain
  intentionally unchanged pending approval.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4`
- [ ] `./build/paritychecker_zig/paritychecker_tests`
- [ ] `EMEL_BENCH_ITERS=1000 EMEL_BENCH_RUNS=3 EMEL_BENCH_WARMUP_ITERS=100 EMEL_BENCH_WARMUP_RUNS=1 scripts/bench.sh --compare`
- [ ] `scripts/quality_gates.sh`
</verification>

<output>
After completion, create `.planning/phases/25.1-restore-canonical-flash-attention-dispatch-on-the-maintained-generator-path/25.1-02-SUMMARY.md`
</output>
