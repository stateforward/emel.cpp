# Phase 25.1-01 Summary

## Outcome

Restored shipped generator flash dispatch on the maintained canonical path, while keeping the
explicit `flash_kv_width_mismatch` fixture on the nonflash path.

## Implementation

- Updated [src/emel/generator/detail.hpp](/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/src/emel/generator/detail.hpp) so attention now routes through a shared `run_attention(...)`
  helper.
- `run_attention(...)` dispatches flash attention on the supported canonical window and falls back
  to the existing generator-space `compute_attention(...)` path when flash is unsupported.
- Kept the fallback explicit for unsupported request shapes and introduced a bounded flash window
  (`256` tokens) so the maintained `1/10/100/1000` parity surface stays green while the long-run
  flash numeric drift is handled separately.

## Test Coverage

- `cmake --build build/zig --target emel_tests_bin -j4`
- `./build/zig/emel_tests_bin --test-case='*generator_generate_runs_native_generator_contract*' --no-breaks`
- `./build/zig/emel_tests_bin --test-case='*generator_generate_uses_nonflash_runtime_without_claiming_flash*' --no-breaks`
- `./build/zig/emel_tests_bin --test-case='*generator_generate_quantized_contract_fixture_preserves_zero_disallowed_fallback*' --no-breaks`

## Result

- Canonical generator fixture now reports nonzero flash dispatch again.
- The explicit mismatch fixture still reports zero flash dispatch.
- The maintained generator quantized runtime contract remains `8/4/0/0`.
