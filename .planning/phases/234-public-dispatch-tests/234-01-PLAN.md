## 234-01 Plan

1. Add focused doctest coverage in existing loader lifecycle tests for public staged-load dispatch:
   - one fully successful `process_event(...)` path with explicit `is(...)` + `visit_current_states(...)` verification (`TST-01`)
   - representative failing `process_event(...)` paths with explicit error callback assertions and ready-state verification (`TST-02`)
2. Keep changes limited to existing machine-scoped files; avoid broad new suites.
3. Run required validation:
   - `ninja -C build emel_tests_bin`
   - `ctest --test-dir build --output-on-failure -R 'emel_tests_(io|model)'`
4. Capture validation evidence, blockers, and residual risk.
