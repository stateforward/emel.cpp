---
phase: 177
plan: 1
name: Resolve Full Closeout Gate
status: blocked
created: 2026-05-01
---

# Plan 177-01: Resolve Full Closeout Gate

## Goal

Close VAL-03 only after the final v1.20 audit has successful source-backed validation evidence.

## Current Evidence

Passing evidence already collected:

- `scripts/check_legacy_sml_surface.sh`
- `EMEL_ZIG_TEST_SHARDS=sm scripts/build_with_zig.sh`
- `ctest --test-dir build/zig -R '^emel_tests_sm$' --output-on-failure`
- `scripts/lint_snapshot.sh`
- `scripts/generate_docs.sh --check`
- Changed-file scoped `scripts/quality_gates.sh`

Blocking evidence:

- `EMEL_QUALITY_GATES_SCOPE=full scripts/quality_gates.sh` exited 124 after the benchmark
  comparison runner exceeded the 1800s gate timeout.

## Tasks

1. Reproduce the benchmark timeout with the narrowest source-backed command that still exercises the
   required full benchmark comparison contract.
2. Identify whether the timeout is a pre-existing full-suite duration issue, a benchmark runner
   regression, or a v1.20 guardrail integration effect.
3. Apply a fix only if the cause is in maintained source or gate orchestration and the fix does not
   weaken benchmark, parity, coverage, fuzz, docs, or lint requirements.
4. Rerun the required closeout validation.
5. Only after successful validation, mark VAL-03 complete and add SUMMARY, VERIFICATION, and
   VALIDATION artifacts for Phase 177.
