---
phase: 40.1-qwen-family-rope-domain-bring-up
verified: 2026-04-03T02:11:03Z
status: passed
score: 3/3 phase truths verified
---

# Phase 40.1 Verification Report

**Phase Goal:** Add an explicit Qwen-family RoPE domain to the shipped EMEL runtime so the
maintained Bonsai slice reaches truthful live-reference generation parity without any Bonsai-only
behavior lane.
**Verified:** 2026-04-03T02:11:03Z
**Status:** passed

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | EMEL-owned model metadata now publishes the rope-domain truth required by the maintained Qwen-family slice, including rope type and YaRN scaling inputs, from GGUF metadata instead of tool-local parsing. | ✓ VERIFIED | [data.hpp](/Users/gabrielwillen/.superset/worktrees/emel.cpp/feat/bonsai/src/emel/model/data.hpp) now defines explicit rope-domain enums, and [detail.hpp](/Users/gabrielwillen/.superset/worktrees/emel.cpp/feat/bonsai/src/emel/model/detail.hpp) now populates Qwen-family rope scaling type, scaling factor, original context length, and related metadata from GGUF. |
| 2 | The shipped generator path now applies explicit Qwen-family RoPE semantics, including `neox` pairing and YaRN scaling, and rejects unsupported rope domains explicitly during prepare. | ✓ VERIFIED | [detail.hpp](/Users/gabrielwillen/.superset/worktrees/emel.cpp/feat/bonsai/src/emel/generator/detail.hpp) now derives the runtime rope domain, validates supported combinations, and applies the Qwen-family rotary behavior through the shipped generator backend. |
| 3 | The maintained Bonsai implementation path now reaches live-reference parity on the real first-token generation boundary, and the prior Qwen maintained anchor uses a truthful live-reference generation contract instead of a stale append-only snapshot. | ✓ VERIFIED | [parity_runner.cpp](/Users/gabrielwillen/.superset/worktrees/emel.cpp/feat/bonsai/tools/paritychecker/parity_runner.cpp) and [generation_bench.cpp](/Users/gabrielwillen/.superset/worktrees/emel.cpp/feat/bonsai/tools/bench/generation_bench.cpp) now consume the shared rope metadata helper, while [generation_fixture_registry.hpp](/Users/gabrielwillen/.superset/worktrees/emel.cpp/feat/bonsai/tools/generation_fixture_registry.hpp) promotes the maintained Qwen generation fixture to live reference generation. |

**Score:** 3/3 phase truths verified

## Automated Checks

- `cmake --build build/zig --parallel --target emel_tests_bin`
- `./build/zig/emel_tests_bin --test-case='*apply_rope*' --no-breaks`
- `./build/zig/emel_tests_bin --test-case='*qwen3_generator_applies_per_head_qk_norm_before_rope*' --no-breaks`
- `./build/zig/emel_tests_bin --test-case='*unsupported_rope_domain*' --no-breaks`
- `./build/zig/emel_tests_bin --test-case='*populate_model_metadata_from_gguf_reads_qwen3_rope_domain*' --no-breaks`
- `./scripts/paritychecker.sh --implementation --model tests/models/Bonsai-1.7B.gguf --text hello --max-tokens 1`
- `./scripts/quality_gates.sh`

## Current Results

- Passed:
  - `cmake --build build/zig --parallel --target emel_tests_bin`
  - `./build/zig/emel_tests_bin --test-case='*apply_rope*' --no-breaks`
  - `./build/zig/emel_tests_bin --test-case='*qwen3_generator_applies_per_head_qk_norm_before_rope*' --no-breaks`
  - `./build/zig/emel_tests_bin --test-case='*unsupported_rope_domain*' --no-breaks`
  - `./build/zig/emel_tests_bin --test-case='*populate_model_metadata_from_gguf_reads_qwen3_rope_domain*' --no-breaks`
  - `./scripts/paritychecker.sh --implementation --model tests/models/Bonsai-1.7B.gguf --text hello --max-tokens 1`
  - `./scripts/quality_gates.sh`

## Verification Notes

- The implementation probe for `tests/models/Bonsai-1.7B.gguf` now reaches live-reference parity
  on the pinned Prism lane at the previously failing first generated-token boundary.
- `./scripts/quality_gates.sh` completed green. The benchmark stage still reports tolerated
  snapshot drift for `logits/validator_sml/*`, but the gate explicitly treats that drift as a
  warning rather than a blocking regression.
- The maintained Qwen generation anchor now compares live against its reference lane because the
  older stored baseline no longer reflected the truthful current runtime contract.
