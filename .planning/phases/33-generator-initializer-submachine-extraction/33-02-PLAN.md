---
phase: 33-generator-initializer-submachine-extraction
plan: 02
type: execute
wave: 1
depends_on:
  - 33-01
files_modified:
  - src/emel/generator/actions.hpp
  - src/emel/generator/guards.hpp
  - src/emel/generator/sm.hpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - INIT-02
  - INIT-03
---

# Phase 33 Plan 02

## Objective

Collapse the parent initialize path to an explicit child seam while preserving initialize
classification and publication behavior.

## Tasks

1. Add parent-owned initializer actor storage and dispatch wiring.
2. Replace the inline initialize pipeline in `generator::model` with one delegation seam plus
   explicit outcome classification.
3. Remove parent-only initialize pipeline guards/actions that moved into the child.
4. Extend generator lifecycle topology assertions to prove the parent no longer exposes child
   internal initialize states.

## Verification

- `./scripts/build_with_zig.sh`
- `build/zig/emel_tests_bin --test-case='*generator_sm_models_explicit_initializer_boundary*'`

## Done Condition

The parent generator validates and publishes initialize, but the initialize orchestration itself is
owned by the child machine.
