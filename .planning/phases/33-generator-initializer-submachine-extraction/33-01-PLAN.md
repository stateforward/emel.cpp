---
phase: 33-generator-initializer-submachine-extraction
plan: 01
type: execute
wave: 1
depends_on:
  - 32-prefill-surface-shrink-and-proof
files_modified:
  - src/emel/generator/context.hpp
  - src/emel/generator/initializer/context.hpp
  - src/emel/generator/initializer/detail.hpp
  - src/emel/generator/initializer/guards.hpp
  - src/emel/generator/initializer/actions.hpp
  - src/emel/generator/initializer/sm.hpp
  - tests/generator/initializer/lifecycle_tests.cpp
  - CMakeLists.txt
autonomous: true
requirements:
  - INIT-01
  - INIT-02
---

# Phase 33 Plan 01

## Objective

Create the generator-owned initializer machine boundary, internal event, and child context seam.

## Tasks

1. Add the new `generator/initializer` component with its own explicit state table.
2. Define the internal initializer run event and child context wiring to the parent generator
   context.
3. Move initialize-phase guards and actions into the child machine.
4. Add focused initializer-machine topology and completion tests.

## Verification

- `./scripts/build_with_zig.sh`
- `build/zig/emel_tests_bin --test-case='*generator_initializer*'`

## Done Condition

`src/emel/generator/initializer` exists as a real machine with typed internal event flow and
focused tests, without widening the public generator API.
