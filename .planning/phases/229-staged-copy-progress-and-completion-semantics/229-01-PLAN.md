---
phase: 229-staged-copy-progress-and-completion-semantics
plan: "01"
type: execute
wave: 1
depends_on:
  - 228-span-target-window-platform-gating
files_modified:
  - .planning/phases/229-staged-copy-progress-and-completion-semantics/229-CONTEXT.md
  - .planning/phases/229-staged-copy-progress-and-completion-semantics/229-01-PLAN.md
  - .planning/phases/229-staged-copy-progress-and-completion-semantics/229-01-SUMMARY.md
  - .planning/phases/229-staged-copy-progress-and-completion-semantics/229-VERIFICATION.md
  - src/emel/io/staged_read/actions.hpp
  - src/emel/io/staged_read/guards.hpp
  - src/emel/io/staged_read/sm.hpp
  - tests/io/staged_read/lifecycle_tests.cpp
autonomous: true
requirements:
  - STG-04
  - STG-05
  - STG-06
rule_constraints:
  - RC-SML-RTC-NO-QUEUE
  - RC-SML-GUARDS-OWN-RUNTIME-CHOICE
  - RC-SML-ACTIONS-BOUNDED-NONBLOCKING
  - RC-SML-NO-DISPATCH-LOCAL-CONTEXT
must_haves:
  truths:
    - "Accepted staged copy deterministically writes the intended contiguous source byte sub-span to target."
    - "Successful copy covers full logical span with strictly monotone forward progress and no backtracking."
    - "Successful dispatch emits exactly one terminal success callback with `bytes_committed == logical_byte_length`."
  artifacts:
    - path: src/emel/io/staged_read/actions.hpp
      provides: "Deterministic staged tiling copy and single done publication."
    - path: src/emel/io/staged_read/sm.hpp
      provides: "Guard-led success routing into staged copy effect; explicit error routing unchanged."
    - path: tests/io/staged_read/lifecycle_tests.cpp
      provides: "Dispatch-through-machine proofs for full-span copy and remainder-chunk monotone completion."
---

<objective>

Reconcile Phase 229 evidence with the currently-landed `io/staged_read` implementation and validate that
STG-04/STG-05/STG-06 are satisfied by source and tests.

Purpose: ensure milestone truth is source-backed and phase artifacts exist for auditability.
Output: Phase 229 context/plan/summary/verification files plus fresh command evidence.

</objective>

<tasks>

<task type="auto">
  <name>Task 1: Verify staged copy semantics in source</name>
  <files>src/emel/io/staged_read/actions.hpp, src/emel/io/staged_read/guards.hpp, src/emel/io/staged_read/sm.hpp</files>
  <behavior>
    Confirm deterministic chunk tiling copy logic, monotone progress arithmetic, copy-source gating, and single success publication.
  </behavior>
</task>

<task type="auto">
  <name>Task 2: Verify dispatch-level tests for STG-04/05/06</name>
  <files>tests/io/staged_read/lifecycle_tests.cpp</files>
  <behavior>
    Confirm tests drive behavior only through `process_event(...)` and verify full-span byte correctness and completion semantics.
  </behavior>
</task>

<task type="auto">
  <name>Task 3: Run requested validation commands</name>
  <behavior>
    Execute:
    1) `ninja -C build emel_tests_bin`
    2) `ctest --test-dir build --output-on-failure -R emel_tests_io`
    3) changed-file scoped `scripts/quality_gates.sh` with `EMEL_QUALITY_GATES_CHANGED_FILES`.
    If timing snapshot rewrites, immediately revert `snapshots/quality_gates/timing.txt`.
  </behavior>
</task>

<task type="auto">
  <name>Task 4: Publish Phase 229 summary and verification</name>
  <files>.planning/phases/229-staged-copy-progress-and-completion-semantics/229-01-SUMMARY.md, .planning/phases/229-staged-copy-progress-and-completion-semantics/229-VERIFICATION.md</files>
  <behavior>
    Record changed files, command exits, blockers, and residual risk without overstating unvalidated lanes.
  </behavior>
</task>

</tasks>

<verification>
- `ninja -C build emel_tests_bin`
- `ctest --test-dir build --output-on-failure -R emel_tests_io`
- `EMEL_QUALITY_GATES_CHANGED_FILES='<actual changed files>' scripts/quality_gates.sh`
</verification>
