---
phase: 128
plan: 01
status: complete
created: 2026-04-28
completed: 2026-04-28
goal: "Stabilize Whisper benchmark closeout evidence and repair superseded closeout prose."
requirements: []
rule_constraints:
  - "Do not reset active v1.16 requirement status; this is tech-debt cleanup only."
  - "Benchmark contradiction checks must remain hard failures."
  - "Do not weaken parity, benchmark, coverage, domain-boundary, or SML checks."
  - "Preserve historical evidence while marking superseded closeout claims explicitly."
---

# Plan 128.1: Whisper Benchmark And Closeout Evidence Cleanup

<rule_constraints>

- Keep active requirements complete; Phase 128 closes audit tech debt only.
- Do not hide or downgrade material performance regressions, transcript drift, model mismatch,
  lane errors, or missing reference transcripts.
- Keep benchmark publication on the maintained public recognizer EMEL lane.
- Keep historical Phase 122/125 evidence intact while clearly superseding obsolete closeout claims.

</rule_constraints>

## Tasks

1. Stabilize default Whisper benchmark sampling.
   - Change `scripts/bench_whisper_single_thread.sh` so its default measured iteration count is
     the current 20-iteration closeout sample.
   - Change `tools/bench/whisper_benchmark.py` so direct invocation uses the same default.
   - Add an explicit small process-wall tolerance so sub-percent timing jitter is not published as
     a closeout contradiction.
   - Preserve explicit command-line and environment overrides.

2. Extend benchmark regression coverage.
   - Add focused tests proving the default benchmark driver uses 20 measured iterations.
   - Add a source-level wrapper test proving the shell closeout wrapper default forwards 20
     measured iterations.
   - Keep existing hard-fail tests for transcript, model, reference, lane, and material
     performance contradictions.

3. Repair superseded evidence prose.
   - Update Phase 122 summary/verification to state that its closeout result was superseded by
     public-recognizer and decoder-ownership repairs.
   - Update Phase 125 summary/verification to state that its closeout result was superseded by
     Phase 126 and Phase 127, with Phase 128 stabilizing evidence only.

4. Record Phase 128 evidence.
   - Write SUMMARY, VERIFICATION, and VALIDATION artifacts after checks pass.
   - Update ROADMAP and STATE to mark Phase 128 complete and Phase 129 next.

## Acceptance Checks

- `cmake --build build/whisper_compare_tools --target whisper_benchmark_tests -j 6`
- `build/whisper_compare_tools/whisper_benchmark_tests --no-breaks`
- `scripts/bench_whisper_single_thread.sh --skip-reference-build --skip-emel-build`
- `git diff --check -- scripts/bench_whisper_single_thread.sh tools/bench/whisper_benchmark.py tools/bench/whisper_benchmark_tests.cpp .planning/phases/122-whisper-final-gap-closeout-rerun .planning/phases/125-whisper-final-recognizer-closeout-rerun .planning/phases/128-whisper-benchmark-and-closeout-evidence-cleanup .planning/ROADMAP.md .planning/STATE.md`
