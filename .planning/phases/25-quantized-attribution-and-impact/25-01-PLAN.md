---
phase: 25-quantized-attribution-and-impact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bench/generation_bench.cpp
  - tools/bench/bench_main.cpp
autonomous: true
requirements:
  - BENCH-10
must_haves:
  truths:
    - maintained compare output publishes the shipped runtime contract alongside existing dispatch evidence.
    - benchmark attribution preserves the approved dense-f32-by-contract seams instead of overstating the path as fully quantized.
    - pre-publication verification stays additive and does not update stored snapshots.
  artifacts:
    - `tools/bench/generation_bench.cpp` captures the runtime contract counts from the shipped generator surface.
    - `tools/bench/bench_main.cpp` prints benchmark-time runtime contract attribution in compare output.
---

<objective>
Add the missing runtime-contract attribution to the maintained benchmark proof surface.

Purpose: satisfy the implementation half of `BENCH-10` without crossing the snapshot-update
approval boundary. Output: local compare output publishes both dispatch evidence and the Phase 24
runtime contract for the canonical workload.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Publish runtime-contract attribution in compare output</name>
  <files>tools/bench/generation_bench.cpp, tools/bench/bench_main.cpp</files>
  <read_first>tools/bench/generation_bench.cpp, tools/bench/bench_main.cpp, .planning/phases/24-quantized-path-proof-and-regression/24-VERIFICATION.md</read_first>
  <action>Thread the shipped generator runtime contract into the benchmark evidence surface and
  print it alongside the existing flash and quantized dispatch evidence during compare runs. Keep
  the work additive and do not refresh stored snapshots yet.</action>
  <verify>scripts/bench.sh --compare</verify>
  <automated>scripts/bench.sh --compare</automated>
  <acceptance_criteria>
    - compare output publishes runtime contract attribution for the canonical generation case
    - attribution preserves the approved dense-f32-by-contract seams honestly
    - no stored snapshot or generated docs file is updated
  </acceptance_criteria>
  <done>The maintained compare output now includes runtime-contract attribution without snapshot churn.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `scripts/bench.sh --compare`
- [ ] `rg -n 'generation_.*contract|generation_quantized_evidence|generation_flash_evidence' tools/bench/generation_bench.cpp tools/bench/bench_main.cpp`
- [ ] no SML transition tables or public C API signatures changed
</verification>

<output>
After completion, create `.planning/phases/25-quantized-attribution-and-impact/25-01-SUMMARY.md`
</output>
