---
phase: 25-quantized-attribution-and-impact
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - snapshots/bench/benchmarks_compare.txt
  - docs/benchmarks.md
autonomous: false
requirements:
  - BENCH-10
must_haves:
  truths:
    - stored benchmark artifacts reflect the new runtime-contract attribution honestly.
    - docs publication records the current warning-only regressions rather than hiding them.
    - execution stops until explicit user approval is granted for snapshot updates.
  artifacts:
    - refreshed `snapshots/bench/benchmarks_compare.txt`
    - refreshed `docs/benchmarks.md`
---

<objective>
Refresh stored compare artifacts and docs after runtime-contract attribution is added.

Purpose: finish `BENCH-10` publication honestly. Output: stored benchmark artifacts and generated
docs match the new attribution surface and current benchmark truth.
</objective>

<tasks>

<task type="approval">
  <name>Task 1: Request approval before updating snapshots and generated docs</name>
  <files>snapshots/bench/benchmarks_compare.txt, docs/benchmarks.md</files>
  <read_first>.planning/phases/25-quantized-attribution-and-impact/25-RESEARCH.md, AGENTS.md</read_first>
  <action>Stop and ask the user for explicit approval before running any compare-update or docs
  regeneration step that would modify stored snapshots.</action>
  <verify>none until approval is granted</verify>
  <acceptance_criteria>
    - no snapshot or docs file is modified without explicit user consent
    - once approved, the execution path will use the maintained compare/docs workflow rather than a custom publication path
  </acceptance_criteria>
  <done>User approval is explicitly granted for snapshot and docs updates.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] explicit user approval recorded
- [ ] `scripts/bench.sh --compare-update`
- [ ] `scripts/generate_docs.sh`
</verification>

<output>
After completion, create `.planning/phases/25-quantized-attribution-and-impact/25-02-SUMMARY.md`
</output>
