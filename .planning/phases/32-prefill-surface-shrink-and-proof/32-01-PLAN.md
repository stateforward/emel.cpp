---
phase: 32-prefill-surface-shrink-and-proof
plan: 01
type: execute
wave: 1
depends_on:
  - 31-generator-prefill-submachine-extraction
files_modified:
  - src/emel/generator/actions.hpp
  - src/emel/generator/guards.hpp
  - src/emel/generator/sm.hpp
  - tests/generator/lifecycle_tests.cpp
  - docs/architecture/generator.md
  - docs/architecture/mermaid/generator.mmd
autonomous: true
requirements:
  - ARCH-01
---

# Phase 32 Plan 01

## Objective

Shrink the remaining top-level generator surface after the prefill child extraction.

## Tasks

1. Remove dead parent prefill-only actions and guards left behind by Phase 31.
2. Tighten the parent generator topology assertions to reflect the final milestone boundary.
3. Regenerate generator architecture docs for the new parent/child structure.

## Verification

- `cmake --build build/zig --parallel --target emel_tests_bin`
- `build/zig/emel_tests_bin --test-case='*generator_prefill*'`
- `build/zig/emel_tests_bin --test-case='*generator_sm_*'`
- `scripts/generate_docs.sh`

## Done Condition

The parent generator surface is smaller and cleaner, and the maintained architecture docs match the
extracted prefill boundary.
