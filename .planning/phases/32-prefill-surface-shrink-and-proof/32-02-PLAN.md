---
phase: 32-prefill-surface-shrink-and-proof
plan: 02
type: execute
wave: 1
depends_on:
  - 32-01
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - VERIFY-01
---

# Phase 32 Plan 02

## Objective

Close the milestone with maintained regression, gate, and performance proof.

## Tasks

1. Rerun the maintained generator/parity/quality-gate verification lane on the refactored code.
2. Run the maintained generation-only compare benchmark and capture the performance impact versus
   the pre-refactor baseline.
3. Record the final phase summaries and mark the milestone ready for audit/completion.

## Verification

- `build/zig/emel_tests_bin --test-case='*qwen3*generator*'`
- `build/paritychecker_zig/paritychecker_tests --test-case='*qwen3*generation*'`
- `scripts/quality_gates.sh`
- `EMEL_BENCH_GENERATION_ITERS=1 EMEL_BENCH_GENERATION_RUNS=1 EMEL_BENCH_GENERATION_WARMUP_ITERS=0 EMEL_BENCH_GENERATION_WARMUP_RUNS=0 scripts/bench.sh --compare --generation-only`

## Done Condition

The refactor is fully re-proved on maintained surfaces, and the user has a concrete statement of
the benchmark impact.
