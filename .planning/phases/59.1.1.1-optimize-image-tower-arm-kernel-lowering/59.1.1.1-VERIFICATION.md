---
phase: 59.1.1.1-optimize-image-tower-arm-kernel-lowering
status: complete
verified: 2026-04-15T18:44:13Z
---

# Phase 59.1.1.1 Verification

## Commands

- `cmake --build build/bench_tools_ninja --target embedding_generator_bench_runner -j4`
- `cmake --build build/zig --target emel_tests_bin -j4`
- `EMEL_BENCH_CASE_FILTER=image EMEL_BENCH_ITERS=10 EMEL_BENCH_RUNS=5 ./build/bench_tools_ninja/embedding_generator_bench_runner`
- `EMEL_BENCH_CASE_FILTER=image EMEL_BENCH_ITERS=1000 EMEL_BENCH_RUNS=1 ./build/bench_tools_ninja/embedding_generator_bench_runner`
- `sample 26565 5 -file /tmp/emel_image_bench_59_1_1_1.sample.txt`
- `scripts/quality_gates.sh`

## Results

- `embedding_generator_bench_runner` rebuilt successfully after the image-tower lowering changes.
- `emel_tests_bin` rebuilt successfully after the image-tower lowering changes.
- Maintained isolated image benchmark result:
  - `ns_per_op=234082950.000`
  - `prepare_ns=4576925.000`
  - `encode_ns=229503854.100`
  - `publish_ns=962.500`
- The focused post-change sample at `/tmp/emel_image_bench_59_1_1_1.sample.txt` shows the old
  per-pixel pointwise lowering is no longer the dominant separate hotspot; the image tower is now
  overwhelmingly kernel-bound in `execute_neon_mul_mat(...)`, with depthwise work the next more
  credible image-specific ROI.
- `scripts/quality_gates.sh` exited `0`.

## Notes

- The long image-only benchmark run used for `sample` was interrupted after sampling; it was a
  profiling aid, not the published throughput measurement.
- The gate tail still emitted the existing warning-only note:
  `warning: benchmark snapshot regression ignored by quality gates`.
- The warning-only snapshot lane remains `logits/sampler_sml/vocab_128000`; it did not block this
  phase closeout.
