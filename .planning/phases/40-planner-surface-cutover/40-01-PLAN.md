---
phase: 40-planner-surface-cutover
plan: 01
type: execute
wave: 1
depends_on:
  - 39-publication-refresh-and-audit-closeout
files_modified:
  - src/emel/batch/planner/sm.hpp
  - src/emel/batch/planner/events.hpp
  - src/emel/generator/actions.hpp
  - src/emel/generator/context.hpp
  - src/emel/machines.hpp
  - tests/batch/planner/planner_sm_flow_tests.cpp
  - tests/batch/planner/planner_sm_transition_tests.cpp
  - tools/bench/batch/planner_bench.cpp
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
---

# Phase 40 Plan 01

## Objective

Canonicalize the top-level planner wrapper and its immediately exposed integration surface under
`src/emel/batch/planner/` without pulling child-mode cleanup or event-boundary redesign into this
phase.

## Tasks

1. Tighten the top-level planner-owned surface so the canonical planner entrypoint, wrapper naming,
   and planner-family includes are readable from `src/emel/batch/planner/`.
2. Update direct planner consumers to use the canonical top-level planner type and include surface
   without widening the changes into child-mode implementation cleanup.
3. Refresh focused planner integration tests or benches as needed so the top-level cutover is
   provably wired after the rename/surface cleanup.

## Verification

- `ctest --test-dir build/zig -R planner`
- `scripts/quality_gates.sh`

## Done Condition

The top-level planner entrypoint is clearly canonical under `src/emel/batch/planner/`, direct
planner consumers follow that surface, and focused planner tests still prove the maintained
batching entry flow is intact.
