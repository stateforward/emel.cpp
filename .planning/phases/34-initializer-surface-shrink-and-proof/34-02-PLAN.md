---
phase: 34-initializer-surface-shrink-and-proof
plan: 02
type: execute
wave: 1
depends_on:
  - 34-01
files_modified:
  - .planning/phases/34-initializer-surface-shrink-and-proof/34-VALIDATION.md
autonomous: true
requirements:
  - VERIFY-02
---

# Phase 34 Plan 02

## Objective

Close the milestone with maintained generator, paritychecker, benchmark, and gate proof.

## Tasks

1. Run the maintained Qwen generator regression lane.
2. Run the maintained paritychecker generation lane.
3. Run the maintained generation-only compare benchmark lane.
4. Run `scripts/quality_gates.sh` and record any scoped exceptions if unrelated failures appear.

## Verification

- `build/zig/emel_tests_bin --test-case='*qwen3*generator*'`
- `build/paritychecker_zig/paritychecker_tests --test-case='*qwen3*generation*'`
- `EMEL_BENCH_GENERATION_ITERS=1 EMEL_BENCH_GENERATION_RUNS=1 EMEL_BENCH_GENERATION_WARMUP_ITERS=0 EMEL_BENCH_GENERATION_WARMUP_RUNS=0 scripts/bench.sh --compare --generation-only`
- `scripts/quality_gates.sh`

## Done Condition

The initializer milestone closes with maintained proof across generator behavior, parity, and the
generation-only compare lane.
