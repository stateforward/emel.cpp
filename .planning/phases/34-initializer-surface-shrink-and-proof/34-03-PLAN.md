---
phase: 34-initializer-surface-shrink-and-proof
plan: 03
type: execute
wave: 1
depends_on:
  - 33-01
files_modified:
  - scripts/embedded_size.sh
  - tools/embedded_size/emel_probe/main.cpp
  - src/emel/text/encoders/events.hpp
  - src/emel/text/encoders/guards.hpp
  - src/emel/text/encoders/bpe/detail.hpp
  - src/emel/text/unicode_data.hpp
  - src/emel/text/unicode_data.cpp
  - tests/text/encoders/common_tests.cpp
autonomous: true
requirements:
  - E2E-01
  - E2E-02
---

# Phase 34 Plan 03

## Objective

Capture and verify the EMEL-owned Qwen3 E2E probe that now powers the maintained executable-size
measurement.

## Tasks

1. Prove the EMEL probe builds as a final executable on the maintained `hello` -> first-token
   slice.
2. Confirm the probe is EMEL-owned end to end, including native GGUF/vocab loading.
3. Remove harness artifacts that distort executable size without weakening the E2E boundary.
4. Record the resulting phase evidence in planning artifacts.

## Verification

- `cmake --build build/debug --target emel_tests_bin`
- `./build/debug/emel_tests_bin --test-case='*encoder*'`
- `./build/debug/emel_tests_bin --test-case='*tokenizer*'`
- `./scripts/embedded_size.sh --json`

## Done Condition

The maintained EMEL probe is verified as a final linked Qwen3 E2E executable and no longer carries
the earlier harness distortions or reference-assisted bootstrap path.
