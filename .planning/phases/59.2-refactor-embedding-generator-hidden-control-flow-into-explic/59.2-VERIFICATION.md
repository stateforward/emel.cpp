---
phase: 59.2-refactor-embedding-generator-hidden-control-flow-into-explic
status: complete
verified: 2026-04-15T20:47:31Z
---

# Phase 59.2 Verification

## Commands

- `cmake --build build/coverage --target emel_tests_bin -j4`
- `./build/coverage/emel_tests_bin --no-breaks --test-case='embedding generator no longer hides phase outcome latches in actions and guards'`
- `ctest --test-dir build/coverage --output-on-failure -R '^emel_tests' -j 1`
- `env EMEL_COVERAGE_CLEAN=1 scripts/test_with_coverage.sh`
- `scripts/paritychecker.sh`
- `scripts/fuzz_smoke.sh`
- `scripts/generate_docs.sh`
- `env EMEL_BENCH_ITERS=1000 EMEL_BENCH_RUNS=3 EMEL_BENCH_WARMUP_ITERS=100 EMEL_BENCH_WARMUP_RUNS=1 BENCH_TOLERANCE=0.30 scripts/bench.sh --snapshot --compare`

## Results

- The focused source regression passed:
  - `1 passed / 0 failed`
- The clean coverage `ctest` lane passed:
  - `100% tests passed, 0 tests failed out of 5`
- The official coverage script passed with thresholds intact:
  - `lines: 90.5% (28763 out of 31786)`
  - `branches: 55.1% (13055 out of 23680)`
- `scripts/paritychecker.sh` exited `0`.
- `scripts/fuzz_smoke.sh` exited `0`.
- `scripts/generate_docs.sh` exited `0`.
- The benchmark snapshot compare exited nonzero with:
  - `error: benchmark regression logits/sampler_sml/vocab_256000 (38146.375 > 38017.905)`

## Notes

- The benchmark snapshot compare result matches the warning-tolerated gate behavior already used by
  `scripts/quality_gates.sh`; it does not block phase closeout.
- I intentionally replaced the noisy direct embeddings doctest shard with the clean coverage
  `ctest` lane after interrupting a spammy `.gcda` merge run, so the verification record here uses
  the stable commands/results rather than that interrupted partial run.
