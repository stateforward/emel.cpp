---
phase: 45-planner-audit-closeout
plan: 01
type: execute
wave: 1
depends_on:
  - 44-behavior-preservation-proof
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/40-planner-surface-cutover/40-01-SUMMARY.md
  - .planning/phases/40-planner-surface-cutover/40-VERIFICATION.md
  - src/emel/batch/planner/actions.hpp
  - src/emel/batch/planner/modes/simple/sm.hpp
  - src/emel/batch/planner/modes/equal/sm.hpp
  - src/emel/batch/planner/modes/sequential/sm.hpp
  - tests/batch/planner/planner_surface_tests.cpp
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - RULE-01
---

# Phase 45 Plan 01

## Objective

Close the remaining v1.10 audit gaps by backfilling the missing Phase 40 proof artifacts and
removing the last planner-family wrapper branch that violates the AGENTS member-method rules.

## Tasks

1. Add a focused failing planner-family test that proves the mode wrapper surfaces still contain the
   reported runtime branch shape.
2. Refactor the three mode wrappers to publish typed outcomes through explicit state inspection
   instead of handwritten runtime `if` selection, and keep planner callback capture aligned.
3. Backfill Phase 40 summary metadata and create `40-VERIFICATION.md` so PLAN-01, PLAN-02, and
   PLAN-03 are no longer orphaned.
4. Re-run the focused planner test slice and `scripts/quality_gates.sh`, then update milestone
   planning state and re-audit the milestone.

## Verification

- `cmake --build build/zig --target emel_tests_bin -j4`
- `./build/zig/emel_tests_bin --no-breaks --source-file='*tests/batch/planner/*'`
- `scripts/quality_gates.sh`

## Done Condition

The planner mode wrappers no longer use runtime branch statements in member methods, Phase 40 has
formal proof artifacts for PLAN-01/02/03, the milestone audit no longer reports blocking gaps, and
the remaining deferred snapshot work is explicitly recorded as deferred.
