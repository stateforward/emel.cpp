---
phase: 29-qwen3-benchmark-publication
plan: 01
type: execute
wave: 1
depends_on:
  - 28-qwen3-parity-and-regression
files_modified:
  - tools/bench/CMakeLists.txt
  - tools/bench/bench_runner_tests.cpp
  - tools/bench/bench_cases.hpp
  - tools/bench/bench_main.cpp
  - tools/bench/generation_bench.cpp
  - tools/docsgen/docsgen.cpp
  - docs/benchmarking.md
autonomous: true
requirements:
  - BENCH-01
---

# Phase 29 Plan 01

## Objective

Add explicit maintained compare publication support for the canonical Qwen3 benchmark slice.

## Tasks

1. Add a narrow maintained `bench_runner` regression target that reproduces the current canonical
   Qwen compare failure before the bench-tool changes.
2. Extend the bench fixture loader and metadata population to accept explicit canonical `qwen3`
   metadata, while still rejecting unsupported architectures.
3. Align maintained benchmark reference tokenization with the GGUF-derived formatter contract
   already proved in parity.
4. Rename generation compare rows and metadata to the canonical Qwen benchmark identity, publish
   explicit native `q8_0` evidence, and keep docsgen aligned to the new maintained snapshot shape.
5. Update operator-facing benchmark workflow docs so they describe the canonical Qwen workload
   honestly.

## Verification

- `cmake -S tools/bench -B build/bench_tools_ninja -G Ninja -DCMAKE_BUILD_TYPE=Release -DEMEL_ENABLE_TESTS=OFF`
- `cmake --build build/bench_tools_ninja --parallel --target bench_runner bench_runner_tests`
- `ctest --test-dir build/bench_tools_ninja --output-on-failure -R bench_runner_tests`
- `EMEL_BENCH_GENERATION_ITERS=1 EMEL_BENCH_GENERATION_RUNS=1 EMEL_BENCH_GENERATION_WARMUP_ITERS=0 EMEL_BENCH_GENERATION_WARMUP_RUNS=0 scripts/bench.sh --compare --generation-only`

## Done Condition

The maintained compare lane succeeds on the canonical Qwen slice, publishes explicit Qwen-native
evidence, and no longer hides behind Llama-labelled generation metadata.
