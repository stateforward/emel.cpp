---
phase: 58.1-arm-embedding-generator-throughput-optimization
plan: 01
type: execute
wave: 1
depends_on:
  - 58-embedding-generator-benchmark-publication
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/58.1-arm-embedding-generator-throughput-optimization/58.1-CONTEXT.md
  - .planning/phases/58.1-arm-embedding-generator-throughput-optimization/58.1-01-PLAN.md
  - .planning/phases/58.1-arm-embedding-generator-throughput-optimization/58.1-01-SUMMARY.md
  - .planning/phases/58.1-arm-embedding-generator-throughput-optimization/58.1-VERIFICATION.md
  - src/emel/embeddings/generator/actions.hpp
  - src/emel/embeddings/generator/detail.hpp
  - src/emel/embeddings/generator/events.hpp
  - tools/bench/bench_common.hpp
  - tools/bench/embedding_generator_bench.cpp
  - tools/bench/embedding_generator_bench_main.cpp
autonomous: true
requirements: []
must_haves:
  truths:
    - The maintained embedding generator exposes steady-state stage timing through its public
      embedding request events without changing request routing semantics.
    - The dedicated embedding benchmark reports median `prepare`, `encode`, and `publish` timings
      for text, image, and audio requests in addition to total `ns_per_op`.
  artifacts:
    - .planning/phases/58.1-arm-embedding-generator-throughput-optimization/58.1-01-SUMMARY.md
    - .planning/phases/58.1-arm-embedding-generator-throughput-optimization/58.1-VERIFICATION.md
    - tools/bench/embedding_generator_bench.cpp
  key_links:
    - The maintained benchmark must time only the real `src/emel/embeddings/generator` path.
    - Stage timing must remain observational and must not reintroduce helper-local control flow.
rule_constraints:
  - source: AGENTS.md
    requirement: "Performance work must stay in EMEL-owned src/ runtime code and benchmarks must drive the owning state machine via public event interfaces only"
    tasks: ["1", "2", "3"]
  - source: AGENTS.md
    requirement: "Runtime behavior choice belongs in guards and sm transitions, not in actions/detail helpers"
    tasks: ["1", "2"]
  - source: docs/rules/sml.rules.md
    requirement: "Keep actions bounded/allocation-free and use explicit state-machine structure instead of hidden runtime branching"
    tasks: ["1", "2"]
---

<objective>
Finish the urgent ARM throughput follow-up by publishing maintained `prepare` / `encode` /
`publish` timing through the real embedding generator benchmark while preserving the already-landed
steady-state ARM hot-path cleanup.

Purpose: Make the current ARM throughput numbers actionable and auditable.
Output: Updated generator timing surface, maintained benchmark output, and phase closeout evidence.
</objective>

<execution_context>
@/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/VSCode/stateforward/emel/emel.cpp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.1-arm-embedding-generator-throughput-optimization/58.1-CONTEXT.md
@src/emel/embeddings/generator/actions.hpp
@src/emel/embeddings/generator/detail.hpp
@src/emel/embeddings/generator/events.hpp
@src/emel/embeddings/generator/sm.hpp
@tools/bench/embedding_generator_bench.cpp
</context>

<rule_constraints>
- `AGENTS.md`: Keep the maintained benchmark on the owning embedding-generator public event
  interface rather than helper-local timing seams, enforced by Task 3.
- `AGENTS.md`: Keep runtime behavior choice in guards and transitions and make any timing surface
  observational only, enforced by Tasks 1 and 2.
- `docs/rules/sml.rules.md`: Keep actions bounded and avoid hidden runtime branching while
  stamping phase boundaries, enforced by Tasks 1 and 2.
</rule_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add observational stage timing to embedding requests</name>
  <files>src/emel/embeddings/generator/events.hpp, src/emel/embeddings/generator/detail.hpp, src/emel/embeddings/generator/actions.hpp</files>
  <action>Add a public benchmark timing payload for text, image, and audio embedding requests, stamp
  the existing prepare/encode/publish action boundaries through that payload, and keep the data
  path observational with no new routing semantics.</action>
  <verify>cmake --build build/bench_tools_ninja --target embedding_generator_bench_runner -j4</verify>
  <done>The generator builds with the new timing payload and request execution semantics remain
  unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the current ARM hot-path cleanup while wiring timing support</name>
  <files>src/emel/embeddings/generator/detail.hpp, src/emel/embeddings/generator/actions.hpp</files>
  <action>Keep the already-landed ARM kernel routing, one-time fp16 expansion, and buffer-swap flow
  intact while threading stage timing through the same request path.</action>
  <verify>cmake --build build/zig --target emel_tests_bin -j4</verify>
  <done>No regression reintroduces scalar fallback decode or buffer ping-pong in the maintained
  path.</done>
</task>

<task type="auto">
  <name>Task 3: Publish steady-state prepare/encode/publish timing in the maintained benchmark</name>
  <files>tools/bench/bench_common.hpp, tools/bench/embedding_generator_bench.cpp, tools/bench/embedding_generator_bench_main.cpp</files>
  <action>Extend the dedicated embedding benchmark runner so each modality reports total `ns_per_op`
  plus median `prepare_ns`, `encode_ns`, and `publish_ns` for steady-state requests through the
  real generator machine.</action>
  <verify>EMEL_BENCH_ITERS=10 EMEL_BENCH_RUNS=5 ./build/bench_tools_ninja/embedding_generator_bench_runner</verify>
  <done>The benchmark output includes stage timings for text, image, and audio and remains clearly
  tied to the maintained generator surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark runner → generator public events | Benchmark-controlled inputs cross into the maintained generator runtime here |
| generator runtime → benchmark timing sink | Observational timing data leaves the runtime here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.1-01 | T | benchmark timing payload | mitigate | Keep timing observational only and avoid using it for routing or validation decisions |
| T-58.1-02 | D | embedding benchmark runner | mitigate | Reuse bounded fixed-size timing payloads and existing request buffers; no dispatch-time allocation |
</threat_model>

<verification>
- `cmake --build build/bench_tools_ninja --target embedding_generator_bench_runner -j4`
- `cmake --build build/zig --target emel_tests_bin -j4`
- `EMEL_BENCH_ITERS=10 EMEL_BENCH_RUNS=5 ./build/bench_tools_ninja/embedding_generator_bench_runner`
</verification>

<success_criteria>
- The maintained embedding request events can carry stage timing output without changing behavior.
- The embedding benchmark prints stage timing for all three maintained modalities.
- The steady-state throughput numbers remain reported from the real generator path after the timing
  split lands.
</success_criteria>

<output>
After completion, create `.planning/phases/58.1-arm-embedding-generator-throughput-optimization/58.1-01-SUMMARY.md`
</output>
