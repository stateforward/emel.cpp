---
phase: 26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3
plan: 02
subsystem: runtime
tags: [qwen3, paritychecker, planner, q8_0]
requires:
  - phase: 26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3
    provides: native q8_0 hot-path support for the canonical blocker tensors
  - phase: 27-qwen3-runtime-architecture-bring-up
    provides: explicit qwen3 execution-view binding and q/k norm topology
provides:
  - maintained Qwen generator bring-up beyond the old backend failure
  - truthful maintained temp-baseline write proof with formatter and runtime contract publication
  - repo-gate closure for the q8_0 unblock slice
affects: [27-02, 28, 29]
tech-stack:
  added: []
  patterns:
    - planner metadata stays explicit on structured-message generator requests
    - maintained bring-up proof uses temp baseline write mode before stored parity publication
key-files:
  created:
    - .planning/phases/26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3/26.1-02-SUMMARY.md
  modified:
    - src/emel/generator/actions.hpp
    - src/emel/generator/guards.hpp
    - tools/paritychecker/parity_runner.cpp
    - tools/paritychecker/paritychecker_tests.cpp
    - tests/generator/action_guard_tests.cpp
key-decisions:
  - "Single-sequence multi-token planner requests send null sequence metadata unless true per-sequence metadata exists."
  - "The maintained Qwen bring-up proof for this unblock slice uses temp-baseline write mode; stored snapshot-backed parity remains Phase 28."
patterns-established:
  - "Combined planner invalid bitmasks are classified with emel::error::has rather than exact enum equality."
requirements-completed: [RUN-00]
completed: 2026-03-28
commit: fed9950
---

# Phase 26.1 Plan 02: Canonical Qwen Backend-Unblock Proof Summary

**The canonical Qwen bring-up path now advances past the old `err=backend` failure and succeeds on
the maintained temp-baseline write surface with truthful formatter, runtime, flash, and native
`q8_0` attribution.**

## Accomplishments
- Fixed the structured-message planning boundary so single-sequence multi-token requests do not
  advertise bogus per-sequence metadata.
- Corrected planner error classification so combined invalid-request bitmasks no longer look like
  backend failures.
- Updated maintained Qwen proof to assert the real `q8_0` runtime path and reject stale q2/q3/q6
  attribution assumptions on the canonical fixture.

## Verification
- `cmake --build build/zig --target emel_tests_bin -j4 && ./build/zig/emel_tests_bin --test-case='*request_planning accepts multi-token single-sequence prompt metadata*,*qwen3*generator*' --no-breaks`
- `cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4 && ./build/paritychecker_zig/paritychecker_tests --test-case='*qwen3*generation*,*canonical generation fixture keeps baseline entry points on the Qwen anchor*' --no-breaks`
- `./build/paritychecker_zig/paritychecker --generation --model tests/models/Qwen3-0.6B-Q8_0.gguf --text hello --max-tokens 1 --write-generation-baseline <tmp>`
- `scripts/quality_gates.sh`

## Outcome
- Maintained Qwen generation now writes a temp baseline successfully, publishes the formatter
  contract from the primary GGUF template, reports `reference_impl: source=maintained_generation_baseline`,
  and shows `native_q8_0_dispatch_calls > 0`.

## Next Phase Readiness
- Phase 27 can complete runtime bring-up without carrying forward the old backend-init blocker.
