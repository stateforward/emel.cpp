---
phase: 26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/emel/kernel/detail.hpp
  - src/emel/model/data.cpp
  - src/emel/generator/detail.hpp
  - tests/kernel/lifecycle_tests.cpp
  - tests/generator/detail_tests.cpp
autonomous: true
requirements:
  - RUN-00
must_haves:
  truths:
    - EMEL-owned `src/emel` code contains an explicit native `q8_0` block contract instead of treating `q8_0` as an implicit variant of the existing `q2_k/q3_k/q6_k` family.
    - Shared kernel scalar execution supports `q8_0 x f32` for both `op_mul_mat` and `op_mul_mat_argmax`.
    - Generator helper row access can dequantize `q8_0` tensor rows natively without tool-only fallback or whole-tensor dequantization.
    - Quantized-path audit classification treats `q8_0` as native quantized once the runtime genuinely supports it.
  artifacts:
    - path: src/emel/kernel/detail.hpp
      provides: explicit `q8_0` row storage, quantize/dequantize, and scalar dot-product support
      contains: block_q8_0
    - path: src/emel/generator/detail.hpp
      provides: native `q8_0` tensor-row access for the generator hot path
      contains: dtype::q8_0
    - path: tests/kernel/lifecycle_tests.cpp
      provides: failing-test-first kernel coverage for `q8_0` matrix execution
      contains: kernel_mul_mat_accepts_q8_0_weights
    - path: tests/generator/detail_tests.cpp
      provides: generator helper coverage for `q8_0` row decode
      contains: generator_detail_dequantizes_q8_0_blocks
  key_links:
    - from: src/emel/generator/detail.hpp
      to: src/emel/kernel/detail.hpp
      via: shared `q8_0` row storage and dequantization contract
      pattern: q8_0
    - from: src/emel/model/data.cpp
      to: src/emel/kernel/detail.hpp
      via: truthful native quantized audit classification
      pattern: q8_0
---

<objective>
Add explicit native `q8_0` support to the shared kernel and generator helpers.

Purpose: land the minimal EMEL-owned runtime support the canonical Qwen3 hot path needs before any
further maintained Qwen bring-up claims are made.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3/26.1-CONTEXT.md
@.planning/phases/26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3/26.1-RESEARCH.md
@.planning/phases/26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3/26.1-VALIDATION.md
@src/emel/kernel/detail.hpp
@src/emel/generator/detail.hpp
@src/emel/model/data.cpp
@tests/kernel/lifecycle_tests.cpp
@tests/generator/detail_tests.cpp

<interfaces>
Current missing native support:

```cpp
inline bool is_quantized_k_dtype(const uint8_t code) noexcept {
  return code == dtype_q2_k || code == dtype_q3_k || code == dtype_q6_k;
}
```

Current reference `q8_0` block contract from `tmp/llama.cpp/ggml/src/ggml-common.h`:

```cpp
#define QK8_0 32
typedef struct {
    ggml_half d;
    int8_t  qs[QK8_0];
} block_q8_0;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the missing native q8_0 kernel and row-copy paths in tests</name>
  <files>tests/kernel/lifecycle_tests.cpp, tests/generator/detail_tests.cpp</files>
  <read_first>tests/kernel/lifecycle_tests.cpp, tests/generator/detail_tests.cpp, src/emel/kernel/detail.hpp, src/emel/generator/detail.hpp, .planning/phases/26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3/26.1-RESEARCH.md</read_first>
  <action>Add failing-test-first coverage that requires:
  1. shared kernel `op_mul_mat` and `op_mul_mat_argmax` accept a canonical `q8_0` matrix with f32
     RHS and return deterministic numeric results,
  2. generator helper `copy_tensor_row` dequantizes a `q8_0` row correctly, and
  3. the new coverage stays separate from existing `q2_k/q3_k/q6_k` tests instead of weakening
     them.
  Use the reference `q8_0` block layout from `tmp/llama.cpp` only as the arithmetic contract.
  </action>
  <verify>
    <automated>cmake --build build/zig --target emel_tests_bin -j4 &amp;&amp; ./build/zig/emel_tests_bin --test-case='*kernel*q8_0*,*generator_detail*q8_0*' --no-breaks</automated>
  </verify>
  <acceptance_criteria>
    - `tests/kernel/lifecycle_tests.cpp` contains `kernel_mul_mat_accepts_q8_0_weights`
    - `tests/kernel/lifecycle_tests.cpp` contains `kernel_mul_mat_argmax_accepts_q8_0_weights`
    - `tests/generator/detail_tests.cpp` contains `generator_detail_dequantizes_q8_0_blocks`
    - the new tests fail before kernel/helper changes
    - existing q2/q3/q6 coverage remains intact
  </acceptance_criteria>
  <done>Tests reproduce the missing native `q8_0` support cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit native q8_0 execution and row access support</name>
  <files>src/emel/kernel/detail.hpp, src/emel/model/data.cpp, src/emel/generator/detail.hpp</files>
  <read_first>src/emel/kernel/detail.hpp, src/emel/model/data.cpp, src/emel/generator/detail.hpp, tests/kernel/lifecycle_tests.cpp, tests/generator/detail_tests.cpp, .planning/phases/26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3/26.1-RESEARCH.md</read_first>
  <action>Implement the explicit native `q8_0` contract in shared EMEL code by:
  1. adding `QK8_0`, `block_q8_0`, scalar quantize/dequantize helpers, and a scalar `q8_0 x q8_0`
     dot path in `src/emel/kernel/detail.hpp`,
  2. extending `run_mul_mat`, `run_mul_mat_argmax`, validation predicates, storage sizing, and
     supported-dtype checks to admit `q8_0 x f32` without treating `q8_0` as a `QK_K` family
     member,
  3. extending `copy_tensor_row` / row-storage helpers in `src/emel/generator/detail.hpp` to
     dequantize `q8_0` rows natively, and
  4. updating quantized-path audit classification so native `q8_0` tensors are reported truthfully.
  Do not add tool-only fallback, whole-tensor dequantization, or new state-machine structure.</action>
  <verify>
    <automated>cmake --build build/zig --target emel_tests_bin -j4 &amp;&amp; ./build/zig/emel_tests_bin --test-case='*kernel*q8_0*,*generator_detail*q8_0*' --no-breaks &amp;&amp; rg -n \"block_q8_0|dtype_q8_0|dequantize_row_q8_0|q8_0\" src/emel/kernel/detail.hpp src/emel/generator/detail.hpp src/emel/model/data.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `src/emel/kernel/detail.hpp` contains `block_q8_0`
    - `src/emel/kernel/detail.hpp` contains `dequantize_row_q8_0`
    - `src/emel/kernel/detail.hpp` contains explicit `q8_0` branches in `run_mul_mat` and `run_mul_mat_argmax`
    - `src/emel/generator/detail.hpp` can copy a `q8_0` tensor row
    - `src/emel/model/data.cpp` reports supported native `q8_0` quantization truthfully
    - no tool-local compute fallback is introduced
  </acceptance_criteria>
  <done>The shared EMEL runtime contains native `q8_0` support for the canonical Qwen3 blocker path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake --build build/zig --target emel_tests_bin -j4`
- [ ] `./build/zig/emel_tests_bin --test-case='*kernel*q8_0*,*generator_detail*q8_0*' --no-breaks`
- [ ] `ctest --test-dir build/zig --output-on-failure -R emel_tests`
- [ ] `rg -n "block_q8_0|dtype_q8_0|dequantize_row_q8_0|q8_0" src/emel/kernel/detail.hpp src/emel/generator/detail.hpp src/emel/model/data.cpp`
</verification>

<output>
After completion, create `.planning/phases/26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3/26.1-01-SUMMARY.md`
</output>
