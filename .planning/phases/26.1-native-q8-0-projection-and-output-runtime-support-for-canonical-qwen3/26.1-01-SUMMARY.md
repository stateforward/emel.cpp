---
phase: 26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3
plan: 01
subsystem: runtime
tags: [q8_0, kernel, generator, canonical-qwen3]
requires:
  - phase: 26
    provides: canonical fixture identity and maintained formatter contract
provides:
  - native q8_0 hot-path support for the canonical Qwen blocker tensors in src/emel
  - explicit native_q8_0 dispatch visibility on the maintained generator path
  - unit coverage for kernel and generator q8_0 blocker behavior
affects: [26.1-02, 27-02, 28, 29]
tech-stack:
  added: []
  patterns:
    - native quantized hot-path support in src/emel with no tool-only fallback
    - runtime attribution keyed to the actual operand class under proof
key-files:
  created:
    - .planning/phases/26.1-native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3/26.1-01-SUMMARY.md
  modified:
    - src/emel/kernel/detail.hpp
    - src/emel/generator/detail.hpp
    - src/emel/model/data.cpp
    - tests/kernel/lifecycle_tests.cpp
    - tests/generator/detail_tests.cpp
key-decisions:
  - "Keep the canonical q8_0 unblock in src/emel and do not substitute any tool-only or whole-tensor dequantize fallback."
  - "Publish native q8_0 proof with native_q8_0_dispatch_calls instead of reusing legacy q2/q3/q6 counters."
patterns-established:
  - "Canonical quantized proof follows the real operand class under execution, not historical attribution assumptions from earlier milestones."
requirements-completed: [RUN-00]
completed: 2026-03-28
commit: fed9950
---

# Phase 26.1 Plan 01: Native q8_0 Hot-Path Support Summary

**The canonical Qwen blocker tensors now execute natively in `src/emel`, and the maintained
generator path exposes explicit `native_q8_0_dispatch_calls` so later proof surfaces can verify
the real operand path.**

## Accomplishments
- Added native `q8_0` matmul coverage in the shared runtime path without widening claims beyond the
  canonical Qwen blocker tensors.
- Carried the required row access and dispatch accounting through the maintained generator backend.
- Added unit coverage around the kernel and generator-side `q8_0` blocker behavior.

## Verification
- `cmake --build build/zig --target emel_tests_bin -j4 && ./build/zig/emel_tests_bin --test-case='*kernel*q8_0*,*generator_detail*q8_0*' --no-breaks`
- `ctest --test-dir build/zig --output-on-failure -R emel_tests`

## Deviations
- The final code checkpoint for `26.1-01`, `26.1-02`, and resumed `27-02` was landed together in
  `fed9950` because the blocked Qwen runtime slice consumed the new native support directly during
  verification.

## Next Phase Readiness
- `26.1-02` can now prove the canonical Qwen bring-up path advances beyond the old backend failure
  without changing the hot-path contract.
