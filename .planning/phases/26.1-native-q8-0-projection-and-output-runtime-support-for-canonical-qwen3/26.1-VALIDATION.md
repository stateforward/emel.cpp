---
phase: 26.1
slug: native-q8-0-projection-and-output-runtime-support-for-canonical-qwen3
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-28
---

# Phase 26.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | doctest via CMake/CTest |
| **Config file** | `CMakeLists.txt` + `tools/paritychecker/CMakeLists.txt` |
| **Quick run command** | `./build/zig/emel_tests_bin --test-case='*q8_0*,*qwen3*generator*' --no-breaks` |
| **Full suite command** | `scripts/quality_gates.sh` |
| **Estimated runtime** | ~30 seconds for the quick lane |

---

## Sampling Rate

- **After every task commit:** Run that task's `<automated>` command
- **After every plan wave:** Run `ctest --test-dir build/zig --output-on-failure -R emel_tests`
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds for the quick-feedback lane

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 26.1-01-01 | 01 | 1 | RUN-00 | unit | `cmake --build build/zig --target emel_tests_bin -j4 && ./build/zig/emel_tests_bin --test-case='*kernel*q8_0*,*generator_detail*q8_0*' --no-breaks` | ✅ | ✅ green |
| 26.1-01-02 | 01 | 1 | RUN-00 | repo test slice | `ctest --test-dir build/zig --output-on-failure -R emel_tests` | ✅ | ✅ green |
| 26.1-02-01 | 02 | 2 | RUN-00, RUN-01 | unit + subprocess | `cmake --build build/zig --target emel_tests_bin -j4 && cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4 && ./build/zig/emel_tests_bin --test-case='*qwen3*generator*' --no-breaks && sh -lc 'tmp=$(mktemp /tmp/phase26_1_qwen.XXXXXX.txt); ./build/paritychecker_zig/paritychecker --generation --model tests/models/Qwen3-0.6B-Q8_0.gguf --text hello --max-tokens 1 --write-generation-baseline \"$tmp\" > /tmp/phase26_1_qwen.out 2>&1; cat /tmp/phase26_1_qwen.out; rg \"generation baseline written|formatter_contract=source=tokenizer.chat_template|reference_impl: source=maintained_generation_baseline|native_q8_0_dispatch_calls=[1-9]\" /tmp/phase26_1_qwen.out'` | ✅ | ✅ green |
| 26.1-02-02 | 02 | 2 | RUN-00 | repo gate | `scripts/quality_gates.sh` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Operator-facing messaging still describes this as a narrow runtime unblock rather than full parity completion | RUN-00 | Automated tests can prove backend bring-up and native `q8_0` dispatch, but human review is still needed to confirm the operator-facing wording does not over-claim full Qwen parity | Run the maintained parity command on the canonical fixture in temp-baseline write mode, inspect stdout/stderr, and confirm the output names the canonical fixture plus formatter/runtime contracts without advertising broader Qwen-family or stored parity completion |

Manual review completed on 2026-03-28: the maintained generation output publishes the canonical
fixture, formatter contract, maintained baseline source, and native `q8_0` dispatch counts without
claiming broad Qwen support or stored parity completion.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency <= 30s in the quick-feedback lane
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validation complete
