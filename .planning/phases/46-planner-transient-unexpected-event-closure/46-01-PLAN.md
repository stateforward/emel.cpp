---
phase: 46-planner-transient-unexpected-event-closure
plan: 01
type: execute
wave: 1
depends_on:
  - 45-planner-audit-closeout
files_modified:
  - .planning/STATE.md
  - .planning/phases/46-planner-transient-unexpected-event-closure/46-01-PLAN.md
  - .planning/phases/46-planner-transient-unexpected-event-closure/46-01-SUMMARY.md
  - .planning/phases/46-planner-transient-unexpected-event-closure/46-VERIFICATION.md
  - src/emel/batch/planner/sm.hpp
  - tests/batch/planner/planner_surface_tests.cpp
autonomous: true
requirements: []
---

# Phase 46 Plan 01

## Objective

Close FINDING-01 from the latest v1.10 milestone audit by making top-level planner handling for the
three transient mode-execution states explicit and audit-complete without widening scope beyond the
planner family.

## Tasks

1. Add a focused failing planner-family test that proves `src/emel/batch/planner/sm.hpp` still
   omits explicit `unexpected_event` rows for `state_simple_mode`, `state_equal_mode`, and
   `state_sequential_mode`.
2. Update the top-level planner transition table so the three transient mode states have explicit
   unexpected-event handling that stays RTC-safe and planner-owned.
3. Re-run the focused planner test slice and `scripts/quality_gates.sh`, then write the Phase 46
   summary and verification artifacts and re-audit the milestone.

## Verification

- `cmake --build build/zig --target emel_tests_bin -j4`
- `./build/zig/emel_tests_bin --no-breaks --source-file='*tests/batch/planner/*'`
- `scripts/quality_gates.sh`

## Done Condition

The top-level planner no longer leaves the three transient mode states without explicit
unexpected-event handling, the new regression test passes, and a follow-up milestone audit no
longer reports FINDING-01 under `gaps.integration`.
