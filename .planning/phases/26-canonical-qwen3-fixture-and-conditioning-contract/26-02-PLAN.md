---
phase: 26-canonical-qwen3-fixture-and-conditioning-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - src/emel/text/formatter/format.hpp
  - src/emel/text/conditioner/events.hpp
  - src/emel/text/conditioner/actions.hpp
  - tests/text/formatter/formatter_tests.cpp
  - tests/text/conditioner/text_conditioner_tests.cpp
autonomous: true
requirements:
  - COND-01
must_haves:
  truths:
    - The formatter and conditioner boundary carries non-owning structured chat messages instead of one flat prompt string.
    - The widened formatter ABI stays generic and injected; it does not hard-code shared Qwen-specific prompt text.
    - Formatter/conditioner events stay non-owning and explicit by using `std::span` and `std::string_view`, not owning containers.
  artifacts:
    - path: src/emel/text/formatter/format.hpp
      provides: structured chat-message formatter ABI
      contains: chat_message
    - path: src/emel/text/conditioner/events.hpp
      provides: structured-message prepare request
      contains: std::span<const emel::text::formatter::chat_message>
    - path: tests/text/formatter/formatter_tests.cpp
      provides: formatter contract regression coverage
      contains: chat_message
    - path: tests/text/conditioner/text_conditioner_tests.cpp
      provides: conditioner contract regression coverage
      contains: structured
  key_links:
    - from: src/emel/text/conditioner/actions.hpp
      to: src/emel/text/formatter/format.hpp
      via: widened format_request population
      pattern: format_request
    - from: src/emel/text/conditioner/events.hpp
      to: src/emel/text/formatter/format.hpp
      via: shared chat_message span type
      pattern: chat_message
---

<objective>
Widen the formatter and conditioner contract to structured chat messages.

Purpose: replace the flat string formatter/conditioner request shape with a non-owning
structured-message ABI that can represent the primary Qwen3 chat template honestly without adding
shared model-specific formatter logic.
Output: `chat_message`-based formatter and conditioner request types plus focused unit coverage for
that contract.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-CONTEXT.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-RESEARCH.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-VALIDATION.md
@src/emel/text/formatter/format.hpp
@src/emel/text/conditioner/events.hpp
@src/emel/text/conditioner/actions.hpp
@tests/text/formatter/formatter_tests.cpp
@tests/text/conditioner/text_conditioner_tests.cpp

<interfaces>
Current formatter ABI from `src/emel/text/formatter/format.hpp`:

```cpp
struct format_request {
  std::string_view input = {};
  char * output = nullptr;
  size_t output_capacity = 0;
  size_t * output_length_out = nullptr;
};
```

Current conditioner request from `src/emel/text/conditioner/events.hpp`:

```cpp
struct prepare {
  std::string_view input = {};
  bool add_special = true;
  bool parse_special = false;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fail first on the formatter/conditioner flat-string contract</name>
  <files>tests/text/formatter/formatter_tests.cpp, tests/text/conditioner/text_conditioner_tests.cpp</files>
  <read_first>src/emel/text/formatter/format.hpp, src/emel/text/conditioner/events.hpp, src/emel/text/conditioner/actions.hpp, tests/text/formatter/formatter_tests.cpp, tests/text/conditioner/text_conditioner_tests.cpp</read_first>
  <action>Add regression coverage that fails while `format_request` and `conditioner::event::prepare`
  still use flat strings. The new tests must require a shared non-owning `chat_message` type with
  `role` and `content`, `std::span<const chat_message>` message carriage, and explicit formatter
  options for `add_generation_prompt` and `enable_thinking`. Keep the tests generic: they prove the
  widened contract shape and option propagation, not a hard-coded Qwen prompt rendering helper.</action>
  <verify>
    <automated>cmake --build build/zig --target emel_tests_bin -j4 &amp;&amp; ./build/zig/emel_tests_bin --test-case='*formatter*contract*,*conditioner*structured*' --no-breaks</automated>
  </verify>
  <done>`./build/zig/emel_tests_bin --test-case='*formatter*contract*,*conditioner*structured*' --no-breaks` passes only when formatter and conditioner tests encode `chat_message`, span-based message carriage, and explicit option propagation instead of flat string input.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the structured-message formatter and conditioner ABI</name>
  <files>src/emel/text/formatter/format.hpp, src/emel/text/conditioner/events.hpp, src/emel/text/conditioner/actions.hpp</files>
  <read_first>src/emel/text/formatter/format.hpp, src/emel/text/conditioner/events.hpp, src/emel/text/conditioner/actions.hpp, src/emel/model/data.hpp</read_first>
  <action>Add `struct chat_message { std::string_view role; std::string_view content; };` to the
  formatter ABI, replace `format_request.input` with `std::span<const chat_message> messages`, and
  add explicit `bool add_generation_prompt` plus `bool enable_thinking` fields. Update
  `conditioner::event::prepare` to carry the same non-owning message span and option booleans
  instead of `input`, and update `src/emel/text/conditioner/actions.hpp` so `dispatch_format`
  fills the widened `format_request` without introducing owning containers, implicit fallback, or
  model-specific prompt rendering logic.</action>
  <verify>
    <automated>cmake --build build/zig --target emel_tests_bin -j4 &amp;&amp; ./build/zig/emel_tests_bin --test-case='*formatter*contract*,*conditioner*structured*' --no-breaks &amp;&amp; rg -n "struct chat_message|std::span<const .*chat_message>|add_generation_prompt|enable_thinking" src/emel/text/formatter/format.hpp src/emel/text/conditioner/events.hpp src/emel/text/conditioner/actions.hpp</automated>
  </verify>
  <done>The formatter and conditioner boundary is grep-verifiable as structured-message based, the targeted doctest slice passes, and no flat `input` string remains in the maintained formatter/conditioner request ABI.</done>
</task>

</tasks>

<verification>
- `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests`
- `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R lint_snapshot`
- `cmake --build build/zig --target emel_tests_bin -j4`
- `./build/zig/emel_tests_bin --test-case='*formatter*contract*,*conditioner*structured*' --no-breaks`
- `scripts/quality_gates.sh`
- `rg -n "struct chat_message|std::span<const .*chat_message>|add_generation_prompt|enable_thinking" src/emel/text/formatter/format.hpp src/emel/text/conditioner/events.hpp src/emel/text/conditioner/actions.hpp`
</verification>

<success_criteria>
- `COND-01` is partially satisfied by an explicit structured-message formatter/conditioner ABI.
- The widened request shape remains non-owning and generic.
- No shared Qwen-specific formatter helper exists in production code.
</success_criteria>

<output>
After completion, create `.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-02-SUMMARY.md`
</output>
