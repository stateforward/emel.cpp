---
phase: 26-canonical-qwen3-fixture-and-conditioning-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/models/README.md
  - tools/paritychecker/parity_main.cpp
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/paritychecker_tests.cpp
  - tools/bench/generation_bench.cpp
autonomous: true
requirements:
  - FIX-01
must_haves:
  truths:
    - Maintained paritychecker and bench generation entry points name `tests/models/Qwen3-0.6B-Q8_0.gguf` as the only canonical v1.6 fixture.
    - `tests/models/README.md` records the official Qwen3 fixture source, checksum, size, and stable maintained path identity.
    - Maintained fixture drift fails explicitly instead of silently reusing the old Llama anchor or a same-basename impostor.
    - Until runtime support lands in Phase 27, maintained Qwen generation fails explicitly on the canonical fixture instead of falling back to the old Llama slice.
  artifacts:
    - path: tests/models/README.md
      provides: Official Qwen3 fixture provenance ledger entry
      contains: Qwen3-0.6B-Q8_0.gguf
    - path: tools/paritychecker/parity_runner.cpp
      provides: Canonical parity fixture constant and strict path validation
      contains: Qwen3-0.6B-Q8_0.gguf
    - path: tools/paritychecker/parity_main.cpp
      provides: Maintained operator help text for the Qwen3 generation slice
      contains: --generation mode requires --model tests/models/Qwen3-0.6B-Q8_0.gguf
    - path: tools/bench/generation_bench.cpp
      provides: Bench-side canonical fixture resolution
      contains: tests/models/Qwen3-0.6B-Q8_0.gguf
  key_links:
    - from: tools/paritychecker/parity_main.cpp
      to: tools/paritychecker/parity_runner.cpp
      via: shared maintained fixture identity
      pattern: Qwen3-0\.6B-Q8_0\.gguf
    - from: tools/paritychecker/parity_runner.cpp
      to: tests/models/Qwen3-0.6B-Q8_0.gguf
      via: normalized canonical fixture-path validation
      pattern: is_expected_generation_fixture
    - from: tools/bench/generation_bench.cpp
      to: tests/models/Qwen3-0.6B-Q8_0.gguf
      via: resolve_generation_model_path
      pattern: Qwen3-0\.6B-Q8_0\.gguf
---

<objective>
Lock the maintained Qwen3 fixture identity and provenance.

Purpose: close the old Llama drift on the maintained paritychecker and bench surfaces before any
formatter work widens the request contract.
Output: official Qwen3 fixture provenance in `tests/models/README.md` plus maintained tool
constants, help text, and regression tests anchored to the same file.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-CONTEXT.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-RESEARCH.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-VALIDATION.md
@tests/models/README.md
@tools/paritychecker/parity_main.cpp
@tools/paritychecker/parity_runner.cpp
@tools/paritychecker/paritychecker_tests.cpp
@tools/bench/generation_bench.cpp

<interfaces>
From `tools/paritychecker/parity_runner.cpp`:

```cpp
constexpr const char * k_generation_fixture_name = "Llama-68M-Chat-v1-Q2_K.gguf";

std::filesystem::path expected_generation_fixture_path();
bool is_expected_generation_fixture(const std::string & model_path);
```

From `tools/bench/generation_bench.cpp`:

```cpp
constexpr char k_generation_fixture_rel[] = "tests/models/Llama-68M-Chat-v1-Q2_K.gguf";

std::string resolve_generation_model_path();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fail first on the stale maintained fixture anchor</name>
  <files>tools/paritychecker/paritychecker_tests.cpp</files>
  <read_first>tools/paritychecker/paritychecker_tests.cpp, tools/paritychecker/parity_main.cpp, tools/paritychecker/parity_runner.cpp, tools/bench/generation_bench.cpp, tests/models/README.md</read_first>
  <action>Update the maintained paritychecker subprocess tests so they require the canonical
  Qwen3 fixture path per the locked Phase 26 fixture decision. Cover the help text, canonical
  generation fixture acceptance, same-basename impostor rejection, and baseline fixture-string
  expectations. Do not leave any maintained generation assertion keyed to the old
  `Llama-68M-Chat-v1-Q2_K.gguf` anchor.</action>
  <verify>
    <automated>cmake --build build/paritychecker_zig --target paritychecker_tests -j4 &amp;&amp; ./build/paritychecker_zig/paritychecker_tests --test-case='*canonical generation fixture*' --no-breaks</automated>
  </verify>
  <done>`./build/paritychecker_zig/paritychecker_tests --test-case='*canonical generation fixture*' --no-breaks` passes only when the maintained generation tests require the Qwen3 fixture, and `rg -n "Qwen3-0\\.6B-Q8_0\\.gguf|Llama-68M-Chat-v1-Q2_K\\.gguf" tools/paritychecker/paritychecker_tests.cpp` shows Qwen3 expectations on the maintained generation cases with no remaining Llama fixture expectation in those cases.</done>
</task>

<task type="auto">
  <name>Task 2: Cut the maintained fixture constants and provenance over to Qwen3</name>
  <files>tests/models/README.md, tools/paritychecker/parity_main.cpp, tools/paritychecker/parity_runner.cpp, tools/bench/generation_bench.cpp</files>
  <read_first>tests/models/README.md, tools/paritychecker/parity_main.cpp, tools/paritychecker/parity_runner.cpp, tools/bench/generation_bench.cpp</read_first>
  <action>Record the official Qwen3 fixture provenance in `tests/models/README.md` with the
  approved checksum `9465e63a22add5354d9bb4b99e90117043c7124007664907259bd16d043bb031`, file size
  `639446688` (`610M`), and stable maintained path identity. Then update the maintained
  generation fixture constants, usage text, normalized path guard, and bench fixture resolver so
  the only maintained v1.6 generation anchor is `tests/models/Qwen3-0.6B-Q8_0.gguf`. Keep the
  maintained acceptance surface on the existing paritychecker and bench entry points; do not add
  a Qwen-only harness. Do not require successful Qwen generation in this plan; require the real
  maintained paritychecker and bench commands to fail explicitly on the canonical Qwen fixture
  until runtime bring-up exists in Phase 27.</action>
  <verify>
    <automated>cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4 &amp;&amp; ./build/paritychecker_zig/paritychecker_tests --test-case='*canonical generation fixture*' --no-breaks &amp;&amp; ! ./build/paritychecker_zig/paritychecker --generation --model tests/models/Qwen3-0.6B-Q8_0.gguf --text hello --max-tokens 1 --write-generation-baseline /tmp/qwen26-baseline.txt &amp;&amp; sh -lc 'set +e; EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare --generation-only > /tmp/phase26_fixture_bench.txt 2&gt;&amp;1; status=$?; cat /tmp/phase26_fixture_bench.txt; test $status -ne 0; rg "Qwen3-0\\.6B-Q8_0\\.gguf|prepare_emel_fixture" /tmp/phase26_fixture_bench.txt' &amp;&amp; rg -n "Qwen3-0\\.6B-Q8_0\\.gguf|9465e63a22add5354d9bb4b99e90117043c7124007664907259bd16d043bb031|639446688|610M" tests/models/README.md tools/paritychecker/parity_main.cpp tools/paritychecker/parity_runner.cpp tools/bench/generation_bench.cpp</automated>
  </verify>
  <done>The maintained fixture identity is grep-verifiable in `tests/models/README.md`, `tools/paritychecker/parity_main.cpp`, `tools/paritychecker/parity_runner.cpp`, and `tools/bench/generation_bench.cpp`; the paritychecker fixture tests pass; the real maintained paritychecker and bench commands both fail explicitly on the canonical Qwen fixture before runtime bring-up; and the maintained generation surface no longer accepts silent reuse of the prior Llama anchor.</done>
</task>

</tasks>

<verification>
- `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests`
- `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R lint_snapshot`
- `cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4`
- `./build/paritychecker_zig/paritychecker_tests --test-case='*canonical generation fixture*' --no-breaks`
- `! ./build/paritychecker_zig/paritychecker --generation --model tests/models/Qwen3-0.6B-Q8_0.gguf --text hello --max-tokens 1 --write-generation-baseline /tmp/qwen26-baseline.txt`
- `sh -lc 'set +e; EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare --generation-only > /tmp/phase26_fixture_bench.txt 2>&1; status=$?; cat /tmp/phase26_fixture_bench.txt; test $status -ne 0; rg "Qwen3-0\\.6B-Q8_0\\.gguf|prepare_emel_fixture" /tmp/phase26_fixture_bench.txt'`
- `scripts/quality_gates.sh`
- `rg -n "Qwen3-0\\.6B-Q8_0\\.gguf|9465e63a22add5354d9bb4b99e90117043c7124007664907259bd16d043bb031|639446688|610M" tests/models/README.md tools/paritychecker/parity_main.cpp tools/paritychecker/parity_runner.cpp tools/bench/generation_bench.cpp`
</verification>

<success_criteria>
- `FIX-01` is satisfied by one documented maintained fixture under `tests/models/`.
- Maintained paritychecker and bench code paths resolve the same canonical Qwen3 fixture.
- Old Llama fixture assumptions fail in maintained generation tests instead of surviving silently.
- Pre-runtime maintained Qwen generation fails explicitly on the canonical fixture until Phase 27 runtime support exists.
</success_criteria>

<output>
After completion, create `.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-01-SUMMARY.md`
</output>
