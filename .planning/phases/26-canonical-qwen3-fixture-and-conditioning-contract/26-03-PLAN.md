---
phase: 26-canonical-qwen3-fixture-and-conditioning-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 26-02
files_modified:
  - src/emel/generator/events.hpp
  - src/emel/generator/actions.hpp
  - src/emel/generator/guards.hpp
  - tests/generator/action_guard_tests.cpp
  - tests/generator/lifecycle_tests.cpp
autonomous: true
requirements:
  - COND-01
must_haves:
  truths:
    - The maintained generator request no longer carries a flat `prompt` string; it carries structured chat messages plus explicit formatter options.
    - Generator validation and prepare-dispatch logic are updated to the widened request model without implicit fallback behavior.
    - Generator regression coverage proves the widened request shape survives initialize/generate contract checks.
  artifacts:
    - path: src/emel/generator/events.hpp
      provides: structured-message generate request
      contains: std::span<const emel::text::formatter::chat_message>
    - path: src/emel/generator/actions.hpp
      provides: structured-message prepare dispatch
      contains: prepare_ev.messages
    - path: src/emel/generator/guards.hpp
      provides: structured-message request validation
      contains: messages
  key_links:
    - from: src/emel/generator/events.hpp
      to: src/emel/text/formatter/format.hpp
      via: shared chat_message span type
      pattern: chat_message
    - from: src/emel/generator/actions.hpp
      to: src/emel/text/conditioner/events.hpp
      via: prepare request construction
      pattern: prepare_ev\.
---

<objective>
Carry the structured-message formatter contract through the generator boundary.

Purpose: make the maintained generator request and dispatch path explicit about chat messages and
formatter options before any maintained tool surface binds Qwen template metadata to it.
Output: generator request/events/actions/guards updated to structured chat messages plus generator
regression coverage for the new contract.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-CONTEXT.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-RESEARCH.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-VALIDATION.md
@src/emel/generator/events.hpp
@src/emel/generator/actions.hpp
@src/emel/generator/guards.hpp
@src/emel/text/conditioner/events.hpp
@src/emel/text/formatter/format.hpp
@tests/generator/action_guard_tests.cpp
@tests/generator/lifecycle_tests.cpp

<interfaces>
Current generator request from `src/emel/generator/events.hpp`:

```cpp
struct generate {
  std::string_view prompt = {};
  int32_t max_tokens = 0;
  std::span<char> output = {};
  size_t & output_length_out;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fail first on the generator flat-prompt request contract</name>
  <files>tests/generator/action_guard_tests.cpp, tests/generator/lifecycle_tests.cpp</files>
  <read_first>src/emel/generator/events.hpp, src/emel/generator/actions.hpp, src/emel/generator/guards.hpp, src/emel/text/conditioner/events.hpp, tests/generator/action_guard_tests.cpp, tests/generator/lifecycle_tests.cpp</read_first>
  <action>Add generator regression coverage that fails until `generator::event::generate` carries
  structured chat messages and explicit formatter options instead of a flat `prompt` string.
  Require the canonical maintained request to be modelled as at least one explicit user-role
  message and verify the widened request propagates into conditioner preparation without silently
  reconstructing raw prompt text.</action>
  <verify>
    <automated>cmake --build build/zig --target emel_tests_bin -j4 &amp;&amp; ./build/zig/emel_tests_bin --test-case='*generator*structured*,*generator*message*' --no-breaks</automated>
  </verify>
  <done>The focused generator doctest slice fails until the request shape is no longer flat-string based and explicit chat-message expectations are encoded in generator tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the structured-message generator request and prepare dispatch</name>
  <files>src/emel/generator/events.hpp, src/emel/generator/actions.hpp, src/emel/generator/guards.hpp</files>
  <read_first>src/emel/generator/events.hpp, src/emel/generator/actions.hpp, src/emel/generator/guards.hpp, src/emel/text/conditioner/events.hpp, src/emel/text/formatter/format.hpp</read_first>
  <action>Replace `generator::event::generate.prompt` with `std::span<const emel::text::formatter::chat_message> messages`
  plus explicit `bool add_generation_prompt` and `bool enable_thinking` fields. Update generator
  guards to reject empty message spans instead of empty prompt strings, and update generator
  actions so `prepare_ev.messages`, `prepare_ev.add_generation_prompt`, and
  `prepare_ev.enable_thinking` are forwarded into conditioner preparation. Keep the request model
  non-owning and additive; do not add owned strings, vectors, or model-specific prompt synthesis.</action>
  <verify>
    <automated>cmake --build build/zig --target emel_tests_bin -j4 &amp;&amp; ./build/zig/emel_tests_bin --test-case='*generator*structured*,*generator*message*' --no-breaks &amp;&amp; rg -n "std::span<const .*chat_message>|add_generation_prompt|enable_thinking|prepare_ev\\.messages" src/emel/generator/events.hpp src/emel/generator/actions.hpp src/emel/generator/guards.hpp</automated>
  </verify>
  <done>The generator boundary is grep-verifiable as structured-message based, generator doctests pass, and the maintained generate path forwards message spans plus explicit formatter options into conditioner preparation.</done>
</task>

</tasks>

<verification>
- `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests`
- `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R lint_snapshot`
- `cmake --build build/zig --target emel_tests_bin -j4`
- `./build/zig/emel_tests_bin --test-case='*generator*structured*,*generator*message*' --no-breaks`
- `scripts/quality_gates.sh`
- `rg -n "std::span<const .*chat_message>|add_generation_prompt|enable_thinking|prepare_ev\\.messages" src/emel/generator/events.hpp src/emel/generator/actions.hpp src/emel/generator/guards.hpp`
</verification>

<success_criteria>
- `COND-01` is partially satisfied by a generator request model that is explicit about chat messages and formatter options.
- The generator no longer depends on a flat prompt string on the maintained path.
- The widened request shape survives generator guard and lifecycle regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-03-SUMMARY.md`
</output>
