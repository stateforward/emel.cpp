---
phase: 26-canonical-qwen3-fixture-and-conditioning-contract
plan: 04
type: execute
wave: 4
depends_on:
  - 26-01
  - 26-03
files_modified:
  - tools/paritychecker/parity_runner.cpp
  - tools/paritychecker/paritychecker_tests.cpp
  - tools/bench/generation_bench.cpp
  - tools/bench/bench_main.cpp
autonomous: true
requirements:
  - COND-01
must_haves:
  truths:
    - Maintained paritychecker and bench resolve the formatter contract from the primary GGUF `tokenizer.chat_template` only.
    - Unsupported declared primary templates hard-fail on maintained paritychecker and bench setup; there is no implicit raw fallback and no named-template selection.
    - Before runtime bring-up exists, the same resolved formatter contract is still published in maintained setup or failure output on paritychecker and bench surfaces.
  artifacts:
    - path: tools/paritychecker/parity_runner.cpp
      provides: primary-template support classification and parity setup/failure publication
      contains: formatter_contract
    - path: tools/paritychecker/paritychecker_tests.cpp
      provides: maintained formatter-contract subprocess coverage
      contains: formatter_contract
    - path: tools/bench/generation_bench.cpp
      provides: bench-side primary-template binding and unsupported-template rejection
      contains: tokenizer.chat_template
    - path: tools/bench/bench_main.cpp
      provides: bench setup/failure output publication for the resolved formatter contract
      contains: generation_formatter_contract
  key_links:
    - from: tools/bench/generation_bench.cpp
      to: tools/bench/bench_main.cpp
      via: bench formatter-contract evidence publication
      pattern: generation_formatter_contract
---

<objective>
Bind and publish the maintained primary-template formatter contract on parity and bench surfaces.

Purpose: resolve support from the canonical fixture's primary `tokenizer.chat_template`, inject the
maintained formatter adapter through the existing seam, hard-fail unsupported templates, and make
the resolved formatter contract visible on maintained setup and failure surfaces before runtime
bring-up exists. Output: primary-template-only maintained tool binding, explicit
unsupported-template failure, and formatter-contract publication on maintained setup/failure
surfaces.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-CONTEXT.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-RESEARCH.md
@.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-VALIDATION.md
@tools/paritychecker/parity_runner.cpp
@tools/paritychecker/paritychecker_tests.cpp
@tools/bench/generation_bench.cpp
@tools/bench/bench_main.cpp
@src/emel/generator/events.hpp
@src/emel/text/formatter/format.hpp
@src/emel/model/data.hpp

<interfaces>
Local primary Qwen template facts from `26-RESEARCH.md`:

```text
source=tokenizer.chat_template
named_template_variants=0
required_option_values:
  tools=none
  add_generation_prompt=true
  enable_thinking=false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fail first on missing formatter-contract publication and unsupported-template rejection</name>
  <files>tools/paritychecker/paritychecker_tests.cpp</files>
  <read_first>tools/paritychecker/paritychecker_tests.cpp, tools/paritychecker/parity_runner.cpp, tools/bench/generation_bench.cpp, tools/bench/bench_main.cpp</read_first>
  <action>Add maintained subprocess coverage that fails until paritychecker generation publishes a
  `formatter_contract=` identity in operator-visible setup or failure output, and until maintained
  setup rejects unsupported declared primary templates instead of silently using `format_raw`.
  Require the maintained bench compare slice to publish the same formatter contract before its
  truthful pre-runtime failure, and require the contract publication to encode the approved maintained values
  `source=tokenizer.chat_template`, `shape=structured_chat_messages_v1`,
  `add_generation_prompt=true`, `enable_thinking=false`, and `tools=none`.</action>
  <verify>
    <automated>cmake --build build/paritychecker_zig --target paritychecker_tests -j4 &amp;&amp; ./build/paritychecker_zig/paritychecker_tests --test-case='*generation*formatter*contract*' --no-breaks &amp;&amp; sh -lc 'set +e; EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare --generation-only > /tmp/phase26_formatter_bench.txt 2&gt;&amp;1; status=$?; cat /tmp/phase26_formatter_bench.txt; test $status -ne 0; rg "generation_formatter_contract|Qwen3-0\\.6B-Q8_0\\.gguf" /tmp/phase26_formatter_bench.txt'</automated>
  </verify>
  <done>The paritychecker subprocess tests fail until formatter-contract publication and unsupported-template rejection are explicit, and the maintained bench compare slice is required to surface the same formatter-contract publication before its pre-runtime failure.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the primary-template-only maintained formatter binding and publication</name>
  <files>tools/paritychecker/parity_runner.cpp, tools/bench/generation_bench.cpp, tools/bench/bench_main.cpp</files>
  <read_first>tools/paritychecker/parity_runner.cpp, tools/bench/generation_bench.cpp, tools/bench/bench_main.cpp, src/emel/generator/events.hpp, src/emel/text/formatter/format.hpp, src/emel/model/data.hpp</read_first>
  <action>Populate and read the canonical fixture's primary GGUF `tokenizer.chat_template` on the
  maintained paritychecker and bench setup paths, classify support as `no_template`,
  `supported_contract`, or `unsupported_template`, and inject a formatter adapter through the
  existing `formatter_ctx + format_fn` seam. Freeze the maintained contract string to include
  `source=tokenizer.chat_template`, `support=supported_contract`, `shape=structured_chat_messages_v1`,
  `tools=none`, `add_generation_prompt=true`, and `enable_thinking=false`. Publish that exact
  resolved string in parity setup or failure output via `formatter_contract=...`, and in bench
  output via `# generation_formatter_contract: ...`, before downstream runtime bring-up exists.
  When a model declares an unsupported primary template, fail maintained paritychecker and bench
  setup immediately. Prove the maintained bench path through the real
  `scripts/bench.sh --compare --generation-only` workflow, but require a truthful pre-runtime
  failure that still surfaces the canonical formatter contract instead of a successful compare row.
  Do not add named-template selection, shared Qwen prompt helpers, or manual/raw override behavior.
  Baseline or proof-artifact publication is downstream work once runtime and parity success exist.</action>
  <verify>
    <automated>cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4 &amp;&amp; ./build/paritychecker_zig/paritychecker_tests --test-case='*generation*formatter*contract*' --no-breaks &amp;&amp; sh -lc 'set +e; ./build/paritychecker_zig/paritychecker --generation --model tests/models/Qwen3-0.6B-Q8_0.gguf --text hello --max-tokens 1 > /tmp/phase26_formatter_parity.txt 2&gt;&amp;1; status=$?; cat /tmp/phase26_formatter_parity.txt; test $status -ne 0; rg "formatter_contract=|model_invalid|Qwen3-0\\.6B-Q8_0\\.gguf" /tmp/phase26_formatter_parity.txt' &amp;&amp; sh -lc 'set +e; EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare --generation-only > /tmp/phase26_formatter_bench.txt 2&gt;&amp;1; status=$?; cat /tmp/phase26_formatter_bench.txt; test $status -ne 0; rg "generation_formatter_contract|Qwen3-0\\.6B-Q8_0\\.gguf" /tmp/phase26_formatter_bench.txt' &amp;&amp; rg -n "formatter_contract|generation_formatter_contract|tokenizer\\.chat_template|supported_contract|unsupported_template" tools/paritychecker/parity_runner.cpp tools/bench/generation_bench.cpp tools/bench/bench_main.cpp</automated>
  </verify>
  <done>The maintained tool surfaces are grep-verifiable as primary-template-only, unsupported-template failure is explicit, paritychecker tests pass, the maintained bench compare workflow runs and fails truthfully before runtime bring-up, and the same resolved formatter contract is visible in maintained parity and bench setup/failure output.</done>
</task>

</tasks>

<verification>
- `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R emel_tests`
- `scripts/build_with_zig.sh && ctest --test-dir build/zig --output-on-failure -R lint_snapshot`
- `cmake --build build/paritychecker_zig --target paritychecker paritychecker_tests -j4`
- `./build/paritychecker_zig/paritychecker_tests --test-case='*generation*formatter*contract*' --no-breaks`
- `sh -lc 'set +e; ./build/paritychecker_zig/paritychecker --generation --model tests/models/Qwen3-0.6B-Q8_0.gguf --text hello --max-tokens 1 > /tmp/phase26_formatter_parity.txt 2>&1; status=$?; cat /tmp/phase26_formatter_parity.txt; test $status -ne 0; rg "formatter_contract=|model_invalid|Qwen3-0\\.6B-Q8_0\\.gguf" /tmp/phase26_formatter_parity.txt'`
- `sh -lc 'set +e; EMEL_BENCH_ITERS=1 EMEL_BENCH_RUNS=1 EMEL_BENCH_WARMUP_ITERS=0 EMEL_BENCH_WARMUP_RUNS=0 scripts/bench.sh --compare --generation-only > /tmp/phase26_formatter_bench.txt 2>&1; status=$?; cat /tmp/phase26_formatter_bench.txt; test $status -ne 0; rg "generation_formatter_contract|Qwen3-0\\.6B-Q8_0\\.gguf" /tmp/phase26_formatter_bench.txt'`
- `scripts/quality_gates.sh`
- `rg -n "formatter_contract|generation_formatter_contract|tokenizer\\.chat_template|supported_contract|unsupported_template" tools/paritychecker/parity_runner.cpp tools/bench/generation_bench.cpp tools/bench/bench_main.cpp`
</verification>

<success_criteria>
- `COND-01` is satisfied by one primary-template-only maintained formatter contract on parity and bench surfaces.
- Unsupported declared templates hard-fail with no implicit raw fallback.
- The resolved formatter contract is published consistently on maintained setup or failure surfaces before downstream runtime/parity success exists.
</success_criteria>

<output>
After completion, create `.planning/phases/26-canonical-qwen3-fixture-and-conditioning-contract/26-04-SUMMARY.md`
</output>
