---
phase: 28-qwen3-parity-and-regression
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - tools/paritychecker/paritychecker_tests.cpp
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - VER-01
---

# Phase 28 Plan 02

## Objective

Add regression coverage for the canonical Qwen slice while preserving the prior maintained Llama
anchor.

## Tasks

1. Convert maintained Qwen subprocess coverage from temp-baseline write mode to stored-baseline
   compare mode for `1/10/100/1000`, `--dump`, and `--attribution`.
2. Prove that the canonical generation fixture entry point remains anchored to the Qwen stored
   baseline path.
3. Re-run the full `paritychecker_tests` suite and repo quality gates so the wider shared lane
   still protects the prior Llama slice.
4. Sync roadmap, requirement, and state tracking after the parity/regression proof lands.

## Verification

- `ctest --test-dir build/paritychecker_zig --output-on-failure -R paritychecker_tests`
- `scripts/quality_gates.sh`

## Done Condition

The maintained Qwen compare-mode regression lane is stored-baseline-backed, the shared parity suite
stays green, and milestone tracking records Phase 28 complete.
