---
phase: 28-qwen3-parity-and-regression
plan: 01
type: execute
wave: 1
depends_on:
  - 27-qwen3-runtime-architecture-bring-up
files_modified:
  - tools/paritychecker/parity_runner.cpp
  - snapshots/parity/generation_qwen3_0_6b_q8_0_prompt_hello_max_tokens_1.txt
  - snapshots/parity/generation_qwen3_0_6b_q8_0_prompt_hello_max_tokens_10.txt
  - snapshots/parity/generation_qwen3_0_6b_q8_0_prompt_hello_max_tokens_100.txt
  - snapshots/parity/generation_qwen3_0_6b_q8_0_prompt_hello_max_tokens_1000.txt
autonomous: true
requirements:
  - PAR-01
---

# Phase 28 Plan 01

## Objective

Extend maintained paritychecker generation and reference alignment for the canonical Qwen3 slice.

## Tasks

1. Reproduce compare-mode failure against stored Qwen baselines on the canonical generation path.
2. Align compare-mode reference tokenization with the GGUF-derived formatter contract instead of
   raw prompt bytes.
3. Align attribution replay with the shipped generator by consuming conditioned prompt tokens,
   using the renderer contract for output bytes, and replaying canonical Qwen3 Q/K RMS norm before
   RoPE.
4. Refresh the approved stored Qwen parity baselines for `max_tokens=1/10/100/1000`.

## Verification

- `cmake --build build/paritychecker_zig --target paritychecker_tests paritychecker -j4`
- `./build/paritychecker_zig/paritychecker_tests --test-case='*qwen3*generation*' --no-breaks`
- `./build/paritychecker_zig/paritychecker --generation --model tests/models/Qwen3-0.6B-Q8_0.gguf --text hello --max-tokens 1 --attribution`

## Done Condition

Maintained Qwen generation compare mode succeeds against stored baselines, and attribution replay
matches the same formatter/runtime contract as the shipped generator.
