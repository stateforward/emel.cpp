---
milestone: v1.25
milestone_name: "I/O Read Loading Strategy"
audited: "2026-05-06T19:49:39Z"
status: passed
scores:
  requirements: "13/13"
  phases: "16/16"
  integration: "6/6"
  flows: "5/5"
gaps:
  requirements: []
  integration: []
  flows: []
  phase_artifacts: []
tech_debt: []
nyquist:
  compliant_phases:
    - "212-read-strategy-component-boundary"
    - "213-read-validation-and-platform-gating"
    - "214.1-rtc-safe-read-execution-boundary-repair"
    - "215-tensor-owned-read-integration"
    - "216-public-runtime-and-evidence-surfaces"
    - "217-behavior-tests-and-scope-guardrails"
    - "218-publication-and-maintained-artifact-updates"
    - "219-maintained-read-source-provenance"
    - "220-explicit-tensor-read-outcome-graph"
    - "221-read-closeout-truth-reconciliation"
    - "222-public-read-source-contract-repair"
    - "223-read-closeout-truth-and-validation-reconciliation"
    - "224-read-closeout-tech-debt-cleanup"
    - "225-read-closeout-runtime-validation-and-sml-repair"
    - "226-read-batch-cap-and-closeout-evidence-refresh"
  partial_phases:
    - "214-read-execution-errors-and-lifetime"
  invalid_phases: []
  missing_phases: []
  overall: compliant_with_superseded_phase
source_backed_audit:
  maintained_path: passed
  synthetic_or_tool_only_contract_found: false
  observations:
    - "Maintained generation, Sortformer, embedded probe, and paritychecker lanes load setup-time source bytes through emel::io::source::load_file_bytes."
    - "Maintained callers provide request-owned io_load_spans and propagate used_io_strategy from public model-loader done/error evidence."
    - "Forbidden maintained-lane scan for effect_dispatch_io_loads, per-tensor io_loader->process_event loops, emel/io/read/detail.hpp reach-through, emel/io/read/events.hpp reach-through, and read_tensor_request returned no matches."
    - "Current audit reran focused direct CTest for emel_tests_io and emel_tests_model_and_batch and both passed."
    - "Domain-boundary checks passed; consistency validation passed with archive-layout warnings only."
    - "The root active audit path .planning/v1.25-MILESTONE-AUDIT.md was regenerated from archived v1.25 artifacts and live source checks."
---

# v1.25 Milestone Audit: I/O Read Loading Strategy

## Verdict

Status: passed.

All 13 active v1.25 requirements are satisfied. No maintained-path blocker,
integration gap, broken E2E flow, orphaned requirement, invalid Nyquist phase, or
current tech-debt item remains. The active root audit file was missing after
archive; this refreshed audit recreates it from the archived v1.25 phase artifacts
and live source checks.

## Scope

`gsd-tools init milestone-op` reports the current shipped milestone as v1.25
I/O Read Loading Strategy. `ROADMAP.md` and `STATE.md` mark v1.25 shipped on
2026-05-06, with Phases 212-226 plus Phase 214.1 complete. The active
`gsd-tools phases list` output still points at the previous active phase
directory, so this audit uses the archived v1.25 scope:

- `.planning/milestones/v1.25-ROADMAP.md`
- `.planning/milestones/v1.25-REQUIREMENTS.md`
- `.planning/milestones/v1.25-phases/`
- live source, tests, and tool entrypoints under `src/`, `tests/`, and `tools/`

All 16 in-scope v1.25 phase directories have SUMMARY, VERIFICATION, and
VALIDATION artifacts. Phase 214 is retained as historical evidence and is
superseded for maintained runtime truth by Phase 214.1. Phase 221 is a
superseded closeout planning stub. Phases 224 and 226 are cleanup-only and do
not reset satisfied requirement coverage.

## Requirements Coverage

| Requirement | Assigned Phase | Final | Evidence |
|-------------|----------------|-------|----------|
| READ-01 | 212 | satisfied | `src/emel/io/read` owns component-local `context`, `events`, `guards`, `actions`, `errors`, and `sm`; `emel::io::read::sm` is the canonical machine. |
| READ-02 | 213 | satisfied | Read request validation is modeled through explicit guards and transitions before accepted copy work; batch count validation is independently capped by `k_max_read_batch_tensors`. |
| READ-03 | 214.1 | satisfied | Read/copy uses event-provided source spans and caller-owned targets, with dispatch-local status and no retained request data in `read::context`. |
| PLAT-01 | 222 | satisfied | Setup-time file loading is exposed through `emel::io::source::load_file_bytes`; `io/read` consumes source spans and fails closed on source/read errors. |
| LIFE-01 | 214.1 | satisfied | Tensor owns target buffers and residency; `io/read` does not retain OS handles or request pointers across `_done`. |
| ERR-01 | 214.1 | satisfied | Deterministic read error categories are defined in `io/read/errors.hpp` and propagated through explicit `_error` events/states. |
| TIO-01 | 215 | satisfied | `model/tensor` can request read/copy loading through public I/O boundaries while retaining load, bind, evict, and residency ownership. |
| TIO-02 | 223 | satisfied | Tensor read outcomes are routed through explicit states/events and typed same-RTC result data, not callback-selected status paths. |
| TIO-03 | 225 | satisfied | `model/loader`, benchmarks, paritychecker, and embedded probes select/report read/copy through public runtime surfaces; actor-internal reach-through scan passed. |
| VAL-01 | 225 | satisfied | Public-dispatch doctests exist across read, I/O loader, tensor, and model loader; focused `build/zig` CTest rerun passed for model/batch and I/O tests. |
| VAL-02 | 222 | satisfied | Domain/source guardrails reject actor-internal read detail reach-through and out-of-scope strategy leakage. |
| VAL-03 | 225 | satisfied | Active and archived milestone docs describe read/copy support truthfully; this audit restores the active root audit path referenced by closeout artifacts. |
| VAL-04 | 225 | satisfied | Benchmark/parity/probe reporting uses `used_io_strategy`, set only from public loader done/error evidence after maintained read-backed runtime execution. |

No orphaned requirements were found in the v1.25 traceability table. The
traceability table, SUMMARY frontmatter, and VERIFICATION requirement tables
agree for every active requirement after accounting for Phase 214.1 superseding
Phase 214 and Phase 223/225 superseding earlier closeout truth.

## Source-Backed Maintained Path

Confirmed live maintained path:

`tools/* fixture -> emel::io::source::load_file_bytes -> model::loader::event::load.file_image -> model::loader io_load_spans -> io::loader::event::load_tensor_batch -> io::read::event::read_tensor_batch -> caller-owned tensor buffers -> used_io_strategy report`

Source checks:

- `src/emel/io/source/any.hpp` exposes the setup-time public source-byte loader.
- `src/emel/model/loader/actions.hpp` builds request-owned `io_load_spans` from
  `file_image` and dispatches one public `io::loader::event::load_tensor_batch`.
- `src/emel/io/loader/actions.hpp` dispatches public
  `io::read::event::read_tensor_batch`.
- `src/emel/io/read/actions.hpp` performs the selected read/copy into caller-owned
  target buffers.
- Generation, Sortformer diarization, embedded probe, and paritychecker lanes all
  use the public source/model-loader strategy path and propagate `used_io_strategy`
  from public done/error evidence.

Forbidden maintained-lane scan:

- `rg -n "effect_dispatch_io_loads|for \\(.*io_loader->process_event|emel/io/read/detail.hpp|emel/io/read/events.hpp|read_tensor_request" src/emel/model/loader tools/bench/generation_bench.cpp tools/bench/diarization/sortformer_fixture.hpp tools/embedded_size/emel_probe/main.cpp tools/paritychecker/parity_engines.cpp`
  returned no matches.

Required maintained-wiring scan:

- `rg -n "io_load_spans|load_file_bytes|used_io_strategy|effect_dispatch_io_load_batch|load_tensor_batch|read_tensor_batch|k_max_read_batch_tensors" src/emel/io src/emel/model tools/bench/generation_bench.cpp tools/bench/diarization/sortformer_fixture.hpp tools/embedded_size/emel_probe/main.cpp tools/paritychecker/parity_engines.cpp tests`
  found the expected public source, batch dispatch, read/copy, cap, and reporting
  wiring.

No synthetic, fabricated, tool-only, or test-only contract was found feeding the
maintained benchmark/parity/read-copy evidence lane.

## Integration Findings

| Finding | Result |
|---------|--------|
| Public read/copy runtime path | Passed. `model/loader -> io/loader -> io/read` is connected through public events. |
| Tensor-owned integration | Passed. `model/tensor::request_read_load` still dispatches through public I/O boundaries and retains residency ownership. |
| Maintained source provenance | Passed. Maintained lanes use `emel::io::source::load_file_bytes`, not actor-internal read helpers or tool-local substitutes. |
| Maintained caller scratch | Passed. Generation, Sortformer, embedded probe, and paritychecker callers provide request-owned `io_load_spans`. |
| Public batch API cap | Passed. `k_max_read_batch_tensors` is guarded before per-span validation and copy work. |
| Closeout evidence freshness | Passed. Current direct focused CTest evidence passed for model/batch and I/O tests. |

Integration checker result: passed, `6/6` integration and `5/5` flows, no blockers.
It found no readiness-blocking AGENTS/SML/CPP rule conflict.

## E2E Flows

| Flow | Result |
|------|--------|
| Public single tensor read | Passed. `io::read::sm::process_event(read_tensor)` validates request/source/platform, copies bytes, and publishes explicit done/error. |
| Public batch tensor read | Passed. `read_tensor_batch` validates count, spans, resources, and copies each target. |
| I/O loader read-copy strategy | Passed. Strategy guards require an injected read actor and dispatch public `io/read`. |
| Tensor read load | Passed. `model/tensor` owns target/residency and classifies read outcomes through explicit states. |
| Maintained tool lane | Passed. Bench/parity/probe lanes load source bytes through public helper, pass public loader spans, and report `used_io_strategy` only after runtime use. |

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 212 | exists | true | none |
| 213 | exists | true | none |
| 214 | exists | partial; superseded by 214.1 | none |
| 214.1 | exists | true | none |
| 215 | exists | true | none |
| 216 | exists | true | none |
| 217 | exists | true | none |
| 218 | exists | true | none |
| 219 | exists | true | none |
| 220 | exists | true | none |
| 221 | exists | true; superseded closeout stub | none |
| 222 | exists | true | none |
| 223 | exists | true | none |
| 224 | exists | true | none |
| 225 | exists | true; historical dyld fallback refreshed by Phase 226 evidence | none |
| 226 | exists | true | none |

No phase requires `$gsd-validate-phase` for milestone readiness.

## Validation Commands

- `ctest --test-dir build/zig --output-on-failure -R 'emel_tests_(io|model_and_batch)'`
  passed: 2/2 tests.
- `scripts/check_domain_boundaries.sh` passed.
- `node .codex/get-shit-done/bin/gsd-tools.cjs validate consistency` passed with
  17 archive-layout warnings and no errors.
- `rg -n "emel/whisper|namespace emel::whisper|kernel/whisper|kernel::whisper" src tests CMakeLists.txt`
  returned no matches.

The full quality gate was not rerun during this audit pass; the audit relied on
existing phase quality-gate evidence plus current focused CTest, domain-boundary,
consistency, and source-backed maintained-path checks.

## Tech Debt

None for milestone readiness. Historical archived plan artifacts still contain
pre-archive `.planning/phases/...` and `.planning/v1.25-MILESTONE-AUDIT.md` references,
but the current root audit path has been regenerated and the authoritative archived
milestone artifacts live under `.planning/milestones/`.

## Deferred

Staged/chunked constrained-memory read policy, cooperative async loading,
device-specific loading strategies, model-family widening, the non-v1.23 quick task,
and the four optimization todos remain outside v1.25.
