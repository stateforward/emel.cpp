---
context: research
phase: cact-needle-exact-single-thread-performance
task: 1
total_tasks: 5
status: in_progress
last_updated: 2026-08-31T03:39:57.198Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through measured failures or explicit user direction.

- [ ] CONSTRAINT: Exact single-thread first — Do not pivot to multicore or approximation. The user explicitly requires exhausting exact single-thread performance first because those gains compound multicore throughput.
- [ ] CONSTRAINT: No silent target rescope — 435 tok/s exact single-core remains unsatisfied; current results must not be described as meeting the target.
- [ ] CONSTRAINT: Reprove after final correction — Graph parity passed before the final raw-lookup guard/test-only correction. Rerun the exact command below before further edits.
- [ ] CONSTRAINT: One build/bench/gate at a time — Shared-host compiler memory rules prohibit concurrent build, gate, or benchmark jobs.

**Do not proceed until all boxes are acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Repeating rejected CQ layouts | Packed selectors, AVX2 gather, activation-product LUT, row-major multirow, target interleave, and input unroll2 all lost controlled A/B measurements. | blocking | Read the experiment reports before proposing a route; require a materially different dataflow and same-binary A/B. |
| Benchmarking across different binaries/host conditions | Historical tok/s varied substantially with route combinations and host noise. | blocking | Compare candidates in the same binary, pinned to one CPU, with medians; retain only coherent direct and graph wins. |
| Treating missing gate infrastructure as proof | Scoped gates have stopped on missing zig and known benchmark/coverage infrastructure failures. | advisory | Report exact passed lanes and exact infrastructure blocker; never claim the aggregate gate passed. |

<current_state>
Branch `feat/cact-needle-runtime` is clean at commit `6b08fe3e Optimize CQ4 raw lookup32 lane ordering`.

Phase 5 exact single-thread optimization is active but paused at a safe restart boundary. Native `.cact` loading, Needle graph execution, exact W4/f32 and W4A8 logits/greedy parity, native tokenizer parity (287/287 prompts), and heldout quality (`0.8362` domain / `0.7596` effort) are complete.

The newest retained CQ change keeps lookup32 values in their natural AVX2 lane ordering through accumulation, prepares norms in the matching order, and performs canonical 128-bit half stores only once per 32-row output block. The inner input loop now has zero `vperm2f128` instructions.
</current_state>

<completed_work>

- Raw lookup32 committed: `6b08fe3e`.
- Focused CQ proof: 5 tests, 286,293 assertions passed.
- Corrected row-unique block32 oracle: 265,219 assertions passed.
- Blind rules, performance, and safety reviews: no remaining findings.
- Direct same-binary measurements:
  - 512x512: `0.047414810 ns/MAC` raw half-store vs `0.065379707 ns/MAC` prior retained.
  - 8192x512: `0.047821267 ns/MAC` vs `0.066057701 ns/MAC`.
  - Batch4: `49,470.400 ns` vs `69,400.656 ns`.
- Graph measurement: `179.657 tok/s` decode, `220.336 tok/s` prefill.
- Instrumented graph: `175.722 tok/s`, CQ `55.120%`.
- SWA GQA2 retained and security-hardened in commits `2812f9a3`, `ad83b462`, `d77e49cb`; final blind security review had no findings.
- AVX2 ZCRMS, AVX2 CQ FWHT128, A8 scale hoist, batch4 block32, and grouped norm transpose retained in prior commits.
</completed_work>

<remaining_work>

1. Rerun graph parity after the final raw-lookup zero-group guard/test-only correction:
   `/tmp/emel-raw-cq-build/emel_tests_bin --test-case="needle graph matches the committed JAX logits fixture on all cases,needle graph W4A8 deployment route matches authoritative JAX fixture,needle graph prepares CQ4 storage once and selects prepared route"`
2. Write `~/.agents/projects/emel-cpp/comments/cact-bench-closeout-impl/raw-lookup32-2026-08-31.md` with mapping, proof, direct/graph measurements, assembly result, and gate limitation.
3. Restore Zig toolchain/PATH if available and rerun the changed-file scoped quality gate. The last attempt passed the legacy scan then stopped because Zig was unavailable; no snapshot was retained.
4. Continue exact single-thread research only after the above proof/report boundary. Re-profile CQ/non-CQ on the new head before selecting the next experiment.
5. When single-thread avenues are genuinely exhausted: review optimized parity/benchmarks, full-scope gates, terminal multi-lane blind review, control-plane closeout, PR.
</remaining_work>

<decisions_made>

- Exhaust exact single-thread performance before multicore. User rationale: single-thread gains also improve multicore.
- Keep 435 tok/s exact single-core as an active target, explicitly not met.
- Retain only measured exact routes. No CQ approximation, requantization, dequantized weight blob, threading, or changed model work.
- Retain raw lookup32 because it materially improves both direct CQ and graph performance while preserving exact operands.
</decisions_made>

<blockers>

- Zig unavailable for the repository scoped gate in the current environment/PATH.
- 435 tok/s remains technically unproven and materially above current `~180 tok/s` result. This is not permission to rescope.
</blockers>

## Required Reading (in order)

1. `.planning/.continue-here.md` — this boundary.
2. `/home/gwillen/.agents/projects/emel-cpp/goals/cact-needle-runtime.mdscript.md` — goal contract and hot path.
3. `/home/gwillen/.agents/projects/emel-cpp/tasks/cact-needle-runtime-root.mdscript.md` — authoritative root claim and proof path.
4. `/home/gwillen/.agents/projects/emel-cpp/comments/cact-bench-closeout-impl/norm-layout-pipeline-2026-08-30.md` — prior CQ layout evidence.
5. `/home/gwillen/.agents/projects/emel-cpp/comments/cact-bench-closeout-impl/product-lut-2026-08-30.md` — rejected exact product routes and prior lower bound.
6. `/home/gwillen/.agents/projects/emel-cpp/comments/cact-bench-closeout-impl/non-cq-profile-2026-08-30.md` — reconciled component profile.
7. `/home/gwillen/.agents/projects/emel-cpp/comments/cact-bench-closeout-impl/swa-gqa2-review-fix-2026-08-31.md` — GQA2 validation/review closeout.
8. `AGENTS.md` from loaded repository context — RTC, guard/action, allocation, kernel ownership, and gate rules.

## Critical Anti-Patterns (do NOT repeat these)

- Do not rerun packed selector, gather, product-LUT, row-major, interleaved batch4, or unroll2 experiments without a structurally new reason; all were measured and rejected.
- Do not compare tok/s from separate binaries as a retention decision.
- Do not claim graph parity after `6b08fe3e` until the named focused command is rerun.
- Do not start multicore work before the user changes the current priority.

## Infrastructure State

- No live subagents or background services are required. `CQRawLookup32` stopped at a clean committed boundary.
- Repository worktree was clean when paused.
- Focused test binary: `/tmp/emel-raw-cq-build/emel_tests_bin`.
- Current branch: `feat/cact-needle-runtime`.
- Current source head: `6b08fe3e`.

<context>
The raw natural-lane lookup was the first recent CQ experiment to produce a large direct kernel improvement: roughly 27–28% versus the preceding lookup route. It removes cross-lane permutes from the per-input hot loop rather than reducing selector bytes or approximating values. The immediate responsibility on restart is proof closure, not another speculative edit. Once parity/report/gate state is closed, rerun component timing on this exact head and choose the next bottleneck from evidence.
</context>

<next_action>
Start with:
`/tmp/emel-raw-cq-build/emel_tests_bin --test-case="needle graph matches the committed JAX logits fixture on all cases,needle graph W4A8 deployment route matches authoritative JAX fixture,needle graph prepares CQ4 storage once and selects prepared route"`
</next_action>
